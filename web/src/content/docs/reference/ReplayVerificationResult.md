---
title: "ReplayVerificationResult"
description: "Documentation for the ReplayVerificationResult type."
slug: "reference/replayverificationresult"
---
<!-- <auto-generated by typra-emitter> -->

Result returned by a replay verifier implementation.

## Class Diagram

```mermaid
---
title: ReplayVerificationResult
config:
  look: handDrawn
  theme: colorful
  class:
    hideEmptyMembersBox: true
---
classDiagram
    class ReplayVerificationResult {
        +string status
        +ReplayMismatch[] mismatches
        +int32 expectedCount
        +int32 actualCount
    }
    class ReplayMismatch {
        +int32 index
        +ReplayJournalRecord expected
        +ReplayJournalRecord actual
        +string message
    }
    ReplayVerificationResult *-- ReplayMismatch
```

## Properties

| Name | Type | Description |
| ---- | ---- | ----------- |
| status | string | Replay verification status |
| mismatches | [ReplayMismatch[]](../replaymismatch/) | Record mismatches, empty when verification passed |
| expectedCount | int32 | Number of expected records |
| actualCount | int32 | Number of actual records |

## Composed Types

The following types are composed within `ReplayVerificationResult`:

- [ReplayMismatch](../replaymismatch/)
