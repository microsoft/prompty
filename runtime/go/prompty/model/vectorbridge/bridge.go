// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Typed @vector adapter bridge (issue #511 Cat 1). Each constructor turns a
// consumer's typed seam impl into the decoded-invoke func the conformance
// harness needs, so the consumer drops the hand-authored per-op marshalling
// closure. The bridge only decodes the runner's resolved input into typed
// params, calls the impl, and returns the raw (value, error) the runner
// canonicalizes — normalization, comparison, expectedError, requires gating
// and waivers all stay in the runner. It imports NEITHER vectoradapters nor
// vectorrunner (the seam registry lives in the consumer's vectoradapters
// package, which imports THIS package — so importing it back would cycle);
// the consumer adapts the returned func into their Adapter with a one-time
// helper. See docs: reference/vector-conformance.

package vectorbridge

import (
	"encoding/json"
	fixtures "prompty/model"
)

// DiscoveryConformanceEnrich decodes DiscoveryConformance.enrich vector input and invokes a
// typed fixtures.DiscoveryConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["DiscoveryConformance.enrich"] = bridged(vectorbridge.DiscoveryConformanceEnrich(impl))
func DiscoveryConformanceEnrich(impl fixtures.DiscoveryConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		base, err := fixtures.LoadModelInfo(payload["base"], fixtures.NewLoadContext())
		if err != nil {
			return nil, err
		}
		return impl.Enrich(base)
	}
}

// DiscoveryConformanceMapModel decodes DiscoveryConformance.mapModel vector input and invokes a
// typed fixtures.DiscoveryConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["DiscoveryConformance.mapModel"] = bridged(vectorbridge.DiscoveryConformanceMapModel(impl))
func DiscoveryConformanceMapModel(impl fixtures.DiscoveryConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		var raw interface{}
		{
			rawBytes, marshalErr := json.Marshal(payload["raw"])
			if marshalErr != nil {
				return nil, marshalErr
			}
			if unmarshalErr := json.Unmarshal(rawBytes, &raw); unmarshalErr != nil {
				return nil, unmarshalErr
			}
		}
		return impl.MapModel(raw)
	}
}

// LoadConformanceLoad decodes LoadConformance.load vector input and invokes a
// typed fixtures.LoadConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["LoadConformance.load"] = bridged(vectorbridge.LoadConformanceLoad(impl))
func LoadConformanceLoad(impl fixtures.LoadConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		var source interface{}
		{
			sourceBytes, marshalErr := json.Marshal(payload["source"])
			if marshalErr != nil {
				return nil, marshalErr
			}
			if unmarshalErr := json.Unmarshal(sourceBytes, &source); unmarshalErr != nil {
				return nil, unmarshalErr
			}
		}
		return impl.Load(source)
	}
}

// TurnConformanceReplay decodes TurnConformance.replay vector input and invokes a
// typed fixtures.TurnConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["TurnConformance.replay"] = bridged(vectorbridge.TurnConformanceReplay(impl))
func TurnConformanceReplay(impl fixtures.TurnConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		var input interface{}
		{
			inputBytes, marshalErr := json.Marshal(payload["input"])
			if marshalErr != nil {
				return nil, marshalErr
			}
			if unmarshalErr := json.Unmarshal(inputBytes, &input); unmarshalErr != nil {
				return nil, unmarshalErr
			}
		}
		return impl.Replay(input)
	}
}

// TurnConformanceRun decodes TurnConformance.run vector input and invokes a
// typed fixtures.TurnConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["TurnConformance.run"] = bridged(vectorbridge.TurnConformanceRun(impl))
func TurnConformanceRun(impl fixtures.TurnConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		var input interface{}
		{
			inputBytes, marshalErr := json.Marshal(payload["input"])
			if marshalErr != nil {
				return nil, marshalErr
			}
			if unmarshalErr := json.Unmarshal(inputBytes, &input); unmarshalErr != nil {
				return nil, unmarshalErr
			}
		}
		return impl.Run(input)
	}
}

// TurnConformanceRunTurn decodes TurnConformance.runTurn vector input and invokes a
// typed fixtures.TurnConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["TurnConformance.runTurn"] = bridged(vectorbridge.TurnConformanceRunTurn(impl))
func TurnConformanceRunTurn(impl fixtures.TurnConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		var input interface{}
		{
			inputBytes, marshalErr := json.Marshal(payload["input"])
			if marshalErr != nil {
				return nil, marshalErr
			}
			if unmarshalErr := json.Unmarshal(inputBytes, &input); unmarshalErr != nil {
				return nil, unmarshalErr
			}
		}
		return impl.RunTurn(input)
	}
}

// WireConformanceToRequest decodes WireConformance.toRequest vector input and invokes a
// typed fixtures.WireConformance. Adapt the returned func into your Adapter, e.g.:
//
//	VectorAdapters["WireConformance.toRequest"] = bridged(vectorbridge.WireConformanceToRequest(impl))
func WireConformanceToRequest(impl fixtures.WireConformance) func(rawInput any) (any, error) {
	return func(rawInput any) (any, error) {
		payload, _ := rawInput.(map[string]any)
		agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
		if err != nil {
			return nil, err
		}
		var messages []fixtures.Message
		{
			messagesBytes, marshalErr := json.Marshal(payload["messages"])
			if marshalErr != nil {
				return nil, marshalErr
			}
			if unmarshalErr := json.Unmarshal(messagesBytes, &messages); unmarshalErr != nil {
				return nil, unmarshalErr
			}
		}
		return impl.ToRequest(agent, messages)
	}
}
