// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

package prompty_test

import (
	"encoding/json"
	"reflect"
	"testing"

	"gopkg.in/yaml.v3"

	prompty "prompty/model"
)

// TestContextCandidateLoadJSON tests loading ContextCandidate from JSON
func TestContextCandidateLoadJSON(t *testing.T) {
	jsonData := `
{
  "id": "memory:project-plan",
  "source": "memory",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadContextCandidate(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate: %v", err)
	}
	if instance.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, instance.Id)
	}
	if instance.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, instance.Source)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertContextCandidateStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateLoadYAML tests loading ContextCandidate from YAML
func TestContextCandidateLoadYAML(t *testing.T) {
	yamlData := `
id: "memory:project-plan"
source: memory
messages:
  - role: user
    parts:
      - kind: text
        value: Hello!
    metadata:
      source: user-input

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadContextCandidate(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate: %v", err)
	}
	if instance.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, instance.Id)
	}
	if instance.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, instance.Source)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertContextCandidateStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateFromJSON tests loading ContextCandidate through the generated JSON helper
func TestContextCandidateFromJSON(t *testing.T) {
	jsonData := `
{
  "id": "memory:project-plan",
  "source": "memory",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ]
}
`

	instance, err := prompty.ContextCandidateFromJSON(jsonData)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate from JSON helper: %v", err)
	}
	if instance.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, instance.Id)
	}
	if instance.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, instance.Source)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertContextCandidateStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateFromYAML tests loading ContextCandidate through the generated YAML helper
func TestContextCandidateFromYAML(t *testing.T) {
	yamlData := `
id: "memory:project-plan"
source: memory
messages:
  - role: user
    parts:
      - kind: text
        value: Hello!
    metadata:
      source: user-input

`

	instance, err := prompty.ContextCandidateFromYAML(yamlData)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate from YAML helper: %v", err)
	}
	if instance.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, instance.Id)
	}
	if instance.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, instance.Source)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertContextCandidateStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateRoundtrip tests load -> save -> load produces equivalent data
func TestContextCandidateRoundtrip(t *testing.T) {
	jsonData := `
{
  "id": "memory:project-plan",
  "source": "memory",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := prompty.NewLoadContext()
	instance, err := prompty.LoadContextCandidate(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate: %v", err)
	}
	saveCtx := prompty.NewSaveContext()
	savedData := instance.Save(saveCtx)

	reloaded, err := prompty.LoadContextCandidate(savedData, loadCtx)
	if err != nil {
		t.Fatalf("Failed to reload ContextCandidate: %v", err)
	}
	if reloaded.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, reloaded.Id)
	}
	if reloaded.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, reloaded.Source)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertContextCandidateStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateToJSON tests that ToJSON produces valid JSON
func TestContextCandidateToJSON(t *testing.T) {
	jsonData := `
{
  "id": "memory:project-plan",
  "source": "memory",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadContextCandidate(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate: %v", err)
	}
	jsonOutput, err := instance.ToJSON()
	if err != nil {
		t.Fatalf("Failed to convert to JSON: %v", err)
	}

	var parsed map[string]interface{}
	if err := json.Unmarshal([]byte(jsonOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated JSON: %v", err)
	}

	reloaded, err := prompty.LoadContextCandidate(parsed, ctx)
	if err != nil {
		t.Fatalf("Failed to reload generated JSON: %v", err)
	}
	if reloaded.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, reloaded.Id)
	}
	if reloaded.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, reloaded.Source)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertContextCandidateStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateToYAML tests that ToYAML produces valid YAML
func TestContextCandidateToYAML(t *testing.T) {
	jsonData := `
{
  "id": "memory:project-plan",
  "source": "memory",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadContextCandidate(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ContextCandidate: %v", err)
	}
	yamlOutput, err := instance.ToYAML()
	if err != nil {
		t.Fatalf("Failed to convert to YAML: %v", err)
	}

	var parsed map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated YAML: %v", err)
	}

	reloaded, err := prompty.LoadContextCandidate(parsed, ctx)
	if err != nil {
		t.Fatalf("Failed to reload generated YAML: %v", err)
	}
	if reloaded.Id != "memory:project-plan" {
		t.Errorf(`Expected Id to be "memory:project-plan", got %v`, reloaded.Id)
	}
	if reloaded.Source != "memory" {
		t.Errorf(`Expected Source to be "memory", got %v`, reloaded.Source)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertContextCandidateStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestContextCandidateFromJSONInvalid rejects malformed JSON instead of silently defaulting
func TestContextCandidateFromJSONInvalid(t *testing.T) {
	if _, err := prompty.ContextCandidateFromJSON("{"); err == nil {
		t.Fatalf("Expected malformed JSON to fail")
	}
}

func assertContextCandidateStringField(t *testing.T, value interface{}, fieldName string, expected string, displayName string) {
	t.Helper()
	field := reflect.ValueOf(value)
	if field.Kind() == reflect.Pointer {
		if field.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		field = field.Elem()
	}
	if field.Kind() != reflect.Struct {
		t.Fatalf("Expected %s receiver to be a struct, got %T", displayName, value)
	}
	member := field.FieldByName(fieldName)
	if !member.IsValid() {
		t.Fatalf("Expected %s to have field %s, got %T", displayName, fieldName, value)
	}
	if member.Kind() == reflect.Pointer {
		if member.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		member = member.Elem()
	}
	if member.Kind() == reflect.Interface {
		if member.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		member = member.Elem()
	}
	if member.Kind() != reflect.String {
		t.Fatalf("Expected %s to be a string field, got %s", displayName, member.Kind())
	}
	if got := member.String(); got != expected {
		t.Errorf("Expected %s to be %q, got %q", displayName, expected, got)
	}
}
