// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

package prompty_test

import (
	"encoding/json"
	"reflect"
	"testing"

	"gopkg.in/yaml.v3"

	"prompty/model"
)

// TestTurnCommitLoadJSON tests loading TurnCommit from JSON
func TestTurnCommitLoadJSON(t *testing.T) {
	jsonData := `
{
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadTurnCommit(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit: %v", err)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertTurnCommitStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitLoadYAML tests loading TurnCommit from YAML
func TestTurnCommitLoadYAML(t *testing.T) {
	yamlData := `
sessionId: sess_abc123
turnId: turn_abc123
messages:
  - role: user
    parts:
      - kind: text
        value: Hello!
    metadata:
      source: user-input
contextState: {}

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadTurnCommit(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit: %v", err)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertTurnCommitStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitFromJSON tests loading TurnCommit through the generated JSON helper
func TestTurnCommitFromJSON(t *testing.T) {
	jsonData := `
{
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`

	instance, err := prompty.TurnCommitFromJSON(jsonData)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit from JSON helper: %v", err)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertTurnCommitStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitFromYAML tests loading TurnCommit through the generated YAML helper
func TestTurnCommitFromYAML(t *testing.T) {
	yamlData := `
sessionId: sess_abc123
turnId: turn_abc123
messages:
  - role: user
    parts:
      - kind: text
        value: Hello!
    metadata:
      source: user-input
contextState: {}

`

	instance, err := prompty.TurnCommitFromYAML(yamlData)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit from YAML helper: %v", err)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertTurnCommitStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitRoundtrip tests load -> save -> load produces equivalent data
func TestTurnCommitRoundtrip(t *testing.T) {
	jsonData := `
{
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := prompty.NewLoadContext()
	instance, err := prompty.LoadTurnCommit(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit: %v", err)
	}
	saveCtx := prompty.NewSaveContext()
	savedData := instance.Save(saveCtx)

	reloaded, err := prompty.LoadTurnCommit(savedData, loadCtx)
	if err != nil {
		t.Fatalf("Failed to reload TurnCommit: %v", err)
	}
	if reloaded.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, reloaded.SessionId)
	}
	if reloaded.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, reloaded.TurnId)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertTurnCommitStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitToJSON tests that ToJSON produces valid JSON
func TestTurnCommitToJSON(t *testing.T) {
	jsonData := `
{
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadTurnCommit(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit: %v", err)
	}
	jsonOutput, err := instance.ToJSON()
	if err != nil {
		t.Fatalf("Failed to convert to JSON: %v", err)
	}

	var parsed map[string]interface{}
	if err := json.Unmarshal([]byte(jsonOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated JSON: %v", err)
	}

	reloaded, err := prompty.LoadTurnCommit(parsed, ctx)
	if err != nil {
		t.Fatalf("Failed to reload generated JSON: %v", err)
	}
	if reloaded.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, reloaded.SessionId)
	}
	if reloaded.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, reloaded.TurnId)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertTurnCommitStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitToYAML tests that ToYAML produces valid YAML
func TestTurnCommitToYAML(t *testing.T) {
	jsonData := `
{
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadTurnCommit(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load TurnCommit: %v", err)
	}
	yamlOutput, err := instance.ToYAML()
	if err != nil {
		t.Fatalf("Failed to convert to YAML: %v", err)
	}

	var parsed map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated YAML: %v", err)
	}

	reloaded, err := prompty.LoadTurnCommit(parsed, ctx)
	if err != nil {
		t.Fatalf("Failed to reload generated YAML: %v", err)
	}
	if reloaded.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, reloaded.SessionId)
	}
	if reloaded.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, reloaded.TurnId)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertTurnCommitStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestTurnCommitFromJSONInvalid rejects malformed JSON instead of silently defaulting
func TestTurnCommitFromJSONInvalid(t *testing.T) {
	if _, err := prompty.TurnCommitFromJSON("{"); err == nil {
		t.Fatalf("Expected malformed JSON to fail")
	}
}

func assertTurnCommitStringField(t *testing.T, value interface{}, fieldName string, expected string, displayName string) {
	t.Helper()
	field := reflect.ValueOf(value)
	if field.Kind() == reflect.Pointer {
		if field.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		field = field.Elem()
	}
	if field.Kind() != reflect.Struct {
		t.Fatalf("Expected %s receiver to be a struct, got %T", displayName, value)
	}
	member := field.FieldByName(fieldName)
	if !member.IsValid() {
		t.Fatalf("Expected %s to have field %s, got %T", displayName, fieldName, value)
	}
	if member.Kind() == reflect.Pointer {
		if member.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		member = member.Elem()
	}
	if member.Kind() == reflect.Interface {
		if member.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		member = member.Elem()
	}
	if member.Kind() != reflect.String {
		t.Fatalf("Expected %s to be a string field, got %s", displayName, member.Kind())
	}
	if got := member.String(); got != expected {
		t.Errorf("Expected %s to be %q, got %q", displayName, expected, got)
	}
}
