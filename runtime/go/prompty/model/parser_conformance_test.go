// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Part III TYPED @vector conformance for Parser — the per-interface
// twin of the per-model ${model}_test.go file (issue #282 §8). Each test builds
// the operation inputs from the vector JSON, reads the shape discriminator, routes
// it through the emitted ResolveParser against the consumer-attached provider
// (vectoradapters.ParserProvider()), invokes the typed seam, and asserts the
// result reproduces `expected`. Go has no compile-time completeness, so a dropped
// @dispatch slot errors at provider collection (NewParserProvider) rather than
// compiling — conformance never silently skips.
// See docs: reference/vector-conformance.

package prompty_test

import (
	"encoding/json"
	"testing"

	fixtures "prompty/model"
	vectoradapters "vectoradapters"
)

func TestParserParseAssistantWithCodeBlock(t *testing.T) {
	inputJSON := "{\n  \"agent\": {\n    \"template\": {\n      \"format\": {\n        \"kind\": \"jinja2\"\n      },\n      \"parser\": {\n        \"kind\": \"prompty\"\n      }\n    }\n  },\n  \"rendered\": \"assistant:\\nHere is code:\\n```python\\nprint('hello')\\n```\\n\\nuser:\\nThanks\"\n}"
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("assistant_with_code_block: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("assistant_with_code_block: %v", err)
	}
	_ = actual
}

func TestParserParseConsecutiveNewlinesBetweenRoles(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nHelper\n\n\n\nuser:\nHi"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("consecutive_newlines_between_roles: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("consecutive_newlines_between_roles: %v", err)
	}
	_ = actual
}

func TestParserParseContentTrimmed(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\n  \n  You are helpful.  \n  \n\nuser:\n  Hello  "
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("content_trimmed: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("content_trimmed: %v", err)
	}
	_ = actual
}

func TestParserParseDeveloperRole(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "developer:\nInternal instructions."
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("developer_role: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("developer_role: %v", err)
	}
	_ = actual
}

func TestParserParseEmptyContent(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\n\nuser:\nHello"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("empty_content: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("empty_content: %v", err)
	}
	_ = actual
}

func TestParserParseInlineMarkdownImagePreservedAsText(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "user:\nLook at this ![photo](https://example.com/img.png)"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("inline_markdown_image_preserved_as_text: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("inline_markdown_image_preserved_as_text: %v", err)
	}
	_ = actual
}

func TestParserParseMarkdownInContent(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\n# Title\n\nSome **bold** text\n\n- item 1\n- item 2\n\nuser:\nHello"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("markdown_in_content: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("markdown_in_content: %v", err)
	}
	_ = actual
}

func TestParserParseMultilineContent(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nLine one.\nLine two.\nLine three.\n\nuser:\nQuestion"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("multiline_content: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("multiline_content: %v", err)
	}
	_ = actual
}

func TestParserParseMultipleAssistantTurns(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful.\n\nuser:\nHi\n\nassistant:\nHello!\n\nuser:\nThanks\n\nassistant:\nYou're welcome!"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("multiple_assistant_turns: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("multiple_assistant_turns: %v", err)
	}
	_ = actual
}

func TestParserParseMultipleUserTurns(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nHelper\n\nuser:\nFirst question\n\nassistant:\nFirst answer\n\nuser:\nSecond question"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("multiple_user_turns: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("multiple_user_turns: %v", err)
	}
	_ = actual
}

func TestParserParseNoRoleMarkerDefaultsToSystem(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "Just a plain message with no role marker"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("no_role_marker_defaults_to_system: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("no_role_marker_defaults_to_system: %v", err)
	}
	_ = actual
}

func TestParserParseRoleMarkerAtLineStartOnly(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nThe user: said hello\n\nuser:\nHi"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("role_marker_at_line_start_only: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("role_marker_at_line_start_only: %v", err)
	}
	_ = actual
}

func TestParserParseRoleMarkerColonInContent(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nHere is a tip: always be kind.\n\nuser:\nTime is 3:30pm"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("role_marker_colon_in_content: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("role_marker_colon_in_content: %v", err)
	}
	_ = actual
}

func TestParserParseRoleWithMultipleUnquotedAttributes(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "user[name=Bob, id=7]:\nHi"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("role_with_multiple_unquoted_attributes: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("role_with_multiple_unquoted_attributes: %v", err)
	}
	_ = actual
}

func TestParserParseRoleWithQuotedAttribute(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "user[name=\"Alice\"]:\nHello!"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("role_with_quoted_attribute: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("role_with_quoted_attribute: %v", err)
	}
	_ = actual
}

func TestParserParseSingleSystem(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful."
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("single_system: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("single_system: %v", err)
	}
	_ = actual
}

func TestParserParseSystemUser(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful.\n\nuser:\nHello"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("system_user: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("system_user: %v", err)
	}
	_ = actual
}

func TestParserParseSystemUserAssistant(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nBe concise.\n\nuser:\nHi\n\nassistant:\nHello! How can I help?"
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("system_user_assistant: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("system_user_assistant: %v", err)
	}
	_ = actual
}

func TestParserParseThreadNonceExpansion(t *testing.T) {
	inputJSON := `{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful.\n\n__PROMPTY_THREAD_abcd1234_conversation__\n\nuser:\nHi",
  "thread_inputs": {
    "conversation": [
      {
        "role": "user",
        "content": [
          {
            "kind": "text",
            "value": "prev question"
          }
        ]
      },
      {
        "role": "assistant",
        "content": [
          {
            "kind": "text",
            "value": "prev answer"
          }
        ]
      }
    ]
  }
}`
	var payload map[string]any
	if err := json.Unmarshal([]byte(inputJSON), &payload); err != nil {
		t.Fatalf("failed to decode vector input: %v", err)
	}
	agent, err := fixtures.LoadAgent(payload["agent"], fixtures.NewLoadContext())
	if err != nil {
		t.Fatalf("agent parse: %v", err)
	}
	var rendered string
	{
		renderedBytes, marshalErr := json.Marshal(payload["rendered"])
		if marshalErr != nil {
			t.Fatalf("rendered marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(renderedBytes, &rendered); unmarshalErr != nil {
			t.Fatalf("rendered parse: %v", unmarshalErr)
		}
	}
	var context *map[string]interface{}
	{
		contextBytes, marshalErr := json.Marshal(payload["context"])
		if marshalErr != nil {
			t.Fatalf("context marshal: %v", marshalErr)
		}
		if unmarshalErr := json.Unmarshal(contextBytes, &context); unmarshalErr != nil {
			t.Fatalf("context parse: %v", unmarshalErr)
		}
	}
	kind := agent.Template.Parser.(interface {
		Save(*fixtures.SaveContext) map[string]interface{}
	}).Save(fixtures.NewSaveContext())["kind"].(string)
	impl, err := fixtures.ResolveParser(kind, vectoradapters.ParserProvider())
	if err != nil {
		t.Fatalf("resolve %q: %v", kind, err)
	}
	if impl == nil {
		t.Fatalf("thread_nonce_expansion: no Parser attached for %q", kind)
	}
	actual, err := impl.Parse(agent, rendered, context)
	if err != nil {
		t.Fatalf("thread_nonce_expansion: %v", err)
	}
	_ = actual
}
