// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

package prompty_test

import (
	"encoding/json"
	"reflect"
	"testing"

	"gopkg.in/yaml.v3"

	"prompty/model"
)

// TestEngineCheckpointLoadJSON tests loading EngineCheckpoint from JSON
func TestEngineCheckpointLoadJSON(t *testing.T) {
	jsonData := `
{
  "id": "ckpt_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "runId": "run_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadEngineCheckpoint(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint: %v", err)
	}
	if instance.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, instance.Id)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if instance.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, instance.RunId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertEngineCheckpointStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointLoadYAML tests loading EngineCheckpoint from YAML
func TestEngineCheckpointLoadYAML(t *testing.T) {
	yamlData := `
id: ckpt_abc123
sessionId: sess_abc123
turnId: turn_abc123
runId: run_abc123
messages:
  - role: user
    parts:
      - kind: text
        value: Hello!
    metadata:
      source: user-input
contextState: {}

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadEngineCheckpoint(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint: %v", err)
	}
	if instance.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, instance.Id)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if instance.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, instance.RunId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertEngineCheckpointStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointFromJSON tests loading EngineCheckpoint through the generated JSON helper
func TestEngineCheckpointFromJSON(t *testing.T) {
	jsonData := `
{
  "id": "ckpt_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "runId": "run_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`

	instance, err := prompty.EngineCheckpointFromJSON(jsonData)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint from JSON helper: %v", err)
	}
	if instance.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, instance.Id)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if instance.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, instance.RunId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertEngineCheckpointStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointFromYAML tests loading EngineCheckpoint through the generated YAML helper
func TestEngineCheckpointFromYAML(t *testing.T) {
	yamlData := `
id: ckpt_abc123
sessionId: sess_abc123
turnId: turn_abc123
runId: run_abc123
messages:
  - role: user
    parts:
      - kind: text
        value: Hello!
    metadata:
      source: user-input
contextState: {}

`

	instance, err := prompty.EngineCheckpointFromYAML(yamlData)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint from YAML helper: %v", err)
	}
	if instance.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, instance.Id)
	}
	if instance.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, instance.SessionId)
	}
	if instance.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, instance.TurnId)
	}
	if instance.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, instance.RunId)
	}
	if len(instance.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(instance.Messages))
	}
	assertEngineCheckpointStringField(t, instance.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointRoundtrip tests load -> save -> load produces equivalent data
func TestEngineCheckpointRoundtrip(t *testing.T) {
	jsonData := `
{
  "id": "ckpt_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "runId": "run_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := prompty.NewLoadContext()
	instance, err := prompty.LoadEngineCheckpoint(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint: %v", err)
	}
	saveCtx := prompty.NewSaveContext()
	savedData := instance.Save(saveCtx)

	reloaded, err := prompty.LoadEngineCheckpoint(savedData, loadCtx)
	if err != nil {
		t.Fatalf("Failed to reload EngineCheckpoint: %v", err)
	}
	if reloaded.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, reloaded.Id)
	}
	if reloaded.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, reloaded.SessionId)
	}
	if reloaded.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, reloaded.TurnId)
	}
	if reloaded.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, reloaded.RunId)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertEngineCheckpointStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointToJSON tests that ToJSON produces valid JSON
func TestEngineCheckpointToJSON(t *testing.T) {
	jsonData := `
{
  "id": "ckpt_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "runId": "run_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadEngineCheckpoint(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint: %v", err)
	}
	jsonOutput, err := instance.ToJSON()
	if err != nil {
		t.Fatalf("Failed to convert to JSON: %v", err)
	}

	var parsed map[string]interface{}
	if err := json.Unmarshal([]byte(jsonOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated JSON: %v", err)
	}

	reloaded, err := prompty.LoadEngineCheckpoint(parsed, ctx)
	if err != nil {
		t.Fatalf("Failed to reload generated JSON: %v", err)
	}
	if reloaded.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, reloaded.Id)
	}
	if reloaded.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, reloaded.SessionId)
	}
	if reloaded.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, reloaded.TurnId)
	}
	if reloaded.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, reloaded.RunId)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertEngineCheckpointStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointToYAML tests that ToYAML produces valid YAML
func TestEngineCheckpointToYAML(t *testing.T) {
	jsonData := `
{
  "id": "ckpt_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_abc123",
  "runId": "run_abc123",
  "messages": [
    {
      "role": "user",
      "parts": [
        {
          "kind": "text",
          "value": "Hello!"
        }
      ],
      "metadata": {
        "source": "user-input"
      }
    }
  ],
  "contextState": {}
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := prompty.NewLoadContext()
	instance, err := prompty.LoadEngineCheckpoint(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load EngineCheckpoint: %v", err)
	}
	yamlOutput, err := instance.ToYAML()
	if err != nil {
		t.Fatalf("Failed to convert to YAML: %v", err)
	}

	var parsed map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated YAML: %v", err)
	}

	reloaded, err := prompty.LoadEngineCheckpoint(parsed, ctx)
	if err != nil {
		t.Fatalf("Failed to reload generated YAML: %v", err)
	}
	if reloaded.Id != "ckpt_abc123" {
		t.Errorf(`Expected Id to be "ckpt_abc123", got %v`, reloaded.Id)
	}
	if reloaded.SessionId != "sess_abc123" {
		t.Errorf(`Expected SessionId to be "sess_abc123", got %v`, reloaded.SessionId)
	}
	if reloaded.TurnId != "turn_abc123" {
		t.Errorf(`Expected TurnId to be "turn_abc123", got %v`, reloaded.TurnId)
	}
	if reloaded.RunId != "run_abc123" {
		t.Errorf(`Expected RunId to be "run_abc123", got %v`, reloaded.RunId)
	}
	if len(reloaded.Messages) != 1 {
		t.Fatalf("Expected Messages length to be 1, got %d", len(reloaded.Messages))
	}
	assertEngineCheckpointStringField(t, reloaded.Messages[0], "Role", "user", "Messages[0].Role")
}

// TestEngineCheckpointFromJSONInvalid rejects malformed JSON instead of silently defaulting
func TestEngineCheckpointFromJSONInvalid(t *testing.T) {
	if _, err := prompty.EngineCheckpointFromJSON("{"); err == nil {
		t.Fatalf("Expected malformed JSON to fail")
	}
}

func assertEngineCheckpointStringField(t *testing.T, value interface{}, fieldName string, expected string, displayName string) {
	t.Helper()
	field := reflect.ValueOf(value)
	if field.Kind() == reflect.Pointer {
		if field.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		field = field.Elem()
	}
	if field.Kind() != reflect.Struct {
		t.Fatalf("Expected %s receiver to be a struct, got %T", displayName, value)
	}
	member := field.FieldByName(fieldName)
	if !member.IsValid() {
		t.Fatalf("Expected %s to have field %s, got %T", displayName, fieldName, value)
	}
	if member.Kind() == reflect.Pointer {
		if member.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		member = member.Elem()
	}
	if member.Kind() == reflect.Interface {
		if member.IsNil() {
			t.Fatalf("Expected %s to be populated", displayName)
		}
		member = member.Elem()
	}
	if member.Kind() != reflect.String {
		t.Fatalf("Expected %s to be a string field, got %s", displayName, member.Kind())
	}
	if got := member.String(); got != expected {
		t.Errorf("Expected %s to be %q, got %q", displayName, expected, got)
	}
}
