// Code generated by AgentSchema emitter; DO NOT EDIT.

package agentschema_test

import (
	"encoding/json"
	"testing"

	"gopkg.in/yaml.v3"

	"github.com/microsoft/agentschema-go/agentschema"
)

// TestMcpToolLoadJSON tests loading McpTool from JSON
func TestMcpToolLoadJSON(t *testing.T) {
	jsonData := `
{
  "kind": "mcp",
  "connection": {
    "kind": "reference"
  },
  "serverName": "My MCP Server",
  "serverDescription": "This tool allows access to MCP services.",
  "approvalMode": {
    "kind": "always"
  },
  "allowedTools": [
    "operation1",
    "operation2"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadMcpTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load McpTool: %v", err)
	}
	if instance.Kind != "mcp" {
		t.Errorf(`Expected Kind to be "mcp", got %v`, instance.Kind)
	}
	if instance.ServerName != "My MCP Server" {
		t.Errorf(`Expected ServerName to be "My MCP Server", got %v`, instance.ServerName)
	}
	if instance.ServerDescription == nil || *instance.ServerDescription != "This tool allows access to MCP services." {
		t.Errorf(`Expected ServerDescription to be "This tool allows access to MCP services.", got %v`, instance.ServerDescription)
	}
}

// TestMcpToolLoadYAML tests loading McpTool from YAML
func TestMcpToolLoadYAML(t *testing.T) {
	yamlData := `
kind: mcp
connection:
  kind: reference
serverName: My MCP Server
serverDescription: This tool allows access to MCP services.
approvalMode:
  kind: always
allowedTools:
  - operation1
  - operation2

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadMcpTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load McpTool: %v", err)
	}
	if instance.Kind != "mcp" {
		t.Errorf(`Expected Kind to be "mcp", got %v`, instance.Kind)
	}
	if instance.ServerName != "My MCP Server" {
		t.Errorf(`Expected ServerName to be "My MCP Server", got %v`, instance.ServerName)
	}
	if instance.ServerDescription == nil || *instance.ServerDescription != "This tool allows access to MCP services." {
		t.Errorf(`Expected ServerDescription to be "This tool allows access to MCP services.", got %v`, instance.ServerDescription)
	}
}

// TestMcpToolRoundtrip tests load -> save -> load produces equivalent data
func TestMcpToolRoundtrip(t *testing.T) {
	jsonData := `
{
  "kind": "mcp",
  "connection": {
    "kind": "reference"
  },
  "serverName": "My MCP Server",
  "serverDescription": "This tool allows access to MCP services.",
  "approvalMode": {
    "kind": "always"
  },
  "allowedTools": [
    "operation1",
    "operation2"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadMcpTool(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load McpTool: %v", err)
	}
	saveCtx := agentschema.NewSaveContext()
	savedData := instance.Save(saveCtx)

	reloaded, err := agentschema.LoadMcpTool(savedData, loadCtx)
	if err != nil {
		t.Fatalf("Failed to reload McpTool: %v", err)
	}
	if reloaded.Kind != "mcp" {
		t.Errorf(`Expected Kind to be "mcp", got %v`, reloaded.Kind)
	}
	if reloaded.ServerName != "My MCP Server" {
		t.Errorf(`Expected ServerName to be "My MCP Server", got %v`, reloaded.ServerName)
	}
	if reloaded.ServerDescription == nil || *reloaded.ServerDescription != "This tool allows access to MCP services." {
		t.Errorf(`Expected ServerDescription to be "This tool allows access to MCP services.", got %v`, reloaded.ServerDescription)
	}
}

// TestMcpToolToJSON tests that ToJSON produces valid JSON
func TestMcpToolToJSON(t *testing.T) {
	jsonData := `
{
  "kind": "mcp",
  "connection": {
    "kind": "reference"
  },
  "serverName": "My MCP Server",
  "serverDescription": "This tool allows access to MCP services.",
  "approvalMode": {
    "kind": "always"
  },
  "allowedTools": [
    "operation1",
    "operation2"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadMcpTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load McpTool: %v", err)
	}
	jsonOutput, err := instance.ToJSON()
	if err != nil {
		t.Fatalf("Failed to convert to JSON: %v", err)
	}

	var parsed map[string]interface{}
	if err := json.Unmarshal([]byte(jsonOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated JSON: %v", err)
	}
}

// TestMcpToolToYAML tests that ToYAML produces valid YAML
func TestMcpToolToYAML(t *testing.T) {
	jsonData := `
{
  "kind": "mcp",
  "connection": {
    "kind": "reference"
  },
  "serverName": "My MCP Server",
  "serverDescription": "This tool allows access to MCP services.",
  "approvalMode": {
    "kind": "always"
  },
  "allowedTools": [
    "operation1",
    "operation2"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadMcpTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load McpTool: %v", err)
	}
	yamlOutput, err := instance.ToYAML()
	if err != nil {
		t.Fatalf("Failed to convert to YAML: %v", err)
	}

	var parsed map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated YAML: %v", err)
	}
}
