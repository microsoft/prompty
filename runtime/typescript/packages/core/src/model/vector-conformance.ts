// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.
//
// Typed @vector conformance entrypoints (issue #511 Cat 1). Each exported
// function takes a consumer's REAL typed seam impl and runs the seam's
// baked-in vectors by calling seam methods directly — the idiomatic
// replacement for the stringly vector-adapters registry + per-op marshalling
// double. Typing the parameter as the emitted <Seam> interface makes tsc prove
// every op is implemented, so completeness is checked at COMPILE time, not by a
// runtime map lookup. Depends only on the model barrel + a tiny inlined
// assert (no Node builtins), so the shipped library stays runtime-neutral for
// web/edge/Deno consumers; emitted additively beside the stringly runner.
// See docs: reference/vector-conformance.

import type { DiscoveryConformance, LoadConformance, ModelInfo } from "./index";

// Runtime-neutral assert shims (no `node:assert`): the entrypoint ships inside
// the library, so it must not import Node builtins. `seamConformanceCanonical`
// gives order-insensitive deep equality by sorting object keys — matching
// `assert.deepStrictEqual` semantics for the plain-JSON vector payloads.
function seamConformanceAssert(condition: boolean, message: string): void {
  if (!condition) throw new Error(message);
}
function seamConformanceCanonical(value: unknown): string {
  if (value === null || typeof value !== "object") {
    return JSON.stringify(value ?? null);
  }
  if (Array.isArray(value)) {
    return `[${value.map(seamConformanceCanonical).join(",")}]`;
  }
  const record = value as Record<string, unknown>;
  const parts: string[] = [];
  for (const key of Object.keys(record).sort()) {
    const encoded = seamConformanceCanonical(record[key]);
    parts.push(`${JSON.stringify(key)}:${encoded}`);
  }
  return `{${parts.join(",")}}`;
}

/** Typed @vector conformance for DiscoveryConformance. Pass your real `implements DiscoveryConformance`;
    the `DiscoveryConformance` parameter makes tsc prove every op is implemented. Call from a
    test, e.g. `await runDiscoveryConformanceConformance(new DiscoveryConformanceImpl());`. */
export async function runDiscoveryConformanceConformance(
  seam: DiscoveryConformance,
): Promise<void> {
  // vector: anthropic_enrich_no_dataset_entry_is_noop
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"claude-sonnet-4-20250514","contextWindow":200000,"inputModalities":["text","image"],"outputModalities":["text"]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "anthropic_enrich_no_dataset_entry_is_noop misrouted",
    );
  }
  // vector: anthropic_model_full
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"claude-sonnet-4-20250514","displayName":"Claude Sonnet 4","ownedBy":"anthropic","contextWindow":200000,"inputModalities":["text","image"],"outputModalities":["text"],"additionalProperties":{"id":"claude-sonnet-4-20250514","display_name":"Claude Sonnet 4","context_length":200000,"input_modalities":["text","image"],"output_modalities":["text"],"type":"model"}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "anthropic_model_full misrouted",
    );
  }
  // vector: anthropic_model_minimal
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"claude-3-haiku-20240307","ownedBy":"anthropic","additionalProperties":{"id":"claude-3-haiku-20240307","type":"model"}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "anthropic_model_minimal misrouted",
    );
  }
  // vector: foundry_catalog_model
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"gpt-4","ownedBy":"openai","contextWindow":8192,"additionalProperties":{"id":"gpt-4","owned_by":"openai","maxContextLength":8192,"status":"succeeded"}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "foundry_catalog_model misrouted",
    );
  }
  // vector: foundry_deployment_flat_v1
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"gpt-4o","displayName":"gpt-4o","ownedBy":"OpenAI","additionalProperties":{"name":"gpt-4o","type":"ModelDeployment","modelName":"gpt-4o","modelVersion":"2024-08-06","modelPublisher":"OpenAI","capabilities":{"chatCompletion":"true"},"sku":{"name":"GlobalStandard","capacity":50}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "foundry_deployment_flat_v1 misrouted",
    );
  }
  // vector: foundry_deployment_nested_arm
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"my-gpt4","displayName":"gpt-4","ownedBy":"OpenAI","contextWindow":8192,"inputModalities":["text"],"outputModalities":["text"],"additionalProperties":{"name":"my-gpt4","properties":{"model":{"name":"gpt-4","publisher":"OpenAI","maxContextLength":8192},"capabilities":{"supportedInputModalities":["text"],"supportedOutputModalities":["text"]}}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "foundry_deployment_nested_arm misrouted",
    );
  }
  // vector: openai_enrich_dalle3_no_context_window
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"dall-e-3","inputModalities":["text"],"outputModalities":["image"]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_dalle3_no_context_window misrouted",
    );
  }
  // vector: openai_enrich_embedding_empty_output_modalities
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"text-embedding-3-small","contextWindow":8191,"inputModalities":["text"],"outputModalities":[]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_embedding_empty_output_modalities misrouted",
    );
  }
  // vector: openai_enrich_gpt4o
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"gpt-4o","contextWindow":128000,"inputModalities":["text","image"],"outputModalities":["text"]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_gpt4o misrouted",
    );
  }
  // vector: openai_enrich_gpt4o_mini_longest_prefix
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"gpt-4o-mini-2024-07-18","contextWindow":128000,"inputModalities":["text","image"],"outputModalities":["text"]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_gpt4o_mini_longest_prefix misrouted",
    );
  }
  // vector: openai_enrich_prefix_requires_token_boundary
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse('{"id":"gpt-45-future"}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_prefix_requires_token_boundary misrouted",
    );
  }
  // vector: openai_enrich_provider_context_window_wins
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"gpt-4o","contextWindow":999,"inputModalities":["text","image"],"outputModalities":["text"]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_provider_context_window_wins misrouted",
    );
  }
  // vector: openai_enrich_provider_empty_modalities_win
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse(
      '{"id":"gpt-4o","contextWindow":128000,"inputModalities":[],"outputModalities":["text"]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_provider_empty_modalities_win misrouted",
    );
  }
  // vector: openai_enrich_unknown_id_is_noop
  {
    const baseJson = "null";
    const base = JSON.parse(baseJson) as ModelInfo;
    const actual = await seam.enrich(base);
    const expected = JSON.parse('{"id":"ft:custom-model:acme::xyz"}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_enrich_unknown_id_is_noop misrouted",
    );
  }
  // vector: openai_model_basic
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"o3-mini","ownedBy":"system","additionalProperties":{"id":"o3-mini","object":"model","created":1715367049,"owned_by":"system"}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_model_basic misrouted",
    );
  }
  // vector: openai_model_finetune_no_owner
  {
    const rawJson = "null";
    const raw = JSON.parse(rawJson) as unknown;
    const actual = await seam.mapModel(raw);
    const expected = JSON.parse(
      '{"id":"ft:custom-model:acme::xyz","additionalProperties":{"id":"ft:custom-model:acme::xyz"}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "openai_model_finetune_no_owner misrouted",
    );
  }
}

/** Typed @vector conformance for LoadConformance. Pass your real `implements LoadConformance`;
    the `LoadConformance` parameter makes tsc prove every op is implemented. Call from a
    test, e.g. `await runLoadConformanceConformance(new LoadConformanceImpl());`. */
export async function runLoadConformanceConformance(
  seam: LoadConformance,
): Promise<void> {
  // vector: basic_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"basic-prompt","description":"A basic prompt for testing","metadata":{"authors":["testauthor"]},"model":{"id":"gpt-4","provider":"openai","apiType":"chat","connection":{"kind":"key","endpoint":"https://test.openai.com"},"options":{"temperature":0.7,"maxOutputTokens":1000}},"inputs":[{"name":"firstName","kind":"string","default":"Jane"},{"name":"lastName","kind":"string","default":"Doe"},{"name":"question","kind":"string","default":"What is the meaning of life?"}],"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}},"instructions":"system:\\nYou are an AI assistant who helps people find information.\\n\\n# Customer\\nYou are helping {{firstName}} {{lastName}} to find answers to their questions.\\n\\nuser:\\n{{question}}"}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "basic_load misrouted",
    );
  }
  // vector: connection_types_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"model":{"id":"gpt-4","connection":{"kind":"anonymous","endpoint":"https://localhost:8080"}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "connection_types_load misrouted",
    );
  }
  // vector: embedding_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"embedding","model":{"id":"text-embedding-3-small","provider":"openai","apiType":"embedding","connection":{"kind":"key","endpoint":"https://test.openai.com"}},"inputs":[{"name":"text","kind":"string","default":"Hello world"}],"instructions":"{{text}}"}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "embedding_load misrouted",
    );
  }
  // vector: empty_frontmatter_body_only
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"empty-fm","model":null,"inputs":null,"tools":null}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "empty_frontmatter_body_only misrouted",
    );
  }
  // vector: env_default
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"model":{"connection":{"kind":"key","endpoint":"fallback_value"}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "env_default misrouted",
    );
  }
  // vector: env_missing_error
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    let caught: unknown;
    try {
      await seam.load(source);
    } catch (error) {
      caught = error;
    }
    seamConformanceAssert(
      caught !== undefined,
      "env_missing_error: expected an error",
    );
  }
  // vector: env_resolution
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected =
      JSON.parse('{"model":{"connection":{"kind":"key","endpoint":"hello"}}}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "env_resolution misrouted",
    );
  }
  // vector: file_reference_in_tree_allowed
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"model":{"connection":{"kind":"key","endpoint":"https://in-tree.example.com"}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "file_reference_in_tree_allowed misrouted",
    );
  }
  // vector: file_reference_traversal_rejected
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    let caught: unknown;
    try {
      await seam.load(source);
    } catch (error) {
      caught = error;
    }
    seamConformanceAssert(
      caught !== undefined,
      "file_reference_traversal_rejected: expected an error",
    );
  }
  // vector: file_resolution
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"model":{"connection":{"kind":"key","endpoint":"https://shared.example.com"}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "file_resolution misrouted",
    );
  }
  // vector: image_apitype_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"image-gen","model":{"id":"dall-e-3","provider":"openai","apiType":"image","options":{"additionalProperties":{"size":"1024x1024","quality":"standard"}}}}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "image_apitype_load misrouted",
    );
  }
  // vector: input_scalar_shorthand
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"inputs":[{"name":"topic","kind":"string","default":"science"},{"name":"count","kind":"integer","default":5}]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "input_scalar_shorthand misrouted",
    );
  }
  // vector: input_validation_default_fill
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse('{"validated_inputs":{"name":"world"}}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "input_validation_default_fill misrouted",
    );
  }
  // vector: input_validation_example_not_used
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse('{"validated_inputs":{}}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "input_validation_example_not_used misrouted",
    );
  }
  // vector: input_validation_optional_omit
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse('{"validated_inputs":{}}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "input_validation_optional_omit misrouted",
    );
  }
  // vector: input_validation_required
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    let caught: unknown;
    try {
      await seam.load(source);
    } catch (error) {
      caught = error;
    }
    seamConformanceAssert(
      caught !== undefined,
      "input_validation_required: expected an error",
    );
  }
  // vector: instructions_from_body
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"instructions":"system:\\nYou are an AI assistant who helps people find information.\\n\\n# Customer\\nYou are helping {{firstName}} {{lastName}} to find answers to their questions.\\n\\nuser:\\n{{question}}"}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "instructions_from_body misrouted",
    );
  }
  // vector: invalid_frontmatter_error
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    let caught: unknown;
    try {
      await seam.load(source);
    } catch (error) {
      caught = error;
    }
    seamConformanceAssert(
      caught !== undefined,
      "invalid_frontmatter_error: expected an error",
    );
  }
  // vector: kind_always_prompt
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse('{"kind":"prompt"}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "kind_always_prompt misrouted",
    );
  }
  // vector: minimal_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"minimal","model":{"id":"gpt-4"},"instructions":"system:\\nHello world.","inputs":null,"outputs":null,"tools":null}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "minimal_load misrouted",
    );
  }
  // vector: missing_file_error
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    let caught: unknown;
    try {
      await seam.load(source);
    } catch (error) {
      caught = error;
    }
    seamConformanceAssert(
      caught !== undefined,
      "missing_file_error: expected an error",
    );
  }
  // vector: model_shorthand
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected =
      JSON.parse('{"kind":"prompt","name":"test","model":{"id":"gpt-4o"}}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "model_shorthand misrouted",
    );
  }
  // vector: structured_outputs_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"structured-output","outputs":[{"name":"temperature","kind":"integer","description":"Temperature in Fahrenheit","required":true},{"name":"condition","kind":"string","description":"Weather condition","required":true},{"name":"city","kind":"string","description":"City name","required":true}]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "structured_outputs_load misrouted",
    );
  }
  // vector: template_string_invalid
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    let caught: unknown;
    try {
      await seam.load(source);
    } catch (error) {
      caught = error;
    }
    seamConformanceAssert(
      caught !== undefined,
      "template_string_invalid: expected an error",
    );
  }
  // vector: tools_custom_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected =
      JSON.parse('{"tools":[{"name":"my_tool","kind":"my_provider"}]}');
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "tools_custom_load misrouted",
    );
  }
  // vector: tools_function_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"kind":"prompt","name":"function-tools","model":{"id":"gpt-4","apiType":"chat"},"tools":[{"name":"get_weather","kind":"function","description":"Get the current weather in a given location","strict":true,"parameters":[{"name":"location","kind":"string","description":"The city and state, e.g. San Francisco, CA"},{"name":"unit","kind":"string","description":"Temperature unit","enumValues":["celsius","fahrenheit"]}],"bindings":{"unit":{"input":"preferred_unit"}}}],"inputs":[{"name":"question","kind":"string","default":"What\'s the weather in Paris?"},{"name":"preferred_unit","kind":"string","default":"celsius"}],"instructions":"system:\\nYou are a helpful assistant with access to tools.\\n\\nuser:\\n{{question}}"}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "tools_function_load misrouted",
    );
  }
  // vector: tools_mcp_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"tools":[{"name":"filesystem","kind":"mcp","serverName":"fs-server"}]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "tools_mcp_load misrouted",
    );
  }
  // vector: tools_openapi_load
  {
    const sourceJson = "null";
    const source = JSON.parse(sourceJson) as unknown;
    const actual = await seam.load(source);
    const expected = JSON.parse(
      '{"tools":[{"name":"weather_api","kind":"openapi","specification":"./weather.json"}]}',
    );
    seamConformanceAssert(
      seamConformanceCanonical(JSON.parse(JSON.stringify(actual))) ===
        seamConformanceCanonical(expected),
      "tools_openapi_load misrouted",
    );
  }
}
