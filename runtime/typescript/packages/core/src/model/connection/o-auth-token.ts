// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";

export class OAuthToken {
  static readonly shorthandProperty: string | undefined = undefined;

  accessToken: string = "";
  tokenType: string = "";
  expiresIn: number = 0;
  refreshToken?: string | undefined;
  scope?: string | undefined;

  constructor(init?: Partial<OAuthToken>) {
    this.accessToken = init?.accessToken ?? "";
    this.tokenType = init?.tokenType ?? "";
    this.expiresIn = init?.expiresIn ?? 0;
    if (init?.refreshToken !== undefined) {
      this.refreshToken = init.refreshToken;
    }
    if (init?.scope !== undefined) {
      this.scope = init.scope;
    }
  }

  //#region Load Methods

  static load(data: Record<string, unknown>, context?: LoadContext): OAuthToken {
    context ??= new LoadContext();
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new OAuthToken();

    if (data["accessToken"] !== undefined && data["accessToken"] !== null) {
      instance.accessToken = String(data["accessToken"]);
    }
    if (data["tokenType"] !== undefined && data["tokenType"] !== null) {
      instance.tokenType = String(data["tokenType"]);
    }
    if (data["expiresIn"] !== undefined && data["expiresIn"] !== null) {
      instance.expiresIn = Number(data["expiresIn"]);
    }
    if (data["refreshToken"] !== undefined && data["refreshToken"] !== null) {
      instance.refreshToken = String(data["refreshToken"]);
    }
    if (data["scope"] !== undefined && data["scope"] !== null) {
      instance.scope = String(data["scope"]);
    }

    if (context) {
      return context.processOutput(instance) as OAuthToken;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.accessToken !== undefined && obj.accessToken !== null) {
      result["accessToken"] = obj.accessToken;
    }
    if (obj.tokenType !== undefined && obj.tokenType !== null) {
      result["tokenType"] = obj.tokenType;
    }
    if (obj.expiresIn !== undefined && obj.expiresIn !== null) {
      result["expiresIn"] = obj.expiresIn;
    }
    if (obj.refreshToken !== undefined && obj.refreshToken !== null) {
      result["refreshToken"] = obj.refreshToken;
    }
    if (obj.scope !== undefined && obj.scope !== null) {
      result["scope"] = obj.scope;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toWire(provider: string): Record<string, unknown> {
    const data = this.save();
    const result: Record<string, unknown> = {};
    const wireMap: Record<string, Record<string, string>> = {
      "accessToken": { "foundry": "access_token" },
      "tokenType": { "foundry": "token_type" },
      "expiresIn": { "foundry": "expires_in" },
      "refreshToken": { "foundry": "refresh_token" },
      "scope": { "foundry": "scope" },
    };
    for (const [key, value] of Object.entries(data)) {
      const mapping = wireMap[key];
      if (mapping?.[provider]) {
        result[mapping[provider]] = value;
      }
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): OAuthToken {
    const data = JSON.parse(json);
    return OAuthToken.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): OAuthToken {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return OAuthToken.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
