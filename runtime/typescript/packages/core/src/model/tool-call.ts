// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 * A tool call requested by the LLM. Contains the function name and serialized
 * arguments that should be dispatched to the appropriate tool handler.
 *
 */
export class ToolCall {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * The unique identifier of the tool call
   */
  id: string = "";

  /**
   * The name of the tool to call
   */
  name: string = "";

  /**
   * The serialized JSON arguments for the tool call
   */
  arguments: string = "";

  /**
   * Initializes a new instance of ToolCall.
   */
  constructor(init?: Partial<ToolCall>) {
    this.id = init?.id ?? "";

    this.name = init?.name ?? "";

    this.arguments = init?.arguments ?? "";
  }

  //#region Load Methods

  /**
   * Load a ToolCall instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ToolCall instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): ToolCall {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new ToolCall();

    if (data["id"] !== undefined && data["id"] !== null) {
      instance.id = String(data["id"]);
    }

    if (data["name"] !== undefined && data["name"] !== null) {
      instance.name = String(data["name"]);
    }

    if (data["arguments"] !== undefined && data["arguments"] !== null) {
      instance.arguments = String(data["arguments"]);
    }

    if (context) {
      return context.processOutput(instance) as ToolCall;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the ToolCall instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as ToolCall) : this;

    const result: Record<string, unknown> = {};

    if (obj.id !== undefined && obj.id !== null) {
      result["id"] = obj.id;
    }

    if (obj.name !== undefined && obj.name !== null) {
      result["name"] = obj.name;
    }

    if (obj.arguments !== undefined && obj.arguments !== null) {
      result["arguments"] = obj.arguments;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the ToolCall instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the ToolCall instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a ToolCall instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ToolCall instance.
   */
  static fromJson(json: string, context?: LoadContext): ToolCall {
    const data = JSON.parse(json);

    return ToolCall.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a ToolCall instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ToolCall instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): ToolCall {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return ToolCall.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
