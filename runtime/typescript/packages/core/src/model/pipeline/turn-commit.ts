// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { InvocationContextState } from "./invocation-context-state";
import { Message } from "../conversation/message";
import { ModelReconciliationState } from "./model-reconciliation-state";

export type EngineTurnStatus =
  | "success"
  | "failed"
  | "cancelled"
  | "reconciliation_required";

export class TurnCommit {
  static readonly shorthandProperty: string | undefined = undefined;

  sessionId: string = "";
  turnId: string = "";
  status: EngineTurnStatus = "success";
  output?: unknown | undefined;
  messages: Message[] = [];
  iterations: number = 0;
  lastSequence: number = 0;
  contextState!: InvocationContextState;
  modelReconciliation?: ModelReconciliationState | undefined;

  constructor(init?: Partial<TurnCommit>) {
    this.sessionId = init?.sessionId ?? "";
    this.turnId = init?.turnId ?? "";
    this.status = init?.status ?? "success";
    if (init?.output !== undefined) {
      this.output = init.output;
    }
    this.messages = init?.messages ?? [];
    this.iterations = init?.iterations ?? 0;
    this.lastSequence = init?.lastSequence ?? 0;
    if (init?.contextState !== undefined) {
      this.contextState = init.contextState;
    }
    if (init?.modelReconciliation !== undefined) {
      this.modelReconciliation = init.modelReconciliation;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): TurnCommit {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new TurnCommit();

    if (data["sessionId"] !== undefined && data["sessionId"] !== null) {
      instance.sessionId = String(data["sessionId"]);
    }
    if (data["turnId"] !== undefined && data["turnId"] !== null) {
      instance.turnId = String(data["turnId"]);
    }
    if (data["status"] !== undefined && data["status"] !== null) {
      instance.status = String(data["status"]) as EngineTurnStatus;
    }
    if (data["output"] !== undefined && data["output"] !== null) {
      instance.output = data["output"] as unknown;
    }
    if (data["messages"] !== undefined && data["messages"] !== null) {
      instance.messages = TurnCommit.loadMessages(
        data["messages"] as unknown[],
        context,
      );
    }
    if (data["iterations"] !== undefined && data["iterations"] !== null) {
      instance.iterations = Number(data["iterations"]);
    }
    if (data["lastSequence"] !== undefined && data["lastSequence"] !== null) {
      instance.lastSequence = Number(data["lastSequence"]);
    }
    if (data["contextState"] !== undefined && data["contextState"] !== null) {
      instance.contextState = InvocationContextState.load(
        data["contextState"] as Record<string, unknown>,
        context,
      );
    }
    if (
      data["modelReconciliation"] !== undefined &&
      data["modelReconciliation"] !== null
    ) {
      instance.modelReconciliation = ModelReconciliationState.load(
        data["modelReconciliation"] as Record<string, unknown>,
        context,
      );
    }

    if (context) {
      return context.processOutput(instance) as TurnCommit;
    }
    return instance;
  }

  static loadMessages(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): Message[] {
    if (!Array.isArray(data)) {
      // Convert dict/object format to array format
      const result: Record<string, unknown>[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push({ name: k, ...(v as Record<string, unknown>) });
        } else {
          result.push({ name: k, role: v });
        }
      }
      data = result;
    }
    return data.map((item) =>
      Message.load(item as Record<string, unknown>, context),
    );
  }

  static saveMessages(
    items: Message[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.sessionId !== undefined && obj.sessionId !== null) {
      result["sessionId"] = obj.sessionId;
    }
    if (obj.turnId !== undefined && obj.turnId !== null) {
      result["turnId"] = obj.turnId;
    }
    if (obj.status !== undefined && obj.status !== null) {
      result["status"] = obj.status;
    }
    if (obj.output !== undefined && obj.output !== null) {
      result["output"] = obj.output;
    }
    if (obj.messages !== undefined && obj.messages !== null) {
      result["messages"] = TurnCommit.saveMessages(obj.messages, context);
    }
    if (obj.iterations !== undefined && obj.iterations !== null) {
      result["iterations"] = obj.iterations;
    }
    if (obj.lastSequence !== undefined && obj.lastSequence !== null) {
      result["lastSequence"] = obj.lastSequence;
    }
    if (obj.contextState !== undefined && obj.contextState !== null) {
      result["contextState"] = obj.contextState.save(context);
    }
    if (
      obj.modelReconciliation !== undefined &&
      obj.modelReconciliation !== null
    ) {
      result["modelReconciliation"] = obj.modelReconciliation.save(context);
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): TurnCommit {
    const data = JSON.parse(json);
    return TurnCommit.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): TurnCommit {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return TurnCommit.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
