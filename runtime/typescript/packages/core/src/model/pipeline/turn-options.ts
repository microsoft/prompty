// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { CompactionConfig } from "./compaction-config";

export class TurnOptions {
  static readonly shorthandProperty: string | undefined = undefined;

  maxIterations?: number | undefined;
  maxLlmRetries?: number | undefined;
  contextBudget?: number | undefined;
  parallelToolCalls?: boolean | undefined;
  raw?: boolean | undefined;
  turn?: number | undefined;
  compaction?: CompactionConfig | undefined;

  constructor(init?: Partial<TurnOptions>) {
    if (init?.maxIterations !== undefined) {
      this.maxIterations = init.maxIterations;
    }
    if (init?.maxLlmRetries !== undefined) {
      this.maxLlmRetries = init.maxLlmRetries;
    }
    if (init?.contextBudget !== undefined) {
      this.contextBudget = init.contextBudget;
    }
    if (init?.parallelToolCalls !== undefined) {
      this.parallelToolCalls = init.parallelToolCalls;
    }
    if (init?.raw !== undefined) {
      this.raw = init.raw;
    }
    if (init?.turn !== undefined) {
      this.turn = init.turn;
    }
    if (init?.compaction !== undefined) {
      this.compaction = init.compaction;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): TurnOptions {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new TurnOptions();

    if (data["maxIterations"] !== undefined && data["maxIterations"] !== null) {
      instance.maxIterations = Number(data["maxIterations"]);
    }
    if (data["maxLlmRetries"] !== undefined && data["maxLlmRetries"] !== null) {
      instance.maxLlmRetries = Number(data["maxLlmRetries"]);
    }
    if (data["contextBudget"] !== undefined && data["contextBudget"] !== null) {
      instance.contextBudget = Number(data["contextBudget"]);
    }
    if (
      data["parallelToolCalls"] !== undefined &&
      data["parallelToolCalls"] !== null
    ) {
      instance.parallelToolCalls = Boolean(data["parallelToolCalls"]);
    }
    if (data["raw"] !== undefined && data["raw"] !== null) {
      instance.raw = Boolean(data["raw"]);
    }
    if (data["turn"] !== undefined && data["turn"] !== null) {
      instance.turn = Number(data["turn"]);
    }
    if (data["compaction"] !== undefined && data["compaction"] !== null) {
      instance.compaction = CompactionConfig.load(
        data["compaction"] as Record<string, unknown>,
        context,
      );
    }

    if (context) {
      return context.processOutput(instance) as TurnOptions;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.maxIterations !== undefined && obj.maxIterations !== null) {
      result["maxIterations"] = obj.maxIterations;
    }
    if (obj.maxLlmRetries !== undefined && obj.maxLlmRetries !== null) {
      result["maxLlmRetries"] = obj.maxLlmRetries;
    }
    if (obj.contextBudget !== undefined && obj.contextBudget !== null) {
      result["contextBudget"] = obj.contextBudget;
    }
    if (obj.parallelToolCalls !== undefined && obj.parallelToolCalls !== null) {
      result["parallelToolCalls"] = obj.parallelToolCalls;
    }
    if (obj.raw !== undefined && obj.raw !== null) {
      result["raw"] = obj.raw;
    }
    if (obj.turn !== undefined && obj.turn !== null) {
      result["turn"] = obj.turn;
    }
    if (obj.compaction !== undefined && obj.compaction !== null) {
      result["compaction"] = obj.compaction.save(context);
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): TurnOptions {
    const data = JSON.parse(json);
    return TurnOptions.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): TurnOptions {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return TurnOptions.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
