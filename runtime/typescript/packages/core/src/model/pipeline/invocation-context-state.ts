// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { DelegatedStateReference } from "./delegated-state-reference";

export type InvocationContextPortability = "portable" | "delegated" | "opaque";

export class InvocationContextState {
  static readonly shorthandProperty: string | undefined = undefined;

  portability: InvocationContextPortability = "portable";
  delegatedState?: DelegatedStateReference[];

  constructor(init?: Partial<InvocationContextState>) {
    this.portability = init?.portability ?? "portable";
    if (init?.delegatedState !== undefined) {
      this.delegatedState = init.delegatedState;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): InvocationContextState {
    context ??= new LoadContext();
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new InvocationContextState();

    if (data["portability"] !== undefined && data["portability"] !== null) {
      instance.portability = String(
        data["portability"],
      ) as InvocationContextPortability;
    }
    if (
      data["delegatedState"] !== undefined &&
      data["delegatedState"] !== null
    ) {
      instance.delegatedState = InvocationContextState.loadDelegatedState(
        data["delegatedState"] as unknown[],
        context.at("delegatedState"),
      );
    }

    if (context) {
      return context.processOutput(instance) as InvocationContextState;
    }
    return instance;
  }

  static loadDelegatedState(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): DelegatedStateReference[] {
    context ??= new LoadContext({ path: "delegatedState" });
    if (!Array.isArray(data)) {
      const result: DelegatedStateReference[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            DelegatedStateReference.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(
            DelegatedStateReference.load(
              { name: k, provider: v },
              context.at(k),
            ),
          );
        }
      }
      return result;
    }
    return data.map((item, index) =>
      DelegatedStateReference.load(
        item as Record<string, unknown>,
        context.atIndex(index),
      ),
    );
  }

  static saveDelegatedState(
    items: DelegatedStateReference[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.portability !== undefined && obj.portability !== null) {
      result["portability"] = obj.portability;
    }
    if (obj.delegatedState !== undefined && obj.delegatedState !== null) {
      result["delegatedState"] = InvocationContextState.saveDelegatedState(
        obj.delegatedState,
        context,
      );
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): InvocationContextState {
    const data = JSON.parse(json);
    return InvocationContextState.load(
      data as Record<string, unknown>,
      context,
    );
  }

  static fromYaml(yaml: string, context?: LoadContext): InvocationContextState {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return InvocationContextState.load(
      data as Record<string, unknown>,
      context,
    );
  }

  //#endregion
}
