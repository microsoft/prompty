// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../../context";

export class DeviceAuthorization {
  static readonly shorthandProperty: string | undefined = undefined;

  deviceCode: string = "";
  userCode: string = "";
  verificationUri: string = "";
  expiresIn: number = 0;
  interval: number = 0;
  message: string = "";

  constructor(init?: Partial<DeviceAuthorization>) {
    this.deviceCode = init?.deviceCode ?? "";
    this.userCode = init?.userCode ?? "";
    this.verificationUri = init?.verificationUri ?? "";
    this.expiresIn = init?.expiresIn ?? 0;
    this.interval = init?.interval ?? 0;
    this.message = init?.message ?? "";
  }

  //#region Load Methods

  static load(data: Record<string, unknown>, context?: LoadContext): DeviceAuthorization {
    context ??= new LoadContext();
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new DeviceAuthorization();

    if (data["deviceCode"] !== undefined && data["deviceCode"] !== null) {
      instance.deviceCode = String(data["deviceCode"]);
    }
    if (data["userCode"] !== undefined && data["userCode"] !== null) {
      instance.userCode = String(data["userCode"]);
    }
    if (data["verificationUri"] !== undefined && data["verificationUri"] !== null) {
      instance.verificationUri = String(data["verificationUri"]);
    }
    if (data["expiresIn"] !== undefined && data["expiresIn"] !== null) {
      instance.expiresIn = Number(data["expiresIn"]);
    }
    if (data["interval"] !== undefined && data["interval"] !== null) {
      instance.interval = Number(data["interval"]);
    }
    if (data["message"] !== undefined && data["message"] !== null) {
      instance.message = String(data["message"]);
    }

    if (context) {
      return context.processOutput(instance) as DeviceAuthorization;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.deviceCode !== undefined && obj.deviceCode !== null) {
      result["deviceCode"] = obj.deviceCode;
    }
    if (obj.userCode !== undefined && obj.userCode !== null) {
      result["userCode"] = obj.userCode;
    }
    if (obj.verificationUri !== undefined && obj.verificationUri !== null) {
      result["verificationUri"] = obj.verificationUri;
    }
    if (obj.expiresIn !== undefined && obj.expiresIn !== null) {
      result["expiresIn"] = obj.expiresIn;
    }
    if (obj.interval !== undefined && obj.interval !== null) {
      result["interval"] = obj.interval;
    }
    if (obj.message !== undefined && obj.message !== null) {
      result["message"] = obj.message;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toWire(provider: string): Record<string, unknown> {
    const data = this.save();
    const result: Record<string, unknown> = {};
    const wireMap: Record<string, Record<string, string>> = {
      "deviceCode": { "foundry": "device_code" },
      "userCode": { "foundry": "user_code" },
      "verificationUri": { "foundry": "verification_uri" },
      "expiresIn": { "foundry": "expires_in" },
      "interval": { "foundry": "interval" },
      "message": { "foundry": "message" },
    };
    for (const [key, value] of Object.entries(data)) {
      const mapping = wireMap[key];
      if (mapping?.[provider]) {
        result[mapping[provider]] = value;
      }
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): DeviceAuthorization {
    const data = JSON.parse(json);
    return DeviceAuthorization.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): DeviceAuthorization {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return DeviceAuthorization.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
