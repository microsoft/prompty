// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";

export class AnthropicUsage {
  static readonly shorthandProperty: string | undefined = undefined;

  input_tokens: number = 0;
  output_tokens: number = 0;

  constructor(init?: Partial<AnthropicUsage>) {
    this.input_tokens = init?.input_tokens ?? 0;
    this.output_tokens = init?.output_tokens ?? 0;
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): AnthropicUsage {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new AnthropicUsage();

    if (data["input_tokens"] !== undefined && data["input_tokens"] !== null) {
      instance.input_tokens = Number(data["input_tokens"]);
    }
    if (data["output_tokens"] !== undefined && data["output_tokens"] !== null) {
      instance.output_tokens = Number(data["output_tokens"]);
    }

    if (context) {
      return context.processOutput(instance) as AnthropicUsage;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.input_tokens !== undefined && obj.input_tokens !== null) {
      result["input_tokens"] = obj.input_tokens;
    }
    if (obj.output_tokens !== undefined && obj.output_tokens !== null) {
      result["output_tokens"] = obj.output_tokens;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): AnthropicUsage {
    const data = JSON.parse(json);
    return AnthropicUsage.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): AnthropicUsage {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return AnthropicUsage.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
