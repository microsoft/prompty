// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { AnthropicToolDefinition } from "./anthropic-tool-definition";
import { AnthropicWireMessage } from "./anthropic-wire-message";

export class AnthropicMessagesRequest {
  static readonly shorthandProperty: string | undefined = undefined;

  model: string = "";
  messages: AnthropicWireMessage[] = [];
  max_tokens: number = 0;
  system?: string | undefined;
  temperature?: number | undefined;
  top_p?: number | undefined;
  top_k?: number | undefined;
  stop_sequences?: string[] = [];
  tools?: AnthropicToolDefinition[] = [];

  constructor(init?: Partial<AnthropicMessagesRequest>) {
    this.model = init?.model ?? "";
    this.messages = init?.messages ?? [];
    this.max_tokens = init?.max_tokens ?? 0;
    if (init?.system !== undefined) {
      this.system = init.system;
    }
    if (init?.temperature !== undefined) {
      this.temperature = init.temperature;
    }
    if (init?.top_p !== undefined) {
      this.top_p = init.top_p;
    }
    if (init?.top_k !== undefined) {
      this.top_k = init.top_k;
    }
    if (init?.stop_sequences !== undefined) {
      this.stop_sequences = init.stop_sequences;
    }
    if (init?.tools !== undefined) {
      this.tools = init.tools;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): AnthropicMessagesRequest {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new AnthropicMessagesRequest();

    if (data["model"] !== undefined && data["model"] !== null) {
      instance.model = String(data["model"]);
    }
    if (data["messages"] !== undefined && data["messages"] !== null) {
      instance.messages = AnthropicMessagesRequest.loadMessages(
        data["messages"] as unknown[],
        context,
      );
    }
    if (data["max_tokens"] !== undefined && data["max_tokens"] !== null) {
      instance.max_tokens = Number(data["max_tokens"]);
    }
    if (data["system"] !== undefined && data["system"] !== null) {
      instance.system = String(data["system"]);
    }
    if (data["temperature"] !== undefined && data["temperature"] !== null) {
      instance.temperature = Number(data["temperature"]);
    }
    if (data["top_p"] !== undefined && data["top_p"] !== null) {
      instance.top_p = Number(data["top_p"]);
    }
    if (data["top_k"] !== undefined && data["top_k"] !== null) {
      instance.top_k = Number(data["top_k"]);
    }
    if (
      data["stop_sequences"] !== undefined &&
      data["stop_sequences"] !== null
    ) {
      instance.stop_sequences = (data["stop_sequences"] as unknown[]).map((v) =>
        String(v),
      );
    }
    if (data["tools"] !== undefined && data["tools"] !== null) {
      instance.tools = AnthropicMessagesRequest.loadTools(
        data["tools"] as unknown[],
        context,
      );
    }

    if (context) {
      return context.processOutput(instance) as AnthropicMessagesRequest;
    }
    return instance;
  }

  static loadMessages(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): AnthropicWireMessage[] {
    if (!Array.isArray(data)) {
      // Convert dict/object format to array format
      const result: Record<string, unknown>[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push({ name: k, ...(v as Record<string, unknown>) });
        } else {
          result.push({ name: k, role: v });
        }
      }
      data = result;
    }
    return data.map((item) =>
      AnthropicWireMessage.load(item as Record<string, unknown>, context),
    );
  }

  static saveMessages(
    items: AnthropicWireMessage[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  static loadTools(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): AnthropicToolDefinition[] {
    if (!Array.isArray(data)) {
      // Convert dict/object format to array format
      const result: Record<string, unknown>[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push({ name: k, ...(v as Record<string, unknown>) });
        } else {
          result.push({ name: k, description: v });
        }
      }
      data = result;
    }
    return data.map((item) =>
      AnthropicToolDefinition.load(item as Record<string, unknown>, context),
    );
  }

  static saveTools(
    items: AnthropicToolDefinition[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    if (context.collectionFormat === "array") {
      return items.map((item) => item.save(context));
    }

    // Object format: use name as key
    const result: Record<string, unknown> = {};
    for (const item of items) {
      const itemData = item.save(context) as Record<string, unknown>;
      const name = itemData["name"] as string | undefined;
      delete itemData["name"];
      if (name) {
        // Check if we can use shorthand (only primary property set)
        const shorthand = (item.constructor as typeof AnthropicToolDefinition)
          .shorthandProperty;
        if (
          context.useShorthand &&
          shorthand &&
          Object.keys(itemData).length === 1 &&
          shorthand in itemData
        ) {
          result[name] = itemData[shorthand];
          continue;
        }
        result[name] = itemData;
      } else {
        // No name, fall back to array format for this item
        if (!result["_unnamed"]) {
          result["_unnamed"] = [];
        }
        (result["_unnamed"] as unknown[]).push(itemData);
      }
    }
    return result;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.model !== undefined && obj.model !== null) {
      result["model"] = obj.model;
    }
    if (obj.messages !== undefined && obj.messages !== null) {
      result["messages"] = AnthropicMessagesRequest.saveMessages(
        obj.messages,
        context,
      );
    }
    if (obj.max_tokens !== undefined && obj.max_tokens !== null) {
      result["max_tokens"] = obj.max_tokens;
    }
    if (obj.system !== undefined && obj.system !== null) {
      result["system"] = obj.system;
    }
    if (obj.temperature !== undefined && obj.temperature !== null) {
      result["temperature"] = obj.temperature;
    }
    if (obj.top_p !== undefined && obj.top_p !== null) {
      result["top_p"] = obj.top_p;
    }
    if (obj.top_k !== undefined && obj.top_k !== null) {
      result["top_k"] = obj.top_k;
    }
    if (obj.stop_sequences !== undefined && obj.stop_sequences !== null) {
      result["stop_sequences"] = obj.stop_sequences;
    }
    if (obj.tools !== undefined && obj.tools !== null) {
      result["tools"] = AnthropicMessagesRequest.saveTools(obj.tools, context);
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(
    json: string,
    context?: LoadContext,
  ): AnthropicMessagesRequest {
    const data = JSON.parse(json);
    return AnthropicMessagesRequest.load(
      data as Record<string, unknown>,
      context,
    );
  }

  static fromYaml(
    yaml: string,
    context?: LoadContext,
  ): AnthropicMessagesRequest {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return AnthropicMessagesRequest.load(
      data as Record<string, unknown>,
      context,
    );
  }

  //#endregion
}
