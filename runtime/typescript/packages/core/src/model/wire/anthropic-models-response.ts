// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { AnthropicModelInfo } from "./anthropic-model-info";

export class AnthropicModelsResponse {
  static readonly shorthandProperty: string | undefined = undefined;

  data: AnthropicModelInfo[] = [];
  first_id?: string | undefined;
  has_more?: boolean | undefined;
  last_id?: string | undefined;

  constructor(init?: Partial<AnthropicModelsResponse>) {
    this.data = init?.data ?? [];
    if (init?.first_id !== undefined) {
      this.first_id = init.first_id;
    }
    if (init?.has_more !== undefined) {
      this.has_more = init.has_more;
    }
    if (init?.last_id !== undefined) {
      this.last_id = init.last_id;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): AnthropicModelsResponse {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new AnthropicModelsResponse();

    if (data["data"] !== undefined && data["data"] !== null) {
      instance.data = AnthropicModelsResponse.loadData(
        data["data"] as unknown[],
        context,
      );
    }
    if (data["first_id"] !== undefined && data["first_id"] !== null) {
      instance.first_id = String(data["first_id"]);
    }
    if (data["has_more"] !== undefined && data["has_more"] !== null) {
      instance.has_more = Boolean(data["has_more"]);
    }
    if (data["last_id"] !== undefined && data["last_id"] !== null) {
      instance.last_id = String(data["last_id"]);
    }

    if (context) {
      return context.processOutput(instance) as AnthropicModelsResponse;
    }
    return instance;
  }

  static loadData(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): AnthropicModelInfo[] {
    if (!Array.isArray(data)) {
      // Convert dict/object format to array format
      const result: Record<string, unknown>[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push({ name: k, ...(v as Record<string, unknown>) });
        } else {
          result.push({ name: k, id: v });
        }
      }
      data = result;
    }
    return data.map((item) =>
      AnthropicModelInfo.load(item as Record<string, unknown>, context),
    );
  }

  static saveData(
    items: AnthropicModelInfo[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.data !== undefined && obj.data !== null) {
      result["data"] = AnthropicModelsResponse.saveData(obj.data, context);
    }
    if (obj.first_id !== undefined && obj.first_id !== null) {
      result["first_id"] = obj.first_id;
    }
    if (obj.has_more !== undefined && obj.has_more !== null) {
      result["has_more"] = obj.has_more;
    }
    if (obj.last_id !== undefined && obj.last_id !== null) {
      result["last_id"] = obj.last_id;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(
    json: string,
    context?: LoadContext,
  ): AnthropicModelsResponse {
    const data = JSON.parse(json);
    return AnthropicModelsResponse.load(
      data as Record<string, unknown>,
      context,
    );
  }

  static fromYaml(
    yaml: string,
    context?: LoadContext,
  ): AnthropicModelsResponse {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return AnthropicModelsResponse.load(
      data as Record<string, unknown>,
      context,
    );
  }

  //#endregion
}
