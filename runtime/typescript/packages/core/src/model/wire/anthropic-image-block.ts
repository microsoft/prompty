// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { AnthropicImageSource } from "./anthropic-image-source";

export class AnthropicImageBlock {
  static readonly shorthandProperty: string | undefined = undefined;

  type: string = "image";
  source!: AnthropicImageSource;

  constructor(init?: Partial<AnthropicImageBlock>) {
    this.type = init?.type ?? "image";
    if (init?.source !== undefined) {
      this.source = init.source;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): AnthropicImageBlock {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new AnthropicImageBlock();

    if (data["type"] !== undefined && data["type"] !== null) {
      instance.type = String(data["type"]);
    }
    if (data["source"] !== undefined && data["source"] !== null) {
      instance.source = AnthropicImageSource.load(
        data["source"] as Record<string, unknown>,
        context,
      );
    }

    if (context) {
      return context.processOutput(instance) as AnthropicImageBlock;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.type !== undefined && obj.type !== null) {
      result["type"] = obj.type;
    }
    if (obj.source !== undefined && obj.source !== null) {
      result["source"] = obj.source.save(context);
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): AnthropicImageBlock {
    const data = JSON.parse(json);
    return AnthropicImageBlock.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): AnthropicImageBlock {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return AnthropicImageBlock.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
