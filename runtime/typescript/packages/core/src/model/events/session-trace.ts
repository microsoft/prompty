// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { Checkpoint } from "./checkpoint";
import { SessionEvent } from "./session-event";
import { SessionFileRef } from "./session-file-ref";
import { SessionRef } from "./session-ref";
import { SessionSummary } from "./session-summary";
import { TrajectoryEvent } from "./trajectory-event";
import { TurnTrace } from "./turn-trace";

export class SessionTrace {
  static readonly shorthandProperty: string | undefined = undefined;

  version: string = "1";
  runtime?: string | undefined;
  promptyVersion?: string | undefined;
  sessionId?: string | undefined;
  events: SessionEvent[] = [];
  turns?: TurnTrace[];
  checkpoints?: Checkpoint[];
  trajectory?: TrajectoryEvent[];
  files?: SessionFileRef[];
  refs?: SessionRef[];
  summary?: SessionSummary | undefined;

  constructor(init?: Partial<SessionTrace>) {
    this.version = init?.version ?? "1";
    if (init?.runtime !== undefined) {
      this.runtime = init.runtime;
    }
    if (init?.promptyVersion !== undefined) {
      this.promptyVersion = init.promptyVersion;
    }
    if (init?.sessionId !== undefined) {
      this.sessionId = init.sessionId;
    }
    this.events = init?.events ?? [];
    if (init?.turns !== undefined) {
      this.turns = init.turns;
    }
    if (init?.checkpoints !== undefined) {
      this.checkpoints = init.checkpoints;
    }
    if (init?.trajectory !== undefined) {
      this.trajectory = init.trajectory;
    }
    if (init?.files !== undefined) {
      this.files = init.files;
    }
    if (init?.refs !== undefined) {
      this.refs = init.refs;
    }
    if (init?.summary !== undefined) {
      this.summary = init.summary;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): SessionTrace {
    context ??= new LoadContext();
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new SessionTrace();

    if (data["version"] !== undefined && data["version"] !== null) {
      instance.version = String(data["version"]);
    }
    if (data["runtime"] !== undefined && data["runtime"] !== null) {
      instance.runtime = String(data["runtime"]);
    }
    if (
      data["promptyVersion"] !== undefined &&
      data["promptyVersion"] !== null
    ) {
      instance.promptyVersion = String(data["promptyVersion"]);
    }
    if (data["sessionId"] !== undefined && data["sessionId"] !== null) {
      instance.sessionId = String(data["sessionId"]);
    }
    if (data["events"] !== undefined && data["events"] !== null) {
      instance.events = SessionTrace.loadEvents(
        data["events"] as unknown[],
        context.at("events"),
      );
    }
    if (data["turns"] !== undefined && data["turns"] !== null) {
      instance.turns = SessionTrace.loadTurns(
        data["turns"] as unknown[],
        context.at("turns"),
      );
    }
    if (data["checkpoints"] !== undefined && data["checkpoints"] !== null) {
      instance.checkpoints = SessionTrace.loadCheckpoints(
        data["checkpoints"] as unknown[],
        context.at("checkpoints"),
      );
    }
    if (data["trajectory"] !== undefined && data["trajectory"] !== null) {
      instance.trajectory = SessionTrace.loadTrajectory(
        data["trajectory"] as unknown[],
        context.at("trajectory"),
      );
    }
    if (data["files"] !== undefined && data["files"] !== null) {
      instance.files = SessionTrace.loadFiles(
        data["files"] as unknown[],
        context.at("files"),
      );
    }
    if (data["refs"] !== undefined && data["refs"] !== null) {
      instance.refs = SessionTrace.loadRefs(
        data["refs"] as unknown[],
        context.at("refs"),
      );
    }
    if (data["summary"] !== undefined && data["summary"] !== null) {
      instance.summary = SessionSummary.load(
        data["summary"] as Record<string, unknown>,
        context.at("summary"),
      );
    }

    if (context) {
      return context.processOutput(instance) as SessionTrace;
    }
    return instance;
  }

  static loadEvents(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): SessionEvent[] {
    context ??= new LoadContext({ path: "events" });
    if (!Array.isArray(data)) {
      const result: SessionEvent[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            SessionEvent.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(SessionEvent.load({ name: k, id: v }, context.at(k)));
        }
      }
      return result;
    }
    return data.map((item, index) =>
      SessionEvent.load(
        item as Record<string, unknown>,
        context.atIndex(index),
      ),
    );
  }

  static saveEvents(
    items: SessionEvent[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  static loadTurns(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): TurnTrace[] {
    context ??= new LoadContext({ path: "turns" });
    if (!Array.isArray(data)) {
      const result: TurnTrace[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            TurnTrace.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(TurnTrace.load({ name: k, version: v }, context.at(k)));
        }
      }
      return result;
    }
    return data.map((item, index) =>
      TurnTrace.load(item as Record<string, unknown>, context.atIndex(index)),
    );
  }

  static saveTurns(
    items: TurnTrace[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  static loadCheckpoints(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): Checkpoint[] {
    context ??= new LoadContext({ path: "checkpoints" });
    if (!Array.isArray(data)) {
      const result: Checkpoint[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            Checkpoint.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(Checkpoint.load({ name: k, id: v }, context.at(k)));
        }
      }
      return result;
    }
    return data.map((item, index) =>
      Checkpoint.load(item as Record<string, unknown>, context.atIndex(index)),
    );
  }

  static saveCheckpoints(
    items: Checkpoint[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  static loadTrajectory(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): TrajectoryEvent[] {
    context ??= new LoadContext({ path: "trajectory" });
    if (!Array.isArray(data)) {
      const result: TrajectoryEvent[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            TrajectoryEvent.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(TrajectoryEvent.load({ name: k, id: v }, context.at(k)));
        }
      }
      return result;
    }
    return data.map((item, index) =>
      TrajectoryEvent.load(
        item as Record<string, unknown>,
        context.atIndex(index),
      ),
    );
  }

  static saveTrajectory(
    items: TrajectoryEvent[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  static loadFiles(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): SessionFileRef[] {
    context ??= new LoadContext({ path: "files" });
    if (!Array.isArray(data)) {
      const result: SessionFileRef[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            SessionFileRef.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(
            SessionFileRef.load({ name: k, sessionId: v }, context.at(k)),
          );
        }
      }
      return result;
    }
    return data.map((item, index) =>
      SessionFileRef.load(
        item as Record<string, unknown>,
        context.atIndex(index),
      ),
    );
  }

  static saveFiles(
    items: SessionFileRef[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  static loadRefs(
    data: Record<string, unknown>[] | unknown[],
    context?: LoadContext,
  ): SessionRef[] {
    context ??= new LoadContext({ path: "refs" });
    if (!Array.isArray(data)) {
      const result: SessionRef[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(
            context.at(k).path +
              ": invalid named collection entry category array",
          );
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(
            SessionRef.load(
              { name: k, ...(v as Record<string, unknown>) },
              context.at(k),
            ),
          );
        } else {
          result.push(
            SessionRef.load({ name: k, sessionId: v }, context.at(k)),
          );
        }
      }
      return result;
    }
    return data.map((item, index) =>
      SessionRef.load(item as Record<string, unknown>, context.atIndex(index)),
    );
  }

  static saveRefs(
    items: SessionRef[],
    context?: SaveContext,
  ): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map((item) => item.save(context));
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.version !== undefined && obj.version !== null) {
      result["version"] = obj.version;
    }
    if (obj.runtime !== undefined && obj.runtime !== null) {
      result["runtime"] = obj.runtime;
    }
    if (obj.promptyVersion !== undefined && obj.promptyVersion !== null) {
      result["promptyVersion"] = obj.promptyVersion;
    }
    if (obj.sessionId !== undefined && obj.sessionId !== null) {
      result["sessionId"] = obj.sessionId;
    }
    if (obj.events !== undefined && obj.events !== null) {
      result["events"] = SessionTrace.saveEvents(obj.events, context);
    }
    if (obj.turns !== undefined && obj.turns !== null) {
      result["turns"] = SessionTrace.saveTurns(obj.turns, context);
    }
    if (obj.checkpoints !== undefined && obj.checkpoints !== null) {
      result["checkpoints"] = SessionTrace.saveCheckpoints(
        obj.checkpoints,
        context,
      );
    }
    if (obj.trajectory !== undefined && obj.trajectory !== null) {
      result["trajectory"] = SessionTrace.saveTrajectory(
        obj.trajectory,
        context,
      );
    }
    if (obj.files !== undefined && obj.files !== null) {
      result["files"] = SessionTrace.saveFiles(obj.files, context);
    }
    if (obj.refs !== undefined && obj.refs !== null) {
      result["refs"] = SessionTrace.saveRefs(obj.refs, context);
    }
    if (obj.summary !== undefined && obj.summary !== null) {
      result["summary"] = obj.summary.save(context);
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): SessionTrace {
    const data = JSON.parse(json);
    return SessionTrace.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): SessionTrace {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return SessionTrace.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
