// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";
import { RedactedField } from "./redacted-field";

export class RedactionMetadata {
  static readonly shorthandProperty: string | undefined = undefined;

  sanitized?: boolean | undefined;
  fields?: RedactedField[];
  policy?: string | undefined;

  constructor(init?: Partial<RedactionMetadata>) {
    if (init?.sanitized !== undefined) {
      this.sanitized = init.sanitized;
    }
    if (init?.fields !== undefined) {
      this.fields = init.fields;
    }
    if (init?.policy !== undefined) {
      this.policy = init.policy;
    }
  }

  //#region Load Methods

  static load(data: Record<string, unknown>, context?: LoadContext): RedactionMetadata {
    context ??= new LoadContext();
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new RedactionMetadata();

    if (data["sanitized"] !== undefined && data["sanitized"] !== null) {
      instance.sanitized = Boolean(data["sanitized"]);
    }
    if (data["fields"] !== undefined && data["fields"] !== null) {
      instance.fields = RedactionMetadata.loadFields(data["fields"] as unknown[], context.at("fields"));
    }
    if (data["policy"] !== undefined && data["policy"] !== null) {
      instance.policy = String(data["policy"]);
    }

    if (context) {
      return context.processOutput(instance) as RedactionMetadata;
    }
    return instance;
  }

  static loadFields(data: Record<string, unknown>[] | unknown[], context?: LoadContext): RedactedField[] {
    context ??= new LoadContext({ path: "fields" });
    if (!Array.isArray(data)) {
      const result: RedactedField[] = [];
      for (const [k, v] of Object.entries(data)) {
        if (Array.isArray(v)) {
          throw new TypeError(context.at(k).path + ": invalid named collection entry category array");
        }
        if (typeof v === "object" && v !== null && !Array.isArray(v)) {
          result.push(RedactedField.load({ name: k, ...(v as Record<string, unknown>) }, context.at(k)));
        } else {
          result.push(RedactedField.load({ name: k, "path": v }, context.at(k)));
        }
      }
      return result;
    }
    return data.map(item => RedactedField.load(item as Record<string, unknown>, context));
  }

  static saveFields(items: RedactedField[], context?: SaveContext): Record<string, unknown>[] | Record<string, unknown> {
    if (!context) {
      context = new SaveContext();
    }

    // This type doesn't have a 'name' property, so always use array format
    return items.map(item => item.save(context));
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.sanitized !== undefined && obj.sanitized !== null) {
      result["sanitized"] = obj.sanitized;
    }
    if (obj.fields !== undefined && obj.fields !== null) {
      result["fields"] = RedactionMetadata.saveFields(obj.fields, context);
    }
    if (obj.policy !== undefined && obj.policy !== null) {
      result["policy"] = obj.policy;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): RedactionMetadata {
    const data = JSON.parse(json);
    return RedactionMetadata.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): RedactionMetadata {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return RedactionMetadata.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
