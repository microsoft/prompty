// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";

export type RedactionMode =
  | "none"
  | "redacted"
  | "hashed"
  | "summary"
  | "reference";

export class RedactedField {
  static readonly shorthandProperty: string | undefined = undefined;

  path: string = "";
  mode: RedactionMode = "none";
  reason?: string | undefined;

  constructor(init?: Partial<RedactedField>) {
    this.path = init?.path ?? "";
    this.mode = init?.mode ?? "none";
    if (init?.reason !== undefined) {
      this.reason = init.reason;
    }
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): RedactedField {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new RedactedField();

    if (data["path"] !== undefined && data["path"] !== null) {
      instance.path = String(data["path"]);
    }
    if (data["mode"] !== undefined && data["mode"] !== null) {
      instance.mode = String(data["mode"]) as RedactionMode;
    }
    if (data["reason"] !== undefined && data["reason"] !== null) {
      instance.reason = String(data["reason"]);
    }

    if (context) {
      return context.processOutput(instance) as RedactedField;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.path !== undefined && obj.path !== null) {
      result["path"] = obj.path;
    }
    if (obj.mode !== undefined && obj.mode !== null) {
      result["mode"] = obj.mode;
    }
    if (obj.reason !== undefined && obj.reason !== null) {
      result["reason"] = obj.reason;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): RedactedField {
    const data = JSON.parse(json);
    return RedactedField.load(data as Record<string, unknown>, context);
  }

  static fromYaml(yaml: string, context?: LoadContext): RedactedField {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return RedactedField.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
