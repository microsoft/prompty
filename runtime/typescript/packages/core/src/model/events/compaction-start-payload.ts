// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "../context";

export class CompactionStartPayload {
  static readonly shorthandProperty: string | undefined = undefined;

  droppedCount: number = 0;

  constructor(init?: Partial<CompactionStartPayload>) {
    this.droppedCount = init?.droppedCount ?? 0;
  }

  //#region Load Methods

  static load(
    data: Record<string, unknown>,
    context?: LoadContext,
  ): CompactionStartPayload {
    if (context) {
      data = context.processInput(data) as Record<string, unknown>;
    }

    const instance = new CompactionStartPayload();

    if (data["droppedCount"] !== undefined && data["droppedCount"] !== null) {
      instance.droppedCount = Number(data["droppedCount"]);
    }

    if (context) {
      return context.processOutput(instance) as CompactionStartPayload;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  save(context?: SaveContext): Record<string, unknown> {
    let obj: this = this;
    if (context) {
      obj = context.processObject(obj) as this;
    }

    const result: Record<string, unknown> = {};

    if (obj.droppedCount !== undefined && obj.droppedCount !== null) {
      result["droppedCount"] = obj.droppedCount;
    }

    if (context) {
      return context.processDict(result);
    }
    return result;
  }

  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  static fromJson(json: string, context?: LoadContext): CompactionStartPayload {
    const data = JSON.parse(json);
    return CompactionStartPayload.load(
      data as Record<string, unknown>,
      context,
    );
  }

  static fromYaml(yaml: string, context?: LoadContext): CompactionStartPayload {
    const { parse } = require("yaml");
    const data = parse(yaml);
    return CompactionStartPayload.load(
      data as Record<string, unknown>,
      context,
    );
  }

  //#endregion
}
