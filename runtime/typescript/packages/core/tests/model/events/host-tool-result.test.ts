// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { HostToolResult } from "../../../src/model/index";

describe("HostToolResult", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new HostToolResult();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new HostToolResult({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "requestId": "exec_abc123",\n  "toolCallId": "call_abc123",\n  "toolName": "powershell",\n  "success": true,\n  "exitCode": 0,\n  "durationMs": 250,\n  "errorKind": "timeout"\n}`;
      const instance = HostToolResult.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.requestId).toEqual("exec_abc123");
      expect(instance.toolCallId).toEqual("call_abc123");
      expect(instance.toolName).toEqual("powershell");
      expect(instance.success).toEqual(true);
      expect(instance.exitCode).toEqual(0);
      expect(instance.durationMs).toEqual(250);
      expect(instance.errorKind).toEqual("timeout");
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "requestId": "exec_abc123",\n  "toolCallId": "call_abc123",\n  "toolName": "powershell",\n  "success": true,\n  "exitCode": 0,\n  "durationMs": 250,\n  "errorKind": "timeout"\n}`;
      const instance = HostToolResult.fromJson(json);
      const output = instance.toJson();
      const reloaded = HostToolResult.fromJson(output);
      expect(reloaded.requestId).toEqual(instance.requestId);
      expect(reloaded.toolCallId).toEqual(instance.toolCallId);
      expect(reloaded.toolName).toEqual(instance.toolName);
      expect(reloaded.success).toEqual(instance.success);
      expect(reloaded.exitCode).toEqual(instance.exitCode);
      expect(reloaded.durationMs).toEqual(instance.durationMs);
      expect(reloaded.errorKind).toEqual(instance.errorKind);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `requestId: exec_abc123\ntoolCallId: call_abc123\ntoolName: powershell\nsuccess: true\nexitCode: 0\ndurationMs: 250\nerrorKind: timeout\n`;
      const instance = HostToolResult.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.requestId).toEqual("exec_abc123");
      expect(instance.toolCallId).toEqual("call_abc123");
      expect(instance.toolName).toEqual("powershell");
      expect(instance.success).toEqual(true);
      expect(instance.exitCode).toEqual(0);
      expect(instance.durationMs).toEqual(250);
      expect(instance.errorKind).toEqual("timeout");
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `requestId: exec_abc123\ntoolCallId: call_abc123\ntoolName: powershell\nsuccess: true\nexitCode: 0\ndurationMs: 250\nerrorKind: timeout\n`;
      const instance = HostToolResult.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = HostToolResult.fromYaml(output);
      expect(reloaded.requestId).toEqual(instance.requestId);
      expect(reloaded.toolCallId).toEqual(instance.toolCallId);
      expect(reloaded.toolName).toEqual(instance.toolName);
      expect(reloaded.success).toEqual(instance.success);
      expect(reloaded.exitCode).toEqual(instance.exitCode);
      expect(reloaded.durationMs).toEqual(instance.durationMs);
      expect(reloaded.errorKind).toEqual(instance.errorKind);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = HostToolResult.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new HostToolResult();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
