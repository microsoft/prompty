// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { RetryPayload } from "../../../src/model/index";

describe("RetryPayload", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new RetryPayload();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new RetryPayload({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "operation": "llm",\n  "attempt": 2,\n  "maxAttempts": 3,\n  "delayMs": 1250,\n  "reason": "rate_limit"\n}`;
      const instance = RetryPayload.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.operation).toEqual("llm");
      expect(instance.attempt).toEqual(2);
      expect(instance.maxAttempts).toEqual(3);
      expect(instance.delayMs).toEqual(1250);
      expect(instance.reason).toEqual("rate_limit");
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "operation": "llm",\n  "attempt": 2,\n  "maxAttempts": 3,\n  "delayMs": 1250,\n  "reason": "rate_limit"\n}`;
      const instance = RetryPayload.fromJson(json);
      const output = instance.toJson();
      const reloaded = RetryPayload.fromJson(output);
      expect(reloaded.operation).toEqual(instance.operation);
      expect(reloaded.attempt).toEqual(instance.attempt);
      expect(reloaded.maxAttempts).toEqual(instance.maxAttempts);
      expect(reloaded.delayMs).toEqual(instance.delayMs);
      expect(reloaded.reason).toEqual(instance.reason);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `operation: llm\nattempt: 2\nmaxAttempts: 3\ndelayMs: 1250\nreason: rate_limit\n`;
      const instance = RetryPayload.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.operation).toEqual("llm");
      expect(instance.attempt).toEqual(2);
      expect(instance.maxAttempts).toEqual(3);
      expect(instance.delayMs).toEqual(1250);
      expect(instance.reason).toEqual("rate_limit");
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `operation: llm\nattempt: 2\nmaxAttempts: 3\ndelayMs: 1250\nreason: rate_limit\n`;
      const instance = RetryPayload.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = RetryPayload.fromYaml(output);
      expect(reloaded.operation).toEqual(instance.operation);
      expect(reloaded.attempt).toEqual(instance.attempt);
      expect(reloaded.maxAttempts).toEqual(instance.maxAttempts);
      expect(reloaded.delayMs).toEqual(instance.delayMs);
      expect(reloaded.reason).toEqual(instance.reason);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = RetryPayload.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new RetryPayload();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
