// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { SessionEndPayload } from "../../../src/model/index";

describe("SessionEndPayload", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new SessionEndPayload();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new SessionEndPayload({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "sessionId": "sess_abc123",\n  "status": "success",\n  "reason": "complete",\n  "durationMs": 12500\n}`;
      const instance = SessionEndPayload.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.sessionId).toEqual("sess_abc123");
      expect(instance.status).toEqual("success");
      expect(instance.reason).toEqual("complete");
      expect(instance.durationMs).toEqual(12500);
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "sessionId": "sess_abc123",\n  "status": "success",\n  "reason": "complete",\n  "durationMs": 12500\n}`;
      const instance = SessionEndPayload.fromJson(json);
      const output = instance.toJson();
      const reloaded = SessionEndPayload.fromJson(output);
      expect(reloaded.sessionId).toEqual(instance.sessionId);
      expect(reloaded.status).toEqual(instance.status);
      expect(reloaded.reason).toEqual(instance.reason);
      expect(reloaded.durationMs).toEqual(instance.durationMs);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `sessionId: sess_abc123\nstatus: success\nreason: complete\ndurationMs: 12500\n`;
      const instance = SessionEndPayload.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.sessionId).toEqual("sess_abc123");
      expect(instance.status).toEqual("success");
      expect(instance.reason).toEqual("complete");
      expect(instance.durationMs).toEqual(12500);
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `sessionId: sess_abc123\nstatus: success\nreason: complete\ndurationMs: 12500\n`;
      const instance = SessionEndPayload.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = SessionEndPayload.fromYaml(output);
      expect(reloaded.sessionId).toEqual(instance.sessionId);
      expect(reloaded.status).toEqual(instance.status);
      expect(reloaded.reason).toEqual(instance.reason);
      expect(reloaded.durationMs).toEqual(instance.durationMs);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = SessionEndPayload.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new SessionEndPayload();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
