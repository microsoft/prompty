// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { TrajectoryEvent } from "../../../src/model/index";

describe("TrajectoryEvent", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new TrajectoryEvent();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new TrajectoryEvent({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "id": "traj_abc123",\n  "sessionId": "sess_abc123",\n  "turnId": "turn_001",\n  "toolCallId": "call_abc123",\n  "turnIndex": 4,\n  "eventType": "command",\n  "createdAt": "2026-06-09T20:00:00Z"\n}`;
      const instance = TrajectoryEvent.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.id).toEqual("traj_abc123");
      expect(instance.sessionId).toEqual("sess_abc123");
      expect(instance.turnId).toEqual("turn_001");
      expect(instance.toolCallId).toEqual("call_abc123");
      expect(instance.turnIndex).toEqual(4);
      expect(instance.eventType).toEqual("command");
      expect(instance.createdAt).toEqual("2026-06-09T20:00:00Z");
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "id": "traj_abc123",\n  "sessionId": "sess_abc123",\n  "turnId": "turn_001",\n  "toolCallId": "call_abc123",\n  "turnIndex": 4,\n  "eventType": "command",\n  "createdAt": "2026-06-09T20:00:00Z"\n}`;
      const instance = TrajectoryEvent.fromJson(json);
      const output = instance.toJson();
      const reloaded = TrajectoryEvent.fromJson(output);
      expect(reloaded.id).toEqual(instance.id);
      expect(reloaded.sessionId).toEqual(instance.sessionId);
      expect(reloaded.turnId).toEqual(instance.turnId);
      expect(reloaded.toolCallId).toEqual(instance.toolCallId);
      expect(reloaded.turnIndex).toEqual(instance.turnIndex);
      expect(reloaded.eventType).toEqual(instance.eventType);
      expect(reloaded.createdAt).toEqual(instance.createdAt);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `id: traj_abc123\nsessionId: sess_abc123\nturnId: turn_001\ntoolCallId: call_abc123\nturnIndex: 4\neventType: command\ncreatedAt: "2026-06-09T20:00:00Z"\n`;
      const instance = TrajectoryEvent.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.id).toEqual("traj_abc123");
      expect(instance.sessionId).toEqual("sess_abc123");
      expect(instance.turnId).toEqual("turn_001");
      expect(instance.toolCallId).toEqual("call_abc123");
      expect(instance.turnIndex).toEqual(4);
      expect(instance.eventType).toEqual("command");
      expect(instance.createdAt).toEqual("2026-06-09T20:00:00Z");
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `id: traj_abc123\nsessionId: sess_abc123\nturnId: turn_001\ntoolCallId: call_abc123\nturnIndex: 4\neventType: command\ncreatedAt: "2026-06-09T20:00:00Z"\n`;
      const instance = TrajectoryEvent.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = TrajectoryEvent.fromYaml(output);
      expect(reloaded.id).toEqual(instance.id);
      expect(reloaded.sessionId).toEqual(instance.sessionId);
      expect(reloaded.turnId).toEqual(instance.turnId);
      expect(reloaded.toolCallId).toEqual(instance.toolCallId);
      expect(reloaded.turnIndex).toEqual(instance.turnIndex);
      expect(reloaded.eventType).toEqual(instance.eventType);
      expect(reloaded.createdAt).toEqual(instance.createdAt);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = TrajectoryEvent.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new TrajectoryEvent();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
