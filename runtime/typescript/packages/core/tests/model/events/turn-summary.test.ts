// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { TurnSummary } from "../../../src/model/index";

describe("TurnSummary", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new TurnSummary();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new TurnSummary({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "turnId": "turn_001",\n  "status": "success",\n  "iterations": 2,\n  "llmCalls": 3,\n  "toolCalls": 2,\n  "retries": 1,\n  "durationMs": 2500\n}`;
      const instance = TurnSummary.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.turnId).toEqual("turn_001");
      expect(instance.status).toEqual("success");
      expect(instance.iterations).toEqual(2);
      expect(instance.llmCalls).toEqual(3);
      expect(instance.toolCalls).toEqual(2);
      expect(instance.retries).toEqual(1);
      expect(instance.durationMs).toEqual(2500);
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "turnId": "turn_001",\n  "status": "success",\n  "iterations": 2,\n  "llmCalls": 3,\n  "toolCalls": 2,\n  "retries": 1,\n  "durationMs": 2500\n}`;
      const instance = TurnSummary.fromJson(json);
      const output = instance.toJson();
      const reloaded = TurnSummary.fromJson(output);
      expect(reloaded.turnId).toEqual(instance.turnId);
      expect(reloaded.status).toEqual(instance.status);
      expect(reloaded.iterations).toEqual(instance.iterations);
      expect(reloaded.llmCalls).toEqual(instance.llmCalls);
      expect(reloaded.toolCalls).toEqual(instance.toolCalls);
      expect(reloaded.retries).toEqual(instance.retries);
      expect(reloaded.durationMs).toEqual(instance.durationMs);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `turnId: turn_001\nstatus: success\niterations: 2\nllmCalls: 3\ntoolCalls: 2\nretries: 1\ndurationMs: 2500\n`;
      const instance = TurnSummary.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.turnId).toEqual("turn_001");
      expect(instance.status).toEqual("success");
      expect(instance.iterations).toEqual(2);
      expect(instance.llmCalls).toEqual(3);
      expect(instance.toolCalls).toEqual(2);
      expect(instance.retries).toEqual(1);
      expect(instance.durationMs).toEqual(2500);
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `turnId: turn_001\nstatus: success\niterations: 2\nllmCalls: 3\ntoolCalls: 2\nretries: 1\ndurationMs: 2500\n`;
      const instance = TurnSummary.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = TurnSummary.fromYaml(output);
      expect(reloaded.turnId).toEqual(instance.turnId);
      expect(reloaded.status).toEqual(instance.status);
      expect(reloaded.iterations).toEqual(instance.iterations);
      expect(reloaded.llmCalls).toEqual(instance.llmCalls);
      expect(reloaded.toolCalls).toEqual(instance.toolCalls);
      expect(reloaded.retries).toEqual(instance.retries);
      expect(reloaded.durationMs).toEqual(instance.durationMs);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = TurnSummary.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new TurnSummary();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
