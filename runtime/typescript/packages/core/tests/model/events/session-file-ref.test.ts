// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { SessionFileRef } from "../../../src/model/index";

describe("SessionFileRef", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new SessionFileRef();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new SessionFileRef({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "sessionId": "sess_abc123",\n  "path": "src/index.ts",\n  "toolName": "view",\n  "turnIndex": 2,\n  "firstSeenAt": "2026-06-09T20:00:00Z"\n}`;
      const instance = SessionFileRef.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.sessionId).toEqual("sess_abc123");
      expect(instance.path).toEqual("src/index.ts");
      expect(instance.toolName).toEqual("view");
      expect(instance.turnIndex).toEqual(2);
      expect(instance.firstSeenAt).toEqual("2026-06-09T20:00:00Z");
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "sessionId": "sess_abc123",\n  "path": "src/index.ts",\n  "toolName": "view",\n  "turnIndex": 2,\n  "firstSeenAt": "2026-06-09T20:00:00Z"\n}`;
      const instance = SessionFileRef.fromJson(json);
      const output = instance.toJson();
      const reloaded = SessionFileRef.fromJson(output);
      expect(reloaded.sessionId).toEqual(instance.sessionId);
      expect(reloaded.path).toEqual(instance.path);
      expect(reloaded.toolName).toEqual(instance.toolName);
      expect(reloaded.turnIndex).toEqual(instance.turnIndex);
      expect(reloaded.firstSeenAt).toEqual(instance.firstSeenAt);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `sessionId: sess_abc123\npath: src/index.ts\ntoolName: view\nturnIndex: 2\nfirstSeenAt: "2026-06-09T20:00:00Z"\n`;
      const instance = SessionFileRef.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.sessionId).toEqual("sess_abc123");
      expect(instance.path).toEqual("src/index.ts");
      expect(instance.toolName).toEqual("view");
      expect(instance.turnIndex).toEqual(2);
      expect(instance.firstSeenAt).toEqual("2026-06-09T20:00:00Z");
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `sessionId: sess_abc123\npath: src/index.ts\ntoolName: view\nturnIndex: 2\nfirstSeenAt: "2026-06-09T20:00:00Z"\n`;
      const instance = SessionFileRef.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = SessionFileRef.fromYaml(output);
      expect(reloaded.sessionId).toEqual(instance.sessionId);
      expect(reloaded.path).toEqual(instance.path);
      expect(reloaded.toolName).toEqual(instance.toolName);
      expect(reloaded.turnIndex).toEqual(instance.turnIndex);
      expect(reloaded.firstSeenAt).toEqual(instance.firstSeenAt);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = SessionFileRef.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new SessionFileRef();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
