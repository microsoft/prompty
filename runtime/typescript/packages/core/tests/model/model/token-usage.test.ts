// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { TokenUsage } from "../../../src/model/index";

describe("TokenUsage", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new TokenUsage();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new TokenUsage({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "promptTokens": 150,\n  "completionTokens": 42,\n  "totalTokens": 192\n}`;
      const instance = TokenUsage.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.promptTokens).toEqual(150);
      expect(instance.completionTokens).toEqual(42);
      expect(instance.totalTokens).toEqual(192);
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "promptTokens": 150,\n  "completionTokens": 42,\n  "totalTokens": 192\n}`;
      const instance = TokenUsage.fromJson(json);
      const output = instance.toJson();
      const reloaded = TokenUsage.fromJson(output);
      expect(reloaded.promptTokens).toEqual(instance.promptTokens);
      expect(reloaded.completionTokens).toEqual(instance.completionTokens);
      expect(reloaded.totalTokens).toEqual(instance.totalTokens);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `promptTokens: 150\ncompletionTokens: 42\ntotalTokens: 192\n`;
      const instance = TokenUsage.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.promptTokens).toEqual(150);
      expect(instance.completionTokens).toEqual(42);
      expect(instance.totalTokens).toEqual(192);
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `promptTokens: 150\ncompletionTokens: 42\ntotalTokens: 192\n`;
      const instance = TokenUsage.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = TokenUsage.fromYaml(output);
      expect(reloaded.promptTokens).toEqual(instance.promptTokens);
      expect(reloaded.completionTokens).toEqual(instance.completionTokens);
      expect(reloaded.totalTokens).toEqual(instance.totalTokens);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = TokenUsage.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new TokenUsage();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
