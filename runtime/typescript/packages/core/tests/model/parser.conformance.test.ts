// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.
//
// Part III TYPED @vector conformance for Parser — the per-interface twin of
// the per-model ${model}.test.ts file (issue #282 §8). Each test builds the
// operation inputs from the vector JSON, reads the shape discriminator, routes
// it through the emitted resolveParser against the consumer-authored
// 'parserProvider' provider, invokes the typed seam, and asserts 'expected'.
// The provider VALUE is authored by the consumer as a 'parserProvider' export
// in the 'vector-adapter-path' module; the emitted 'ParserProvider' Record
// makes a dropped @dispatch slot a compile error, so conformance never skips.
// See docs: reference/vector-conformance.

import { Agent } from "../../src/model/index";
import { resolveParser } from "../../src/model/parser-resolver";
import { parserProvider } from "./vector-adapters";

describe("Parser @vector conformance", () => {
  it("assistant_with_code_block", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"assistant:\\nHere is code:\\n```python\\nprint(\'hello\')\\n```\\n\\nuser:\\nThanks"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("consecutive_newlines_between_roles", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nHelper\\n\\n\\n\\nuser:\\nHi"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("content_trimmed", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\n  \\n  You are helpful.  \\n  \\n\\nuser:\\n  Hello  "}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("developer_role", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"developer:\\nInternal instructions."}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("empty_content", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\n\\nuser:\\nHello"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("inline_markdown_image_preserved_as_text", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"user:\\nLook at this ![photo](https://example.com/img.png)"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("markdown_in_content", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\n# Title\\n\\nSome **bold** text\\n\\n- item 1\\n- item 2\\n\\nuser:\\nHello"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("multiline_content", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nLine one.\\nLine two.\\nLine three.\\n\\nuser:\\nQuestion"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("multiple_assistant_turns", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nYou are helpful.\\n\\nuser:\\nHi\\n\\nassistant:\\nHello!\\n\\nuser:\\nThanks\\n\\nassistant:\\nYou\'re welcome!"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("multiple_user_turns", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nHelper\\n\\nuser:\\nFirst question\\n\\nassistant:\\nFirst answer\\n\\nuser:\\nSecond question"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("no_role_marker_defaults_to_system", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"Just a plain message with no role marker"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("role_marker_at_line_start_only", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nThe user: said hello\\n\\nuser:\\nHi"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("role_marker_colon_in_content", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nHere is a tip: always be kind.\\n\\nuser:\\nTime is 3:30pm"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("role_with_multiple_unquoted_attributes", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"user[name=Bob, id=7]:\\nHi"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("role_with_quoted_attribute", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"user[name=\\"Alice\\"]:\\nHello!"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("single_system", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nYou are helpful."}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("system_user", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nYou are helpful.\\n\\nuser:\\nHello"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("system_user_assistant", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nBe concise.\\n\\nuser:\\nHi\\n\\nassistant:\\nHello! How can I help?"}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
  it("thread_nonce_expansion", async () => {
    const payload = JSON.parse(
      '{"agent":{"template":{"format":{"kind":"jinja2"},"parser":{"kind":"prompty"}}},"rendered":"system:\\nYou are helpful.\\n\\n__PROMPTY_THREAD_abcd1234_conversation__\\n\\nuser:\\nHi","thread_inputs":{"conversation":[{"role":"user","content":[{"kind":"text","value":"prev question"}]},{"role":"assistant","content":[{"kind":"text","value":"prev answer"}]}]}}',
    ) as Record<string, unknown>;
    const agent = Agent.load(payload["agent"] as Record<string, unknown>);
    const rendered = payload["rendered"] as string;
    const context = payload["context"] as Record<string, unknown> | null;
    const kind = agent.template.parser.kind;
    const impl = resolveParser(kind, parserProvider);
    expect(impl).not.toBeNull();
    const result = await impl!.parse(agent, rendered, context);
    expect(result).toBeDefined();
  });
});
