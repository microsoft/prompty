// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { ValidationError } from "../../src/model/index";

describe("ValidationError", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new ValidationError();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new ValidationError({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "message": "Missing required input: firstName",\n  "property": "firstName",\n  "constraint": "required"\n}`;
      const instance = ValidationError.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.message).toEqual("Missing required input: firstName");
      expect(instance.property).toEqual("firstName");
      expect(instance.constraint).toEqual("required");
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "message": "Missing required input: firstName",\n  "property": "firstName",\n  "constraint": "required"\n}`;
      const instance = ValidationError.fromJson(json);
      const output = instance.toJson();
      const reloaded = ValidationError.fromJson(output);
      expect(reloaded.message).toEqual(instance.message);
      expect(reloaded.property).toEqual(instance.property);
      expect(reloaded.constraint).toEqual(instance.constraint);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `message: "Missing required input: firstName"\nproperty: firstName\nconstraint: required\n`;
      const instance = ValidationError.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.message).toEqual("Missing required input: firstName");
      expect(instance.property).toEqual("firstName");
      expect(instance.constraint).toEqual("required");
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `message: "Missing required input: firstName"\nproperty: firstName\nconstraint: required\n`;
      const instance = ValidationError.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = ValidationError.fromYaml(output);
      expect(reloaded.message).toEqual(instance.message);
      expect(reloaded.property).toEqual(instance.property);
      expect(reloaded.constraint).toEqual(instance.constraint);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = ValidationError.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new ValidationError();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
