// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { AnthropicToolDefinition } from "../../../src/model/index";

describe("AnthropicToolDefinition", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new AnthropicToolDefinition();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new AnthropicToolDefinition({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "name": "get_weather",\n  "description": "Get the current weather for a city"\n}`;
      const instance = AnthropicToolDefinition.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.name).toEqual("get_weather");
      expect(instance.description).toEqual(
        "Get the current weather for a city",
      );
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "name": "get_weather",\n  "description": "Get the current weather for a city"\n}`;
      const instance = AnthropicToolDefinition.fromJson(json);
      const output = instance.toJson();
      const reloaded = AnthropicToolDefinition.fromJson(output);
      expect(reloaded.name).toEqual(instance.name);
      expect(reloaded.description).toEqual(instance.description);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `name: get_weather\ndescription: Get the current weather for a city\n`;
      const instance = AnthropicToolDefinition.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.name).toEqual("get_weather");
      expect(instance.description).toEqual(
        "Get the current weather for a city",
      );
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `name: get_weather\ndescription: Get the current weather for a city\n`;
      const instance = AnthropicToolDefinition.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = AnthropicToolDefinition.fromYaml(output);
      expect(reloaded.name).toEqual(instance.name);
      expect(reloaded.description).toEqual(instance.description);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = AnthropicToolDefinition.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new AnthropicToolDefinition();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
