// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { TurnOptions } from "../../../src/model/index";

describe("TurnOptions", () => {
  describe("construction", () => {
    it("should create a new instance with defaults", () => {
      const instance = new TurnOptions();
      expect(instance).toBeDefined();
    });

    it("should create a new instance with partial initialization", () => {
      const instance = new TurnOptions({});
      expect(instance).toBeDefined();
    });
  });

  describe("JSON serialization", () => {
    it("should load from JSON - example 1", () => {
      const json = `{\n  "maxIterations": 10,\n  "maxLlmRetries": 3,\n  "contextBudget": 100000,\n  "parallelToolCalls": true,\n  "raw": false,\n  "turn": 1,\n  "compaction": {\n    "strategy": "summarize"\n  }\n}`;
      const instance = TurnOptions.fromJson(json);
      expect(instance).toBeDefined();
      expect(instance.maxIterations).toEqual(10);
      expect(instance.maxLlmRetries).toEqual(3);
      expect(instance.contextBudget).toEqual(100000);
      expect(instance.parallelToolCalls).toEqual(true);
      expect(instance.raw).toEqual(false);
      expect(instance.turn).toEqual(1);
    });

    it("should round-trip JSON - example 1", () => {
      const json = `{\n  "maxIterations": 10,\n  "maxLlmRetries": 3,\n  "contextBudget": 100000,\n  "parallelToolCalls": true,\n  "raw": false,\n  "turn": 1,\n  "compaction": {\n    "strategy": "summarize"\n  }\n}`;
      const instance = TurnOptions.fromJson(json);
      const output = instance.toJson();
      const reloaded = TurnOptions.fromJson(output);
      expect(reloaded.maxIterations).toEqual(instance.maxIterations);
      expect(reloaded.maxLlmRetries).toEqual(instance.maxLlmRetries);
      expect(reloaded.contextBudget).toEqual(instance.contextBudget);
      expect(reloaded.parallelToolCalls).toEqual(instance.parallelToolCalls);
      expect(reloaded.raw).toEqual(instance.raw);
      expect(reloaded.turn).toEqual(instance.turn);
    });
  });

  describe("YAML serialization", () => {
    it("should load from YAML - example 1", () => {
      const yaml = `maxIterations: 10\nmaxLlmRetries: 3\ncontextBudget: 100000\nparallelToolCalls: true\nraw: false\nturn: 1\ncompaction:\n  strategy: summarize\n`;
      const instance = TurnOptions.fromYaml(yaml);
      expect(instance).toBeDefined();
      expect(instance.maxIterations).toEqual(10);
      expect(instance.maxLlmRetries).toEqual(3);
      expect(instance.contextBudget).toEqual(100000);
      expect(instance.parallelToolCalls).toEqual(true);
      expect(instance.raw).toEqual(false);
      expect(instance.turn).toEqual(1);
    });

    it("should round-trip YAML - example 1", () => {
      const yaml = `maxIterations: 10\nmaxLlmRetries: 3\ncontextBudget: 100000\nparallelToolCalls: true\nraw: false\nturn: 1\ncompaction:\n  strategy: summarize\n`;
      const instance = TurnOptions.fromYaml(yaml);
      const output = instance.toYaml();
      const reloaded = TurnOptions.fromYaml(output);
      expect(reloaded.maxIterations).toEqual(instance.maxIterations);
      expect(reloaded.maxLlmRetries).toEqual(instance.maxLlmRetries);
      expect(reloaded.contextBudget).toEqual(instance.contextBudget);
      expect(reloaded.parallelToolCalls).toEqual(instance.parallelToolCalls);
      expect(reloaded.raw).toEqual(instance.raw);
      expect(reloaded.turn).toEqual(instance.turn);
    });
  });

  describe("load and save", () => {
    it("should load from dictionary", () => {
      const data: Record<string, unknown> = {};
      const instance = TurnOptions.load(data);
      expect(instance).toBeDefined();
    });

    it("should save to dictionary", () => {
      const instance = new TurnOptions();
      const data = instance.save();
      expect(data).toBeDefined();
      expect(typeof data).toBe("object");
    });
  });
});
