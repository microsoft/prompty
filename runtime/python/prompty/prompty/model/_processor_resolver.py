# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass

from ._Processor import Processor

PROCESSOR_VARIANTS: tuple[str, ...] = (
    "openai",
    "azure",
)
"""Every @dispatch variant the Processor seam must cover, in shape order."""


@dataclass
class ProcessorProvider:
    """Carries one Processor impl per @dispatch variant, the twin of the shape
    discriminator's variant set. A ``None`` slot models a valid-but-
    unimplemented variant; build via :func:`new_processor_provider`
    so a forgotten variant is a collection-time error rather than a silent
    ``None`` (the slots are required, so a bare constructor call cannot skip
    a variant either).
    """

    openai: Processor | None
    azure: Processor | None
    custom: Processor | None = None


def new_processor_provider(impls: Mapping[str, Processor | None]) -> ProcessorProvider:
    """Build a :class:`ProcessorProvider` from a variant->impl mapping, raising when
    a variant is ABSENT (a forgotten attachment). A key present with a
    ``None`` value is allowed and marks a variant as valid-but-unimplemented.
    """
    missing = [kind for kind in PROCESSOR_VARIANTS if kind not in impls]
    if missing:
        raise ValueError(f"Processor provider is missing @dispatch variant(s): {missing}")
    return ProcessorProvider(
        openai=impls["openai"],
        azure=impls["azure"],
        custom=impls.get("custom"),
    )


def resolve_processor(provider: str, registry: ProcessorProvider) -> Processor | None:
    """Map a 'provider' discriminator to the attached Processor impl — the
    behavioral twin of the shape discriminator load switch. A known variant
    returns its slot; an unknown discriminator routes to the wildcard
    ``custom`` default slot (the declared ``*`` child), mirroring the
    shape loader's `*`-tolerant fallback — never a silent miss.
    """
    if provider == "openai":
        return registry.openai
    elif provider == "azure":
        return registry.azure
    return registry.custom
