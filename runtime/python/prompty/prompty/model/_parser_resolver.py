# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass

from ._Parser import Parser

PARSER_VARIANTS: tuple[str, ...] = ("prompty",)
"""Every @dispatch variant the Parser seam must cover, in shape order."""


@dataclass
class ParserProvider:
    """Carries one Parser impl per @dispatch variant, the twin of the shape
    discriminator's variant set. A ``None`` slot models a valid-but-
    unimplemented variant; build via :func:`new_parser_provider`
    so a forgotten variant is a collection-time error rather than a silent
    ``None`` (the slots are required, so a bare constructor call cannot skip
    a variant either).
    """

    prompty: Parser | None
    custom: Parser | None = None


def new_parser_provider(impls: Mapping[str, Parser | None]) -> ParserProvider:
    """Build a :class:`ParserProvider` from a variant->impl mapping, raising when
    a variant is ABSENT (a forgotten attachment). A key present with a
    ``None`` value is allowed and marks a variant as valid-but-unimplemented.
    """
    missing = [kind for kind in PARSER_VARIANTS if kind not in impls]
    if missing:
        raise ValueError(f"Parser provider is missing @dispatch variant(s): {missing}")
    return ParserProvider(
        prompty=impls["prompty"],
        custom=impls.get("custom"),
    )


def resolve_parser(kind: str, registry: ParserProvider) -> Parser | None:
    """Map a 'kind' discriminator to the attached Parser impl — the
    behavioral twin of the shape discriminator load switch. A known variant
    returns its slot; an unknown discriminator routes to the wildcard
    ``custom`` default slot (the declared ``*`` child), mirroring the
    shape loader's `*`-tolerant fallback — never a silent miss.
    """
    if kind == "prompty":
        return registry.prompty
    return registry.custom
