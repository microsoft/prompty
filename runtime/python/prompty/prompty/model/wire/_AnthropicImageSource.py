##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class AnthropicImageSource:
    """Source descriptor for an Anthropic base64 image.

    Attributes
    ----------
    type : str
        The encoding type (always 'base64' for inline images)
    media_type : str
        The MIME type of the image (e.g., 'image/png', 'image/jpeg')
    data : str
        The base64-encoded image data
    """

    _shorthand_property: ClassVar[str | None] = None

    type: str = field(default="base64")
    media_type: str = field(default="")
    data: str = field(default="")

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "AnthropicImageSource":
        """Load a AnthropicImageSource instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            AnthropicImageSource: The loaded AnthropicImageSource instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for AnthropicImageSource: {data}")

        # create new instance
        instance = AnthropicImageSource()

        if data is not None and "type" in data:
            instance.type = data["type"]
        if data is not None and "media_type" in data:
            instance.media_type = data["media_type"]
        if data is not None and "data" in data:
            instance.data = data["data"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the AnthropicImageSource instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.type is not None:
            result["type"] = obj.type
        if obj.media_type is not None:
            result["media_type"] = obj.media_type
        if obj.data is not None:
            result["data"] = obj.data

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the AnthropicImageSource instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the AnthropicImageSource instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
