# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class AnthropicUsage:
    """Usage statistics returned in an Anthropic Messages API response.

    Attributes
    ----------
    input_tokens : int
        Number of input tokens consumed
    output_tokens : int
        Number of output tokens generated
    """

    _shorthand_property: ClassVar[str | None] = None

    input_tokens: int = field(default=0)
    output_tokens: int = field(default=0)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "AnthropicUsage":
        """Load a AnthropicUsage instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            AnthropicUsage: The loaded AnthropicUsage instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for AnthropicUsage: {data}")

        # create new instance
        instance = AnthropicUsage()

        if data is not None and "input_tokens" in data:
            instance.input_tokens = data["input_tokens"]
        if data is not None and "output_tokens" in data:
            instance.output_tokens = data["output_tokens"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the AnthropicUsage instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.input_tokens is not None:
            result["input_tokens"] = obj.input_tokens
        if obj.output_tokens is not None:
            result["output_tokens"] = obj.output_tokens

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the AnthropicUsage instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the AnthropicUsage instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
