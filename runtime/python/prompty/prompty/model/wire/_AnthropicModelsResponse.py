# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._AnthropicModelInfo import AnthropicModelInfo


@dataclass
class AnthropicModelsResponse:
    """The response body from Anthropic's model listing API.

    Attributes
    ----------
    data : list[AnthropicModelInfo]
        Models available to the authenticated caller
    first_id : Optional[str]
        The first model identifier in this page
    has_more : Optional[bool]
        Whether another page is available
    last_id : Optional[str]
        The last model identifier in this page
    """

    _shorthand_property: ClassVar[str | None] = None

    data: list[AnthropicModelInfo] = field(default_factory=list)
    first_id: str | None = None
    has_more: bool | None = None
    last_id: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "AnthropicModelsResponse":
        """Load a AnthropicModelsResponse instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            AnthropicModelsResponse: The loaded AnthropicModelsResponse instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for AnthropicModelsResponse: {data}")

        # create new instance
        instance = AnthropicModelsResponse()

        if data is not None and "data" in data:
            instance.data = AnthropicModelsResponse.load_data(data["data"], context)
        if data is not None and "first_id" in data:
            instance.first_id = data["first_id"]
        if data is not None and "has_more" in data:
            instance.has_more = data["has_more"]
        if data is not None and "last_id" in data:
            instance.last_id = data["last_id"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_data(data: dict | list, context: LoadContext | None) -> list[AnthropicModelInfo]:
        if isinstance(data, dict):
            # convert simple named data to list of AnthropicModelInfo
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "id": v})
            data = result
        return [AnthropicModelInfo.load(item, context) for item in data]

    @staticmethod
    def save_data(
        items: list[AnthropicModelInfo], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the AnthropicModelsResponse instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.data is not None:
            result["data"] = AnthropicModelsResponse.save_data(obj.data, context)
        if obj.first_id is not None:
            result["first_id"] = obj.first_id
        if obj.has_more is not None:
            result["has_more"] = obj.has_more
        if obj.last_id is not None:
            result["last_id"] = obj.last_id

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the AnthropicModelsResponse instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the AnthropicModelsResponse instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
