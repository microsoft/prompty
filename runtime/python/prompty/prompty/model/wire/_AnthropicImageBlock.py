##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._AnthropicImageSource import AnthropicImageSource


@dataclass
class AnthropicImageBlock:
    """An image content block using base64-encoded data.
    Anthropic requires images as base64 with an explicit media type.

    Attributes
    ----------
    type : str
        The content block type
    source : AnthropicImageSource
        The image source (base64-encoded)
    """

    _shorthand_property: ClassVar[str | None] = None

    type: str = field(default="image")
    source: AnthropicImageSource = field(default_factory=AnthropicImageSource)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "AnthropicImageBlock":
        """Load a AnthropicImageBlock instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            AnthropicImageBlock: The loaded AnthropicImageBlock instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for AnthropicImageBlock: {data}")

        # create new instance
        instance = AnthropicImageBlock()

        if data is not None and "type" in data:
            instance.type = data["type"]
        if data is not None and "source" in data:
            instance.source = AnthropicImageSource.load(data["source"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the AnthropicImageBlock instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.type is not None:
            result["type"] = obj.type
        if obj.source is not None:
            result["source"] = obj.source.save(context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the AnthropicImageBlock instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the AnthropicImageBlock instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
