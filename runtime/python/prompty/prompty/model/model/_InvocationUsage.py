# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class InvocationUsage:
    """Complete cumulative token usage for one completed model invocation.

    Providers emit this value at most once per invocation. `totalTokens` uses
    the provider total when available; otherwise the provider adapter computes
    it as `inputTokens + outputTokens`.

    Attributes
    ----------
    input_tokens : int
        Number of input tokens consumed by the completed invocation
    output_tokens : int
        Number of output tokens generated by the completed invocation
    total_tokens : int
        Total tokens consumed by the completed invocation
    """

    _shorthand_property: ClassVar[str | None] = None

    input_tokens: int = field(default=0)
    output_tokens: int = field(default=0)
    total_tokens: int = field(default=0)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "InvocationUsage":
        """Load a InvocationUsage instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            InvocationUsage: The loaded InvocationUsage instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for InvocationUsage: {data}")

        # create new instance
        instance = InvocationUsage()

        if data is not None and "inputTokens" in data:
            instance.input_tokens = data["inputTokens"]
        if data is not None and "outputTokens" in data:
            instance.output_tokens = data["outputTokens"]
        if data is not None and "totalTokens" in data:
            instance.total_tokens = data["totalTokens"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the InvocationUsage instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.input_tokens is not None:
            result["inputTokens"] = obj.input_tokens
        if obj.output_tokens is not None:
            result["outputTokens"] = obj.output_tokens
        if obj.total_tokens is not None:
            result["totalTokens"] = obj.total_tokens

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_wire(self, provider: str) -> dict[str, Any]:
        """Convert to provider-specific wire format.
        Args:
            provider (str): The provider to convert to (e.g., "openai", "anthropic").
        Returns:
            dict[str, Any]: The wire-format dictionary with provider-specific field names.

        """
        data = self.save()
        result: dict[str, Any] = {}
        wire_map: dict[str, dict[str, str]] = {
            "inputTokens": {"openai": "prompt_tokens", "anthropic": "input_tokens"},
            "outputTokens": {"openai": "completion_tokens", "anthropic": "output_tokens"},
            "totalTokens": {"openai": "total_tokens"},
        }
        for key, value in data.items():
            mapping = wire_map.get(key)
            if mapping is not None and provider in mapping:
                result[mapping[provider]] = value
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the InvocationUsage instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the InvocationUsage instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
