# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class AiResourceInfo:
    """An AI service resource that can host models or projects.

    Attributes
    ----------
    name : str
        Provider resource name
    kind : str
        Provider resource kind
    endpoint : str
        Inference endpoint
    location : str
        Provider region or location
    resource_group : str
        Provider resource group or equivalent administrative grouping
    service_url : Optional[str]
        Optional service root used to enumerate projects
    """

    _shorthand_property: ClassVar[str | None] = None

    name: str = field(default="")
    kind: str = field(default="")
    endpoint: str = field(default="")
    location: str = field(default="")
    resource_group: str = field(default="")
    service_url: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "AiResourceInfo":
        """Load a AiResourceInfo instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            AiResourceInfo: The loaded AiResourceInfo instance.

        """

        if context is None:
            context = LoadContext()
        data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for AiResourceInfo: {data}")

        # create new instance
        instance = AiResourceInfo()

        if data is not None and "name" in data:
            instance.name = data["name"]
        if data is not None and "kind" in data:
            instance.kind = data["kind"]
        if data is not None and "endpoint" in data:
            instance.endpoint = data["endpoint"]
        if data is not None and "location" in data:
            instance.location = data["location"]
        if data is not None and "resourceGroup" in data:
            instance.resource_group = data["resourceGroup"]
        if data is not None and "serviceUrl" in data:
            instance.service_url = data["serviceUrl"]
        if context is not None:
            instance = context.process_output(instance)
        return instance



    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the AiResourceInfo instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)


        result: dict[str, Any] = {}

        if obj.name is not None:
            result["name"] = obj.name
        if obj.kind is not None:
            result["kind"] = obj.kind
        if obj.endpoint is not None:
            result["endpoint"] = obj.endpoint
        if obj.location is not None:
            result["location"] = obj.location
        if obj.resource_group is not None:
            result["resourceGroup"] = obj.resource_group
        if obj.service_url is not None:
            result["serviceUrl"] = obj.service_url

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_wire(self, provider: str) -> dict[str, Any]:
        """Convert to provider-specific wire format.
        Args:
            provider (str): The provider to convert to (e.g., "openai", "anthropic").
        Returns:
            dict[str, Any]: The wire-format dictionary with provider-specific field names.

        """
        data = self.save()
        result: dict[str, Any] = {}
        wire_map: dict[str, dict[str, str]] = {
            "name": {"foundry": "name"},
            "kind": {"foundry": "kind"},
            "endpoint": {"foundry": "endpoint"},
            "location": {"foundry": "location"},
            "resourceGroup": {"foundry": "resource_group"},
            "serviceUrl": {"foundry": "foundry_url"},
        }
        for key, value in data.items():
            mapping = wire_map.get(key)
            if mapping is not None and provider in mapping:
                result[mapping[provider]] = value
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the AiResourceInfo instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the AiResourceInfo instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
