# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class TokenUsage:
    """Tracks token consumption for a single LLM call. Provider-specific field
    names (e.g., OpenAI's `prompt_tokens` vs Anthropic's `input_tokens`)
    are mapped via `knownAs` augments in the wire directory.

    Attributes
    ----------
    prompt_tokens : Optional[int]
        Number of tokens in the prompt/input sent to the model
    completion_tokens : Optional[int]
        Number of tokens generated in the model's completion/output
    total_tokens : Optional[int]
        Total tokens consumed (prompt + completion). May be provided by the API or computed.
    """

    _shorthand_property: ClassVar[str | None] = None

    prompt_tokens: int | None = None
    completion_tokens: int | None = None
    total_tokens: int | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "TokenUsage":
        """Load a TokenUsage instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            TokenUsage: The loaded TokenUsage instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for TokenUsage: {data}")

        # create new instance
        instance = TokenUsage()

        if data is not None and "promptTokens" in data:
            instance.prompt_tokens = data["promptTokens"]
        if data is not None and "completionTokens" in data:
            instance.completion_tokens = data["completionTokens"]
        if data is not None and "totalTokens" in data:
            instance.total_tokens = data["totalTokens"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the TokenUsage instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.prompt_tokens is not None:
            result["promptTokens"] = obj.prompt_tokens
        if obj.completion_tokens is not None:
            result["completionTokens"] = obj.completion_tokens
        if obj.total_tokens is not None:
            result["totalTokens"] = obj.total_tokens

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_wire(self, provider: str) -> dict[str, Any]:
        """Convert to provider-specific wire format.
        Args:
            provider (str): The provider to convert to (e.g., "openai", "anthropic").
        Returns:
            dict[str, Any]: The wire-format dictionary with provider-specific field names.

        """
        data = self.save()
        result: dict[str, Any] = {}
        wire_map: dict[str, dict[str, str]] = {
            "promptTokens": {"openai": "prompt_tokens", "anthropic": "input_tokens"},
            "completionTokens": {"openai": "completion_tokens", "anthropic": "output_tokens"},
            "totalTokens": {"openai": "total_tokens"},
        }
        for key, value in data.items():
            mapping = wire_map.get(key)
            if mapping is not None and provider in mapping:
                result[mapping[provider]] = value
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the TokenUsage instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the TokenUsage instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
