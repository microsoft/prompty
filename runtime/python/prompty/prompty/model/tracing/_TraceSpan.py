##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ..model._TokenUsage import TokenUsage
from ._TraceTime import TraceTime


@dataclass
class TraceSpan:
    """A single trace span capturing one pipeline stage or function invocation.
    Spans nest via the `__frames` field to form a tree representing the
    full execution (§3.6.1).

    Attributes
    ----------
    name : str
        The name of this span (typically the function signature)
    __time : TraceTime
        Timing information for this span
    signature : Optional[str]
        Fully-qualified function signature that produced this span
    inputs : Optional[dict[str, Any]]
        Serialized input parameters (redacted per §3.4)
    output : Optional[Any]
        Serialized return value or error information (redacted per §3.4)
    error : Optional[str]
        Error message if the span ended with an exception
    __usage : Optional[TokenUsage]
        Aggregated token usage hoisted from child spans (§3.5)
    attributes : Optional[dict[str, Any]]
        Additional span attributes (e.g., OpenTelemetry GenAI attributes)
    __frames : Optional[list[Any]]
        Nested child spans forming the execution tree (recursive; each element is a TraceSpan)
    """

    _shorthand_property: ClassVar[str | None] = None

    name: str = field(default="")
    __time: TraceTime = field(default_factory=TraceTime)
    signature: str | None = None
    inputs: dict[str, Any] | None = None
    output: Any | None = None
    error: str | None = None
    __usage: TokenUsage | None = None
    attributes: dict[str, Any] | None = None
    __frames: list[Any] = field(default_factory=list)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "TraceSpan":
        """Load a TraceSpan instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            TraceSpan: The loaded TraceSpan instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for TraceSpan: {data}")

        # create new instance
        instance = TraceSpan()

        if data is not None and "name" in data:
            instance.name = data["name"]
        if data is not None and "__time" in data:
            instance.__time = TraceTime.load(data["__time"], context)
        if data is not None and "signature" in data:
            instance.signature = data["signature"]
        if data is not None and "inputs" in data:
            instance.inputs = data["inputs"]
        if data is not None and "output" in data:
            instance.output = data["output"]
        if data is not None and "error" in data:
            instance.error = data["error"]
        if data is not None and "__usage" in data:
            instance.__usage = TokenUsage.load(data["__usage"], context)
        if data is not None and "attributes" in data:
            instance.attributes = data["attributes"]
        if data is not None and "__frames" in data:
            instance.__frames = data["__frames"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the TraceSpan instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.name is not None:
            result["name"] = obj.name
        if obj.__time is not None:
            result["__time"] = obj.__time.save(context)
        if obj.signature is not None:
            result["signature"] = obj.signature
        if obj.inputs is not None:
            result["inputs"] = obj.inputs
        if obj.output is not None:
            result["output"] = obj.output
        if obj.error is not None:
            result["error"] = obj.error
        if obj.__usage is not None:
            result["__usage"] = obj.__usage.save(context)
        if obj.attributes is not None:
            result["attributes"] = obj.attributes
        if obj.__frames is not None:
            result["__frames"] = obj.__frames

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the TraceSpan instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the TraceSpan instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
