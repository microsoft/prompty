##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class RetryPayload:
    """Payload for "retry" events — a transient operation will be retried.

    Attributes
    ----------
    operation : str
        Operation being retried
    attempt : int
        Attempt number about to run
    max_attempts : Optional[int]
        Maximum configured attempts
    delay_ms : Optional[float]
        Backoff delay before the next attempt in milliseconds
    reason : Optional[str]
        Reason for the retry
    """

    _shorthand_property: ClassVar[str | None] = None

    operation: str = field(default="")
    attempt: int = field(default=0)
    max_attempts: int | None = None
    delay_ms: float | None = None
    reason: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "RetryPayload":
        """Load a RetryPayload instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            RetryPayload: The loaded RetryPayload instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for RetryPayload: {data}")

        # create new instance
        instance = RetryPayload()

        if data is not None and "operation" in data:
            instance.operation = data["operation"]
        if data is not None and "attempt" in data:
            instance.attempt = data["attempt"]
        if data is not None and "maxAttempts" in data:
            instance.max_attempts = data["maxAttempts"]
        if data is not None and "delayMs" in data:
            instance.delay_ms = data["delayMs"]
        if data is not None and "reason" in data:
            instance.reason = data["reason"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the RetryPayload instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.operation is not None:
            result["operation"] = obj.operation
        if obj.attempt is not None:
            result["attempt"] = obj.attempt
        if obj.max_attempts is not None:
            result["maxAttempts"] = obj.max_attempts
        if obj.delay_ms is not None:
            result["delayMs"] = obj.delay_ms
        if obj.reason is not None:
            result["reason"] = obj.reason

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the RetryPayload instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the RetryPayload instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
