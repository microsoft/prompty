##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from .._context import LoadContext, SaveContext
from ._RedactionMetadata import RedactionMetadata

HookStartScope = Literal["turn", "session"]


@dataclass
class HookStartPayload:
    """Payload for "hook_start" events — a host lifecycle hook is beginning.

    Attributes
    ----------
    hook_invocation_id : str
        Stable hook invocation identifier
    hook_type : str
        Host-defined hook type
    scope : Optional[str]
        Whether the hook is scoped to a turn or the outer session
    input : Optional[dict[str, Any]]
        Hook input after host-side sanitization
    redaction : Optional[RedactionMetadata]
        Redaction state for sensitive hook input fields
    """

    _shorthand_property: ClassVar[str | None] = None

    hook_invocation_id: str = field(default="")
    hook_type: str = field(default="")
    scope: HookStartScope | None = None
    input: dict[str, Any] | None = None
    redaction: RedactionMetadata | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "HookStartPayload":
        """Load a HookStartPayload instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            HookStartPayload: The loaded HookStartPayload instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for HookStartPayload: {data}")

        # create new instance
        instance = HookStartPayload()

        if data is not None and "hookInvocationId" in data:
            instance.hook_invocation_id = data["hookInvocationId"]
        if data is not None and "hookType" in data:
            instance.hook_type = data["hookType"]
        if data is not None and "scope" in data:
            instance.scope = data["scope"]
        if data is not None and "input" in data:
            instance.input = data["input"]
        if data is not None and "redaction" in data:
            instance.redaction = RedactionMetadata.load(data["redaction"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the HookStartPayload instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.hook_invocation_id is not None:
            result["hookInvocationId"] = obj.hook_invocation_id
        if obj.hook_type is not None:
            result["hookType"] = obj.hook_type
        if obj.scope is not None:
            result["scope"] = obj.scope
        if obj.input is not None:
            result["input"] = obj.input
        if obj.redaction is not None:
            result["redaction"] = obj.redaction.save(context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the HookStartPayload instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the HookStartPayload instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
