##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._RedactedField import RedactedField


@dataclass
class RedactionMetadata:
    """Metadata describing whether and how a payload was sanitized.

    Attributes
    ----------
    sanitized : Optional[bool]
        Whether the payload has been sanitized for persistence or external display
    fields : Optional[list[RedactedField]]
        Field-level redaction details
    policy : Optional[str]
        Host policy or sanitizer version that produced this metadata
    """

    _shorthand_property: ClassVar[str | None] = None

    sanitized: bool | None = None
    fields: list[RedactedField] = field(default_factory=list)
    policy: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "RedactionMetadata":
        """Load a RedactionMetadata instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            RedactionMetadata: The loaded RedactionMetadata instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for RedactionMetadata: {data}")

        # create new instance
        instance = RedactionMetadata()

        if data is not None and "sanitized" in data:
            instance.sanitized = data["sanitized"]
        if data is not None and "fields" in data:
            instance.fields = RedactionMetadata.load_fields(data["fields"], context)
        if data is not None and "policy" in data:
            instance.policy = data["policy"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_fields(data: dict | list, context: LoadContext | None) -> list[RedactedField]:
        if isinstance(data, dict):
            # convert simple named fields to list of RedactedField
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "path": v})
            data = result
        return [RedactedField.load(item, context) for item in data]

    @staticmethod
    def save_fields(items: list[RedactedField], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the RedactionMetadata instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.sanitized is not None:
            result["sanitized"] = obj.sanitized
        if obj.fields is not None:
            result["fields"] = RedactionMetadata.save_fields(obj.fields, context)
        if obj.policy is not None:
            result["policy"] = obj.policy

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the RedactionMetadata instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the RedactionMetadata instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
