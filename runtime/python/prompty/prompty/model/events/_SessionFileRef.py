# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class SessionFileRef:
    """A file observed or touched by a harness session.

    Attributes
    ----------
    session_id : Optional[str]
        Stable session identifier
    path : str
        File path, relative to the harness workspace when possible
    tool_name : Optional[str]
        Tool that first observed the file, when known
    turn_index : Optional[int]
        Zero-based turn index where the file was first observed
    first_seen_at : Optional[str]
        ISO 8601 UTC timestamp when the file was first observed
    """

    _shorthand_property: ClassVar[str | None] = None

    session_id: str | None = None
    path: str = field(default="")
    tool_name: str | None = None
    turn_index: int | None = None
    first_seen_at: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "SessionFileRef":
        """Load a SessionFileRef instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            SessionFileRef: The loaded SessionFileRef instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for SessionFileRef: {data}")

        # create new instance
        instance = SessionFileRef()

        if data is not None and "sessionId" in data:
            instance.session_id = data["sessionId"]
        if data is not None and "path" in data:
            instance.path = data["path"]
        if data is not None and "toolName" in data:
            instance.tool_name = data["toolName"]
        if data is not None and "turnIndex" in data:
            instance.turn_index = data["turnIndex"]
        if data is not None and "firstSeenAt" in data:
            instance.first_seen_at = data["firstSeenAt"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the SessionFileRef instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.session_id is not None:
            result["sessionId"] = obj.session_id
        if obj.path is not None:
            result["path"] = obj.path
        if obj.tool_name is not None:
            result["toolName"] = obj.tool_name
        if obj.turn_index is not None:
            result["turnIndex"] = obj.turn_index
        if obj.first_seen_at is not None:
            result["firstSeenAt"] = obj.first_seen_at

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the SessionFileRef instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the SessionFileRef instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
