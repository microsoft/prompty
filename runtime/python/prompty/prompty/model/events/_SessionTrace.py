# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._Checkpoint import Checkpoint
from ._SessionEvent import SessionEvent
from ._SessionFileRef import SessionFileRef
from ._SessionRef import SessionRef
from ._SessionSummary import SessionSummary
from ._TrajectoryEvent import TrajectoryEvent
from ._TurnTrace import TurnTrace


@dataclass
class SessionTrace:
    """Portable replay container for an outer harness session.

    Attributes
    ----------
    version : str
        Trace schema version
    runtime : Optional[str]
        Runtime name that produced the trace
    prompty_version : Optional[str]
        Prompty library version that produced the trace
    session_id : Optional[str]
        Stable session identifier
    events : list[SessionEvent]
        Recorded session events in emission order
    turns : Optional[list[TurnTrace]]
        Recorded turn traces associated with the session
    checkpoints : Optional[list[Checkpoint]]
        Checkpoints created during the session
    trajectory : Optional[list[TrajectoryEvent]]
        Compact trajectory records associated with the session
    files : Optional[list[SessionFileRef]]
        Files observed or touched during the session
    refs : Optional[list[SessionRef]]
        Non-file references observed during the session
    summary : Optional[SessionSummary]
        Optional summary computed from the event stream
    """

    _shorthand_property: ClassVar[str | None] = None

    version: str = field(default="1")
    runtime: str | None = None
    prompty_version: str | None = None
    session_id: str | None = None
    events: list[SessionEvent] = field(default_factory=list)
    turns: list[TurnTrace] = field(default_factory=list)
    checkpoints: list[Checkpoint] = field(default_factory=list)
    trajectory: list[TrajectoryEvent] = field(default_factory=list)
    files: list[SessionFileRef] = field(default_factory=list)
    refs: list[SessionRef] = field(default_factory=list)
    summary: SessionSummary | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "SessionTrace":
        """Load a SessionTrace instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            SessionTrace: The loaded SessionTrace instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for SessionTrace: {data}")

        # create new instance
        instance = SessionTrace()

        if data is not None and "version" in data:
            instance.version = data["version"]
        if data is not None and "runtime" in data:
            instance.runtime = data["runtime"]
        if data is not None and "promptyVersion" in data:
            instance.prompty_version = data["promptyVersion"]
        if data is not None and "sessionId" in data:
            instance.session_id = data["sessionId"]
        if data is not None and "events" in data:
            instance.events = SessionTrace.load_events(data["events"], context)
        if data is not None and "turns" in data:
            instance.turns = SessionTrace.load_turns(data["turns"], context)
        if data is not None and "checkpoints" in data:
            instance.checkpoints = SessionTrace.load_checkpoints(data["checkpoints"], context)
        if data is not None and "trajectory" in data:
            instance.trajectory = SessionTrace.load_trajectory(data["trajectory"], context)
        if data is not None and "files" in data:
            instance.files = SessionTrace.load_files(data["files"], context)
        if data is not None and "refs" in data:
            instance.refs = SessionTrace.load_refs(data["refs"], context)
        if data is not None and "summary" in data:
            instance.summary = SessionSummary.load(data["summary"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_events(data: dict | list, context: LoadContext | None) -> list[SessionEvent]:
        if isinstance(data, dict):
            # convert simple named events to list of SessionEvent
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "id": v})
            data = result
        return [SessionEvent.load(item, context) for item in data]

    @staticmethod
    def save_events(items: list[SessionEvent], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    @staticmethod
    def load_turns(data: dict | list, context: LoadContext | None) -> list[TurnTrace]:
        if isinstance(data, dict):
            # convert simple named turns to list of TurnTrace
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "version": v})
            data = result
        return [TurnTrace.load(item, context) for item in data]

    @staticmethod
    def save_turns(items: list[TurnTrace], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    @staticmethod
    def load_checkpoints(data: dict | list, context: LoadContext | None) -> list[Checkpoint]:
        if isinstance(data, dict):
            # convert simple named checkpoints to list of Checkpoint
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "id": v})
            data = result
        return [Checkpoint.load(item, context) for item in data]

    @staticmethod
    def save_checkpoints(items: list[Checkpoint], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    @staticmethod
    def load_trajectory(data: dict | list, context: LoadContext | None) -> list[TrajectoryEvent]:
        if isinstance(data, dict):
            # convert simple named trajectory to list of TrajectoryEvent
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "id": v})
            data = result
        return [TrajectoryEvent.load(item, context) for item in data]

    @staticmethod
    def save_trajectory(
        items: list[TrajectoryEvent], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    @staticmethod
    def load_files(data: dict | list, context: LoadContext | None) -> list[SessionFileRef]:
        if isinstance(data, dict):
            # convert simple named files to list of SessionFileRef
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "sessionId": v})
            data = result
        return [SessionFileRef.load(item, context) for item in data]

    @staticmethod
    def save_files(items: list[SessionFileRef], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    @staticmethod
    def load_refs(data: dict | list, context: LoadContext | None) -> list[SessionRef]:
        if isinstance(data, dict):
            # convert simple named refs to list of SessionRef
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "sessionId": v})
            data = result
        return [SessionRef.load(item, context) for item in data]

    @staticmethod
    def save_refs(items: list[SessionRef], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the SessionTrace instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.version is not None:
            result["version"] = obj.version
        if obj.runtime is not None:
            result["runtime"] = obj.runtime
        if obj.prompty_version is not None:
            result["promptyVersion"] = obj.prompty_version
        if obj.session_id is not None:
            result["sessionId"] = obj.session_id
        if obj.events is not None:
            result["events"] = SessionTrace.save_events(obj.events, context)
        if obj.turns is not None:
            result["turns"] = SessionTrace.save_turns(obj.turns, context)
        if obj.checkpoints is not None:
            result["checkpoints"] = SessionTrace.save_checkpoints(obj.checkpoints, context)
        if obj.trajectory is not None:
            result["trajectory"] = SessionTrace.save_trajectory(obj.trajectory, context)
        if obj.files is not None:
            result["files"] = SessionTrace.save_files(obj.files, context)
        if obj.refs is not None:
            result["refs"] = SessionTrace.save_refs(obj.refs, context)
        if obj.summary is not None:
            result["summary"] = obj.summary.save(context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the SessionTrace instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the SessionTrace instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
