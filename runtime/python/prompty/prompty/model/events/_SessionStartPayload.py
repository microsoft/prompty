##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._HarnessContext import HarnessContext


@dataclass
class SessionStartPayload:
    """Payload for "session_start" events.

    Attributes
    ----------
    session_id : str
        Stable session identifier
    schema_version : Optional[str]
        Session event schema version
    producer : Optional[str]
        Producer that started the session
    runtime : Optional[str]
        Runtime that produced the session
    prompty_version : Optional[str]
        Prompty library version
    start_time : Optional[str]
        ISO 8601 UTC timestamp when the session started
    selected_model : Optional[str]
        Selected model identifier, when known
    reasoning_effort : Optional[str]
        Selected reasoning effort or equivalent model setting, when known
    context : Optional[HarnessContext]
        Repository and execution context
    """

    _shorthand_property: ClassVar[str | None] = None

    session_id: str = field(default="")
    schema_version: str | None = None
    producer: str | None = None
    runtime: str | None = None
    prompty_version: str | None = None
    start_time: str | None = None
    selected_model: str | None = None
    reasoning_effort: str | None = None
    context: HarnessContext | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "SessionStartPayload":
        """Load a SessionStartPayload instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            SessionStartPayload: The loaded SessionStartPayload instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for SessionStartPayload: {data}")

        # create new instance
        instance = SessionStartPayload()

        if data is not None and "sessionId" in data:
            instance.session_id = data["sessionId"]
        if data is not None and "schemaVersion" in data:
            instance.schema_version = data["schemaVersion"]
        if data is not None and "producer" in data:
            instance.producer = data["producer"]
        if data is not None and "runtime" in data:
            instance.runtime = data["runtime"]
        if data is not None and "promptyVersion" in data:
            instance.prompty_version = data["promptyVersion"]
        if data is not None and "startTime" in data:
            instance.start_time = data["startTime"]
        if data is not None and "selectedModel" in data:
            instance.selected_model = data["selectedModel"]
        if data is not None and "reasoningEffort" in data:
            instance.reasoning_effort = data["reasoningEffort"]
        if data is not None and "context" in data:
            instance.context = HarnessContext.load(data["context"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the SessionStartPayload instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.session_id is not None:
            result["sessionId"] = obj.session_id
        if obj.schema_version is not None:
            result["schemaVersion"] = obj.schema_version
        if obj.producer is not None:
            result["producer"] = obj.producer
        if obj.runtime is not None:
            result["runtime"] = obj.runtime
        if obj.prompty_version is not None:
            result["promptyVersion"] = obj.prompty_version
        if obj.start_time is not None:
            result["startTime"] = obj.start_time
        if obj.selected_model is not None:
            result["selectedModel"] = obj.selected_model
        if obj.reasoning_effort is not None:
            result["reasoningEffort"] = obj.reasoning_effort
        if obj.context is not None:
            result["context"] = obj.context.save(context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the SessionStartPayload instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the SessionStartPayload instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
