# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from .._context import LoadContext, SaveContext
from ._RedactionMetadata import RedactionMetadata

SessionEventType = Literal[
    "session_start",
    "session_end",
    "session_warning",
    "session_hook_start",
    "session_hook_end",
    "checkpoint_created",
    "trajectory_event",
]


@dataclass
class SessionEvent:
    """A canonical event envelope emitted by an outer harness session.

    Attributes
    ----------
    id : str
        Unique identifier for this event
    type : str
        Event type discriminator
    timestamp : str
        ISO 8601 UTC timestamp when the event was emitted
    session_id : Optional[str]
        Stable identifier for the outer session
    turn_id : Optional[str]
        Associated turn identifier, when this session event is linked to a turn
    parent_id : Optional[str]
        Parent event or span identifier for reconstructing event hierarchy
    span_id : Optional[str]
        Trace span identifier associated with this event
    payload : dict[str, Any]
        Event-specific payload. Use the typed payload model matching 'type'.
    redaction : Optional[RedactionMetadata]
        Redaction state for sensitive payload fields
    """

    _shorthand_property: ClassVar[str | None] = None

    id: str = field(default="")
    type: SessionEventType = field(default="session_start")
    timestamp: str = field(default="")
    session_id: str | None = None
    turn_id: str | None = None
    parent_id: str | None = None
    span_id: str | None = None
    payload: dict[str, Any] = field(default_factory=dict)
    redaction: RedactionMetadata | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "SessionEvent":
        """Load a SessionEvent instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            SessionEvent: The loaded SessionEvent instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for SessionEvent: {data}")

        # create new instance
        instance = SessionEvent()

        if data is not None and "id" in data:
            instance.id = data["id"]
        if data is not None and "type" in data:
            instance.type = data["type"]
        if data is not None and "timestamp" in data:
            instance.timestamp = data["timestamp"]
        if data is not None and "sessionId" in data:
            instance.session_id = data["sessionId"]
        if data is not None and "turnId" in data:
            instance.turn_id = data["turnId"]
        if data is not None and "parentId" in data:
            instance.parent_id = data["parentId"]
        if data is not None and "spanId" in data:
            instance.span_id = data["spanId"]
        if data is not None and "payload" in data:
            instance.payload = data["payload"]
        if data is not None and "redaction" in data:
            instance.redaction = RedactionMetadata.load(data["redaction"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the SessionEvent instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.id is not None:
            result["id"] = obj.id
        if obj.type is not None:
            result["type"] = obj.type
        if obj.timestamp is not None:
            result["timestamp"] = obj.timestamp
        if obj.session_id is not None:
            result["sessionId"] = obj.session_id
        if obj.turn_id is not None:
            result["turnId"] = obj.turn_id
        if obj.parent_id is not None:
            result["parentId"] = obj.parent_id
        if obj.span_id is not None:
            result["spanId"] = obj.span_id
        if obj.payload is not None:
            result["payload"] = obj.payload
        if obj.redaction is not None:
            result["redaction"] = obj.redaction.save(context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the SessionEvent instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the SessionEvent instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
