# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ..model._TokenUsage import TokenUsage


@dataclass
class TurnSummary:
    """Summary statistics for a completed turn trace.

    Attributes
    ----------
    turn_id : str
        Stable identifier for the outer turn
    status : str
        Final turn status: 'success', 'error', or 'cancelled'
    iterations : int
        Number of agent-loop iterations
    llm_calls : Optional[int]
        Number of LLM calls made during the turn
    tool_calls : Optional[int]
        Number of tool calls dispatched during the turn
    retries : Optional[int]
        Number of retry events during the turn
    usage : Optional[TokenUsage]
        Aggregated token usage for the turn
    duration_ms : Optional[float]
        Total elapsed turn duration in milliseconds
    """

    _shorthand_property: ClassVar[str | None] = None

    turn_id: str = field(default="")
    status: str = field(default="")
    iterations: int = field(default=0)
    llm_calls: int | None = None
    tool_calls: int | None = None
    retries: int | None = None
    usage: TokenUsage | None = None
    duration_ms: float | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "TurnSummary":
        """Load a TurnSummary instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            TurnSummary: The loaded TurnSummary instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for TurnSummary: {data}")

        # create new instance
        instance = TurnSummary()

        if data is not None and "turnId" in data:
            instance.turn_id = data["turnId"]
        if data is not None and "status" in data:
            instance.status = data["status"]
        if data is not None and "iterations" in data:
            instance.iterations = data["iterations"]
        if data is not None and "llmCalls" in data:
            instance.llm_calls = data["llmCalls"]
        if data is not None and "toolCalls" in data:
            instance.tool_calls = data["toolCalls"]
        if data is not None and "retries" in data:
            instance.retries = data["retries"]
        if data is not None and "usage" in data:
            instance.usage = TokenUsage.load(data["usage"], context)
        if data is not None and "durationMs" in data:
            instance.duration_ms = data["durationMs"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the TurnSummary instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.turn_id is not None:
            result["turnId"] = obj.turn_id
        if obj.status is not None:
            result["status"] = obj.status
        if obj.iterations is not None:
            result["iterations"] = obj.iterations
        if obj.llm_calls is not None:
            result["llmCalls"] = obj.llm_calls
        if obj.tool_calls is not None:
            result["toolCalls"] = obj.tool_calls
        if obj.retries is not None:
            result["retries"] = obj.retries
        if obj.usage is not None:
            result["usage"] = obj.usage.save(context)
        if obj.duration_ms is not None:
            result["durationMs"] = obj.duration_ms

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the TurnSummary instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the TurnSummary instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
