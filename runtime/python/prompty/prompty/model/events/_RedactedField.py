##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from .._context import LoadContext, SaveContext

RedactionMode = Literal["none", "redacted", "hashed", "summary", "reference"]


@dataclass
class RedactedField:
    """Redaction handling for one JSON-shaped field path.

    Attributes
    ----------
    path : str
        JSONPath-like field path, relative to the containing payload
    mode : str
        How the field was represented
    reason : Optional[str]
        Human-readable reason or policy that caused this handling
    """

    _shorthand_property: ClassVar[str | None] = None

    path: str = field(default="")
    mode: RedactionMode = field(default="none")
    reason: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "RedactedField":
        """Load a RedactedField instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            RedactedField: The loaded RedactedField instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for RedactedField: {data}")

        # create new instance
        instance = RedactedField()

        if data is not None and "path" in data:
            instance.path = data["path"]
        if data is not None and "mode" in data:
            instance.mode = data["mode"]
        if data is not None and "reason" in data:
            instance.reason = data["reason"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the RedactedField instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.path is not None:
            result["path"] = obj.path
        if obj.mode is not None:
            result["mode"] = obj.mode
        if obj.reason is not None:
            result["reason"] = obj.reason

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the RedactedField instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the RedactedField instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
