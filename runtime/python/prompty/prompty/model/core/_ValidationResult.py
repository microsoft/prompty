# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._ValidationError import ValidationError


@dataclass
class ValidationResult:
    """The result of validating inputs against a Prompty's inputs.
    Returned by `validate_inputs` (§12.2) to indicate whether all
    required inputs are present and satisfy their constraints.

    Attributes
    ----------
    valid : bool
        Whether all inputs passed validation
    errors : list[ValidationError]
        List of validation errors (empty when valid is true)
    """

    _shorthand_property: ClassVar[str | None] = None

    valid: bool = field(default=False)
    errors: list[ValidationError] = field(default_factory=list)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "ValidationResult":
        """Load a ValidationResult instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            ValidationResult: The loaded ValidationResult instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for ValidationResult: {data}")

        # create new instance
        instance = ValidationResult()

        if data is not None and "valid" in data:
            instance.valid = data["valid"]
        if data is not None and "errors" in data:
            instance.errors = ValidationResult.load_errors(data["errors"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_errors(data: dict | list, context: LoadContext | None) -> list[ValidationError]:
        if isinstance(data, dict):
            # convert simple named errors to list of ValidationError
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "message": v})
            data = result
        return [ValidationError.load(item, context) for item in data]

    @staticmethod
    def save_errors(items: list[ValidationError], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the ValidationResult instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.valid is not None:
            result["valid"] = obj.valid
        if obj.errors is not None:
            result["errors"] = ValidationResult.save_errors(obj.errors, context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the ValidationResult instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the ValidationResult instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
