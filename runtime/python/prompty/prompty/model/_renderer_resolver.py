# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass

from ._Renderer import Renderer

RENDERER_VARIANTS: tuple[str, ...] = (
    "jinja2",
    "mustache",
)
"""Every @dispatch variant the Renderer seam must cover, in shape order."""


@dataclass
class RendererProvider:
    """Carries one Renderer impl per @dispatch variant, the twin of the shape
    discriminator's variant set. A ``None`` slot models a valid-but-
    unimplemented variant; build via :func:`new_renderer_provider`
    so a forgotten variant is a collection-time error rather than a silent
    ``None`` (the slots are required, so a bare constructor call cannot skip
    a variant either).
    """

    jinja2: Renderer | None
    mustache: Renderer | None
    custom: Renderer | None = None


def new_renderer_provider(impls: Mapping[str, Renderer | None]) -> RendererProvider:
    """Build a :class:`RendererProvider` from a variant->impl mapping, raising when
    a variant is ABSENT (a forgotten attachment). A key present with a
    ``None`` value is allowed and marks a variant as valid-but-unimplemented.
    """
    missing = [kind for kind in RENDERER_VARIANTS if kind not in impls]
    if missing:
        raise ValueError(f"Renderer provider is missing @dispatch variant(s): {missing}")
    return RendererProvider(
        jinja2=impls["jinja2"],
        mustache=impls["mustache"],
        custom=impls.get("custom"),
    )


def resolve_renderer(kind: str, registry: RendererProvider) -> Renderer | None:
    """Map a 'kind' discriminator to the attached Renderer impl — the
    behavioral twin of the shape discriminator load switch. A known variant
    returns its slot; an unknown discriminator routes to the wildcard
    ``custom`` default slot (the declared ``*`` child), mirroring the
    shape loader's `*`-tolerant fallback — never a silent miss.
    """
    if kind == "jinja2":
        return registry.jinja2
    elif kind == "mustache":
        return registry.mustache
    return registry.custom
