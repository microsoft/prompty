# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class OAuthToken:
    """Provider-neutral OAuth token material returned by an authorization server.

    Attributes
    ----------
    access_token : str
        The bearer access token
    token_type : str
        The token type, typically Bearer
    expires_in : int
        Lifetime of the access token in seconds
    refresh_token : Optional[str]
        Refresh token when offline access was granted
    scope : Optional[str]
        Scopes granted by the authorization server
    """

    _shorthand_property: ClassVar[str | None] = None

    access_token: str = field(default="")
    token_type: str = field(default="")
    expires_in: int = field(default=0)
    refresh_token: str | None = None
    scope: str | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "OAuthToken":
        """Load a OAuthToken instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            OAuthToken: The loaded OAuthToken instance.

        """

        if context is None:
            context = LoadContext()
        data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for OAuthToken: {data}")

        # create new instance
        instance = OAuthToken()

        if data is not None and "accessToken" in data:
            instance.access_token = data["accessToken"]
        if data is not None and "tokenType" in data:
            instance.token_type = data["tokenType"]
        if data is not None and "expiresIn" in data:
            instance.expires_in = data["expiresIn"]
        if data is not None and "refreshToken" in data:
            instance.refresh_token = data["refreshToken"]
        if data is not None and "scope" in data:
            instance.scope = data["scope"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the OAuthToken instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.access_token is not None:
            result["accessToken"] = obj.access_token
        if obj.token_type is not None:
            result["tokenType"] = obj.token_type
        if obj.expires_in is not None:
            result["expiresIn"] = obj.expires_in
        if obj.refresh_token is not None:
            result["refreshToken"] = obj.refresh_token
        if obj.scope is not None:
            result["scope"] = obj.scope

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_wire(self, provider: str) -> dict[str, Any]:
        """Convert to provider-specific wire format.
        Args:
            provider (str): The provider to convert to (e.g., "openai", "anthropic").
        Returns:
            dict[str, Any]: The wire-format dictionary with provider-specific field names.

        """
        data = self.save()
        result: dict[str, Any] = {}
        wire_map: dict[str, dict[str, str]] = {
            "accessToken": {"foundry": "access_token"},
            "tokenType": {"foundry": "token_type"},
            "expiresIn": {"foundry": "expires_in"},
            "refreshToken": {"foundry": "refresh_token"},
            "scope": {"foundry": "scope"},
        }
        for key, value in data.items():
            mapping = wire_map.get(key)
            if mapping is not None and provider in mapping:
                result[mapping[provider]] = value
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the OAuthToken instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the OAuthToken instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
