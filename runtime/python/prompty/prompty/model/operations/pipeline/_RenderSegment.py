# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from ..._context import LoadContext, SaveContext

RenderSegmentKind = Literal["literal", "interp"]


@dataclass
class RenderSegment:
    """One provenance-tagged span of rendered output (Prompty Jinja Subset §7,
    spec/jinja-grammar.md). Concatenating every segment's `text` in source order
    yields the flat string returned by `render`, so `renderSegments` is the
    provenance-carrying superset of `render`.

    `literal` spans originate in the template source; `interp` spans originate in
    an interpolated input value and carry the originating root property `source`.
    That provenance lets a downstream structural parser distinguish an
    author-authored role boundary from one forged inside untrusted input — the
    injection-deterrence contract that replaces in-band sentinel scanning. A span
    whose `source` property is declared strict is flagged `strict`; a strict span
    that forges a role boundary is rejected loudly rather than emitted.

    Attributes
    ----------
    kind : str
        Whether this span came from the template (literal) or an input value (interp)
    text : str
        The rendered text of this span
    source : Optional[str]
        For interp spans, the root input property the value came from
    strict : Optional[bool]
        Whether the originating input property is declared strict
    """

    _shorthand_property: ClassVar[str | None] = None

    kind: RenderSegmentKind = field(default="literal")
    text: str = field(default="")
    source: str | None = None
    strict: bool | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "RenderSegment":
        """Load a RenderSegment instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            RenderSegment: The loaded RenderSegment instance.

        """

        if context is None:
            context = LoadContext()
        data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for RenderSegment: {data}")

        # create new instance
        instance = RenderSegment()

        if data is not None and "kind" in data:
            instance.kind = data["kind"]
        if data is not None and "text" in data:
            instance.text = data["text"]
        if data is not None and "source" in data:
            instance.source = data["source"]
        if data is not None and "strict" in data:
            instance.strict = data["strict"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the RenderSegment instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.kind is not None:
            result["kind"] = obj.kind
        if obj.text is not None:
            result["text"] = obj.text
        if obj.source is not None:
            result["source"] = obj.source
        if obj.strict is not None:
            result["strict"] = obj.strict

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the RenderSegment instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the RenderSegment instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
