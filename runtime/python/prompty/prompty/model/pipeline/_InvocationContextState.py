# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from .._context import LoadContext, SaveContext
from ._DelegatedStateReference import DelegatedStateReference

InvocationContextPortability = Literal["portable", "delegated", "opaque"]


@dataclass
class InvocationContextState:
    """Provider-context state carried into or out of an invocation.

    Attributes
    ----------
    portability : str
        Whether the model-visible context is portable across providers
    delegated_state : Optional[list[DelegatedStateReference]]
        Explicit references to provider-held context state
    """

    _shorthand_property: ClassVar[str | None] = None

    portability: InvocationContextPortability = field(default="portable")
    delegated_state: list[DelegatedStateReference] = field(default_factory=list)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "InvocationContextState":
        """Load a InvocationContextState instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            InvocationContextState: The loaded InvocationContextState instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for InvocationContextState: {data}")

        # create new instance
        instance = InvocationContextState()

        if data is not None and "portability" in data:
            instance.portability = data["portability"]
        if data is not None and "delegatedState" in data:
            instance.delegated_state = InvocationContextState.load_delegated_state(data["delegatedState"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_delegated_state(data: dict | list, context: LoadContext | None) -> list[DelegatedStateReference]:
        if isinstance(data, dict):
            # convert simple named delegatedState to list of DelegatedStateReference
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "provider": v})
            data = result
        return [DelegatedStateReference.load(item, context) for item in data]

    @staticmethod
    def save_delegated_state(
        items: list[DelegatedStateReference], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the InvocationContextState instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.portability is not None:
            result["portability"] = obj.portability
        if obj.delegated_state is not None:
            result["delegatedState"] = InvocationContextState.save_delegated_state(obj.delegated_state, context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the InvocationContextState instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the InvocationContextState instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
