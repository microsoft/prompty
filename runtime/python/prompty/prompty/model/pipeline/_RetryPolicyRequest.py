# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext


@dataclass
class RetryPolicyRequest:
    """Context supplied to the retry policy after a retryable model failure.

    Attributes
    ----------
    failed_attempts : int
        Number of failures observed for this invocation, starting at one
    next_attempt : int
        One-based attempt number that will run after backoff
    max_attempts : int
        Maximum attempts permitted for the invocation
    reason : str
        Human-readable reason for the retry
    """

    _shorthand_property: ClassVar[str | None] = None

    failed_attempts: int = field(default=0)
    next_attempt: int = field(default=0)
    max_attempts: int = field(default=0)
    reason: str = field(default="")

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "RetryPolicyRequest":
        """Load a RetryPolicyRequest instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            RetryPolicyRequest: The loaded RetryPolicyRequest instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for RetryPolicyRequest: {data}")

        # create new instance
        instance = RetryPolicyRequest()

        if data is not None and "failedAttempts" in data:
            instance.failed_attempts = data["failedAttempts"]
        if data is not None and "nextAttempt" in data:
            instance.next_attempt = data["nextAttempt"]
        if data is not None and "maxAttempts" in data:
            instance.max_attempts = data["maxAttempts"]
        if data is not None and "reason" in data:
            instance.reason = data["reason"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the RetryPolicyRequest instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.failed_attempts is not None:
            result["failedAttempts"] = obj.failed_attempts
        if obj.next_attempt is not None:
            result["nextAttempt"] = obj.next_attempt
        if obj.max_attempts is not None:
            result["maxAttempts"] = obj.max_attempts
        if obj.reason is not None:
            result["reason"] = obj.reason

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the RetryPolicyRequest instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the RetryPolicyRequest instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
