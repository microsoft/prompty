# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from .._context import LoadContext, SaveContext
from ._ReplayMismatch import ReplayMismatch

ReplayVerificationStatus = Literal["passed", "failed"]


@dataclass
class ReplayVerificationResult:
    """Result returned by a replay verifier implementation.

    Attributes
    ----------
    status : str
        Replay verification status
    mismatches : Optional[list[ReplayMismatch]]
        Record mismatches, empty when verification passed
    expected_count : int
        Number of expected records
    actual_count : int
        Number of actual records
    """

    _shorthand_property: ClassVar[str | None] = None

    status: ReplayVerificationStatus = field(default="passed")
    mismatches: list[ReplayMismatch] = field(default_factory=list)
    expected_count: int = field(default=0)
    actual_count: int = field(default=0)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "ReplayVerificationResult":
        """Load a ReplayVerificationResult instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            ReplayVerificationResult: The loaded ReplayVerificationResult instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for ReplayVerificationResult: {data}")

        # create new instance
        instance = ReplayVerificationResult()

        if data is not None and "status" in data:
            instance.status = data["status"]
        if data is not None and "mismatches" in data:
            instance.mismatches = ReplayVerificationResult.load_mismatches(data["mismatches"], context)
        if data is not None and "expectedCount" in data:
            instance.expected_count = data["expectedCount"]
        if data is not None and "actualCount" in data:
            instance.actual_count = data["actualCount"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_mismatches(data: dict | list, context: LoadContext | None) -> list[ReplayMismatch]:
        if isinstance(data, dict):
            # convert simple named mismatches to list of ReplayMismatch
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "index": v})
            data = result
        return [ReplayMismatch.load(item, context) for item in data]

    @staticmethod
    def save_mismatches(
        items: list[ReplayMismatch], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the ReplayVerificationResult instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.status is not None:
            result["status"] = obj.status
        if obj.mismatches is not None:
            result["mismatches"] = ReplayVerificationResult.save_mismatches(obj.mismatches, context)
        if obj.expected_count is not None:
            result["expectedCount"] = obj.expected_count
        if obj.actual_count is not None:
            result["actualCount"] = obj.actual_count

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the ReplayVerificationResult instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the ReplayVerificationResult instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
