# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ._ModelInvocationRequest import ModelInvocationRequest


@dataclass
class ModelReconciliationState:
    """Durable state required to reconcile one indeterminate model invocation.

    Retained when a model effect completed with an unknown outcome so a resumed
    run can determine success or failure without re-invoking the provider.

    Attributes
    ----------
    invocation_id : str
        Model invocation identifier awaiting reconciliation
    request : ModelInvocationRequest
        The request whose outcome must be reconciled
    failed_attempt : int
        Zero-based attempt number that failed with an unknown outcome
    message : str
        Human-readable reason the outcome is indeterminate
    metadata : Optional[dict[str, Any]]
        Opaque host-specific reconciliation metadata
    """

    _shorthand_property: ClassVar[str | None] = None

    invocation_id: str = field(default="")
    request: ModelInvocationRequest = field(default_factory=ModelInvocationRequest)
    failed_attempt: int = field(default=0)
    message: str = field(default="")
    metadata: dict[str, Any] | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "ModelReconciliationState":
        """Load a ModelReconciliationState instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            ModelReconciliationState: The loaded ModelReconciliationState instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for ModelReconciliationState: {data}")

        # create new instance
        instance = ModelReconciliationState()

        if data is not None and "invocationId" in data:
            instance.invocation_id = data["invocationId"]
        if data is not None and "request" in data:
            instance.request = ModelInvocationRequest.load(data["request"], context)
        if data is not None and "failedAttempt" in data:
            instance.failed_attempt = data["failedAttempt"]
        if data is not None and "message" in data:
            instance.message = data["message"]
        if data is not None and "metadata" in data:
            instance.metadata = data["metadata"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the ModelReconciliationState instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.invocation_id is not None:
            result["invocationId"] = obj.invocation_id
        if obj.request is not None:
            result["request"] = obj.request.save(context)
        if obj.failed_attempt is not None:
            result["failedAttempt"] = obj.failed_attempt
        if obj.message is not None:
            result["message"] = obj.message
        if obj.metadata is not None:
            result["metadata"] = obj.metadata

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the ModelReconciliationState instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the ModelReconciliationState instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
