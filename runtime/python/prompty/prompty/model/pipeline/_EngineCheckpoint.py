# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ..conversation._Message import Message
from ._InvocationContextState import InvocationContextState
from ._ModelInvocationResponse import ModelInvocationResponse
from ._ModelReconciliationState import ModelReconciliationState
from ._ModelToolRequest import ModelToolRequest
from ._ModelToolResult import ModelToolResult


@dataclass
class EngineCheckpoint:
    """Portable durable checkpoint emitted after a committed model/tool round.

    A resumed run rebuilt from a checkpoint MUST NOT duplicate a model or tool
    effect that the checkpoint already records as committed. Run identity is
    carried so delegated runs checkpoint independently under their own run.

    Attributes
    ----------
    id : str
        Stable unique identifier for this checkpoint
    session_id : str
        Stable session identifier
    turn_id : str
        Stable turn identifier
    run_id : str
        Stable identifier of this engine run
    parent_run_id : Optional[str]
        Run identifier of the parent run when this run was delegated
    delegation_depth : int
        Zero-based delegation nesting depth; 0 for a top-level run
    iteration : int
        Zero-based model loop iteration captured by this checkpoint
    last_sequence : int
        Last committed event sequence included in this checkpoint
    messages : list[Message]
        Canonical conversation messages at checkpoint time
    stable_prefix_messages : int
        Number of leading messages eligible for provider prefix-cache reuse
    inputs : Optional[Any]
        Turn inputs captured for deterministic resume
    active_invocation_id : Optional[str]
        Active model invocation identifier, when one is in flight
    pending_tool_requests : Optional[list[ModelToolRequest]]
        Tool requests awaiting execution or commitment
    completed_tool_results : Optional[list[ModelToolResult]]
        Tool results already executed in this round
    completed_model_iterations : int
        Number of fully completed model iterations
    reconciliation_required : bool
        Whether the turn is blocked pending model outcome reconciliation
    model_reconciliation : Optional[ModelReconciliationState]
        Typed provider state when model outcome reconciliation is required
    pending_output : Optional[Any]
        Final output pending commit, when computed
    final_output_ready : bool
        Whether the final output is ready to commit
    pending_model_response : Optional[ModelInvocationResponse]
        Model response retained until all tool results form one conversation batch
    resume_same_iteration : bool
        Resume this exact iteration because the checkpoint precedes an external model effect
    policy_applied_for_iteration : bool
        Whether the host policy rewrite is already durable for this iteration
    context_state : InvocationContextState
        Provider-context state carried by this checkpoint
    metadata : Optional[dict[str, Any]]
        Opaque host-specific checkpoint metadata
    """

    _shorthand_property: ClassVar[str | None] = None

    id: str = field(default="")
    session_id: str = field(default="")
    turn_id: str = field(default="")
    run_id: str = field(default="")
    parent_run_id: str | None = None
    delegation_depth: int = field(default=0)
    iteration: int = field(default=0)
    last_sequence: int = field(default=0)
    messages: list[Message] = field(default_factory=list)
    stable_prefix_messages: int = field(default=0)
    inputs: Any | None = None
    active_invocation_id: str | None = None
    pending_tool_requests: list[ModelToolRequest] = field(default_factory=list)
    completed_tool_results: list[ModelToolResult] = field(default_factory=list)
    completed_model_iterations: int = field(default=0)
    reconciliation_required: bool = field(default=False)
    model_reconciliation: ModelReconciliationState | None = None
    pending_output: Any | None = None
    final_output_ready: bool = field(default=False)
    pending_model_response: ModelInvocationResponse | None = None
    resume_same_iteration: bool = field(default=False)
    policy_applied_for_iteration: bool = field(default=False)
    context_state: InvocationContextState = field(default_factory=InvocationContextState)
    metadata: dict[str, Any] | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "EngineCheckpoint":
        """Load a EngineCheckpoint instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            EngineCheckpoint: The loaded EngineCheckpoint instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for EngineCheckpoint: {data}")

        # create new instance
        instance = EngineCheckpoint()

        if data is not None and "id" in data:
            instance.id = data["id"]
        if data is not None and "sessionId" in data:
            instance.session_id = data["sessionId"]
        if data is not None and "turnId" in data:
            instance.turn_id = data["turnId"]
        if data is not None and "runId" in data:
            instance.run_id = data["runId"]
        if data is not None and "parentRunId" in data:
            instance.parent_run_id = data["parentRunId"]
        if data is not None and "delegationDepth" in data:
            instance.delegation_depth = data["delegationDepth"]
        if data is not None and "iteration" in data:
            instance.iteration = data["iteration"]
        if data is not None and "lastSequence" in data:
            instance.last_sequence = data["lastSequence"]
        if data is not None and "messages" in data:
            instance.messages = EngineCheckpoint.load_messages(data["messages"], context)
        if data is not None and "stablePrefixMessages" in data:
            instance.stable_prefix_messages = data["stablePrefixMessages"]
        if data is not None and "inputs" in data:
            instance.inputs = data["inputs"]
        if data is not None and "activeInvocationId" in data:
            instance.active_invocation_id = data["activeInvocationId"]
        if data is not None and "pendingToolRequests" in data:
            instance.pending_tool_requests = EngineCheckpoint.load_pending_tool_requests(
                data["pendingToolRequests"], context
            )
        if data is not None and "completedToolResults" in data:
            instance.completed_tool_results = EngineCheckpoint.load_completed_tool_results(
                data["completedToolResults"], context
            )
        if data is not None and "completedModelIterations" in data:
            instance.completed_model_iterations = data["completedModelIterations"]
        if data is not None and "reconciliationRequired" in data:
            instance.reconciliation_required = data["reconciliationRequired"]
        if data is not None and "modelReconciliation" in data:
            instance.model_reconciliation = ModelReconciliationState.load(data["modelReconciliation"], context)
        if data is not None and "pendingOutput" in data:
            instance.pending_output = data["pendingOutput"]
        if data is not None and "finalOutputReady" in data:
            instance.final_output_ready = data["finalOutputReady"]
        if data is not None and "pendingModelResponse" in data:
            instance.pending_model_response = ModelInvocationResponse.load(data["pendingModelResponse"], context)
        if data is not None and "resumeSameIteration" in data:
            instance.resume_same_iteration = data["resumeSameIteration"]
        if data is not None and "policyAppliedForIteration" in data:
            instance.policy_applied_for_iteration = data["policyAppliedForIteration"]
        if data is not None and "contextState" in data:
            instance.context_state = InvocationContextState.load(data["contextState"], context)
        if data is not None and "metadata" in data:
            instance.metadata = data["metadata"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_messages(data: dict | list, context: LoadContext | None) -> list[Message]:
        if isinstance(data, dict):
            # convert simple named messages to list of Message
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "role": v})
            data = result
        return [Message.load(item, context) for item in data]

    @staticmethod
    def save_messages(items: list[Message], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    @staticmethod
    def load_pending_tool_requests(data: dict | list, context: LoadContext | None) -> list[ModelToolRequest]:
        if isinstance(data, dict):
            # convert simple named pendingToolRequests to list of ModelToolRequest
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "id": v})
            data = result
        return [ModelToolRequest.load(item, context) for item in data]

    @staticmethod
    def save_pending_tool_requests(
        items: list[ModelToolRequest], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        if context.collection_format == "array":
            return [item.save(context) for item in items]

        # Object format: use name as key
        result: dict[str, Any] = {}
        for item in items:
            item_data = item.save(context)
            name = item_data.pop("name", None)
            if name:
                # Check if we can use shorthand (only primary property set)
                if context.use_shorthand and hasattr(item, "_shorthand_property"):
                    shorthand_prop = item._shorthand_property
                    if shorthand_prop and len(item_data) == 1 and shorthand_prop in item_data:
                        result[name] = item_data[shorthand_prop]
                        continue
                result[name] = item_data
            else:
                # No name, fall back to array format for this item
                if "_unnamed" not in result:
                    result["_unnamed"] = []
                result["_unnamed"].append(item_data)
        return result

    @staticmethod
    def load_completed_tool_results(data: dict | list, context: LoadContext | None) -> list[ModelToolResult]:
        if isinstance(data, dict):
            # convert simple named completedToolResults to list of ModelToolResult
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "requestId": v})
            data = result
        return [ModelToolResult.load(item, context) for item in data]

    @staticmethod
    def save_completed_tool_results(
        items: list[ModelToolResult], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        if context.collection_format == "array":
            return [item.save(context) for item in items]

        # Object format: use name as key
        result: dict[str, Any] = {}
        for item in items:
            item_data = item.save(context)
            name = item_data.pop("name", None)
            if name:
                # Check if we can use shorthand (only primary property set)
                if context.use_shorthand and hasattr(item, "_shorthand_property"):
                    shorthand_prop = item._shorthand_property
                    if shorthand_prop and len(item_data) == 1 and shorthand_prop in item_data:
                        result[name] = item_data[shorthand_prop]
                        continue
                result[name] = item_data
            else:
                # No name, fall back to array format for this item
                if "_unnamed" not in result:
                    result["_unnamed"] = []
                result["_unnamed"].append(item_data)
        return result

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the EngineCheckpoint instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.id is not None:
            result["id"] = obj.id
        if obj.session_id is not None:
            result["sessionId"] = obj.session_id
        if obj.turn_id is not None:
            result["turnId"] = obj.turn_id
        if obj.run_id is not None:
            result["runId"] = obj.run_id
        if obj.parent_run_id is not None:
            result["parentRunId"] = obj.parent_run_id
        if obj.delegation_depth is not None:
            result["delegationDepth"] = obj.delegation_depth
        if obj.iteration is not None:
            result["iteration"] = obj.iteration
        if obj.last_sequence is not None:
            result["lastSequence"] = obj.last_sequence
        if obj.messages is not None:
            result["messages"] = EngineCheckpoint.save_messages(obj.messages, context)
        if obj.stable_prefix_messages is not None:
            result["stablePrefixMessages"] = obj.stable_prefix_messages
        if obj.inputs is not None:
            result["inputs"] = obj.inputs
        if obj.active_invocation_id is not None:
            result["activeInvocationId"] = obj.active_invocation_id
        if obj.pending_tool_requests is not None:
            result["pendingToolRequests"] = EngineCheckpoint.save_pending_tool_requests(
                obj.pending_tool_requests, context
            )
        if obj.completed_tool_results is not None:
            result["completedToolResults"] = EngineCheckpoint.save_completed_tool_results(
                obj.completed_tool_results, context
            )
        if obj.completed_model_iterations is not None:
            result["completedModelIterations"] = obj.completed_model_iterations
        if obj.reconciliation_required is not None:
            result["reconciliationRequired"] = obj.reconciliation_required
        if obj.model_reconciliation is not None:
            result["modelReconciliation"] = obj.model_reconciliation.save(context)
        if obj.pending_output is not None:
            result["pendingOutput"] = obj.pending_output
        if obj.final_output_ready is not None:
            result["finalOutputReady"] = obj.final_output_ready
        if obj.pending_model_response is not None:
            result["pendingModelResponse"] = obj.pending_model_response.save(context)
        if obj.resume_same_iteration is not None:
            result["resumeSameIteration"] = obj.resume_same_iteration
        if obj.policy_applied_for_iteration is not None:
            result["policyAppliedForIteration"] = obj.policy_applied_for_iteration
        if obj.context_state is not None:
            result["contextState"] = obj.context_state.save(context)
        if obj.metadata is not None:
            result["metadata"] = obj.metadata

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the EngineCheckpoint instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the EngineCheckpoint instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
