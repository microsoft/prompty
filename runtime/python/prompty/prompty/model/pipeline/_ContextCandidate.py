# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ..conversation._Message import Message


@dataclass
class ContextCandidate:
    """A context contribution before filtering, ranking, and packing.

    Attributes
    ----------
    id : str
        Stable candidate identifier
    source : str
        Host-defined source of the candidate
    messages : list[Message]
        Messages contributed by the candidate
    metadata : Optional[dict[str, Any]]
        Opaque host-specific candidate metadata
    """

    _shorthand_property: ClassVar[str | None] = None

    id: str = field(default="")
    source: str = field(default="")
    messages: list[Message] = field(default_factory=list)
    metadata: dict[str, Any] | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "ContextCandidate":
        """Load a ContextCandidate instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            ContextCandidate: The loaded ContextCandidate instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for ContextCandidate: {data}")

        # create new instance
        instance = ContextCandidate()

        if data is not None and "id" in data:
            instance.id = data["id"]
        if data is not None and "source" in data:
            instance.source = data["source"]
        if data is not None and "messages" in data:
            instance.messages = ContextCandidate.load_messages(data["messages"], context)
        if data is not None and "metadata" in data:
            instance.metadata = data["metadata"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_messages(data: dict | list, context: LoadContext | None) -> list[Message]:
        if isinstance(data, dict):
            # convert simple named messages to list of Message
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "role": v})
            data = result
        return [Message.load(item, context) for item in data]

    @staticmethod
    def save_messages(items: list[Message], context: SaveContext | None) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the ContextCandidate instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.id is not None:
            result["id"] = obj.id
        if obj.source is not None:
            result["source"] = obj.source
        if obj.messages is not None:
            result["messages"] = ContextCandidate.save_messages(obj.messages, context)
        if obj.metadata is not None:
            result["metadata"] = obj.metadata

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the ContextCandidate instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the ContextCandidate instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
