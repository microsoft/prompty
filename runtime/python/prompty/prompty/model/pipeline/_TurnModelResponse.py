# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ..events._HostToolRequest import HostToolRequest
from ..model._InvocationUsage import InvocationUsage


@dataclass
class TurnModelResponse:
    """Response returned by the injected model callback to the reference turn runner.

    Attributes
    ----------
    output : Optional[Any]
        Provider-neutral final model output for the turn when no more tools are requested
    usage : Optional[InvocationUsage]
        Complete cumulative token usage reported for this model invocation, when available
    tool_requests : Optional[list[HostToolRequest]]
        Host tool execution requests emitted by the model callback
    checkpoint_state : Optional[dict[str, Any]]
        Additional deterministic state to merge into the iteration checkpoint
    """

    _shorthand_property: ClassVar[str | None] = None

    output: Any | None = None
    usage: InvocationUsage | None = None
    tool_requests: list[HostToolRequest] = field(default_factory=list)
    checkpoint_state: dict[str, Any] | None = None

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "TurnModelResponse":
        """Load a TurnModelResponse instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            TurnModelResponse: The loaded TurnModelResponse instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for TurnModelResponse: {data}")

        # create new instance
        instance = TurnModelResponse()

        if data is not None and "output" in data:
            instance.output = data["output"]
        if data is not None and "usage" in data:
            instance.usage = InvocationUsage.load(data["usage"], context)
        if data is not None and "toolRequests" in data:
            instance.tool_requests = TurnModelResponse.load_tool_requests(data["toolRequests"], context)
        if data is not None and "checkpointState" in data:
            instance.checkpoint_state = data["checkpointState"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_tool_requests(data: dict | list, context: LoadContext | None) -> list[HostToolRequest]:
        if isinstance(data, dict):
            # convert simple named toolRequests to list of HostToolRequest
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "requestId": v})
            data = result
        return [HostToolRequest.load(item, context) for item in data]

    @staticmethod
    def save_tool_requests(
        items: list[HostToolRequest], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the TurnModelResponse instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.output is not None:
            result["output"] = obj.output
        if obj.usage is not None:
            result["usage"] = obj.usage.save(context)
        if obj.tool_requests is not None:
            result["toolRequests"] = TurnModelResponse.save_tool_requests(obj.tool_requests, context)
        if obj.checkpoint_state is not None:
            result["checkpointState"] = obj.checkpoint_state

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the TurnModelResponse instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the TurnModelResponse instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
