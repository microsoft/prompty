# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar

from .._context import LoadContext, SaveContext
from ..events._HostToolResult import HostToolResult
from ._TurnOptions import TurnOptions


@dataclass
class TurnModelRequest:
    """Request passed by the reference turn runner to the injected model callback.

    The runner owns deterministic orchestration semantics; model/provider-specific
    execution stays behind this callback boundary.

    Attributes
    ----------
    session_id : str
        Stable harness session identifier
    turn_id : str
        Stable turn identifier within the session
    iteration : int
        Zero-based model loop iteration
    inputs : Optional[dict[str, Any]]
        Inputs supplied to the deterministic single-turn run
    options : Optional[TurnOptions]
        Canonical turn execution options
    tool_results : Optional[list[HostToolResult]]
        Host tool results produced by the previous iteration
    """

    _shorthand_property: ClassVar[str | None] = None

    session_id: str = field(default="")
    turn_id: str = field(default="")
    iteration: int = field(default=0)
    inputs: dict[str, Any] | None = None
    options: TurnOptions | None = None
    tool_results: list[HostToolResult] = field(default_factory=list)

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "TurnModelRequest":
        """Load a TurnModelRequest instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            TurnModelRequest: The loaded TurnModelRequest instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for TurnModelRequest: {data}")

        # create new instance
        instance = TurnModelRequest()

        if data is not None and "sessionId" in data:
            instance.session_id = data["sessionId"]
        if data is not None and "turnId" in data:
            instance.turn_id = data["turnId"]
        if data is not None and "iteration" in data:
            instance.iteration = data["iteration"]
        if data is not None and "inputs" in data:
            instance.inputs = data["inputs"]
        if data is not None and "options" in data:
            instance.options = TurnOptions.load(data["options"], context)
        if data is not None and "toolResults" in data:
            instance.tool_results = TurnModelRequest.load_tool_results(data["toolResults"], context)
        if context is not None:
            instance = context.process_output(instance)
        return instance

    @staticmethod
    def load_tool_results(data: dict | list, context: LoadContext | None) -> list[HostToolResult]:
        if isinstance(data, dict):
            # convert simple named toolResults to list of HostToolResult
            result = []
            for k, v in data.items():
                if isinstance(v, dict):
                    # value is an object, spread its properties
                    result.append({"name": k, **v})
                else:
                    # value is a scalar, use it as the primary property
                    result.append({"name": k, "requestId": v})
            data = result
        return [HostToolResult.load(item, context) for item in data]

    @staticmethod
    def save_tool_results(
        items: list[HostToolResult], context: SaveContext | None
    ) -> dict[str, Any] | list[dict[str, Any]]:
        if context is None:
            context = SaveContext()

        # This type doesn't have a 'name' property, so always use array format
        return [item.save(context) for item in items]

    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the TurnModelRequest instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.session_id is not None:
            result["sessionId"] = obj.session_id
        if obj.turn_id is not None:
            result["turnId"] = obj.turn_id
        if obj.iteration is not None:
            result["iteration"] = obj.iteration
        if obj.inputs is not None:
            result["inputs"] = obj.inputs
        if obj.options is not None:
            result["options"] = obj.options.save(context)
        if obj.tool_results is not None:
            result["toolResults"] = TurnModelRequest.save_tool_results(obj.tool_results, context)

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the TurnModelRequest instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the TurnModelRequest instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
