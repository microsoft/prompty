# <auto-generated by typra-emitter>
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Literal

from ..._context import LoadContext, SaveContext

StreamFailureOutcome = Literal["determinate", "indeterminate"]


@dataclass
class StreamFailure:
    """A classified terminal failure from an LLM response stream.

    Attributes
    ----------
    outcome : str
        Whether the provider outcome is known or requires reconciliation
    message : str
        The human-readable failure message
    """

    _shorthand_property: ClassVar[str | None] = None

    outcome: StreamFailureOutcome = field(default="determinate")
    message: str = field(default="")

    @staticmethod
    def load(data: Any, context: LoadContext | None = None) -> "StreamFailure":
        """Load a StreamFailure instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            StreamFailure: The loaded StreamFailure instance.

        """

        if context is None:
            context = LoadContext()
        data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for StreamFailure: {data}")

        # create new instance
        instance = StreamFailure()

        if data is not None and "outcome" in data:
            instance.outcome = data["outcome"]
        if data is not None and "message" in data:
            instance.message = data["message"]
        if context is not None:
            instance = context.process_output(instance)
        return instance



    def save(self, context: SaveContext | None = None) -> dict[str, Any]:
        """Save the StreamFailure instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)


        result: dict[str, Any] = {}

        if obj.outcome is not None:
            result["outcome"] = obj.outcome
        if obj.message is not None:
            result["message"] = obj.message

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: SaveContext | None = None) -> str:
        """Convert the StreamFailure instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: SaveContext | None = None, indent: int = 2) -> str:
        """Convert the StreamFailure instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
