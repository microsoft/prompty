# <auto-generated by typra-emitter>
import json

import yaml

from prompty.model import AuthorizationCodeFlow


def test_load_json_authorizationcodeflow():
    json_data = r"""
    {
      "authUrl": "sample",
      "codeVerifier": "sample"
    }
    """
    data = json.loads(json_data, strict=False)
    instance = AuthorizationCodeFlow.load(data)
    assert instance is not None
    assert instance.auth_url == "sample"
    assert instance.code_verifier == "sample"


def test_load_yaml_authorizationcodeflow():
    yaml_data = r"""
    authUrl: sample
    codeVerifier: sample

    """
    data = yaml.load(yaml_data, Loader=yaml.FullLoader)
    instance = AuthorizationCodeFlow.load(data)
    assert instance is not None
    assert instance.auth_url == "sample"
    assert instance.code_verifier == "sample"


def test_roundtrip_json_authorizationcodeflow():
    """Test that load -> save -> load produces equivalent data."""
    json_data = r"""
    {
      "authUrl": "sample",
      "codeVerifier": "sample"
    }
    """
    original_data = json.loads(json_data, strict=False)
    instance = AuthorizationCodeFlow.load(original_data)
    saved_data = instance.save()
    reloaded = AuthorizationCodeFlow.load(saved_data)
    assert reloaded is not None
    assert reloaded.auth_url == "sample"
    assert reloaded.code_verifier == "sample"


def test_to_json_authorizationcodeflow():
    """Test that to_json produces valid JSON."""
    json_data = r"""
    {
      "authUrl": "sample",
      "codeVerifier": "sample"
    }
    """
    data = json.loads(json_data, strict=False)
    instance = AuthorizationCodeFlow.load(data)
    json_output = instance.to_json()
    assert json_output is not None
    parsed = json.loads(json_output)
    assert isinstance(parsed, dict)


def test_to_yaml_authorizationcodeflow():
    """Test that to_yaml produces valid YAML."""
    json_data = r"""
    {
      "authUrl": "sample",
      "codeVerifier": "sample"
    }
    """
    data = json.loads(json_data, strict=False)
    instance = AuthorizationCodeFlow.load(data)
    yaml_output = instance.to_yaml()
    assert yaml_output is not None
    parsed = yaml.safe_load(yaml_output)
    assert isinstance(parsed, dict)
