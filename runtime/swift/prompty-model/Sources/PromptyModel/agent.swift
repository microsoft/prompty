// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

/// An Agent is the root model produced from a .prompty markdown file for LLM prompts. The frontmatter defines structured metadata including model configuration, input/output schemas, tools, and template settings. The markdown body becomes the instructions.  This is the single root type for the Prompty schema — there is no abstract base class or kind discriminator. A .prompty file always produces an Agent instance.  Runtime loaders may resolve frontmatter references such as `${env:VAR}` and `${file:relative/path}`. File references must be treated as a host-controlled capability: by default they are scoped to the containing .prompty file's directory tree after canonicalization, and any additional allowed roots must be supplied by the host application's load options rather than frontmatter.
public struct Agent: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var displayName: String? = nil
  public var description: String? = nil
  public var metadata: [String: Any]? = nil
  public var inputs: [Property]? = nil
  public var outputs: [Property]? = nil
  public var model: Model? = nil
  public var tools: [Tool]? = nil
  public var template: Template? = nil
  public var instructions: String? = nil

  public init(name: String = "", displayName: String? = nil, description: String? = nil, metadata: [String: Any]? = nil, inputs: [Property]? = nil, outputs: [Property]? = nil, model: Model? = nil, tools: [Tool]? = nil, template: Template? = nil, instructions: String? = nil) {
    self.name = name
    self.displayName = displayName
    self.description = description
    self.metadata = metadata
    self.inputs = inputs
    self.outputs = outputs
    self.model = model
    self.tools = tools
    self.template = template
    self.instructions = instructions
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> Agent {
    let object = try TypraRuntime.object(data, typeName: "Agent")
    var instance = Agent()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    else {
      instance.name = ""
    }
    if let value = object["displayName"] {
      instance.displayName = try TypraRuntime.string(value, field: "displayName")
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["metadata"] {
      instance.metadata = try TypraRuntime.dictionary(value, field: "metadata")
    }
    if let value = object["inputs"] {
      instance.inputs = try loadInputs(value, context: context.at("inputs"))
    }
    if let value = object["outputs"] {
      instance.outputs = try loadOutputs(value, context: context.at("outputs"))
    }
    if let value = object["model"] {
      instance.model = try Model.load(value, context: context.at("model"))
    }
    if let value = object["tools"] {
      instance.tools = try loadTools(value, context: context.at("tools"))
    }
    if let value = object["template"] {
      instance.template = try Template.load(value, context: context.at("template"))
    }
    if let value = object["instructions"] {
      instance.instructions = try TypraRuntime.string(value, field: "instructions")
    }
    return instance
  }

  private static func loadInputs(_ data: Any, context: LoadContext) throws -> [Property] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Property.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "inputs")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Property.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Property.load(itemData, context: context.at(name))
    }
  }

  private static func saveInputs(_ items: [Property], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      result[name] = itemData
    }
    return result
  }

  private static func loadOutputs(_ data: Any, context: LoadContext) throws -> [Property] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Property.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "outputs")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Property.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Property.load(itemData, context: context.at(name))
    }
  }

  private static func saveOutputs(_ items: [Property], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      result[name] = itemData
    }
    return result
  }

  private static func loadTools(_ data: Any, context: LoadContext) throws -> [Tool] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Tool.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "tools")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Tool.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Tool.load(itemData, context: context.at(name))
    }
  }

  private static func saveTools(_ items: [Tool], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      result[name] = itemData
    }
    return result
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    if let value = self.displayName {
      result["displayName"] = value
    }
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.metadata {
      result["metadata"] = value
    }
    if let value = self.inputs {
      result["inputs"] = try Self.saveInputs(value, context: context)
    }
    if let value = self.outputs {
      result["outputs"] = try Self.saveOutputs(value, context: context)
    }
    if let value = self.model {
      result["model"] = try value.save(context)
    }
    if let value = self.tools {
      result["tools"] = try Self.saveTools(value, context: context)
    }
    if let value = self.template {
      result["template"] = try value.save(context)
    }
    if let value = self.instructions {
      result["instructions"] = value
    }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> Agent {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "Agent"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> Agent {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "Agent"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
