// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

/// A Prompty is a markdown file format for LLM prompts. The frontmatter defines structured metadata including model configuration, input/output schemas, tools, and template settings. The markdown body becomes the instructions.  This is the single root type for the Prompty schema — there is no abstract base class or kind discriminator. A .prompty file always produces a Prompty instance.  Runtime loaders may resolve frontmatter references such as `${env:VAR}` and `${file:relative/path}`. File references must be treated as a host-controlled capability: by default they are scoped to the containing .prompty file's directory tree after canonicalization, and any additional allowed roots must be supplied by the host application's load options rather than frontmatter.
public struct Prompty: TypraModel {
  public var name: String = ""
  public var displayName: String? = nil
  public var description: String? = nil
  public var metadata: [String: Any]? = nil
  public var inputs: [Property]? = nil
  public var outputs: [Property]? = nil
  public var model: Model = Model()
  public var tools: [Tool]? = nil
  public var template: Template? = nil
  public var instructions: String? = nil

  public init(
    name: String = "", displayName: String? = nil, description: String? = nil,
    metadata: [String: Any]? = nil, inputs: [Property]? = nil, outputs: [Property]? = nil,
    model: Model = Model(), tools: [Tool]? = nil, template: Template? = nil,
    instructions: String? = nil
  ) {
    self.name = name
    self.displayName = displayName
    self.description = description
    self.metadata = metadata
    self.inputs = inputs
    self.outputs = outputs
    self.model = model
    self.tools = tools
    self.template = template
    self.instructions = instructions
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> Prompty {
    let object = try TypraRuntime.object(data, typeName: "Prompty")
    var instance = Prompty()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    } else {
      instance.name = ""
    }
    if let value = object["displayName"] {
      instance.displayName = try TypraRuntime.string(value, field: "displayName")
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["metadata"] {
      instance.metadata = try TypraRuntime.dictionary(value, field: "metadata")
    }
    if let value = object["inputs"] {
      instance.inputs = try TypraRuntime.array(value, field: "inputs").map {
        try Property.load($0, context: context)
      }
    }
    if let value = object["outputs"] {
      instance.outputs = try TypraRuntime.array(value, field: "outputs").map {
        try Property.load($0, context: context)
      }
    }
    if let value = object["model"] {
      instance.model = try Model.load(value, context: context)
    }
    if let value = object["tools"] {
      instance.tools = try TypraRuntime.array(value, field: "tools").map {
        try Tool.load($0, context: context)
      }
    }
    if let value = object["template"] {
      instance.template = try Template.load(value, context: context)
    }
    if let value = object["instructions"] {
      instance.instructions = try TypraRuntime.string(value, field: "instructions")
    }
    return instance
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    if let value = self.displayName {
      result["displayName"] = value
    }
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.metadata {
      result["metadata"] = value
    }
    if let value = self.inputs {
      result["inputs"] = try value.map { try $0.save(context) }
    }
    if let value = self.outputs {
      result["outputs"] = try value.map { try $0.save(context) }
    }
    result["model"] = try self.model.save(context)
    if let value = self.tools {
      result["tools"] = try value.map { try $0.save(context) }
    }
    if let value = self.template {
      result["template"] = try value.save(context)
    }
    if let value = self.instructions {
      result["instructions"] = value
    }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws
    -> Prompty
  {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "Prompty"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws
    -> Prompty
  {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "Prompty"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
