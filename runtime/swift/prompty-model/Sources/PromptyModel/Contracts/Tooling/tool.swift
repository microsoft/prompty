// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

public enum Tool: TypraModel {
  case functionTool(FunctionTool)
  case mcpTool(McpTool)
  case openApiTool(OpenApiTool)
  case promptyTool(PromptyTool)
  case customTool(CustomTool, [String: Any])

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> Tool {
    let normalizedData: Any = data
    let object = try TypraRuntime.object(normalizedData, typeName: "Tool")
    let discriminator = try TypraRuntime.string(object["kind"] ?? NSNull(), field: "kind")
    if discriminator.isEmpty {
      throw TypraRuntimeError.invalidField(field: context.at("kind").path, expected: "non-blank string")
    }
    switch discriminator {
    case "function": return .functionTool(try FunctionTool.load(normalizedData, context: context))
    case "mcp": return .mcpTool(try McpTool.load(normalizedData, context: context))
    case "openapi": return .openApiTool(try OpenApiTool.load(normalizedData, context: context))
    case "prompty": return .promptyTool(try PromptyTool.load(normalizedData, context: context))
    default: return .customTool(try CustomTool.load(normalizedData, context: context), object)
    }
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    switch self {
    case .functionTool(let value): return try value.save(context)
    case .mcpTool(let value): return try value.save(context)
    case .openApiTool(let value): return try value.save(context)
    case .promptyTool(let value): return try value.save(context)
    case .customTool(let value, let raw):
      var result = raw
      for (key, item) in try value.save(context) {
        result[key] = item
      }
      return result
    }
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> Tool {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "Tool"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> Tool {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "Tool"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}

/// Represents a local function tool.
public struct FunctionTool: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var kind: String = "function"
  public var description: String? = nil
  public var bindings: [Binding]? = nil
  public var parameters: [Property] = []
  public var strict: Bool? = nil

  public init(name: String = "", kind: String = "function", description: String? = nil, bindings: [Binding]? = nil, parameters: [Property] = [], strict: Bool? = nil) {
    self.name = name
    self.kind = kind
    self.description = description
    self.bindings = bindings
    self.parameters = parameters
    self.strict = strict
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> FunctionTool {
    let object = try TypraRuntime.object(data, typeName: "FunctionTool")
    var instance = FunctionTool()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    else {
      instance.name = ""
    }
    if let value = object["kind"] {
      instance.kind = try TypraRuntime.string(value, field: "kind")
    }
    else {
      instance.kind = "function"
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["bindings"] {
      instance.bindings = try loadBindings(value, context: context.at("bindings"))
    }
    if let value = object["parameters"] {
      instance.parameters = try loadParameters(value, context: context.at("parameters"))
    }
    if let value = object["strict"] {
      instance.strict = try TypraRuntime.bool(value, field: "strict")
    }
    return instance
  }

  private static func loadBindings(_ data: Any, context: LoadContext) throws -> [Binding] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Binding.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "bindings")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Binding.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Binding.load(itemData, context: context.at(name))
    }
  }

  private static func saveBindings(_ items: [Binding], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      if context.useShorthand, itemData.count == 1, let value = itemData["input"] {
        result[name] = value
      } else {
        result[name] = itemData
      }
    }
    return result
  }

  private static func loadParameters(_ data: Any, context: LoadContext) throws -> [Property] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Property.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "parameters")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Property.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Property.load(itemData, context: context.at(name))
    }
  }

  private static func saveParameters(_ items: [Property], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      result[name] = itemData
    }
    return result
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    result["kind"] = self.kind
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.bindings {
      result["bindings"] = try Self.saveBindings(value, context: context)
    }
    result["parameters"] = try Self.saveParameters(self.parameters, context: context)
    if let value = self.strict {
      result["strict"] = value
    }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> FunctionTool {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "FunctionTool"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> FunctionTool {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "FunctionTool"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}

/// Represents a generic server tool that runs on a server This tool kind is designed for operations that require server-side execution It may include features such as authentication, data storage, and long-running processes This tool kind is ideal for tasks that involve complex computations or access to secure resources Server tools can be used to offload heavy processing from client applications
public struct CustomTool: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var kind: String = "*"
  public var description: String? = nil
  public var bindings: [Binding]? = nil
  public var connection: Connection = .referenceConnection(ReferenceConnection())
  public var options: [String: Any] = [:]

  public init(name: String = "", kind: String = "*", description: String? = nil, bindings: [Binding]? = nil, connection: Connection = .referenceConnection(ReferenceConnection()), options: [String: Any] = [:]) {
    self.name = name
    self.kind = kind
    self.description = description
    self.bindings = bindings
    self.connection = connection
    self.options = options
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> CustomTool {
    let object = try TypraRuntime.object(data, typeName: "CustomTool")
    var instance = CustomTool()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    else {
      instance.name = ""
    }
    if let value = object["kind"] {
      instance.kind = try TypraRuntime.string(value, field: "kind")
    }
    else {
      instance.kind = "*"
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["bindings"] {
      instance.bindings = try loadBindings(value, context: context.at("bindings"))
    }
    if object["connection"] == nil || object["connection"] is NSNull {
      throw TypraRuntimeError.unsupported(context.at("connection").path + ": missing required field")
    }
    if let value = object["connection"] {
      instance.connection = try Connection.load(value, context: context.at("connection"))
    }
    if let value = object["options"] {
      instance.options = try TypraRuntime.dictionary(value, field: "options")
    }
    return instance
  }

  private static func loadBindings(_ data: Any, context: LoadContext) throws -> [Binding] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Binding.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "bindings")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Binding.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Binding.load(itemData, context: context.at(name))
    }
  }

  private static func saveBindings(_ items: [Binding], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      if context.useShorthand, itemData.count == 1, let value = itemData["input"] {
        result[name] = value
      } else {
        result[name] = itemData
      }
    }
    return result
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    result["kind"] = self.kind
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.bindings {
      result["bindings"] = try Self.saveBindings(value, context: context)
    }
    result["connection"] = try self.connection.save(context)
    result["options"] = self.options
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> CustomTool {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "CustomTool"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> CustomTool {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "CustomTool"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}

/// The MCP Server tool.
public struct McpTool: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var kind: String = "mcp"
  public var description: String? = nil
  public var bindings: [Binding]? = nil
  public var connection: Connection = .referenceConnection(ReferenceConnection())
  public var serverName: String = ""
  public var serverDescription: String? = nil
  public var approvalMode: McpApprovalMode? = nil
  public var allowedTools: [String]? = nil

  public init(name: String = "", kind: String = "mcp", description: String? = nil, bindings: [Binding]? = nil, connection: Connection = .referenceConnection(ReferenceConnection()), serverName: String = "", serverDescription: String? = nil, approvalMode: McpApprovalMode? = nil, allowedTools: [String]? = nil) {
    self.name = name
    self.kind = kind
    self.description = description
    self.bindings = bindings
    self.connection = connection
    self.serverName = serverName
    self.serverDescription = serverDescription
    self.approvalMode = approvalMode
    self.allowedTools = allowedTools
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> McpTool {
    let object = try TypraRuntime.object(data, typeName: "McpTool")
    var instance = McpTool()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    else {
      instance.name = ""
    }
    if let value = object["kind"] {
      instance.kind = try TypraRuntime.string(value, field: "kind")
    }
    else {
      instance.kind = "mcp"
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["bindings"] {
      instance.bindings = try loadBindings(value, context: context.at("bindings"))
    }
    if object["connection"] == nil || object["connection"] is NSNull {
      throw TypraRuntimeError.unsupported(context.at("connection").path + ": missing required field")
    }
    if let value = object["connection"] {
      instance.connection = try Connection.load(value, context: context.at("connection"))
    }
    if let value = object["serverName"] {
      instance.serverName = try TypraRuntime.string(value, field: "serverName")
    }
    if let value = object["serverDescription"] {
      instance.serverDescription = try TypraRuntime.string(value, field: "serverDescription")
    }
    if let value = object["approvalMode"] {
      instance.approvalMode = try McpApprovalMode.load(value, context: context.at("approvalMode"))
    }
    if let value = object["allowedTools"] {
      instance.allowedTools = try TypraRuntime.array(value, field: "allowedTools").map { try TypraRuntime.string($0, field: "allowedTools") }
    }
    return instance
  }

  private static func loadBindings(_ data: Any, context: LoadContext) throws -> [Binding] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Binding.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "bindings")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Binding.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Binding.load(itemData, context: context.at(name))
    }
  }

  private static func saveBindings(_ items: [Binding], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      if context.useShorthand, itemData.count == 1, let value = itemData["input"] {
        result[name] = value
      } else {
        result[name] = itemData
      }
    }
    return result
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    result["kind"] = self.kind
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.bindings {
      result["bindings"] = try Self.saveBindings(value, context: context)
    }
    result["connection"] = try self.connection.save(context)
    result["serverName"] = self.serverName
    if let value = self.serverDescription {
      result["serverDescription"] = value
    }
    if let value = self.approvalMode {
      result["approvalMode"] = try value.save(context)
    }
    if let value = self.allowedTools {
      result["allowedTools"] = value
    }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> McpTool {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "McpTool"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> McpTool {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "McpTool"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}

public struct OpenApiTool: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var kind: String = "openapi"
  public var description: String? = nil
  public var bindings: [Binding]? = nil
  public var connection: Connection = .referenceConnection(ReferenceConnection())
  public var specification: String = ""

  public init(name: String = "", kind: String = "openapi", description: String? = nil, bindings: [Binding]? = nil, connection: Connection = .referenceConnection(ReferenceConnection()), specification: String = "") {
    self.name = name
    self.kind = kind
    self.description = description
    self.bindings = bindings
    self.connection = connection
    self.specification = specification
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> OpenApiTool {
    let object = try TypraRuntime.object(data, typeName: "OpenApiTool")
    var instance = OpenApiTool()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    else {
      instance.name = ""
    }
    if let value = object["kind"] {
      instance.kind = try TypraRuntime.string(value, field: "kind")
    }
    else {
      instance.kind = "openapi"
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["bindings"] {
      instance.bindings = try loadBindings(value, context: context.at("bindings"))
    }
    if object["connection"] == nil || object["connection"] is NSNull {
      throw TypraRuntimeError.unsupported(context.at("connection").path + ": missing required field")
    }
    if let value = object["connection"] {
      instance.connection = try Connection.load(value, context: context.at("connection"))
    }
    if let value = object["specification"] {
      instance.specification = try TypraRuntime.string(value, field: "specification")
    }
    return instance
  }

  private static func loadBindings(_ data: Any, context: LoadContext) throws -> [Binding] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Binding.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "bindings")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Binding.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Binding.load(itemData, context: context.at(name))
    }
  }

  private static func saveBindings(_ items: [Binding], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      if context.useShorthand, itemData.count == 1, let value = itemData["input"] {
        result[name] = value
      } else {
        result[name] = itemData
      }
    }
    return result
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    result["kind"] = self.kind
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.bindings {
      result["bindings"] = try Self.saveBindings(value, context: context)
    }
    result["connection"] = try self.connection.save(context)
    result["specification"] = self.specification
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> OpenApiTool {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "OpenApiTool"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> OpenApiTool {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "OpenApiTool"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}

/// A tool that references another .prompty file to be invoked as a tool.  The child prompty is executed as a single prompt invocation. Nested agent loops are intentionally not started from PromptyTool.
public struct PromptyTool: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var kind: String = "prompty"
  public var description: String? = nil
  public var bindings: [Binding]? = nil
  public var path: String = ""
  public var mode: String = "single"

  public init(name: String = "", kind: String = "prompty", description: String? = nil, bindings: [Binding]? = nil, path: String = "", mode: String = "single") {
    self.name = name
    self.kind = kind
    self.description = description
    self.bindings = bindings
    self.path = path
    self.mode = mode
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> PromptyTool {
    let object = try TypraRuntime.object(data, typeName: "PromptyTool")
    var instance = PromptyTool()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    else {
      instance.name = ""
    }
    if let value = object["kind"] {
      instance.kind = try TypraRuntime.string(value, field: "kind")
    }
    else {
      instance.kind = "prompty"
    }
    if let value = object["description"] {
      instance.description = try TypraRuntime.string(value, field: "description")
    }
    if let value = object["bindings"] {
      instance.bindings = try loadBindings(value, context: context.at("bindings"))
    }
    if let value = object["path"] {
      instance.path = try TypraRuntime.string(value, field: "path")
    }
    if let value = object["mode"] {
      instance.mode = try TypraRuntime.string(value, field: "mode")
    }
    else {
      instance.mode = "single"
    }
    return instance
  }

  private static func loadBindings(_ data: Any, context: LoadContext) throws -> [Binding] {
    if let values = data as? [Any] {
      return try values.enumerated().map { try Binding.load($1, context: context.atIndex($0)) }
    }
    let values = try TypraRuntime.dictionary(data, field: "bindings")
    return try values.keys.sorted().map { name in
      let value = values[name]!
      if value is [Any] {
        throw TypraRuntimeError.invalidField(field: context.at(name).path, expected: "non-array named collection entry; received category array")
      }
      var itemData: [String: Any]
      if let object = value as? [String: Any] {
        itemData = object
      } else {
        itemData = try Binding.load(value, context: context).save()
      }
      itemData["name"] = name
      return try Binding.load(itemData, context: context.at(name))
    }
  }

  private static func saveBindings(_ items: [Binding], context: SaveContext) throws -> Any {
    let serialized = try items.map { try $0.save(context) }
    if context.collectionFormat == "array" {
      return serialized
    }
    var names = Set<String>()
    for itemData in serialized {
      guard let name = itemData["name"] as? String, !name.isEmpty, names.insert(name).inserted else {
        return serialized
      }
    }
    var result: [String: Any] = [:]
    for (_, originalData) in zip(items, serialized) {
      var itemData = originalData
      let name = itemData.removeValue(forKey: "name") as! String
      if context.useShorthand, itemData.count == 1, let value = itemData["input"] {
        result[name] = value
      } else {
        result[name] = itemData
      }
    }
    return result
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    result["kind"] = self.kind
    if let value = self.description {
      result["description"] = value
    }
    if let value = self.bindings {
      result["bindings"] = try Self.saveBindings(value, context: context)
    }
    result["path"] = self.path
    result["mode"] = self.mode
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> PromptyTool {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "PromptyTool"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> PromptyTool {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "PromptyTool"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
