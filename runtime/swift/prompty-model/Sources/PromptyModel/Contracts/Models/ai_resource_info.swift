// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

/// An AI service resource that can host models or projects.
public struct AiResourceInfo: TypraModel {
  public static let shorthandProperty: String? = nil
  public var name: String = ""
  public var kind: String = ""
  public var endpoint: String = ""
  public var location: String = ""
  public var resourceGroup: String = ""
  public var serviceUrl: String? = nil

  public init(name: String = "", kind: String = "", endpoint: String = "", location: String = "", resourceGroup: String = "", serviceUrl: String? = nil) {
    self.name = name
    self.kind = kind
    self.endpoint = endpoint
    self.location = location
    self.resourceGroup = resourceGroup
    self.serviceUrl = serviceUrl
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> AiResourceInfo {
    let object = try TypraRuntime.object(data, typeName: "AiResourceInfo")
    var instance = AiResourceInfo()
    if let value = object["name"] {
      instance.name = try TypraRuntime.string(value, field: "name")
    }
    if let value = object["kind"] {
      instance.kind = try TypraRuntime.string(value, field: "kind")
    }
    if let value = object["endpoint"] {
      instance.endpoint = try TypraRuntime.string(value, field: "endpoint")
    }
    if let value = object["location"] {
      instance.location = try TypraRuntime.string(value, field: "location")
    }
    if let value = object["resourceGroup"] {
      instance.resourceGroup = try TypraRuntime.string(value, field: "resourceGroup")
    }
    if let value = object["serviceUrl"] {
      instance.serviceUrl = try TypraRuntime.string(value, field: "serviceUrl")
    }
    return instance
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["name"] = self.name
    result["kind"] = self.kind
    result["endpoint"] = self.endpoint
    result["location"] = self.location
    result["resourceGroup"] = self.resourceGroup
    if let value = self.serviceUrl {
      result["serviceUrl"] = value
    }
    return result
  }

  public func toWire(_ provider: String, context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    let wireNameName: String?
    switch provider {
    case "foundry": wireNameName = "name"
    default: wireNameName = nil
    }
    if let wireKey = wireNameName { result[wireKey] = self.name }
    let wireNameKind: String?
    switch provider {
    case "foundry": wireNameKind = "kind"
    default: wireNameKind = nil
    }
    if let wireKey = wireNameKind { result[wireKey] = self.kind }
    let wireNameEndpoint: String?
    switch provider {
    case "foundry": wireNameEndpoint = "endpoint"
    default: wireNameEndpoint = nil
    }
    if let wireKey = wireNameEndpoint { result[wireKey] = self.endpoint }
    let wireNameLocation: String?
    switch provider {
    case "foundry": wireNameLocation = "location"
    default: wireNameLocation = nil
    }
    if let wireKey = wireNameLocation { result[wireKey] = self.location }
    let wireNameResourceGroup: String?
    switch provider {
    case "foundry": wireNameResourceGroup = "resource_group"
    default: wireNameResourceGroup = nil
    }
    if let wireKey = wireNameResourceGroup { result[wireKey] = self.resourceGroup }
    let wireNameServiceUrl: String?
    switch provider {
    case "foundry": wireNameServiceUrl = "foundry_url"
    default: wireNameServiceUrl = nil
    }
    if let wireKey = wireNameServiceUrl, let value = self.serviceUrl { result[wireKey] = value }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> AiResourceInfo {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "AiResourceInfo"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> AiResourceInfo {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "AiResourceInfo"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
