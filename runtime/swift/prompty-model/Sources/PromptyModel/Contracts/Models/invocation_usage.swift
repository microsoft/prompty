// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

/// Complete cumulative token usage for one completed model invocation.  Providers emit this value at most once per invocation. `totalTokens` uses the provider total when available; otherwise the provider adapter computes it as `inputTokens + outputTokens`.
public struct InvocationUsage: TypraModel {
  public static let shorthandProperty: String? = nil
  public var inputTokens: Int64 = 0
  public var outputTokens: Int64 = 0
  public var totalTokens: Int64 = 0

  public init(inputTokens: Int64 = 0, outputTokens: Int64 = 0, totalTokens: Int64 = 0) {
    self.inputTokens = inputTokens
    self.outputTokens = outputTokens
    self.totalTokens = totalTokens
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> InvocationUsage {
    let object = try TypraRuntime.object(data, typeName: "InvocationUsage")
    var instance = InvocationUsage()
    if let value = object["inputTokens"] {
      instance.inputTokens = try TypraRuntime.int64(value, field: "inputTokens")
    }
    if let value = object["outputTokens"] {
      instance.outputTokens = try TypraRuntime.int64(value, field: "outputTokens")
    }
    if let value = object["totalTokens"] {
      instance.totalTokens = try TypraRuntime.int64(value, field: "totalTokens")
    }
    return instance
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["inputTokens"] = self.inputTokens
    result["outputTokens"] = self.outputTokens
    result["totalTokens"] = self.totalTokens
    return result
  }

  public func toWire(_ provider: String, context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    let wireNameInputTokens: String?
    switch provider {
    case "openai": wireNameInputTokens = "prompt_tokens"
    case "anthropic": wireNameInputTokens = "input_tokens"
    default: wireNameInputTokens = nil
    }
    if let wireKey = wireNameInputTokens { result[wireKey] = self.inputTokens }
    let wireNameOutputTokens: String?
    switch provider {
    case "openai": wireNameOutputTokens = "completion_tokens"
    case "anthropic": wireNameOutputTokens = "output_tokens"
    default: wireNameOutputTokens = nil
    }
    if let wireKey = wireNameOutputTokens { result[wireKey] = self.outputTokens }
    let wireNameTotalTokens: String?
    switch provider {
    case "openai": wireNameTotalTokens = "total_tokens"
    default: wireNameTotalTokens = nil
    }
    if let wireKey = wireNameTotalTokens { result[wireKey] = self.totalTokens }
    return result
  }

  public static func fromWire(_ provider: String, _ data: [String: Any], context: LoadContext = LoadContext()) throws -> InvocationUsage {
    let wireMap: [String: [String: String]] = [
      "inputTokens": ["openai": "prompt_tokens", "anthropic": "input_tokens"],
      "outputTokens": ["openai": "completion_tokens", "anthropic": "output_tokens"],
      "totalTokens": ["openai": "total_tokens"],
    ]
    var inverse: [String: String] = [:]
    for (field, mapping) in wireMap {
      if let wireName = mapping[provider] { inverse[wireName] = field }
    }
    var canonical: [String: Any] = [:]
    for (key, value) in data {
      canonical[inverse[key] ?? key] = value
    }
    return try load(canonical, context: context)
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> InvocationUsage {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "InvocationUsage"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> InvocationUsage {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "InvocationUsage"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
