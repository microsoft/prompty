// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

/// Options for configuring the behavior of the AI model.
public struct ModelOptions: TypraModel {
  public static let shorthandProperty: String? = nil
  public var frequencyPenalty: Float? = nil
  public var maxOutputTokens: Int32? = nil
  public var presencePenalty: Float? = nil
  public var seed: Int32? = nil
  public var temperature: Float? = nil
  public var topK: Int32? = nil
  public var topP: Float? = nil
  public var stopSequences: [String]? = nil
  public var allowMultipleToolCalls: Bool? = nil
  public var additionalProperties: [String: Any]? = nil

  public init(
    frequencyPenalty: Float? = nil, maxOutputTokens: Int32? = nil, presencePenalty: Float? = nil,
    seed: Int32? = nil, temperature: Float? = nil, topK: Int32? = nil, topP: Float? = nil,
    stopSequences: [String]? = nil, allowMultipleToolCalls: Bool? = nil,
    additionalProperties: [String: Any]? = nil
  ) {
    self.frequencyPenalty = frequencyPenalty
    self.maxOutputTokens = maxOutputTokens
    self.presencePenalty = presencePenalty
    self.seed = seed
    self.temperature = temperature
    self.topK = topK
    self.topP = topP
    self.stopSequences = stopSequences
    self.allowMultipleToolCalls = allowMultipleToolCalls
    self.additionalProperties = additionalProperties
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> ModelOptions
  {
    let object = try TypraRuntime.object(data, typeName: "ModelOptions")
    var instance = ModelOptions()
    if let value = object["frequencyPenalty"] {
      instance.frequencyPenalty = try TypraRuntime.float(value, field: "frequencyPenalty")
    }
    if let value = object["maxOutputTokens"] {
      instance.maxOutputTokens = try TypraRuntime.int32(value, field: "maxOutputTokens")
    }
    if let value = object["presencePenalty"] {
      instance.presencePenalty = try TypraRuntime.float(value, field: "presencePenalty")
    }
    if let value = object["seed"] {
      instance.seed = try TypraRuntime.int32(value, field: "seed")
    }
    if let value = object["temperature"] {
      instance.temperature = try TypraRuntime.float(value, field: "temperature")
    }
    if let value = object["topK"] {
      instance.topK = try TypraRuntime.int32(value, field: "topK")
    }
    if let value = object["topP"] {
      instance.topP = try TypraRuntime.float(value, field: "topP")
    }
    if let value = object["stopSequences"] {
      instance.stopSequences = try TypraRuntime.array(value, field: "stopSequences").map {
        try TypraRuntime.string($0, field: "stopSequences")
      }
    }
    if let value = object["allowMultipleToolCalls"] {
      instance.allowMultipleToolCalls = try TypraRuntime.bool(
        value, field: "allowMultipleToolCalls")
    }
    if let value = object["additionalProperties"] {
      instance.additionalProperties = try TypraRuntime.dictionary(
        value, field: "additionalProperties")
    }
    return instance
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    if let value = self.frequencyPenalty {
      result["frequencyPenalty"] = value
    }
    if let value = self.maxOutputTokens {
      result["maxOutputTokens"] = value
    }
    if let value = self.presencePenalty {
      result["presencePenalty"] = value
    }
    if let value = self.seed {
      result["seed"] = value
    }
    if let value = self.temperature {
      result["temperature"] = value
    }
    if let value = self.topK {
      result["topK"] = value
    }
    if let value = self.topP {
      result["topP"] = value
    }
    if let value = self.stopSequences {
      result["stopSequences"] = value
    }
    if let value = self.allowMultipleToolCalls {
      result["allowMultipleToolCalls"] = value
    }
    if let value = self.additionalProperties {
      result["additionalProperties"] = value
    }
    return result
  }

  public func toWire(_ provider: String, context: SaveContext = SaveContext()) throws -> [String:
    Any]
  {
    var result: [String: Any] = [:]
    let wireNameFrequencyPenalty: String?
    switch provider {
    case "openai": wireNameFrequencyPenalty = "frequency_penalty"
    default: wireNameFrequencyPenalty = nil
    }
    if let wireKey = wireNameFrequencyPenalty, let value = self.frequencyPenalty {
      result[wireKey] = value
    }
    let wireNameMaxOutputTokens: String?
    switch provider {
    case "openai": wireNameMaxOutputTokens = "max_completion_tokens"
    case "responses": wireNameMaxOutputTokens = "max_output_tokens"
    case "anthropic": wireNameMaxOutputTokens = "max_tokens"
    default: wireNameMaxOutputTokens = nil
    }
    if let wireKey = wireNameMaxOutputTokens, let value = self.maxOutputTokens {
      result[wireKey] = value
    }
    let wireNamePresencePenalty: String?
    switch provider {
    case "openai": wireNamePresencePenalty = "presence_penalty"
    default: wireNamePresencePenalty = nil
    }
    if let wireKey = wireNamePresencePenalty, let value = self.presencePenalty {
      result[wireKey] = value
    }
    let wireNameSeed: String?
    switch provider {
    case "openai": wireNameSeed = "seed"
    default: wireNameSeed = nil
    }
    if let wireKey = wireNameSeed, let value = self.seed { result[wireKey] = value }
    let wireNameTemperature: String?
    switch provider {
    case "openai": wireNameTemperature = "temperature"
    case "responses": wireNameTemperature = "temperature"
    case "anthropic": wireNameTemperature = "temperature"
    default: wireNameTemperature = nil
    }
    if let wireKey = wireNameTemperature, let value = self.temperature { result[wireKey] = value }
    let wireNameTopK: String?
    switch provider {
    case "openai": wireNameTopK = "top_k"
    case "anthropic": wireNameTopK = "top_k"
    default: wireNameTopK = nil
    }
    if let wireKey = wireNameTopK, let value = self.topK { result[wireKey] = value }
    let wireNameTopP: String?
    switch provider {
    case "openai": wireNameTopP = "top_p"
    case "responses": wireNameTopP = "top_p"
    case "anthropic": wireNameTopP = "top_p"
    default: wireNameTopP = nil
    }
    if let wireKey = wireNameTopP, let value = self.topP { result[wireKey] = value }
    let wireNameStopSequences: String?
    switch provider {
    case "openai": wireNameStopSequences = "stop"
    case "anthropic": wireNameStopSequences = "stop_sequences"
    default: wireNameStopSequences = nil
    }
    if let wireKey = wireNameStopSequences, let value = self.stopSequences {
      result[wireKey] = value
    }
    let wireNameAllowMultipleToolCalls: String?
    switch provider {
    case "openai": wireNameAllowMultipleToolCalls = "parallel_tool_calls"
    default: wireNameAllowMultipleToolCalls = nil
    }
    if let wireKey = wireNameAllowMultipleToolCalls, let value = self.allowMultipleToolCalls {
      result[wireKey] = value
    }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws
    -> ModelOptions
  {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "ModelOptions"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws
    -> ModelOptions
  {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "ModelOptions"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
