// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

public enum MemoryCategory: String, Codable, CaseIterable {
  case core = "core"
  case archival = "archival"
  case insight = "insight"
  public static func parse(_ value: String) throws -> MemoryCategory {
    switch value {
    case "core": return .core
    case "archival": return .archival
    case "insight": return .insight
    default: throw TypraRuntimeError.invalidEnum(type: "MemoryCategory", value: value)
    }
  }
}

/// A single agent memory — the canonical, host-neutral unit of agent memory.  `content` is the memory text, `category` places it in a general tier, `createdAt` records when the memory was formed (intrinsic, portable data), and `tags` are general labels used for keyword recall, grouping, and core deduplication. Host-specific associations (for example a session association) are expressed through the general `tags` field by convention — e.g. a `session:{id}` tag — never as a canonical field. A host needing per-entry bookkeeping, a stable id, or a stored embedding vector layers it in host storage; those are not canonical fields.
public struct MemoryEntry: TypraModel {
  public static let shorthandProperty: String? = nil
  public var content: String = ""
  public var category: MemoryCategory = (try! MemoryCategory.parse("core"))
  public var createdAt: String? = nil
  public var tags: [String]? = nil

  public init(
    content: String = "", category: MemoryCategory = (try! MemoryCategory.parse("core")),
    createdAt: String? = nil, tags: [String]? = nil
  ) {
    self.content = content
    self.category = category
    self.createdAt = createdAt
    self.tags = tags
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> MemoryEntry {
    let object = try TypraRuntime.object(data, typeName: "MemoryEntry")
    var instance = MemoryEntry()
    if let value = object["content"] {
      instance.content = try TypraRuntime.string(value, field: "content")
    } else {
      instance.content = ""
    }
    if let value = object["category"] {
      instance.category = try MemoryCategory.parse(
        try TypraRuntime.string(value, field: "category"))
    } else {
      instance.category = (try! MemoryCategory.parse("core"))
    }
    if let value = object["createdAt"] {
      instance.createdAt = try TypraRuntime.string(value, field: "createdAt")
    }
    if let value = object["tags"] {
      instance.tags = try TypraRuntime.array(value, field: "tags").map {
        try TypraRuntime.string($0, field: "tags")
      }
    }
    return instance
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["content"] = self.content
    result["category"] = self.category.rawValue
    if let value = self.createdAt {
      result["createdAt"] = value
    }
    if let value = self.tags {
      result["tags"] = value
    }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws
    -> MemoryEntry
  {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "MemoryEntry"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws
    -> MemoryEntry
  {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "MemoryEntry"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
