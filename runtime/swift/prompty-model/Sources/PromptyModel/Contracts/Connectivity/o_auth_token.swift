// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation

/// Provider-neutral OAuth token material returned by an authorization server.
public struct OAuthToken: TypraModel {
  public static let shorthandProperty: String? = nil
  public var accessToken: String = ""
  public var tokenType: String = ""
  public var expiresIn: Int64 = 0
  public var refreshToken: String? = nil
  public var scope: String? = nil

  public init(accessToken: String = "", tokenType: String = "", expiresIn: Int64 = 0, refreshToken: String? = nil, scope: String? = nil) {
    self.accessToken = accessToken
    self.tokenType = tokenType
    self.expiresIn = expiresIn
    self.refreshToken = refreshToken
    self.scope = scope
  }

  public static func load(_ data: Any, context: LoadContext = LoadContext()) throws -> OAuthToken {
    let object = try TypraRuntime.object(data, typeName: "OAuthToken")
    var instance = OAuthToken()
    if let value = object["accessToken"] {
      instance.accessToken = try TypraRuntime.string(value, field: "accessToken")
    }
    if let value = object["tokenType"] {
      instance.tokenType = try TypraRuntime.string(value, field: "tokenType")
    }
    if let value = object["expiresIn"] {
      instance.expiresIn = try TypraRuntime.int64(value, field: "expiresIn")
    }
    if let value = object["refreshToken"] {
      instance.refreshToken = try TypraRuntime.string(value, field: "refreshToken")
    }
    if let value = object["scope"] {
      instance.scope = try TypraRuntime.string(value, field: "scope")
    }
    return instance
  }

  public func save(_ context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    result["accessToken"] = self.accessToken
    result["tokenType"] = self.tokenType
    result["expiresIn"] = self.expiresIn
    if let value = self.refreshToken {
      result["refreshToken"] = value
    }
    if let value = self.scope {
      result["scope"] = value
    }
    return result
  }

  public func toWire(_ provider: String, context: SaveContext = SaveContext()) throws -> [String: Any] {
    var result: [String: Any] = [:]
    let wireNameAccessToken: String?
    switch provider {
    case "foundry": wireNameAccessToken = "access_token"
    default: wireNameAccessToken = nil
    }
    if let wireKey = wireNameAccessToken { result[wireKey] = self.accessToken }
    let wireNameTokenType: String?
    switch provider {
    case "foundry": wireNameTokenType = "token_type"
    default: wireNameTokenType = nil
    }
    if let wireKey = wireNameTokenType { result[wireKey] = self.tokenType }
    let wireNameExpiresIn: String?
    switch provider {
    case "foundry": wireNameExpiresIn = "expires_in"
    default: wireNameExpiresIn = nil
    }
    if let wireKey = wireNameExpiresIn { result[wireKey] = self.expiresIn }
    let wireNameRefreshToken: String?
    switch provider {
    case "foundry": wireNameRefreshToken = "refresh_token"
    default: wireNameRefreshToken = nil
    }
    if let wireKey = wireNameRefreshToken, let value = self.refreshToken { result[wireKey] = value }
    let wireNameScope: String?
    switch provider {
    case "foundry": wireNameScope = "scope"
    default: wireNameScope = nil
    }
    if let wireKey = wireNameScope, let value = self.scope { result[wireKey] = value }
    return result
  }

  public static func fromJSON(_ json: String, context: LoadContext = LoadContext()) throws -> OAuthToken {
    return try load(TypraRuntime.jsonObject(from: json, typeName: "OAuthToken"), context: context)
  }

  public func toJSON(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.jsonString(from: save(context))
  }

  public static func fromYAML(_ yaml: String, context: LoadContext = LoadContext()) throws -> OAuthToken {
    return try load(TypraRuntime.yamlObject(from: yaml, typeName: "OAuthToken"), context: context)
  }

  public func toYAML(_ context: SaveContext = SaveContext()) throws -> String {
    return try TypraRuntime.yamlString(from: save(context))
  }
}
