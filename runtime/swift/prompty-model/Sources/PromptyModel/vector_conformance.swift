// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Typed @vector conformance entrypoints (issue #511 Cat 1). Each function
// takes a consumer's REAL typed seam impl and runs the seam's baked-in vectors
// by calling seam methods directly — the idiomatic replacement for the
// stringly VectorAdapters registry + per-op marshalling double. The generic
// constraint `S: <Seam>` makes the Swift compiler prove every op is
// implemented, so completeness is checked at COMPILE time, not by a runtime
// registry lookup. Lives in the library module and throws on mismatch (no
// XCTest dependency); emitted additively beside the stringly runner.
// See docs: reference/vector-conformance.

import Foundation

/// Thrown when a typed @vector conformance check fails.
public struct SeamConformanceError: Error, CustomStringConvertible {
  public let message: String
  public init(_ message: String) { self.message = message }
  public var description: String { message }
}

// Canonical JSON of a scalar value (wrapped so a fragment is a valid top-level
// document), used to compare an actual seam result against the vector's
// `expected` independent of Swift's static element type.
private func seamConformanceCanonicalJSON(_ value: Any) throws -> String {
  let data = try JSONSerialization.data(withJSONObject: [value], options: [.sortedKeys])
  return String(data: data, encoding: .utf8) ?? ""
}

/// Typed @vector conformance for DiscoveryConformance. Pass your real `DiscoveryConformance` impl; the
/// `S: DiscoveryConformance` constraint makes the compiler prove every op is implemented.
public func runDiscoveryConformanceConformance<S: DiscoveryConformance>(_ seam: S) async throws {
  // vector: anthropic_enrich_no_dataset_entry_is_noop
  do {
    let inputData = "{\"id\":\"claude-sonnet-4-20250514\",\"contextWindow\":200000,\"inputModalities\":[\"text\",\"image\"],\"outputModalities\":[\"text\"]}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("anthropic_enrich_no_dataset_entry_is_noop: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"claude-sonnet-4-20250514\",\"contextWindow\":200000,\"inputModalities\":[\"text\",\"image\"],\"outputModalities\":[\"text\"]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("anthropic_enrich_no_dataset_entry_is_noop misrouted")
    }
  }
  // vector: anthropic_model_full
  do {
    let inputData = "{\"id\":\"claude-sonnet-4-20250514\",\"display_name\":\"Claude Sonnet 4\",\"context_length\":200000,\"input_modalities\":[\"text\",\"image\"],\"output_modalities\":[\"text\"],\"type\":\"model\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("anthropic_model_full: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"claude-sonnet-4-20250514\",\"displayName\":\"Claude Sonnet 4\",\"ownedBy\":\"anthropic\",\"contextWindow\":200000,\"inputModalities\":[\"text\",\"image\"],\"outputModalities\":[\"text\"],\"additionalProperties\":{\"id\":\"claude-sonnet-4-20250514\",\"display_name\":\"Claude Sonnet 4\",\"context_length\":200000,\"input_modalities\":[\"text\",\"image\"],\"output_modalities\":[\"text\"],\"type\":\"model\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("anthropic_model_full misrouted")
    }
  }
  // vector: anthropic_model_minimal
  do {
    let inputData = "{\"id\":\"claude-3-haiku-20240307\",\"type\":\"model\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("anthropic_model_minimal: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"claude-3-haiku-20240307\",\"ownedBy\":\"anthropic\",\"additionalProperties\":{\"id\":\"claude-3-haiku-20240307\",\"type\":\"model\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("anthropic_model_minimal misrouted")
    }
  }
  // vector: foundry_catalog_model
  do {
    let inputData = "{\"id\":\"gpt-4\",\"owned_by\":\"openai\",\"maxContextLength\":8192,\"status\":\"succeeded\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("foundry_catalog_model: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"gpt-4\",\"ownedBy\":\"openai\",\"contextWindow\":8192,\"additionalProperties\":{\"id\":\"gpt-4\",\"owned_by\":\"openai\",\"maxContextLength\":8192,\"status\":\"succeeded\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("foundry_catalog_model misrouted")
    }
  }
  // vector: foundry_deployment_flat_v1
  do {
    let inputData = "{\"name\":\"gpt-4o\",\"type\":\"ModelDeployment\",\"modelName\":\"gpt-4o\",\"modelVersion\":\"2024-08-06\",\"modelPublisher\":\"OpenAI\",\"capabilities\":{\"chatCompletion\":\"true\"},\"sku\":{\"name\":\"GlobalStandard\",\"capacity\":50}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("foundry_deployment_flat_v1: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"gpt-4o\",\"displayName\":\"gpt-4o\",\"ownedBy\":\"OpenAI\",\"additionalProperties\":{\"name\":\"gpt-4o\",\"type\":\"ModelDeployment\",\"modelName\":\"gpt-4o\",\"modelVersion\":\"2024-08-06\",\"modelPublisher\":\"OpenAI\",\"capabilities\":{\"chatCompletion\":\"true\"},\"sku\":{\"name\":\"GlobalStandard\",\"capacity\":50}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("foundry_deployment_flat_v1 misrouted")
    }
  }
  // vector: foundry_deployment_nested_arm
  do {
    let inputData = "{\"name\":\"my-gpt4\",\"properties\":{\"model\":{\"name\":\"gpt-4\",\"publisher\":\"OpenAI\",\"maxContextLength\":8192},\"capabilities\":{\"supportedInputModalities\":[\"text\"],\"supportedOutputModalities\":[\"text\"]}}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("foundry_deployment_nested_arm: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"my-gpt4\",\"displayName\":\"gpt-4\",\"ownedBy\":\"OpenAI\",\"contextWindow\":8192,\"inputModalities\":[\"text\"],\"outputModalities\":[\"text\"],\"additionalProperties\":{\"name\":\"my-gpt4\",\"properties\":{\"model\":{\"name\":\"gpt-4\",\"publisher\":\"OpenAI\",\"maxContextLength\":8192},\"capabilities\":{\"supportedInputModalities\":[\"text\"],\"supportedOutputModalities\":[\"text\"]}}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("foundry_deployment_nested_arm misrouted")
    }
  }
  // vector: openai_enrich_dalle3_no_context_window
  do {
    let inputData = "{\"id\":\"dall-e-3\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_dalle3_no_context_window: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"dall-e-3\",\"inputModalities\":[\"text\"],\"outputModalities\":[\"image\"]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_dalle3_no_context_window misrouted")
    }
  }
  // vector: openai_enrich_embedding_empty_output_modalities
  do {
    let inputData = "{\"id\":\"text-embedding-3-small\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_embedding_empty_output_modalities: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"text-embedding-3-small\",\"contextWindow\":8191,\"inputModalities\":[\"text\"],\"outputModalities\":[]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_embedding_empty_output_modalities misrouted")
    }
  }
  // vector: openai_enrich_gpt4o
  do {
    let inputData = "{\"id\":\"gpt-4o\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_gpt4o: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"gpt-4o\",\"contextWindow\":128000,\"inputModalities\":[\"text\",\"image\"],\"outputModalities\":[\"text\"]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_gpt4o misrouted")
    }
  }
  // vector: openai_enrich_gpt4o_mini_longest_prefix
  do {
    let inputData = "{\"id\":\"gpt-4o-mini-2024-07-18\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_gpt4o_mini_longest_prefix: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"gpt-4o-mini-2024-07-18\",\"contextWindow\":128000,\"inputModalities\":[\"text\",\"image\"],\"outputModalities\":[\"text\"]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_gpt4o_mini_longest_prefix misrouted")
    }
  }
  // vector: openai_enrich_prefix_requires_token_boundary
  do {
    let inputData = "{\"id\":\"gpt-45-future\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_prefix_requires_token_boundary: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"gpt-45-future\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_prefix_requires_token_boundary misrouted")
    }
  }
  // vector: openai_enrich_provider_context_window_wins
  do {
    let inputData = "{\"id\":\"gpt-4o\",\"contextWindow\":999}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_provider_context_window_wins: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"gpt-4o\",\"contextWindow\":999,\"inputModalities\":[\"text\",\"image\"],\"outputModalities\":[\"text\"]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_provider_context_window_wins misrouted")
    }
  }
  // vector: openai_enrich_provider_empty_modalities_win
  do {
    let inputData = "{\"id\":\"gpt-4o\",\"inputModalities\":[]}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_provider_empty_modalities_win: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"gpt-4o\",\"contextWindow\":128000,\"inputModalities\":[],\"outputModalities\":[\"text\"]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_provider_empty_modalities_win misrouted")
    }
  }
  // vector: openai_enrich_unknown_id_is_noop
  do {
    let inputData = "{\"id\":\"ft:custom-model:acme::xyz\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_enrich_unknown_id_is_noop: failed to parse embedded vector input")
    }
    let base = try ModelInfo.load(input["base"]!)
    let actual = try await seam.enrich(base: base)
    let expectedData = "{\"id\":\"ft:custom-model:acme::xyz\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_enrich_unknown_id_is_noop misrouted")
    }
  }
  // vector: openai_model_basic
  do {
    let inputData = "{\"id\":\"o3-mini\",\"object\":\"model\",\"created\":1715367049,\"owned_by\":\"system\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_model_basic: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"o3-mini\",\"ownedBy\":\"system\",\"additionalProperties\":{\"id\":\"o3-mini\",\"object\":\"model\",\"created\":1715367049,\"owned_by\":\"system\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_model_basic misrouted")
    }
  }
  // vector: openai_model_finetune_no_owner
  do {
    let inputData = "{\"id\":\"ft:custom-model:acme::xyz\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("openai_model_finetune_no_owner: failed to parse embedded vector input")
    }
    let raw = input["raw"] as! Any
    let actual = try await seam.mapModel(raw: raw)
    let expectedData = "{\"id\":\"ft:custom-model:acme::xyz\",\"additionalProperties\":{\"id\":\"ft:custom-model:acme::xyz\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("openai_model_finetune_no_owner misrouted")
    }
  }
}

/// Typed @vector conformance for LoadConformance. Pass your real `LoadConformance` impl; the
/// `S: LoadConformance` constraint makes the compiler prove every op is implemented.
public func runLoadConformanceConformance<S: LoadConformance>(_ seam: S) async throws {
  // vector: basic_load
  do {
    let inputData = "{\"fixture\":\"basic.prompty\",\"env\":{\"OPENAI_ENDPOINT\":\"https://test.openai.com\",\"OPENAI_API_KEY\":\"sk-test123\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("basic_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"basic-prompt\",\"description\":\"A basic prompt for testing\",\"metadata\":{\"authors\":[\"testauthor\"]},\"model\":{\"id\":\"gpt-4\",\"provider\":\"openai\",\"apiType\":\"chat\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"https://test.openai.com\"},\"options\":{\"temperature\":0.7,\"maxOutputTokens\":1000}},\"inputs\":[{\"name\":\"firstName\",\"kind\":\"string\",\"default\":\"Jane\"},{\"name\":\"lastName\",\"kind\":\"string\",\"default\":\"Doe\"},{\"name\":\"question\",\"kind\":\"string\",\"default\":\"What is the meaning of life?\"}],\"template\":{\"format\":{\"kind\":\"jinja2\"},\"parser\":{\"kind\":\"prompty\"}},\"instructions\":\"system:\\nYou are an AI assistant who helps people find information.\\n\\n# Customer\\nYou are helping {{firstName}} {{lastName}} to find answers to their questions.\\n\\nuser:\\n{{question}}\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("basic_load misrouted")
    }
  }
  // vector: connection_types_load
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"connection-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":{\"kind\":\"anonymous\",\"endpoint\":\"https://localhost:8080\"}}}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("connection_types_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"model\":{\"id\":\"gpt-4\",\"connection\":{\"kind\":\"anonymous\",\"endpoint\":\"https://localhost:8080\"}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("connection_types_load misrouted")
    }
  }
  // vector: embedding_load
  do {
    let inputData = "{\"fixture\":\"embedding.prompty\",\"env\":{\"OPENAI_ENDPOINT\":\"https://test.openai.com\",\"OPENAI_API_KEY\":\"sk-test123\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("embedding_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"embedding\",\"model\":{\"id\":\"text-embedding-3-small\",\"provider\":\"openai\",\"apiType\":\"embedding\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"https://test.openai.com\"}},\"inputs\":[{\"name\":\"text\",\"kind\":\"string\",\"default\":\"Hello world\"}],\"instructions\":\"{{text}}\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("embedding_load misrouted")
    }
  }
  // vector: empty_frontmatter_body_only
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"empty-fm\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("empty_frontmatter_body_only: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"empty-fm\",\"model\":null,\"inputs\":null,\"tools\":null}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("empty_frontmatter_body_only misrouted")
    }
  }
  // vector: env_default
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"env-default-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"${env:MISSING_VAR:fallback_value}\"}}},\"env\":{}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("env_default: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"model\":{\"connection\":{\"kind\":\"key\",\"endpoint\":\"fallback_value\"}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("env_default misrouted")
    }
  }
  // vector: env_missing_error
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"env-error-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"${env:NONEXISTENT}\"}}},\"env\":{}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("env_missing_error: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    var caught: Error? = nil
    do {
      _ = try await seam.load(source: source)
    } catch {
      caught = error
    }
    guard let caughtError = caught else {
      throw SeamConformanceError("env_missing_error: expected an error")
    }
  }
  // vector: env_resolution
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"env-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"${env:MY_VAR}\"}}},\"env\":{\"MY_VAR\":\"hello\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("env_resolution: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"model\":{\"connection\":{\"kind\":\"key\",\"endpoint\":\"hello\"}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("env_resolution misrouted")
    }
  }
  // vector: file_reference_in_tree_allowed
  do {
    let inputData = "{\"agent_subdir\":\"app\",\"frontmatter\":{\"name\":\"file-in-tree-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":\"${file:sub/data.json}\"}},\"files\":{\"sub/data.json\":{\"kind\":\"key\",\"endpoint\":\"https://in-tree.example.com\",\"apiKey\":\"ok\"}}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("file_reference_in_tree_allowed: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"model\":{\"connection\":{\"kind\":\"key\",\"endpoint\":\"https://in-tree.example.com\"}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("file_reference_in_tree_allowed misrouted")
    }
  }
  // vector: file_reference_traversal_rejected
  do {
    let inputData = "{\"agent_subdir\":\"app\",\"frontmatter\":{\"name\":\"file-traversal-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":\"${file:../secret.json}\"}},\"files\":{\"../secret.json\":{\"kind\":\"key\",\"endpoint\":\"https://attacker.example.com\",\"apiKey\":\"leaked\"}}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("file_reference_traversal_rejected: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    var caught: Error? = nil
    do {
      _ = try await seam.load(source: source)
    } catch {
      caught = error
    }
    guard let caughtError = caught else {
      throw SeamConformanceError("file_reference_traversal_rejected: expected an error")
    }
  }
  // vector: file_resolution
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"file-ref-test\",\"model\":{\"id\":\"gpt-4\",\"connection\":\"${file:shared_connection.json}\"}},\"env\":{\"OPENAI_API_KEY\":\"shared-key\"},\"files\":{\"shared_connection.json\":{\"kind\":\"key\",\"endpoint\":\"https://shared.example.com\",\"apiKey\":\"shared-key\"}}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("file_resolution: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"model\":{\"connection\":{\"kind\":\"key\",\"endpoint\":\"https://shared.example.com\"}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("file_resolution misrouted")
    }
  }
  // vector: image_apitype_load
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"image-gen\",\"model\":{\"id\":\"dall-e-3\",\"provider\":\"openai\",\"apiType\":\"image\",\"options\":{\"additionalProperties\":{\"size\":\"1024x1024\",\"quality\":\"standard\"}}}}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("image_apitype_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"image-gen\",\"model\":{\"id\":\"dall-e-3\",\"provider\":\"openai\",\"apiType\":\"image\",\"options\":{\"additionalProperties\":{\"size\":\"1024x1024\",\"quality\":\"standard\"}}}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("image_apitype_load misrouted")
    }
  }
  // vector: input_scalar_shorthand
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"scalar-test\",\"model\":\"gpt-4\",\"inputs\":[{\"name\":\"topic\",\"kind\":\"string\",\"default\":\"science\"},{\"name\":\"count\",\"kind\":\"integer\",\"default\":5}]}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("input_scalar_shorthand: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"inputs\":[{\"name\":\"topic\",\"kind\":\"string\",\"default\":\"science\"},{\"name\":\"count\",\"kind\":\"integer\",\"default\":5}]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("input_scalar_shorthand misrouted")
    }
  }
  // vector: input_validation_default_fill
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"default-fill-test\",\"model\":\"gpt-4\",\"inputs\":[{\"name\":\"name\",\"kind\":\"string\",\"default\":\"world\"}]},\"inputs\":{}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("input_validation_default_fill: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"validated_inputs\":{\"name\":\"world\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("input_validation_default_fill misrouted")
    }
  }
  // vector: input_validation_example_not_used
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"example-not-used-test\",\"model\":\"gpt-4\",\"inputs\":[{\"name\":\"topic\",\"kind\":\"string\",\"required\":false,\"example\":\"sample\"}]},\"inputs\":{}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("input_validation_example_not_used: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"validated_inputs\":{}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("input_validation_example_not_used misrouted")
    }
  }
  // vector: input_validation_optional_omit
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"optional-omit-test\",\"model\":\"gpt-4\",\"inputs\":[{\"name\":\"nickname\",\"kind\":\"string\",\"required\":false}]},\"inputs\":{}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("input_validation_optional_omit: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"validated_inputs\":{}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("input_validation_optional_omit misrouted")
    }
  }
  // vector: input_validation_required
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"required-input-test\",\"model\":\"gpt-4\",\"inputs\":[{\"name\":\"city\",\"kind\":\"string\",\"required\":true}]},\"inputs\":{}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("input_validation_required: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    var caught: Error? = nil
    do {
      _ = try await seam.load(source: source)
    } catch {
      caught = error
    }
    guard let caughtError = caught else {
      throw SeamConformanceError("input_validation_required: expected an error")
    }
  }
  // vector: instructions_from_body
  do {
    let inputData = "{\"fixture\":\"basic.prompty\",\"env\":{\"OPENAI_ENDPOINT\":\"https://test.openai.com\",\"OPENAI_API_KEY\":\"sk-test123\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("instructions_from_body: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"instructions\":\"system:\\nYou are an AI assistant who helps people find information.\\n\\n# Customer\\nYou are helping {{firstName}} {{lastName}} to find answers to their questions.\\n\\nuser:\\n{{question}}\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("instructions_from_body misrouted")
    }
  }
  // vector: invalid_frontmatter_error
  do {
    let inputData = "{\"frontmatter_raw\":\"---\\nname: [invalid\\n---\\nHello\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("invalid_frontmatter_error: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    var caught: Error? = nil
    do {
      _ = try await seam.load(source: source)
    } catch {
      caught = error
    }
    guard let caughtError = caught else {
      throw SeamConformanceError("invalid_frontmatter_error: expected an error")
    }
  }
  // vector: kind_always_prompt
  do {
    let inputData = "{\"fixture\":\"minimal.prompty\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("kind_always_prompt: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("kind_always_prompt misrouted")
    }
  }
  // vector: minimal_load
  do {
    let inputData = "{\"fixture\":\"minimal.prompty\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("minimal_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"minimal\",\"model\":{\"id\":\"gpt-4\"},\"instructions\":\"system:\\nHello world.\",\"inputs\":null,\"outputs\":null,\"tools\":null}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("minimal_load misrouted")
    }
  }
  // vector: missing_file_error
  do {
    let inputData = "{\"fixture\":\"nonexistent.prompty\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("missing_file_error: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    var caught: Error? = nil
    do {
      _ = try await seam.load(source: source)
    } catch {
      caught = error
    }
    guard let caughtError = caught else {
      throw SeamConformanceError("missing_file_error: expected an error")
    }
  }
  // vector: model_shorthand
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"test\",\"model\":\"gpt-4o\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("model_shorthand: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"test\",\"model\":{\"id\":\"gpt-4o\"}}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("model_shorthand misrouted")
    }
  }
  // vector: structured_outputs_load
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"structured-output\",\"model\":{\"id\":\"gpt-4\",\"apiType\":\"chat\"},\"outputs\":[{\"name\":\"temperature\",\"kind\":\"integer\",\"description\":\"Temperature in Fahrenheit\",\"required\":true},{\"name\":\"condition\",\"kind\":\"string\",\"description\":\"Weather condition\",\"required\":true},{\"name\":\"city\",\"kind\":\"string\",\"description\":\"City name\",\"required\":true}]}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("structured_outputs_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"structured-output\",\"outputs\":[{\"name\":\"temperature\",\"kind\":\"integer\",\"description\":\"Temperature in Fahrenheit\",\"required\":true},{\"name\":\"condition\",\"kind\":\"string\",\"description\":\"Weather condition\",\"required\":true},{\"name\":\"city\",\"kind\":\"string\",\"description\":\"City name\",\"required\":true}]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("structured_outputs_load misrouted")
    }
  }
  // vector: template_string_invalid
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"shorthand-test\",\"model\":{\"id\":\"gpt-4\"},\"template\":\"jinja2\"}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("template_string_invalid: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    var caught: Error? = nil
    do {
      _ = try await seam.load(source: source)
    } catch {
      caught = error
    }
    guard let caughtError = caught else {
      throw SeamConformanceError("template_string_invalid: expected an error")
    }
  }
  // vector: tools_custom_load
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"custom-tool-test\",\"model\":\"gpt-4\",\"tools\":[{\"name\":\"my_tool\",\"kind\":\"my_provider\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"https://custom.example.com\"}}]}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("tools_custom_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"tools\":[{\"name\":\"my_tool\",\"kind\":\"my_provider\"}]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("tools_custom_load misrouted")
    }
  }
  // vector: tools_function_load
  do {
    let inputData = "{\"fixture\":\"tools_function.prompty\"}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("tools_function_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"kind\":\"prompt\",\"name\":\"function-tools\",\"model\":{\"id\":\"gpt-4\",\"apiType\":\"chat\"},\"tools\":[{\"name\":\"get_weather\",\"kind\":\"function\",\"description\":\"Get the current weather in a given location\",\"strict\":true,\"parameters\":[{\"name\":\"location\",\"kind\":\"string\",\"description\":\"The city and state, e.g. San Francisco, CA\"},{\"name\":\"unit\",\"kind\":\"string\",\"description\":\"Temperature unit\",\"enumValues\":[\"celsius\",\"fahrenheit\"]}],\"bindings\":{\"unit\":{\"input\":\"preferred_unit\"}}}],\"inputs\":[{\"name\":\"question\",\"kind\":\"string\",\"default\":\"What's the weather in Paris?\"},{\"name\":\"preferred_unit\",\"kind\":\"string\",\"default\":\"celsius\"}],\"instructions\":\"system:\\nYou are a helpful assistant with access to tools.\\n\\nuser:\\n{{question}}\"}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("tools_function_load misrouted")
    }
  }
  // vector: tools_mcp_load
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"mcp-test\",\"model\":\"gpt-4\",\"tools\":[{\"name\":\"filesystem\",\"kind\":\"mcp\",\"serverName\":\"fs-server\",\"connection\":{\"kind\":\"reference\",\"name\":\"my-mcp\"}}]}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("tools_mcp_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"tools\":[{\"name\":\"filesystem\",\"kind\":\"mcp\",\"serverName\":\"fs-server\"}]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("tools_mcp_load misrouted")
    }
  }
  // vector: tools_openapi_load
  do {
    let inputData = "{\"frontmatter\":{\"name\":\"openapi-test\",\"model\":\"gpt-4\",\"tools\":[{\"name\":\"weather_api\",\"kind\":\"openapi\",\"specification\":\"./weather.json\",\"connection\":{\"kind\":\"key\",\"endpoint\":\"https://api.weather.com\"}}]}}".data(using: .utf8)!
    guard let input = try JSONSerialization.jsonObject(with: inputData) as? [String: Any] else {
      throw SeamConformanceError("tools_openapi_load: failed to parse embedded vector input")
    }
    let source = input["source"] as! Any
    let actual = try await seam.load(source: source)
    let expectedData = "{\"tools\":[{\"name\":\"weather_api\",\"kind\":\"openapi\",\"specification\":\"./weather.json\"}]}".data(using: .utf8)!
    let expected = try JSONSerialization.jsonObject(with: expectedData, options: [.fragmentsAllowed])
    guard try seamConformanceCanonicalJSON(try actual.save()) == seamConformanceCanonicalJSON(expected) else {
      throw SeamConformanceError("tools_openapi_load misrouted")
    }
  }
}
