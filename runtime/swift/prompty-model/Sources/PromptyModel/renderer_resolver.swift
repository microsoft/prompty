// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Part III behavioral @dispatch resolver for Renderer — the twin of the shape
// discriminator load switch, emitted into the library beside the seam
// protocol. The provider protocol below has one accessor per @dispatch
// variant; a consumer attaches concrete impls by conforming to it in an
// external, non-emitted type, so a forgotten slot fails to compile.
// See docs: reference/vector-conformance.

import Foundation

/// Consumer-attached provider of Renderer impls, one accessor per @dispatch
/// variant. Return nil to signal a valid-but-unimplemented variant to the
/// caller (e.g. the conformance harness skips it), never a silent miss.
public protocol RendererProvider {
  var jinja2: (any Renderer)? { get }
  var mustache: (any Renderer)? { get }
  /// Catch-all for an unknown discriminator (the declared `*` child).
  var custom: (any Renderer)? { get }
}

/// Maps a 'kind' discriminator value to the selected Renderer impl — the
/// behavioral twin of the shape discriminator load switch.
public enum RendererResolver {
  public static func resolve(kind: String, registry: any RendererProvider) -> (any Renderer)? {
    switch kind {
    case "jinja2": return registry.jinja2
    case "mustache": return registry.mustache
    default: return registry.custom
    }
  }
}
