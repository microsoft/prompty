// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation
import XCTest
@testable import PromptyModel

final class ToolTests: XCTestCase {
  func testJSONRoundTrip1() throws {
    let json = """
{
  "name": "my-tool",
  "kind": "function",
  "description": "A description of the tool",
  "bindings": {
    "input": "value"
  }
}
"""
    let instance = try Tool.fromJSON(json)
    if case .functionTool(let concrete) = instance {
      XCTAssertEqual(concrete.name, "my-tool")
      XCTAssertEqual((try XCTUnwrap(concrete.description)), "A description of the tool")
    } else {
      XCTFail("Expected FunctionTool")
    }
    let reloaded = try Tool.fromJSON(try instance.toJSON())
    if case .functionTool(let concrete) = reloaded {
      XCTAssertEqual(concrete.name, "my-tool")
      XCTAssertEqual((try XCTUnwrap(concrete.description)), "A description of the tool")
    } else {
      XCTFail("Expected FunctionTool")
    }
  }

  func testYAMLRoundTrip1() throws {
    let yaml = """
name: my-tool
kind: function
description: A description of the tool
bindings:
  input: value

"""
    let instance = try Tool.fromYAML(yaml)
    if case .functionTool(let concrete) = instance {
      XCTAssertEqual(concrete.name, "my-tool")
      XCTAssertEqual((try XCTUnwrap(concrete.description)), "A description of the tool")
    } else {
      XCTFail("Expected FunctionTool")
    }
    let reloaded = try Tool.fromYAML(try instance.toYAML())
    if case .functionTool(let concrete) = reloaded {
      XCTAssertEqual(concrete.name, "my-tool")
      XCTAssertEqual((try XCTUnwrap(concrete.description)), "A description of the tool")
    } else {
      XCTFail("Expected FunctionTool")
    }
  }

}
