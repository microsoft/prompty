// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation
import XCTest
@testable import PromptyModel

final class ContentPartTests: XCTestCase {
  func testJSONRoundTrip1() throws {
    let json = """
{
  "kind": "text",
  "value": "Hello, world!"
}
"""
    let instance = try ContentPart.fromJSON(json)
    if case .textPart(let concrete) = instance {
      XCTAssertEqual(concrete.value, "Hello, world!")
    } else {
      XCTFail("Expected TextPart")
    }
    let reloaded = try ContentPart.fromJSON(try instance.toJSON())
    if case .textPart(let concrete) = reloaded {
      XCTAssertEqual(concrete.value, "Hello, world!")
    } else {
      XCTFail("Expected TextPart")
    }
  }

  func testYAMLRoundTrip1() throws {
    let yaml = """
kind: text
value: Hello, world!

"""
    let instance = try ContentPart.fromYAML(yaml)
    if case .textPart(let concrete) = instance {
      XCTAssertEqual(concrete.value, "Hello, world!")
    } else {
      XCTFail("Expected TextPart")
    }
    let reloaded = try ContentPart.fromYAML(try instance.toYAML())
    if case .textPart(let concrete) = reloaded {
      XCTAssertEqual(concrete.value, "Hello, world!")
    } else {
      XCTFail("Expected TextPart")
    }
  }

}
