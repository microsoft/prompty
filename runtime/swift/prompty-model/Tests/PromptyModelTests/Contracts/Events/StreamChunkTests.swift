// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation
import XCTest

@testable import PromptyModel

final class StreamChunkTests: XCTestCase {
  func testJSONRoundTrip1() throws {
    let json = """
      {
        "kind": "text",
        "value": "Hello"
      }
      """
    let instance = try StreamChunk.fromJSON(json)
    if case .textChunk(let concrete) = instance {
      XCTAssertEqual(concrete.value, "Hello")
    } else {
      XCTFail("Expected TextChunk")
    }
    let reloaded = try StreamChunk.fromJSON(try instance.toJSON())
    if case .textChunk(let concrete) = reloaded {
      XCTAssertEqual(concrete.value, "Hello")
    } else {
      XCTFail("Expected TextChunk")
    }
  }

  func testYAMLRoundTrip1() throws {
    let yaml = """
      kind: text
      value: Hello

      """
    let instance = try StreamChunk.fromYAML(yaml)
    if case .textChunk(let concrete) = instance {
      XCTAssertEqual(concrete.value, "Hello")
    } else {
      XCTFail("Expected TextChunk")
    }
    let reloaded = try StreamChunk.fromYAML(try instance.toYAML())
    if case .textChunk(let concrete) = reloaded {
      XCTAssertEqual(concrete.value, "Hello")
    } else {
      XCTFail("Expected TextChunk")
    }
  }

}
