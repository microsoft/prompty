// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation
import XCTest
@testable import PromptyModel

final class SessionRefTests: XCTestCase {
  func testJSONRoundTrip1() throws {
    let json = """
{
  "sessionId": "sess_abc123",
  "refType": "issue",
  "refValue": "owner/repo#123",
  "turnIndex": 2,
  "createdAt": "2026-06-09T20:00:00Z"
}
"""
    let instance = try SessionRef.fromJSON(json)
    XCTAssertEqual((try XCTUnwrap(instance.sessionId)), "sess_abc123")
    XCTAssertEqual(instance.refType, "issue")
    XCTAssertEqual(instance.refValue, "owner/repo#123")
    XCTAssertEqual((try XCTUnwrap(instance.turnIndex)), 2)
    XCTAssertEqual((try XCTUnwrap(instance.createdAt)), "2026-06-09T20:00:00Z")
    let reloaded = try SessionRef.fromJSON(try instance.toJSON())
    XCTAssertEqual((try XCTUnwrap(reloaded.sessionId)), "sess_abc123")
    XCTAssertEqual(reloaded.refType, "issue")
    XCTAssertEqual(reloaded.refValue, "owner/repo#123")
    XCTAssertEqual((try XCTUnwrap(reloaded.turnIndex)), 2)
    XCTAssertEqual((try XCTUnwrap(reloaded.createdAt)), "2026-06-09T20:00:00Z")
  }

  func testYAMLRoundTrip1() throws {
    let yaml = """
sessionId: sess_abc123
refType: issue
refValue: "owner/repo#123"
turnIndex: 2
createdAt: "2026-06-09T20:00:00Z"

"""
    let instance = try SessionRef.fromYAML(yaml)
    XCTAssertEqual((try XCTUnwrap(instance.sessionId)), "sess_abc123")
    XCTAssertEqual(instance.refType, "issue")
    XCTAssertEqual(instance.refValue, "owner/repo#123")
    XCTAssertEqual((try XCTUnwrap(instance.turnIndex)), 2)
    XCTAssertEqual((try XCTUnwrap(instance.createdAt)), "2026-06-09T20:00:00Z")
    let reloaded = try SessionRef.fromYAML(try instance.toYAML())
    XCTAssertEqual((try XCTUnwrap(reloaded.sessionId)), "sess_abc123")
    XCTAssertEqual(reloaded.refType, "issue")
    XCTAssertEqual(reloaded.refValue, "owner/repo#123")
    XCTAssertEqual((try XCTUnwrap(reloaded.turnIndex)), 2)
    XCTAssertEqual((try XCTUnwrap(reloaded.createdAt)), "2026-06-09T20:00:00Z")
  }

}
