// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

import Foundation
import XCTest

@testable import PromptyModel

final class TurnOptionsTests: XCTestCase {
  func testJSONRoundTrip1() throws {
    let json = """
      {
        "maxIterations": 10,
        "maxLlmRetries": 3,
        "contextBudget": 100000,
        "parallelToolCalls": true,
        "raw": false,
        "turn": 1,
        "compaction": {
          "strategy": "summarize"
        }
      }
      """
    let instance = try TurnOptions.fromJSON(json)
    XCTAssertEqual((try XCTUnwrap(instance.maxIterations)), 10)
    XCTAssertEqual((try XCTUnwrap(instance.maxLlmRetries)), 3)
    XCTAssertEqual((try XCTUnwrap(instance.contextBudget)), 100000)
    XCTAssertEqual((try XCTUnwrap(instance.parallelToolCalls)), true)
    XCTAssertEqual((try XCTUnwrap(instance.raw)), false)
    XCTAssertEqual((try XCTUnwrap(instance.turn)), 1)
    XCTAssertEqual((try XCTUnwrap((try XCTUnwrap(instance.compaction)).strategy)), "summarize")
    let reloaded = try TurnOptions.fromJSON(try instance.toJSON())
    XCTAssertEqual((try XCTUnwrap(reloaded.maxIterations)), 10)
    XCTAssertEqual((try XCTUnwrap(reloaded.maxLlmRetries)), 3)
    XCTAssertEqual((try XCTUnwrap(reloaded.contextBudget)), 100000)
    XCTAssertEqual((try XCTUnwrap(reloaded.parallelToolCalls)), true)
    XCTAssertEqual((try XCTUnwrap(reloaded.raw)), false)
    XCTAssertEqual((try XCTUnwrap(reloaded.turn)), 1)
    XCTAssertEqual((try XCTUnwrap((try XCTUnwrap(reloaded.compaction)).strategy)), "summarize")
  }

  func testYAMLRoundTrip1() throws {
    let yaml = """
      maxIterations: 10
      maxLlmRetries: 3
      contextBudget: 100000
      parallelToolCalls: true
      raw: false
      turn: 1
      compaction:
        strategy: summarize

      """
    let instance = try TurnOptions.fromYAML(yaml)
    XCTAssertEqual((try XCTUnwrap(instance.maxIterations)), 10)
    XCTAssertEqual((try XCTUnwrap(instance.maxLlmRetries)), 3)
    XCTAssertEqual((try XCTUnwrap(instance.contextBudget)), 100000)
    XCTAssertEqual((try XCTUnwrap(instance.parallelToolCalls)), true)
    XCTAssertEqual((try XCTUnwrap(instance.raw)), false)
    XCTAssertEqual((try XCTUnwrap(instance.turn)), 1)
    XCTAssertEqual((try XCTUnwrap((try XCTUnwrap(instance.compaction)).strategy)), "summarize")
    let reloaded = try TurnOptions.fromYAML(try instance.toYAML())
    XCTAssertEqual((try XCTUnwrap(reloaded.maxIterations)), 10)
    XCTAssertEqual((try XCTUnwrap(reloaded.maxLlmRetries)), 3)
    XCTAssertEqual((try XCTUnwrap(reloaded.contextBudget)), 100000)
    XCTAssertEqual((try XCTUnwrap(reloaded.parallelToolCalls)), true)
    XCTAssertEqual((try XCTUnwrap(reloaded.raw)), false)
    XCTAssertEqual((try XCTUnwrap(reloaded.turn)), 1)
    XCTAssertEqual((try XCTUnwrap((try XCTUnwrap(reloaded.compaction)).strategy)), "summarize")
  }

}
