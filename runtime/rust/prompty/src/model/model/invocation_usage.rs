// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

/// Complete cumulative token usage for one completed model invocation. Providers emit this value at most once per invocation. `totalTokens` uses the provider total when available; otherwise the provider adapter computes it as `inputTokens + outputTokens`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct InvocationUsage {
    /// Number of input tokens consumed by the completed invocation
    pub input_tokens: i64,
    /// Number of output tokens generated by the completed invocation
    pub output_tokens: i64,
    /// Total tokens consumed by the completed invocation
    pub total_tokens: i64,
}

impl InvocationUsage {
    /// Create a new InvocationUsage with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load InvocationUsage from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load InvocationUsage from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load InvocationUsage from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            input_tokens: value
                .get("inputTokens")
                .and_then(|v| v.as_i64())
                .unwrap_or(0),
            output_tokens: value
                .get("outputTokens")
                .and_then(|v| v.as_i64())
                .unwrap_or(0),
            total_tokens: value
                .get("totalTokens")
                .and_then(|v| v.as_i64())
                .unwrap_or(0),
        }
    }

    /// Serialize InvocationUsage to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if self.input_tokens != 0 {
            result.insert(
                "inputTokens".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.input_tokens)),
            );
        }
        if self.output_tokens != 0 {
            result.insert(
                "outputTokens".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.output_tokens)),
            );
        }
        if self.total_tokens != 0 {
            result.insert(
                "totalTokens".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.total_tokens)),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize InvocationUsage to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize InvocationUsage to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Convert to provider-specific wire format.
    pub fn to_wire(&self, provider: &str) -> serde_json::Value {
        let data = serde_json::to_value(self).unwrap_or_default();
        let mut result = serde_json::Map::new();
        let wire_map: std::collections::HashMap<&str, std::collections::HashMap<&str, &str>> =
            std::collections::HashMap::from([
                (
                    "inputTokens",
                    std::collections::HashMap::from([
                        ("openai", "prompt_tokens"),
                        ("anthropic", "input_tokens"),
                    ]),
                ),
                (
                    "outputTokens",
                    std::collections::HashMap::from([
                        ("openai", "completion_tokens"),
                        ("anthropic", "output_tokens"),
                    ]),
                ),
                (
                    "totalTokens",
                    std::collections::HashMap::from([("openai", "total_tokens")]),
                ),
            ]);
        if let serde_json::Value::Object(map) = data {
            for (key, value) in map {
                if let Some(mapping) = wire_map.get(key.as_str()) {
                    if let Some(wire_name) = mapping.get(provider) {
                        result.insert(wire_name.to_string(), value);
                    }
                }
            }
        }
        serde_json::Value::Object(result)
    }
}

// Serde for `InvocationUsage` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
impl serde::Serialize for InvocationUsage {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

impl<'de> serde::Deserialize<'de> for InvocationUsage {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
