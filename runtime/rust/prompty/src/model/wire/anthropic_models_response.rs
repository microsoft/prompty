// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::anthropic_model_info::AnthropicModelInfo;

/// The response body from Anthropic's model listing API.
#[derive(Debug, Clone, Default)]
pub struct AnthropicModelsResponse {
    /// Models available to the authenticated caller
    pub data: Vec<AnthropicModelInfo>,
    /// The first model identifier in this page
    pub first_id: Option<String>,
    /// Whether another page is available
    pub has_more: Option<bool>,
    /// The last model identifier in this page
    pub last_id: Option<String>,
}

impl AnthropicModelsResponse {
    /// Create a new AnthropicModelsResponse with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load AnthropicModelsResponse from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load AnthropicModelsResponse from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load AnthropicModelsResponse from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            data: value
                .get("data")
                .map(|v| Self::load_data(v, ctx))
                .unwrap_or_default(),
            first_id: value
                .get("first_id")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            has_more: value.get("has_more").and_then(|v| v.as_bool()),
            last_id: value
                .get("last_id")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
        }
    }

    /// Serialize AnthropicModelsResponse to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.data.is_empty() {
            result.insert("data".to_string(), Self::save_data(&self.data, ctx));
        }
        if let Some(ref val) = self.first_id {
            result.insert(
                "first_id".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if let Some(val) = self.has_more {
            result.insert("has_more".to_string(), serde_json::Value::Bool(val));
        }
        if let Some(ref val) = self.last_id {
            result.insert(
                "last_id".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize AnthropicModelsResponse to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize AnthropicModelsResponse to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of AnthropicModelInfo from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_data(data: &serde_json::Value, ctx: &LoadContext) -> Vec<AnthropicModelInfo> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| AnthropicModelInfo::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of AnthropicModelInfo to a JSON value.
    fn save_data(items: &[AnthropicModelInfo], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}
