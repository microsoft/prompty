// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use super::super::super::context::{LoadContext, SaveContext};

/// Provider-neutral OAuth token material returned by an authorization server.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct OAuthToken {
    /// The bearer access token
    pub access_token: String,
    /// The token type, typically Bearer
    pub token_type: String,
    /// Lifetime of the access token in seconds
    pub expires_in: i64,
    /// Refresh token when offline access was granted
    pub refresh_token: Option<String>,
    /// Scopes granted by the authorization server
    pub scope: Option<String>,
}

impl OAuthToken {
    /// Create a new OAuthToken with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load OAuthToken from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load OAuthToken from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load OAuthToken from an already-parsed JSON value, returning an error
    /// instead of panicking on invalid input. Fallible companion to
    /// `load_from_value` with the same validation policy as `from_json`.
    pub fn try_load_from_value(
        value: &serde_json::Value,
        ctx: &LoadContext,
    ) -> Result<Self, serde_json::Error> {
        Self::validate_input_at(value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(value, ctx))
    }

    /// Load OAuthToken from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            access_token: value
                .get("accessToken")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            token_type: value
                .get("tokenType")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            expires_in: value.get("expiresIn").and_then(|v| v.as_i64()).unwrap_or(0),
            refresh_token: value
                .get("refreshToken")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            scope: value
                .get("scope")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        Ok(())
    }

    /// Serialize OAuthToken to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert(
            "accessToken".to_string(),
            serde_json::Value::String(self.access_token.clone()),
        );
        result.insert(
            "tokenType".to_string(),
            serde_json::Value::String(self.token_type.clone()),
        );
        result.insert(
            "expiresIn".to_string(),
            serde_json::Value::Number(serde_json::Number::from(self.expires_in)),
        );
        if let Some(val) = self.refresh_token.as_ref() {
            result.insert(
                "refreshToken".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if let Some(val) = self.scope.as_ref() {
            result.insert("scope".to_string(), serde_json::Value::String(val.clone()));
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize OAuthToken to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize OAuthToken to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Convert to provider-specific wire format.
    pub fn to_wire(&self, provider: &str) -> serde_json::Value {
        let data = self.to_value(&SaveContext::default());
        let mut result = serde_json::Map::new();
        let wire_map: std::collections::HashMap<&str, std::collections::HashMap<&str, &str>> =
            std::collections::HashMap::from([
                (
                    "accessToken",
                    std::collections::HashMap::from([("foundry", "access_token")]),
                ),
                (
                    "tokenType",
                    std::collections::HashMap::from([("foundry", "token_type")]),
                ),
                (
                    "expiresIn",
                    std::collections::HashMap::from([("foundry", "expires_in")]),
                ),
                (
                    "refreshToken",
                    std::collections::HashMap::from([("foundry", "refresh_token")]),
                ),
                (
                    "scope",
                    std::collections::HashMap::from([("foundry", "scope")]),
                ),
            ]);
        if let serde_json::Value::Object(map) = data {
            for (key, value) in map {
                if let Some(mapping) = wire_map.get(key.as_str()) {
                    if let Some(wire_name) = mapping.get(provider) {
                        result.insert(wire_name.to_string(), value);
                    }
                }
            }
        }
        serde_json::Value::Object(result)
    }
}

// Serde for `OAuthToken` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for OAuthToken {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for OAuthToken {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
