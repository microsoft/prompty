// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, unexpected_cfgs, clippy::all)]

use super::super::super::context::{LoadContext, SaveContext};


/// Variant-specific data for [`Property`], discriminated by `kind`.
#[derive(Debug, Clone, PartialEq)]
pub enum PropertyKind {
    /// `kind` = `"array"`
    Array {
        /// The type of items contained in the array
        items: serde_json::Value,
    },
    /// `kind` = `"object"`
    Object {
        /// The properties contained in the object
        properties: Vec<Property>,
    },
    /// `kind` = `"union"`
    Union {
        /// Alternative property schemas where exactly one branch must match
        one_of: Option<Vec<Property>>,
        /// Alternative property schemas where one or more branches may match
        any_of: Option<Vec<Property>>,
    },
    /// Wildcard / catch-all variant for unrecognized `kind` values.
    Custom {
        /// The raw `kind` string for this unknown variant.
        kind_name: String,
        /// Unmodeled fields preserved for forward-compatible round trips.
        raw: serde_json::Map<String, serde_json::Value>,
    },
}

impl Default for PropertyKind {
    fn default() -> Self {
        PropertyKind::Custom {
            kind_name: String::new(),
            raw: serde_json::Map::new(),
        }
    }
}
/// Represents a single property. - This model defines the structure of properties that can be used in prompts, including their type, description, whether they are required, and other attributes. - It allows for the definition of dynamic inputs that can be filled with data and processed to generate prompts for AI models.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct Property {
    /// Name of the property
    pub name: String,
    /// A short description of the input property
    pub description: Option<String>,
    /// Whether the property is required
    pub required: Option<bool>,
    /// Whether the property also accepts a JSON null value
    pub nullable: Option<bool>,
    /// The default value of the property - this represents the default value if none is provided
    pub default: Option<serde_json::Value>,
    /// Example value used for either initialization or tooling
    pub example: Option<serde_json::Value>,
    /// Allowed enumeration values for the property
    pub enum_values: Option<Vec<serde_json::Value>>,
    /// Variant-specific data, discriminated by `kind`.
    pub kind: PropertyKind,
}

impl Property {
    /// Create a new Property with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load Property from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load Property from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load Property from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        if let Some(value) = value.as_bool() {
            return Property { kind: PropertyKind::Custom { kind_name: "boolean".to_string(), raw: serde_json::Map::new() }, example: Some(value.into()), ..Default::default() };
        }
        if let Some(s) = value.as_str() {
            let value = s.to_string();
            return Property { kind: PropertyKind::Custom { kind_name: "string".to_string(), raw: serde_json::Map::new() }, example: Some(value.into()), ..Default::default() };
        }
        if let Some(value) = value.as_i64() {
            return Property { kind: PropertyKind::Custom { kind_name: "integer".to_string(), raw: serde_json::Map::new() }, example: Some(value.into()), ..Default::default() };
        }
        if let Some(value) = value.as_f64() {
            return Property { kind: PropertyKind::Custom { kind_name: "float".to_string(), raw: serde_json::Map::new() }, example: Some(value.into()), ..Default::default() };
        }
        let kind_str = value.get("kind").and_then(|v| v.as_str()).unwrap_or("");
        let kind = match kind_str {
            "array" => PropertyKind::Array {
                items: value.get("items").cloned().unwrap_or(serde_json::Value::Null),
            },
            "object" => PropertyKind::Object {
                properties: value.get("properties").map(|v| Self::load_properties(v, ctx)).unwrap_or_default(),
            },
            "union" => PropertyKind::Union {
                one_of: value.get("oneOf").map(|v| Self::load_one_of(v, ctx)),
                any_of: value.get("anyOf").map(|v| Self::load_any_of(v, ctx)),
            },
            _ => PropertyKind::Custom {
                kind_name: kind_str.to_string(),
                raw: {
                    let mut raw = value.as_object().cloned().unwrap_or_default();
                    raw.remove("name");
                    raw.remove("kind");
                    raw.remove("description");
                    raw.remove("required");
                    raw.remove("nullable");
                    raw.remove("default");
                    raw.remove("example");
                    raw.remove("enumValues");
                    raw
                },
            },
        };
        Self {
            name: value.get("name").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            description: value.get("description").and_then(|v| v.as_str()).map(|s| s.to_string()),
            required: value.get("required").and_then(|v| v.as_bool()),
            nullable: value.get("nullable").and_then(|v| v.as_bool()),
            default: value.get("default").cloned(),
            example: value.get("example").cloned(),
            enum_values: value.get("enumValues").and_then(|v| v.as_array()).map(|arr| arr.to_vec()),
            kind: kind,
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        if !value.is_object() {
            return Ok(());
        }
        let discriminator = value.get("kind")
            .ok_or_else(|| "Missing Property discriminator property: 'kind'".to_string())?;
        let discriminator = match discriminator {
            serde_json::Value::String(value) if !value.is_empty() => value.as_str(),
            _ => return Err("Invalid Property discriminator field 'kind': expected non-blank string".to_string()),
        };
        match discriminator {
            "array" => {
                let child_path = if path.is_empty() { "items".to_string() } else { format!("{}.items", path) };
                let child = value.get("items").filter(|candidate| !candidate.is_null())
                    .ok_or_else(|| format!("{}: missing required field", child_path))?;
                Property::validate_input_at(child, &child_path)?;
            }
            "object" => {
                if let Some(collection) = value.get("properties") {
                    let collection_path = if path.is_empty() { "properties".to_string() } else { format!("{}.properties", path) };
                    match collection {
                        serde_json::Value::Object(entries) => {
                            for (name, entry) in entries {
                                let entry_path = format!("{}.{}", collection_path, name);
                                if entry.is_array() {
                                    return Err(format!("{}: invalid named collection entry category array", entry_path));
                                }
                                let mut candidate = if entry.is_object() {
                                    entry.clone()
                                } else {
                                    serde_json::json!({ "kind": entry })
                                };
                                if let serde_json::Value::Object(ref mut map) = candidate {
                                    map.insert("name".to_string(), serde_json::Value::String(name.clone()));
                                }
                                Property::validate_input_at(&candidate, &entry_path)?;
                            }
                        }
                        serde_json::Value::Array(entries) => {
                            for (index, entry) in entries.iter().enumerate() {
                                let entry_path = format!("{}[{}]", collection_path, index);
                                Property::validate_input_at(entry, &entry_path)?;
                            }
                        }
                        _ => {}
                    }
                }
            }
            "union" => {
                if let Some(entries) = value.get("oneOf").and_then(|candidate| candidate.as_array()) {
                    let collection_path = if path.is_empty() { "oneOf".to_string() } else { format!("{}.oneOf", path) };
                    for (index, entry) in entries.iter().enumerate() {
                        let entry_path = format!("{}[{}]", collection_path, index);
                        Property::validate_input_at(entry, &entry_path)?;
                    }
                }
                if let Some(entries) = value.get("anyOf").and_then(|candidate| candidate.as_array()) {
                    let collection_path = if path.is_empty() { "anyOf".to_string() } else { format!("{}.anyOf", path) };
                    for (index, entry) in entries.iter().enumerate() {
                        let entry_path = format!("{}[{}]", collection_path, index);
                        Property::validate_input_at(entry, &entry_path)?;
                    }
                }
            }
            _ => {}
        }
        Ok(())
    }

    /// Returns the `kind` discriminator string for this instance.
    pub fn kind_str(&self) -> &str {
        match &self.kind {
            PropertyKind::Array { .. } => "array",
            PropertyKind::Object { .. } => "object",
            PropertyKind::Union { .. } => "union",
            PropertyKind::Custom { kind_name, .. } => kind_name.as_str(),
        }
    }

    /// Serialize Property to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write the discriminator
        result.insert("kind".to_string(), serde_json::Value::String(self.kind_str().to_string()));
        // Write base fields
        result.insert("name".to_string(), serde_json::Value::String(self.name.clone()));
        if let Some(val) = self.description.as_ref() {
            result.insert("description".to_string(), serde_json::Value::String(val.clone()));
        }
        if let Some(val) = self.required.as_ref() {
            result.insert("required".to_string(), serde_json::Value::Bool(*val));
        }
        if let Some(val) = self.nullable.as_ref() {
            result.insert("nullable".to_string(), serde_json::Value::Bool(*val));
        }
        if let Some(val) = self.default.as_ref() {
            result.insert("default".to_string(), val.clone());
        }
        if let Some(val) = self.example.as_ref() {
            result.insert("example".to_string(), val.clone());
        }
        if let Some(items) = self.enum_values.as_ref() {
            result.insert("enumValues".to_string(), serde_json::to_value(items).unwrap_or(serde_json::Value::Null));
        }
        // Write variant-specific fields
        match &self.kind {
            PropertyKind::Array { items,  .. } => {
                result.insert("items".to_string(), items.clone());
            }
            PropertyKind::Object { properties,  .. } => {
                result.insert("properties".to_string(), Self::save_properties(properties, ctx));
            }
            PropertyKind::Union { one_of, any_of,  .. } => {
                if let Some(items) = one_of.as_ref() {
                    result.insert("oneOf".to_string(), serde_json::Value::Array(items.iter().map(|item| item.to_value(ctx)).collect()));
                }
                if let Some(items) = any_of.as_ref() {
                    result.insert("anyOf".to_string(), serde_json::Value::Array(items.iter().map(|item| item.to_value(ctx)).collect()));
                }
            }
            PropertyKind::Custom { raw, .. } => {
                for (key, value) in raw {
                    if matches!(key.as_str(), "name" | "kind" | "description" | "required" | "nullable" | "default" | "example" | "enumValues") { continue; }
                    result.insert(key.clone(), value.clone());
                }
            }
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize Property to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize Property to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of Property from a JSON value.
    /// Handles both array format `[{...}]` and dict format `{"name": {...}}`.
    fn load_properties(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Property> {
        match data {
            serde_json::Value::Array(arr) => {
                arr.iter().map(|v| Property::load_from_value(v, ctx)).collect()
            }

            serde_json::Value::Object(obj) => {
                obj.iter()
                    .map(|(name, value)| {
                        if value.is_array() {
                            panic!("properties.{}: invalid named collection entry category array", name);
                        }
                        let mut v = if value.is_object() {
                            value.clone()
                        } else {
                            serde_json::json!({ "example": value })
                        };
                        if let serde_json::Value::Object(ref mut m) = v {
                            m.entry("name".to_string()).or_insert_with(|| serde_json::Value::String(name.clone()));
                        }
                        Property::load_from_value(&v, ctx)
                    })
                    .collect()
            }
            _ => Vec::new(),

        }
    }

    /// Save a collection of Property to a JSON value.
    fn save_properties(items: &[Property], ctx: &SaveContext) -> serde_json::Value {

        let mut serialized = items.iter().map(|item| item.to_value(ctx)).collect::<Vec<_>>();
        for item_data in &mut serialized {
            if let serde_json::Value::Object(map) = item_data {
                if matches!(map.get("name"), Some(serde_json::Value::String(name)) if name.is_empty()) { map.remove("name"); }
            }
        }

        if ctx.collection_format == "array" {
            return serde_json::Value::Array(serialized);
        }
        let mut names = std::collections::HashSet::new();
        for item_data in &serialized {
            let Some(name) = item_data.get("name").and_then(|value| value.as_str()) else {
                return serde_json::Value::Array(serialized);
            };
            if name.is_empty() || !names.insert(name.to_string()) {
                return serde_json::Value::Array(serialized);
            }
        }
        // Object format: use name as key
        let mut result = serde_json::Map::new();
        for item_data in serialized {
            let mut item_data = match item_data {
                serde_json::Value::Object(m) => m,
                other => { let mut m = serde_json::Map::new(); m.insert("value".to_string(), other); m },
            };
            let serde_json::Value::String(name) = item_data.remove("name").expect("validated named collection item") else { unreachable!() };
            if ctx.use_shorthand && item_data.len() == 1 {
                if let Some(shorthand) = item_data.get("example") {
                    result.insert(name, shorthand.clone());
                    continue;
                }
            }
            result.insert(name, serde_json::Value::Object(item_data));
        }
        serde_json::Value::Object(result)

    }

    /// Load a collection of Property from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_one_of(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Property> {
        match data {
            serde_json::Value::Array(arr) => {
                arr.iter().map(|v| Property::load_from_value(v, ctx)).collect()
            }

            _ => Vec::new(),

        }
    }

    /// Save a collection of Property to a JSON value.
    fn save_one_of(items: &[Property], ctx: &SaveContext) -> serde_json::Value {

        serde_json::Value::Array(items.iter().map(|item| item.to_value(ctx)).collect::<Vec<_>>())

    }

    /// Load a collection of Property from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_any_of(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Property> {
        match data {
            serde_json::Value::Array(arr) => {
                arr.iter().map(|v| Property::load_from_value(v, ctx)).collect()
            }

            _ => Vec::new(),

        }
    }

    /// Save a collection of Property to a JSON value.
    fn save_any_of(items: &[Property], ctx: &SaveContext) -> serde_json::Value {

        serde_json::Value::Array(items.iter().map(|item| item.to_value(ctx)).collect::<Vec<_>>())

    }
}

// Serde for `Property` delegates to the canonical to_value/load_from_value
// logic so the `kind` discriminator round-trips to its exact wire value. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for Property {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for Property {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}

// Serde for `PropertyKind` wraps the variant into its parent `Property` and delegates
// to the canonical to_value/load_from_value logic, so a bare `PropertyKind`
// serializes to internally-tagged `{"kind": "<value>", ...}` — the same wire
// form as its parent — instead of serde's externally-tagged `{"<Variant>": {...}}`.
#[cfg(feature = "serde")]
impl serde::Serialize for PropertyKind {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        let parent = Property { kind: self.clone(), ..Default::default() };
        serde::Serialize::serialize(&parent.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for PropertyKind {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Property::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Property::load_from_value(&value, &LoadContext::default()).kind)
    }
}
