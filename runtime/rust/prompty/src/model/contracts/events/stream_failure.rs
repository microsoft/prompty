// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use super::super::super::context::{LoadContext, SaveContext};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum StreamFailureOutcome {
    Determinate,
    Indeterminate,
}

impl Default for StreamFailureOutcome {
    fn default() -> Self {
        Self::Determinate
    }
}

impl std::fmt::Display for StreamFailureOutcome {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Determinate => write!(f, "determinate"),
            Self::Indeterminate => write!(f, "indeterminate"),
        }
    }
}

impl StreamFailureOutcome {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "determinate" => Some(Self::Determinate),
            "indeterminate" => Some(Self::Indeterminate),
            _ => None,
        }
    }

    pub fn from_str_ignore_case_opt(s: &str) -> Option<Self> {
        if s.eq_ignore_ascii_case("determinate") {
            return Some(Self::Determinate);
        }
        if s.eq_ignore_ascii_case("indeterminate") {
            return Some(Self::Indeterminate);
        }
        None
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Determinate => "determinate",
            Self::Indeterminate => "indeterminate",
        }
    }
}

#[cfg(feature = "serde")]
impl serde::Serialize for StreamFailureOutcome {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.as_str())
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for StreamFailureOutcome {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let s = <String as serde::Deserialize>::deserialize(deserializer)?;
        Self::from_str_opt(&s).ok_or_else(|| {
            serde::de::Error::custom(format!("invalid StreamFailureOutcome value: {}", s))
        })
    }
}

/// A classified terminal failure from an LLM response stream.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct StreamFailure {
    /// Whether the provider outcome is known or requires reconciliation
    pub outcome: StreamFailureOutcome,
    /// The human-readable failure message
    pub message: String,
}

impl StreamFailure {
    /// Create a new StreamFailure with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load StreamFailure from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load StreamFailure from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load StreamFailure from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            outcome: value
                .get("outcome")
                .and_then(|v| v.as_str())
                .and_then(|s| StreamFailureOutcome::from_str_opt(s))
                .unwrap_or(StreamFailureOutcome::Determinate),
            message: value
                .get("message")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        Ok(())
    }

    /// Serialize StreamFailure to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert(
            "outcome".to_string(),
            serde_json::Value::String(self.outcome.to_string()),
        );
        result.insert(
            "message".to_string(),
            serde_json::Value::String(self.message.clone()),
        );
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize StreamFailure to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize StreamFailure to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}

// Serde for `StreamFailure` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for StreamFailure {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for StreamFailure {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
