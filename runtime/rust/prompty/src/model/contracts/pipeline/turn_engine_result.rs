// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use super::super::super::context::{LoadContext, SaveContext};

use super::model_invocation_context_snapshot::ModelInvocationContextSnapshot;

use super::model_tool_result::ModelToolResult;

use super::turn_commit::TurnCommit;

/// The result returned by the live turn engine.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct TurnEngineResult {
    /// The committed turn outcome
    pub commit: TurnCommit,
    /// Immutable context snapshots produced during the turn
    pub snapshots: Option<Vec<ModelInvocationContextSnapshot>>,
    /// Normalized tool results produced during the turn
    pub tool_results: Option<Vec<ModelToolResult>>,
    /// A non-fatal post-commit failure; the turn itself remains committed
    pub post_commit_error: Option<String>,
}

impl TurnEngineResult {
    /// Create a new TurnEngineResult with default values.
    pub fn new() -> Self {
        Self::default()
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        let child_path = if path.is_empty() {
            "commit".to_string()
        } else {
            format!("{}.commit", path)
        };
        let child = value
            .get("commit")
            .filter(|candidate| !candidate.is_null())
            .ok_or_else(|| format!("{}: missing required field", child_path))?;
        TurnCommit::validate_input_at(child, &child_path)?;
        if let Some(entries) = value
            .get("snapshots")
            .and_then(|candidate| candidate.as_array())
        {
            let collection_path = if path.is_empty() {
                "snapshots".to_string()
            } else {
                format!("{}.snapshots", path)
            };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                ModelInvocationContextSnapshot::validate_input_at(entry, &entry_path)?;
            }
        }
        if let Some(entries) = value
            .get("toolResults")
            .and_then(|candidate| candidate.as_array())
        {
            let collection_path = if path.is_empty() {
                "toolResults".to_string()
            } else {
                format!("{}.toolResults", path)
            };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                ModelToolResult::validate_input_at(entry, &entry_path)?;
            }
        }
        Ok(())
    }
}
