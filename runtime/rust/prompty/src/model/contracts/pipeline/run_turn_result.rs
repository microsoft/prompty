// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use super::super::super::context::{LoadContext, SaveContext};

use super::super::super::contracts::events::checkpoint::Checkpoint;

use super::super::super::contracts::events::host_tool_result::HostToolResult;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum RunTurnStatus {
    Success,
    Error,
    Cancelled,
}

impl Default for RunTurnStatus {
    fn default() -> Self {
        Self::Success
    }
}

impl std::fmt::Display for RunTurnStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Success => write!(f, "success"),
            Self::Error => write!(f, "error"),
            Self::Cancelled => write!(f, "cancelled"),
        }
    }
}

impl RunTurnStatus {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "success" => Some(Self::Success),
            "error" => Some(Self::Error),
            "cancelled" => Some(Self::Cancelled),
            _ => None,
        }
    }

    pub fn from_str_ignore_case_opt(s: &str) -> Option<Self> {
        if s.eq_ignore_ascii_case("success") {
            return Some(Self::Success);
        }
        if s.eq_ignore_ascii_case("error") {
            return Some(Self::Error);
        }
        if s.eq_ignore_ascii_case("cancelled") {
            return Some(Self::Cancelled);
        }
        None
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Success => "success",
            Self::Error => "error",
            Self::Cancelled => "cancelled",
        }
    }
}

#[cfg(feature = "serde")]
impl serde::Serialize for RunTurnStatus {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.as_str())
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for RunTurnStatus {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let s = <String as serde::Deserialize>::deserialize(deserializer)?;
        Self::from_str_opt(&s)
            .ok_or_else(|| serde::de::Error::custom(format!("invalid RunTurnStatus value: {}", s)))
    }
}

/// Result returned by a reference turn runner implementation.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RunTurnResult {
    /// Stable harness session identifier
    pub session_id: String,
    /// Stable turn identifier within the session
    pub turn_id: String,
    /// Final semantic status for the deterministic turn
    pub status: RunTurnStatus,
    /// Provider-neutral final output returned by the injected model callback
    pub output: Option<serde_json::Value>,
    /// Number of model loop iterations executed
    pub iterations: i32,
    /// Host tool results produced during the turn
    pub tool_results: Option<Vec<HostToolResult>>,
    /// Checkpoints created during the turn
    pub checkpoints: Option<Vec<Checkpoint>>,
}

impl RunTurnResult {
    /// Create a new RunTurnResult with default values.
    pub fn new() -> Self {
        Self::default()
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        if let Some(entries) = value
            .get("toolResults")
            .and_then(|candidate| candidate.as_array())
        {
            let collection_path = if path.is_empty() {
                "toolResults".to_string()
            } else {
                format!("{}.toolResults", path)
            };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                HostToolResult::validate_input_at(entry, &entry_path)?;
            }
        }
        if let Some(entries) = value
            .get("checkpoints")
            .and_then(|candidate| candidate.as_array())
        {
            let collection_path = if path.is_empty() {
                "checkpoints".to_string()
            } else {
                format!("{}.checkpoints", path)
            };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                Checkpoint::validate_input_at(entry, &entry_path)?;
            }
        }
        Ok(())
    }
}
