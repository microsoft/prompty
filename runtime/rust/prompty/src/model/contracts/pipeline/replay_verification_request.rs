// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use super::super::super::context::{LoadContext, SaveContext};

use super::replay_journal_record::ReplayJournalRecord;

/// Request accepted by a replay verifier implementation.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ReplayVerificationRequest {
    /// Expected normalized replay records
    pub expected: Vec<ReplayJournalRecord>,
    /// Actual normalized replay records
    pub actual: Vec<ReplayJournalRecord>,
}

impl ReplayVerificationRequest {
    /// Create a new ReplayVerificationRequest with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ReplayVerificationRequest from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ReplayVerificationRequest from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ReplayVerificationRequest from an already-parsed JSON value, returning an error
    /// instead of panicking on invalid input. Fallible companion to
    /// `load_from_value` with the same validation policy as `from_json`.
    pub fn try_load_from_value(
        value: &serde_json::Value,
        ctx: &LoadContext,
    ) -> Result<Self, serde_json::Error> {
        Self::validate_input_at(value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(value, ctx))
    }

    /// Load ReplayVerificationRequest from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            expected: value
                .get("expected")
                .map(|v| Self::load_expected(v, ctx))
                .unwrap_or_default(),
            actual: value
                .get("actual")
                .map(|v| Self::load_actual(v, ctx))
                .unwrap_or_default(),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        if let Some(entries) = value
            .get("expected")
            .and_then(|candidate| candidate.as_array())
        {
            let collection_path = if path.is_empty() {
                "expected".to_string()
            } else {
                format!("{}.expected", path)
            };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                ReplayJournalRecord::validate_input_at(entry, &entry_path)?;
            }
        }
        if let Some(entries) = value
            .get("actual")
            .and_then(|candidate| candidate.as_array())
        {
            let collection_path = if path.is_empty() {
                "actual".to_string()
            } else {
                format!("{}.actual", path)
            };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                ReplayJournalRecord::validate_input_at(entry, &entry_path)?;
            }
        }
        Ok(())
    }

    /// Serialize ReplayVerificationRequest to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert(
            "expected".to_string(),
            Self::save_expected(&self.expected, ctx),
        );
        result.insert("actual".to_string(), Self::save_actual(&self.actual, ctx));
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ReplayVerificationRequest to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ReplayVerificationRequest to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of ReplayJournalRecord from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_expected(data: &serde_json::Value, ctx: &LoadContext) -> Vec<ReplayJournalRecord> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| ReplayJournalRecord::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of ReplayJournalRecord to a JSON value.
    fn save_expected(items: &[ReplayJournalRecord], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }

    /// Load a collection of ReplayJournalRecord from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_actual(data: &serde_json::Value, ctx: &LoadContext) -> Vec<ReplayJournalRecord> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| ReplayJournalRecord::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of ReplayJournalRecord to a JSON value.
    fn save_actual(items: &[ReplayJournalRecord], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}

// Serde for `ReplayVerificationRequest` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for ReplayVerificationRequest {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for ReplayVerificationRequest {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
