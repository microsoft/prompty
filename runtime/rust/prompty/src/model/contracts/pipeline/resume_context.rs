// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, unexpected_cfgs, clippy::all)]

use super::super::super::context::{LoadContext, SaveContext};

use super::engine_checkpoint::EngineCheckpoint;

/// Input that drives resuming a turn from a durable checkpoint. A host supplies this to restart an interrupted turn without duplicating a committed model or tool effect.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ResumeContext {
    /// Checkpoint to resume from
    pub checkpoint: EngineCheckpoint,
    /// Maximum model loop iterations permitted for the resumed run
    pub max_iterations: i32,
    /// Maximum model attempts permitted per invocation in the resumed run
    pub max_model_attempts: i32,
    /// Last durably persisted journal sequence when the journal tail is ahead of the checkpoint; the resumed run continues numbering after this value. Zero resumes from the checkpoint's own lastSequence.
    pub last_journal_sequence: i64,
    /// Opaque host-specific resume metadata
    pub metadata: serde_json::Value,
}

impl ResumeContext {
    /// Create a new ResumeContext with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ResumeContext from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ResumeContext from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ResumeContext from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            checkpoint: value.get("checkpoint").filter(|v| v.is_object() || v.is_array() || v.is_string()).map(|v| EngineCheckpoint::load_from_value(v, ctx)).unwrap_or_default(),
            max_iterations: value.get("maxIterations").and_then(|v| v.as_i64()).unwrap_or(0) as i32,
            max_model_attempts: value.get("maxModelAttempts").and_then(|v| v.as_i64()).unwrap_or(0) as i32,
            last_journal_sequence: value.get("lastJournalSequence").and_then(|v| v.as_i64()).unwrap_or(0),
            metadata: value.get("metadata").cloned().unwrap_or(serde_json::Value::Null),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        let child_path = if path.is_empty() { "checkpoint".to_string() } else { format!("{}.checkpoint", path) };
        let child = value.get("checkpoint").filter(|candidate| !candidate.is_null())
            .ok_or_else(|| format!("{}: missing required field", child_path))?;
        EngineCheckpoint::validate_input_at(child, &child_path)?;
        Ok(())
    }

    /// Serialize ResumeContext to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        {
            let nested = self.checkpoint.to_value(ctx);
            result.insert("checkpoint".to_string(), nested);
        }
        result.insert("maxIterations".to_string(), serde_json::Value::Number(serde_json::Number::from(self.max_iterations)));
        result.insert("maxModelAttempts".to_string(), serde_json::Value::Number(serde_json::Number::from(self.max_model_attempts)));
        result.insert("lastJournalSequence".to_string(), serde_json::Value::Number(serde_json::Number::from(self.last_journal_sequence)));
        if !self.metadata.is_null() {
            result.insert("metadata".to_string(), self.metadata.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ResumeContext to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ResumeContext to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_metadata_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.metadata.as_object()
    }

}

// Serde for `ResumeContext` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for ResumeContext {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for ResumeContext {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
