// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, unexpected_cfgs, clippy::all)]

use super::super::super::context::{LoadContext, SaveContext};

use super::super::super::contracts::events::host_tool_result::HostToolResult;

use super::turn_options::TurnOptions;

/// Request passed by the reference turn runner to the injected model callback. The runner owns deterministic orchestration semantics; model/provider-specific execution stays behind this callback boundary.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct TurnModelRequest {
    /// Stable harness session identifier
    pub session_id: String,
    /// Stable turn identifier within the session
    pub turn_id: String,
    /// Zero-based model loop iteration
    pub iteration: i32,
    /// Inputs supplied to the deterministic single-turn run
    pub inputs: serde_json::Value,
    /// Canonical turn execution options
    pub options: Option<TurnOptions>,
    /// Host tool results produced by the previous iteration
    pub tool_results: Option<Vec<HostToolResult>>,
}

impl TurnModelRequest {
    /// Create a new TurnModelRequest with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load TurnModelRequest from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnModelRequest from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnModelRequest from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            session_id: value.get("sessionId").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            turn_id: value.get("turnId").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            iteration: value.get("iteration").and_then(|v| v.as_i64()).unwrap_or(0) as i32,
            inputs: value.get("inputs").cloned().unwrap_or(serde_json::Value::Null),
            options: value.get("options").filter(|v| v.is_object() || v.is_array() || v.is_string()).map(|v| TurnOptions::load_from_value(v, ctx)),
            tool_results: value.get("toolResults").map(|v| Self::load_tool_results(v, ctx)),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        let child_path = if path.is_empty() { "options".to_string() } else { format!("{}.options", path) };
        if let Some(child) = value.get("options") {
            TurnOptions::validate_input_at(child, &child_path)?;
        }
        if let Some(entries) = value.get("toolResults").and_then(|candidate| candidate.as_array()) {
            let collection_path = if path.is_empty() { "toolResults".to_string() } else { format!("{}.toolResults", path) };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                HostToolResult::validate_input_at(entry, &entry_path)?;
            }
        }
        Ok(())
    }

    /// Serialize TurnModelRequest to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert("sessionId".to_string(), serde_json::Value::String(self.session_id.clone()));
        result.insert("turnId".to_string(), serde_json::Value::String(self.turn_id.clone()));
        result.insert("iteration".to_string(), serde_json::Value::Number(serde_json::Number::from(self.iteration)));
        if !self.inputs.is_null() {
            result.insert("inputs".to_string(), self.inputs.clone());
        }
        if let Some(val) = self.options.as_ref() {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("options".to_string(), nested);
            }
        }
        if let Some(items) = self.tool_results.as_ref() {
            result.insert("toolResults".to_string(), Self::save_tool_results(items, ctx));
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize TurnModelRequest to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize TurnModelRequest to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_inputs_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.inputs.as_object()
    }


    /// Load a collection of HostToolResult from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_tool_results(data: &serde_json::Value, ctx: &LoadContext) -> Vec<HostToolResult> {
        match data {
            serde_json::Value::Array(arr) => {
                arr.iter().map(|v| HostToolResult::load_from_value(v, ctx)).collect()
            }

            _ => Vec::new(),

        }
    }

    /// Save a collection of HostToolResult to a JSON value.
    fn save_tool_results(items: &[HostToolResult], ctx: &SaveContext) -> serde_json::Value {

        serde_json::Value::Array(items.iter().map(|item| item.to_value(ctx)).collect::<Vec<_>>())

    }
}

// Serde for `TurnModelRequest` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for TurnModelRequest {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for TurnModelRequest {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
