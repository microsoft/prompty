// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use super::super::context::{LoadContext, SaveContext};

/// Payload for "retry" events — a transient operation will be retried.
#[derive(Debug, Clone, Default)]
pub struct RetryPayload {
    /// Operation being retried
    pub operation: String,
    /// Attempt number about to run
    pub attempt: i32,
    /// Maximum configured attempts
    pub max_attempts: Option<i32>,
    /// Backoff delay before the next attempt in milliseconds
    pub delay_ms: Option<f64>,
    /// Reason for the retry
    pub reason: Option<String>,
}

impl RetryPayload {
    /// Create a new RetryPayload with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load RetryPayload from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load RetryPayload from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load RetryPayload from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            operation: value.get("operation").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            attempt: value.get("attempt").and_then(|v| v.as_i64()).unwrap_or(0) as i32,
            max_attempts: value.get("maxAttempts").and_then(|v| v.as_i64()).map(|v| v as i32),
            delay_ms: value.get("delayMs").and_then(|v| v.as_f64()),
            reason: value.get("reason").and_then(|v| v.as_str()).map(|s| s.to_string()),
        }
    }

    /// Serialize RetryPayload to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.operation.is_empty() {
            result.insert("operation".to_string(), serde_json::Value::String(self.operation.clone()));
        }
        if self.attempt != 0 {
            result.insert("attempt".to_string(), serde_json::Value::Number(serde_json::Number::from(self.attempt)));
        }
        if let Some(val) = self.max_attempts {
            result.insert("maxAttempts".to_string(), serde_json::Value::Number(serde_json::Number::from(val)));
        }
        if let Some(val) = self.delay_ms {
            result.insert("delayMs".to_string(), serde_json::Number::from_f64(val as f64).map(serde_json::Value::Number).unwrap_or(serde_json::Value::Null));
        }
        if let Some(ref val) = self.reason {
            result.insert("reason".to_string(), serde_json::Value::String(val.clone()));
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize RetryPayload to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize RetryPayload to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}
