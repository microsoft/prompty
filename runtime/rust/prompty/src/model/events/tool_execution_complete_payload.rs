// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::redaction_metadata::RedactionMetadata;

/// Payload for "tool_execution_complete" events — a concrete host tool execution finished.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ToolExecutionCompletePayload {
    /// Stable host execution request identifier
    pub request_id: Option<String>,
    /// Associated model tool call identifier, when available
    pub tool_call_id: Option<String>,
    /// Name of the host tool that executed
    pub tool_name: String,
    /// Whether the host execution completed successfully
    pub success: bool,
    /// Host-normalized execution result
    pub result: Option<serde_json::Value>,
    /// Process or host exit code, when applicable
    pub exit_code: Option<i32>,
    /// Tool execution duration in milliseconds
    pub duration_ms: Option<f64>,
    /// Machine-readable error category when success is false
    pub error_kind: Option<String>,
    /// Host-specific telemetry for the execution
    pub telemetry: serde_json::Value,
    /// Redaction state for sensitive result fields
    pub redaction: Option<RedactionMetadata>,
}

impl ToolExecutionCompletePayload {
    /// Create a new ToolExecutionCompletePayload with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ToolExecutionCompletePayload from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ToolExecutionCompletePayload from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ToolExecutionCompletePayload from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            request_id: value
                .get("requestId")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            tool_call_id: value
                .get("toolCallId")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            tool_name: value
                .get("toolName")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            success: value
                .get("success")
                .and_then(|v| v.as_bool())
                .unwrap_or(false),
            result: value.get("result").cloned(),
            exit_code: value
                .get("exitCode")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
            duration_ms: value.get("durationMs").and_then(|v| v.as_f64()),
            error_kind: value
                .get("errorKind")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            telemetry: value
                .get("telemetry")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
            redaction: value
                .get("redaction")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| RedactionMetadata::load_from_value(v, ctx)),
        }
    }

    /// Serialize ToolExecutionCompletePayload to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(ref val) = self.request_id {
            result.insert(
                "requestId".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if let Some(ref val) = self.tool_call_id {
            result.insert(
                "toolCallId".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if !self.tool_name.is_empty() {
            result.insert(
                "toolName".to_string(),
                serde_json::Value::String(self.tool_name.clone()),
            );
        }
        result.insert("success".to_string(), serde_json::Value::Bool(self.success));
        if let Some(ref val) = self.result {
            result.insert("result".to_string(), val.clone());
        }
        if let Some(val) = self.exit_code {
            result.insert(
                "exitCode".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if let Some(val) = self.duration_ms {
            result.insert(
                "durationMs".to_string(),
                serde_json::Number::from_f64(val as f64)
                    .map(serde_json::Value::Number)
                    .unwrap_or(serde_json::Value::Null),
            );
        }
        if let Some(ref val) = self.error_kind {
            result.insert(
                "errorKind".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if !self.telemetry.is_null() {
            result.insert("telemetry".to_string(), self.telemetry.clone());
        }
        if let Some(ref val) = self.redaction {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("redaction".to_string(), nested);
            }
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ToolExecutionCompletePayload to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ToolExecutionCompletePayload to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_telemetry_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.telemetry.as_object()
    }
}

// Serde for `ToolExecutionCompletePayload` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
impl serde::Serialize for ToolExecutionCompletePayload {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

impl<'de> serde::Deserialize<'de> for ToolExecutionCompletePayload {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
