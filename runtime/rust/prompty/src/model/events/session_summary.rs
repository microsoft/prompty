// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use super::super::context::{LoadContext, SaveContext};

use super::super::model::token_usage::TokenUsage;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum SessionSummaryStatus {
    Success,
    Error,
    Cancelled,
    Interrupted,
}

impl Default for SessionSummaryStatus {
    fn default() -> Self {
        Self::Success
    }
}

impl std::fmt::Display for SessionSummaryStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Success => write!(f, "success"),
            Self::Error => write!(f, "error"),
            Self::Cancelled => write!(f, "cancelled"),
            Self::Interrupted => write!(f, "interrupted"),
        }
    }
}

impl SessionSummaryStatus {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "success" => Some(Self::Success),
            "error" => Some(Self::Error),
            "cancelled" => Some(Self::Cancelled),
            "interrupted" => Some(Self::Interrupted),
            _ => None,
        }
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Success => "success",
            Self::Error => "error",
            Self::Cancelled => "cancelled",
            Self::Interrupted => "interrupted",
        }
    }
}

/// Summary statistics for a completed session trace.
#[derive(Debug, Clone, Default)]
pub struct SessionSummary {
    /// Stable session identifier
    pub session_id: String,
    /// Final session status
    pub status: Option<SessionSummaryStatus>,
    /// Number of user/assistant turns in the session
    pub turns: Option<i32>,
    /// Number of checkpoints created
    pub checkpoints: Option<i32>,
    /// Aggregated token usage for the session
    pub usage: Option<TokenUsage>,
    /// Total elapsed session duration in milliseconds
    pub duration_ms: Option<f64>,
}

impl SessionSummary {
    /// Create a new SessionSummary with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load SessionSummary from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load SessionSummary from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load SessionSummary from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            session_id: value.get("sessionId").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            status: value.get("status").and_then(|v| v.as_str()).and_then(|s| SessionSummaryStatus::from_str_opt(s)),
            turns: value.get("turns").and_then(|v| v.as_i64()).map(|v| v as i32),
            checkpoints: value.get("checkpoints").and_then(|v| v.as_i64()).map(|v| v as i32),
            usage: value.get("usage").filter(|v| v.is_object() || v.is_array() || v.is_string()).map(|v| TokenUsage::load_from_value(v, ctx)),
            duration_ms: value.get("durationMs").and_then(|v| v.as_f64()),
        }
    }

    /// Serialize SessionSummary to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.session_id.is_empty() {
            result.insert("sessionId".to_string(), serde_json::Value::String(self.session_id.clone()));
        }
        if let Some(ref val) = self.status {
            result.insert("status".to_string(), serde_json::Value::String(val.to_string()));
        }
        if let Some(val) = self.turns {
            result.insert("turns".to_string(), serde_json::Value::Number(serde_json::Number::from(val)));
        }
        if let Some(val) = self.checkpoints {
            result.insert("checkpoints".to_string(), serde_json::Value::Number(serde_json::Number::from(val)));
        }
        if let Some(ref val) = self.usage {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("usage".to_string(), nested);
            }
        }
        if let Some(val) = self.duration_ms {
            result.insert("durationMs".to_string(), serde_json::Number::from_f64(val as f64).map(serde_json::Value::Number).unwrap_or(serde_json::Value::Null));
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize SessionSummary to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize SessionSummary to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}
