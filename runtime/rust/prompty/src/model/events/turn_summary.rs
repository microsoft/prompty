// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::super::model::token_usage::TokenUsage;

/// Summary statistics for a completed turn trace.
#[derive(Debug, Clone, Default)]
pub struct TurnSummary {
    /// Stable identifier for the outer turn
    pub turn_id: String,
    /// Final turn status: 'success', 'error', or 'cancelled'
    pub status: String,
    /// Number of agent-loop iterations
    pub iterations: i32,
    /// Number of LLM calls made during the turn
    pub llm_calls: Option<i32>,
    /// Number of tool calls dispatched during the turn
    pub tool_calls: Option<i32>,
    /// Number of retry events during the turn
    pub retries: Option<i32>,
    /// Aggregated token usage for the turn
    pub usage: Option<TokenUsage>,
    /// Total elapsed turn duration in milliseconds
    pub duration_ms: Option<f64>,
}

impl TurnSummary {
    /// Create a new TurnSummary with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load TurnSummary from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnSummary from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnSummary from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            turn_id: value
                .get("turnId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            status: value
                .get("status")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            iterations: value
                .get("iterations")
                .and_then(|v| v.as_i64())
                .unwrap_or(0) as i32,
            llm_calls: value
                .get("llmCalls")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
            tool_calls: value
                .get("toolCalls")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
            retries: value
                .get("retries")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
            usage: value
                .get("usage")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| TokenUsage::load_from_value(v, ctx)),
            duration_ms: value.get("durationMs").and_then(|v| v.as_f64()),
        }
    }

    /// Serialize TurnSummary to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.turn_id.is_empty() {
            result.insert(
                "turnId".to_string(),
                serde_json::Value::String(self.turn_id.clone()),
            );
        }
        if !self.status.is_empty() {
            result.insert(
                "status".to_string(),
                serde_json::Value::String(self.status.clone()),
            );
        }
        if self.iterations != 0 {
            result.insert(
                "iterations".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.iterations)),
            );
        }
        if let Some(val) = self.llm_calls {
            result.insert(
                "llmCalls".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if let Some(val) = self.tool_calls {
            result.insert(
                "toolCalls".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if let Some(val) = self.retries {
            result.insert(
                "retries".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if let Some(ref val) = self.usage {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("usage".to_string(), nested);
            }
        }
        if let Some(val) = self.duration_ms {
            result.insert(
                "durationMs".to_string(),
                serde_json::Number::from_f64(val as f64)
                    .map(serde_json::Value::Number)
                    .unwrap_or(serde_json::Value::Null),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize TurnSummary to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize TurnSummary to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}
