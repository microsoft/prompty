// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::super::conversation::message::Message;

/// Payload for "messages_updated" events — the conversation state has changed.
#[derive(Debug, Clone, Default)]
pub struct MessagesUpdatedPayload {
    /// The current full message list after the update
    pub messages: Vec<Message>,
    /// Why the message list changed
    pub reason: Option<String>,
    /// Messages appended by this update, when available
    pub appended: Vec<Message>,
    /// Number of messages removed by this update, when available
    pub removed: Option<i32>,
}

impl MessagesUpdatedPayload {
    /// Create a new MessagesUpdatedPayload with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load MessagesUpdatedPayload from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load MessagesUpdatedPayload from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load MessagesUpdatedPayload from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            messages: value
                .get("messages")
                .map(|v| Self::load_messages(v, ctx))
                .unwrap_or_default(),
            reason: value
                .get("reason")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            appended: value
                .get("appended")
                .map(|v| Self::load_appended(v, ctx))
                .unwrap_or_default(),
            removed: value
                .get("removed")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
        }
    }

    /// Serialize MessagesUpdatedPayload to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.messages.is_empty() {
            result.insert(
                "messages".to_string(),
                Self::save_messages(&self.messages, ctx),
            );
        }
        if let Some(ref val) = self.reason {
            result.insert("reason".to_string(), serde_json::Value::String(val.clone()));
        }
        if !self.appended.is_empty() {
            result.insert(
                "appended".to_string(),
                Self::save_appended(&self.appended, ctx),
            );
        }
        if let Some(val) = self.removed {
            result.insert(
                "removed".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize MessagesUpdatedPayload to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize MessagesUpdatedPayload to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of Message from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_messages(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Message> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| Message::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of Message to a JSON value.
    fn save_messages(items: &[Message], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }

    /// Load a collection of Message from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_appended(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Message> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| Message::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of Message to a JSON value.
    fn save_appended(items: &[Message], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}
