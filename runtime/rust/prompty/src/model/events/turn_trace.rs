// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::turn_event::TurnEvent;

use super::turn_summary::TurnSummary;

/// Portable JSONL/replay container for a recorded turn harness run.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct TurnTrace {
    /// Trace schema version
    pub version: String,
    /// Runtime name that produced the trace
    pub runtime: Option<String>,
    /// Prompty library version that produced the trace
    pub prompty_version: Option<String>,
    /// Recorded turn events in emission order
    pub events: Vec<TurnEvent>,
    /// Optional summary computed from the event stream
    pub summary: Option<TurnSummary>,
}

impl TurnTrace {
    /// Create a new TurnTrace with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load TurnTrace from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnTrace from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnTrace from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            version: value
                .get("version")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            runtime: value
                .get("runtime")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            prompty_version: value
                .get("promptyVersion")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            events: value
                .get("events")
                .map(|v| Self::load_events(v, ctx))
                .unwrap_or_default(),
            summary: value
                .get("summary")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| TurnSummary::load_from_value(v, ctx)),
        }
    }

    /// Serialize TurnTrace to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.version.is_empty() {
            result.insert(
                "version".to_string(),
                serde_json::Value::String(self.version.clone()),
            );
        }
        if let Some(ref val) = self.runtime {
            result.insert(
                "runtime".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if let Some(ref val) = self.prompty_version {
            result.insert(
                "promptyVersion".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if !self.events.is_empty() {
            result.insert("events".to_string(), Self::save_events(&self.events, ctx));
        }
        if let Some(ref val) = self.summary {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("summary".to_string(), nested);
            }
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize TurnTrace to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize TurnTrace to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of TurnEvent from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_events(data: &serde_json::Value, ctx: &LoadContext) -> Vec<TurnEvent> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| TurnEvent::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of TurnEvent to a JSON value.
    fn save_events(items: &[TurnEvent], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}

// Serde for `TurnTrace` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
impl serde::Serialize for TurnTrace {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

impl<'de> serde::Deserialize<'de> for TurnTrace {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
