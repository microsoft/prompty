// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

/// A file observed or touched by a harness session.
#[derive(Debug, Clone, Default)]
pub struct SessionFileRef {
    /// Stable session identifier
    pub session_id: Option<String>,
    /// File path, relative to the harness workspace when possible
    pub path: String,
    /// Tool that first observed the file, when known
    pub tool_name: Option<String>,
    /// Zero-based turn index where the file was first observed
    pub turn_index: Option<i32>,
    /// ISO 8601 UTC timestamp when the file was first observed
    pub first_seen_at: Option<String>,
}

impl SessionFileRef {
    /// Create a new SessionFileRef with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load SessionFileRef from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load SessionFileRef from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load SessionFileRef from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            session_id: value
                .get("sessionId")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            path: value
                .get("path")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            tool_name: value
                .get("toolName")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            turn_index: value
                .get("turnIndex")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
            first_seen_at: value
                .get("firstSeenAt")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
        }
    }

    /// Serialize SessionFileRef to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(ref val) = self.session_id {
            result.insert(
                "sessionId".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if !self.path.is_empty() {
            result.insert(
                "path".to_string(),
                serde_json::Value::String(self.path.clone()),
            );
        }
        if let Some(ref val) = self.tool_name {
            result.insert(
                "toolName".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if let Some(val) = self.turn_index {
            result.insert(
                "turnIndex".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if let Some(ref val) = self.first_seen_at {
            result.insert(
                "firstSeenAt".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize SessionFileRef to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize SessionFileRef to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}
