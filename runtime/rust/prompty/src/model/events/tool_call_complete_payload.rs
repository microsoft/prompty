// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::super::conversation::tool_result::ToolResult;

/// Payload for "tool_call_complete" events — a tool dispatch finished.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ToolCallCompletePayload {
    /// The unique identifier of the tool call
    pub id: Option<String>,
    /// The name of the tool that completed
    pub name: String,
    /// Whether the tool dispatch succeeded semantically
    pub success: bool,
    /// Normalized tool result
    pub result: Option<ToolResult>,
    /// Tool execution duration in milliseconds
    pub duration_ms: Option<f64>,
    /// Machine-readable error category when success is false
    pub error_kind: Option<String>,
}

impl ToolCallCompletePayload {
    /// Create a new ToolCallCompletePayload with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ToolCallCompletePayload from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ToolCallCompletePayload from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ToolCallCompletePayload from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            id: value
                .get("id")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            name: value
                .get("name")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            success: value
                .get("success")
                .and_then(|v| v.as_bool())
                .unwrap_or(false),
            result: value
                .get("result")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| ToolResult::load_from_value(v, ctx)),
            duration_ms: value.get("durationMs").and_then(|v| v.as_f64()),
            error_kind: value
                .get("errorKind")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        let child_path = if path.is_empty() {
            "result".to_string()
        } else {
            format!("{}.result", path)
        };
        if let Some(child) = value.get("result") {
            ToolResult::validate_input_at(child, &child_path)?;
        }
        Ok(())
    }

    /// Serialize ToolCallCompletePayload to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(val) = self.id.as_ref() {
            result.insert("id".to_string(), serde_json::Value::String(val.clone()));
        }
        if !self.name.is_empty() {
            result.insert(
                "name".to_string(),
                serde_json::Value::String(self.name.clone()),
            );
        }
        result.insert("success".to_string(), serde_json::Value::Bool(self.success));
        if let Some(val) = self.result.as_ref() {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("result".to_string(), nested);
            }
        }
        if let Some(val) = self.duration_ms.as_ref() {
            result.insert(
                "durationMs".to_string(),
                serde_json::Number::from_f64(*val as f64)
                    .map(serde_json::Value::Number)
                    .unwrap_or(serde_json::Value::Null),
            );
        }
        if let Some(val) = self.error_kind.as_ref() {
            result.insert(
                "errorKind".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ToolCallCompletePayload to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ToolCallCompletePayload to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}

// Serde for `ToolCallCompletePayload` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
impl serde::Serialize for ToolCallCompletePayload {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

impl<'de> serde::Deserialize<'de> for ToolCallCompletePayload {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
