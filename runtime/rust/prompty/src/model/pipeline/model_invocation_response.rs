// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::invocation_context_state::InvocationContextState;

use super::super::model::invocation_usage::InvocationUsage;

use super::super::conversation::message::Message;

use super::model_tool_request::ModelToolRequest;

/// Provider-neutral result of a single model invocation. This is the live provider boundary. It is intentionally distinct from TurnModelResponse, which is the deterministic reference-runner callback.
#[derive(Debug, Clone, Default)]
pub struct ModelInvocationResponse {
    /// Provider-neutral final output when no additional tool calls are requested
    pub output: Option<serde_json::Value>,
    /// Complete cumulative token usage for this completed invocation, when available
    pub usage: Option<InvocationUsage>,
    /// Assistant messages to commit before tool results are made visible
    pub assistant_messages: Vec<Message>,
    /// Tool requests returned by the provider
    pub tool_requests: Vec<ModelToolRequest>,
    /// Provider-context state to carry into the next invocation
    pub next_context_state: Option<InvocationContextState>,
    /// Opaque provider-specific response metadata
    pub metadata: serde_json::Value,
}

impl ModelInvocationResponse {
    /// Create a new ModelInvocationResponse with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ModelInvocationResponse from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelInvocationResponse from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelInvocationResponse from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            output: value.get("output").cloned(),
            usage: value
                .get("usage")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| InvocationUsage::load_from_value(v, ctx)),
            assistant_messages: value
                .get("assistantMessages")
                .map(|v| Self::load_assistant_messages(v, ctx))
                .unwrap_or_default(),
            tool_requests: value
                .get("toolRequests")
                .map(|v| Self::load_tool_requests(v, ctx))
                .unwrap_or_default(),
            next_context_state: value
                .get("nextContextState")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| InvocationContextState::load_from_value(v, ctx)),
            metadata: value
                .get("metadata")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize ModelInvocationResponse to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(ref val) = self.output {
            result.insert("output".to_string(), val.clone());
        }
        if let Some(ref val) = self.usage {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("usage".to_string(), nested);
            }
        }
        if !self.assistant_messages.is_empty() {
            result.insert(
                "assistantMessages".to_string(),
                Self::save_assistant_messages(&self.assistant_messages, ctx),
            );
        }
        if !self.tool_requests.is_empty() {
            result.insert(
                "toolRequests".to_string(),
                Self::save_tool_requests(&self.tool_requests, ctx),
            );
        }
        if let Some(ref val) = self.next_context_state {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("nextContextState".to_string(), nested);
            }
        }
        if !self.metadata.is_null() {
            result.insert("metadata".to_string(), self.metadata.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ModelInvocationResponse to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ModelInvocationResponse to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_metadata_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.metadata.as_object()
    }

    /// Load a collection of Message from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_assistant_messages(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Message> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| Message::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of Message to a JSON value.
    fn save_assistant_messages(items: &[Message], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }

    /// Load a collection of ModelToolRequest from a JSON value.
    /// Handles both array format `[{...}]` and dict format `{"name": {...}}`.
    fn load_tool_requests(data: &serde_json::Value, ctx: &LoadContext) -> Vec<ModelToolRequest> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| ModelToolRequest::load_from_value(v, ctx))
                .collect(),

            serde_json::Value::Object(obj) => obj
                .iter()
                .filter_map(|(name, value)| {
                    if value.is_array() {
                        return None;
                    }
                    let mut v = if value.is_object() {
                        value.clone()
                    } else {
                        serde_json::json!({ "id": value })
                    };
                    if let serde_json::Value::Object(ref mut m) = v {
                        m.entry("name".to_string())
                            .or_insert_with(|| serde_json::Value::String(name.clone()));
                    }
                    Some(ModelToolRequest::load_from_value(&v, ctx))
                })
                .collect(),
            _ => Vec::new(),
        }
    }

    /// Save a collection of ModelToolRequest to a JSON value.
    fn save_tool_requests(items: &[ModelToolRequest], ctx: &SaveContext) -> serde_json::Value {
        if ctx.collection_format == "array" {
            return serde_json::Value::Array(
                items
                    .iter()
                    .map(|item| item.to_value(ctx))
                    .collect::<Vec<_>>(),
            );
        }
        // Object format: use name as key
        let mut result = serde_json::Map::new();
        for item in items {
            let mut item_data = match item.to_value(ctx) {
                serde_json::Value::Object(m) => m,
                other => {
                    let mut m = serde_json::Map::new();
                    m.insert("value".to_string(), other);
                    m
                }
            };
            if let Some(serde_json::Value::String(name)) = item_data.remove("name") {
                result.insert(name, serde_json::Value::Object(item_data));
            }
        }
        serde_json::Value::Object(result)
    }
}
