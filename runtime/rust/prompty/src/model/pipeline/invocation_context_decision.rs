// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum InvocationContextDisposition {
    Included,
    Excluded,
}

impl Default for InvocationContextDisposition {
    fn default() -> Self {
        Self::Included
    }
}

impl std::fmt::Display for InvocationContextDisposition {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Included => write!(f, "included"),
            Self::Excluded => write!(f, "excluded"),
        }
    }
}

impl InvocationContextDisposition {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "included" => Some(Self::Included),
            "excluded" => Some(Self::Excluded),
            _ => None,
        }
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Included => "included",
            Self::Excluded => "excluded",
        }
    }
}

/// An auditable decision made while preparing model-visible context.
#[derive(Debug, Clone, Default)]
pub struct InvocationContextDecision {
    /// Stable identifier of the evaluated context candidate
    pub candidate_id: String,
    /// Whether the candidate was included in the invocation context
    pub disposition: InvocationContextDisposition,
    /// Human-readable reason for the disposition
    pub reason: String,
    /// Zero-based rank among included candidates, when applicable
    pub rank: Option<i32>,
    /// Estimated token count for the candidate, when available
    pub estimated_tokens: Option<i32>,
    /// Opaque host-specific decision metadata
    pub metadata: serde_json::Value,
}

impl InvocationContextDecision {
    /// Create a new InvocationContextDecision with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load InvocationContextDecision from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load InvocationContextDecision from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load InvocationContextDecision from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            candidate_id: value
                .get("candidateId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            disposition: value
                .get("disposition")
                .and_then(|v| v.as_str())
                .and_then(|s| InvocationContextDisposition::from_str_opt(s))
                .unwrap_or(InvocationContextDisposition::Included),
            reason: value
                .get("reason")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            rank: value.get("rank").and_then(|v| v.as_i64()).map(|v| v as i32),
            estimated_tokens: value
                .get("estimatedTokens")
                .and_then(|v| v.as_i64())
                .map(|v| v as i32),
            metadata: value
                .get("metadata")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize InvocationContextDecision to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.candidate_id.is_empty() {
            result.insert(
                "candidateId".to_string(),
                serde_json::Value::String(self.candidate_id.clone()),
            );
        }
        result.insert(
            "disposition".to_string(),
            serde_json::Value::String(self.disposition.to_string()),
        );
        if !self.reason.is_empty() {
            result.insert(
                "reason".to_string(),
                serde_json::Value::String(self.reason.clone()),
            );
        }
        if let Some(val) = self.rank {
            result.insert(
                "rank".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if let Some(val) = self.estimated_tokens {
            result.insert(
                "estimatedTokens".to_string(),
                serde_json::Value::Number(serde_json::Number::from(val)),
            );
        }
        if !self.metadata.is_null() {
            result.insert("metadata".to_string(), self.metadata.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize InvocationContextDecision to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize InvocationContextDecision to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_metadata_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.metadata.as_object()
    }
}
