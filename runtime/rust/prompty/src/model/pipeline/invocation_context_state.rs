// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::delegated_state_reference::DelegatedStateReference;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum InvocationContextPortability {
    Portable,
    Delegated,
    Opaque,
}

impl Default for InvocationContextPortability {
    fn default() -> Self {
        Self::Portable
    }
}

impl std::fmt::Display for InvocationContextPortability {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Portable => write!(f, "portable"),
            Self::Delegated => write!(f, "delegated"),
            Self::Opaque => write!(f, "opaque"),
        }
    }
}

impl InvocationContextPortability {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "portable" => Some(Self::Portable),
            "delegated" => Some(Self::Delegated),
            "opaque" => Some(Self::Opaque),
            _ => None,
        }
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Portable => "portable",
            Self::Delegated => "delegated",
            Self::Opaque => "opaque",
        }
    }
}

/// Provider-context state carried into or out of an invocation.
#[derive(Debug, Clone, Default)]
pub struct InvocationContextState {
    /// Whether the model-visible context is portable across providers
    pub portability: InvocationContextPortability,
    /// Explicit references to provider-held context state
    pub delegated_state: Vec<DelegatedStateReference>,
}

impl InvocationContextState {
    /// Create a new InvocationContextState with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load InvocationContextState from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load InvocationContextState from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load InvocationContextState from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            portability: value
                .get("portability")
                .and_then(|v| v.as_str())
                .and_then(|s| InvocationContextPortability::from_str_opt(s))
                .unwrap_or(InvocationContextPortability::Portable),
            delegated_state: value
                .get("delegatedState")
                .map(|v| Self::load_delegated_state(v, ctx))
                .unwrap_or_default(),
        }
    }

    /// Serialize InvocationContextState to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert(
            "portability".to_string(),
            serde_json::Value::String(self.portability.to_string()),
        );
        if !self.delegated_state.is_empty() {
            result.insert(
                "delegatedState".to_string(),
                Self::save_delegated_state(&self.delegated_state, ctx),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize InvocationContextState to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize InvocationContextState to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of DelegatedStateReference from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_delegated_state(
        data: &serde_json::Value,
        ctx: &LoadContext,
    ) -> Vec<DelegatedStateReference> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| DelegatedStateReference::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of DelegatedStateReference to a JSON value.
    fn save_delegated_state(
        items: &[DelegatedStateReference],
        ctx: &SaveContext,
    ) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}
