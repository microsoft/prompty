// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use super::super::context::{LoadContext, SaveContext};

/// Configuration for context window compaction. When the message history exceeds the context budget, the compaction strategy is applied to reduce the message list while preserving essential information.
#[derive(Debug, Clone, Default)]
pub struct CompactionConfig {
    /// The compaction strategy identifier. Built-in strategies include 'summarize'. Can also be a path to a .prompty file used as the summarization prompt.
    pub strategy: Option<String>,
    /// Character budget for the compacted context. Overrides TurnOptions.contextBudget when set.
    pub budget: Option<i32>,
    /// Additional strategy-specific options
    pub options: serde_json::Value,
}

impl CompactionConfig {
    /// Create a new CompactionConfig with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load CompactionConfig from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load CompactionConfig from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load CompactionConfig from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            strategy: value.get("strategy").and_then(|v| v.as_str()).map(|s| s.to_string()),
            budget: value.get("budget").and_then(|v| v.as_i64()).map(|v| v as i32),
            options: value.get("options").cloned().unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize CompactionConfig to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(ref val) = self.strategy {
            result.insert("strategy".to_string(), serde_json::Value::String(val.clone()));
        }
        if let Some(val) = self.budget {
            result.insert("budget".to_string(), serde_json::Value::Number(serde_json::Number::from(val)));
        }
        if !self.options.is_null() {
            result.insert("options".to_string(), self.options.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize CompactionConfig to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize CompactionConfig to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_options_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.options.as_object()
    }

}
