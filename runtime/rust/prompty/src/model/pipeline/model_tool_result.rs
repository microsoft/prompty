// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum ModelToolOutcome {
    Success,
    Failed,
    Indeterminate,
}

impl Default for ModelToolOutcome {
    fn default() -> Self {
        Self::Success
    }
}

impl std::fmt::Display for ModelToolOutcome {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Success => write!(f, "success"),
            Self::Failed => write!(f, "failed"),
            Self::Indeterminate => write!(f, "indeterminate"),
        }
    }
}

impl ModelToolOutcome {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "success" => Some(Self::Success),
            "failed" => Some(Self::Failed),
            "indeterminate" => Some(Self::Indeterminate),
            _ => None,
        }
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Success => "success",
            Self::Failed => "failed",
            Self::Indeterminate => "indeterminate",
        }
    }
}

/// Normalized result of executing one model-requested tool.
#[derive(Debug, Clone, Default)]
pub struct ModelToolResult {
    /// Identifier of the corresponding model tool request
    pub request_id: String,
    /// Name of the executed tool
    pub name: String,
    /// Semantic outcome of the external tool effect
    pub outcome: ModelToolOutcome,
    /// JSON-shaped tool output when available
    pub output: Option<serde_json::Value>,
    /// Stable error category for failed or indeterminate results
    pub error_kind: Option<String>,
    /// Opaque host-specific tool result metadata
    pub metadata: serde_json::Value,
}

impl ModelToolResult {
    /// Create a new ModelToolResult with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ModelToolResult from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelToolResult from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelToolResult from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            request_id: value
                .get("requestId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            name: value
                .get("name")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            outcome: value
                .get("outcome")
                .and_then(|v| v.as_str())
                .and_then(|s| ModelToolOutcome::from_str_opt(s))
                .unwrap_or(ModelToolOutcome::Success),
            output: value.get("output").cloned(),
            error_kind: value
                .get("errorKind")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            metadata: value
                .get("metadata")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize ModelToolResult to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.request_id.is_empty() {
            result.insert(
                "requestId".to_string(),
                serde_json::Value::String(self.request_id.clone()),
            );
        }
        if !self.name.is_empty() {
            result.insert(
                "name".to_string(),
                serde_json::Value::String(self.name.clone()),
            );
        }
        result.insert(
            "outcome".to_string(),
            serde_json::Value::String(self.outcome.to_string()),
        );
        if let Some(ref val) = self.output {
            result.insert("output".to_string(), val.clone());
        }
        if let Some(ref val) = self.error_kind {
            result.insert(
                "errorKind".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        if !self.metadata.is_null() {
            result.insert("metadata".to_string(), self.metadata.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ModelToolResult to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ModelToolResult to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_metadata_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.metadata.as_object()
    }
}
