// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::model_invocation_request::ModelInvocationRequest;

/// Durable state required to reconcile one indeterminate model invocation. Retained when a model effect completed with an unknown outcome so a resumed run can determine success or failure without re-invoking the provider.
#[derive(Debug, Clone, Default)]
pub struct ModelReconciliationState {
    /// Model invocation identifier awaiting reconciliation
    pub invocation_id: String,
    /// The request whose outcome must be reconciled
    pub request: ModelInvocationRequest,
    /// Zero-based attempt number that failed with an unknown outcome
    pub failed_attempt: i32,
    /// Human-readable reason the outcome is indeterminate
    pub message: String,
    /// Opaque host-specific reconciliation metadata
    pub metadata: serde_json::Value,
}

impl ModelReconciliationState {
    /// Create a new ModelReconciliationState with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ModelReconciliationState from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelReconciliationState from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelReconciliationState from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            invocation_id: value
                .get("invocationId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            request: value
                .get("request")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| ModelInvocationRequest::load_from_value(v, ctx))
                .unwrap_or_default(),
            failed_attempt: value
                .get("failedAttempt")
                .and_then(|v| v.as_i64())
                .unwrap_or(0) as i32,
            message: value
                .get("message")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            metadata: value
                .get("metadata")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize ModelReconciliationState to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.invocation_id.is_empty() {
            result.insert(
                "invocationId".to_string(),
                serde_json::Value::String(self.invocation_id.clone()),
            );
        }
        {
            let nested = self.request.to_value(ctx);
            if !nested.is_null() {
                result.insert("request".to_string(), nested);
            }
        }
        if self.failed_attempt != 0 {
            result.insert(
                "failedAttempt".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.failed_attempt)),
            );
        }
        if !self.message.is_empty() {
            result.insert(
                "message".to_string(),
                serde_json::Value::String(self.message.clone()),
            );
        }
        if !self.metadata.is_null() {
            result.insert("metadata".to_string(), self.metadata.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ModelReconciliationState to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ModelReconciliationState to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_metadata_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.metadata.as_object()
    }
}
