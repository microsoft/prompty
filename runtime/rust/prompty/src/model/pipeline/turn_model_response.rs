// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, unexpected_cfgs, clippy::all)]

use super::super::context::{LoadContext, SaveContext};

use super::super::events::host_tool_request::HostToolRequest;

use super::super::model::invocation_usage::InvocationUsage;

/// Response returned by the injected model callback to the reference turn runner.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct TurnModelResponse {
    /// Provider-neutral final model output for the turn when no more tools are requested
    pub output: Option<serde_json::Value>,
    /// Complete cumulative token usage reported for this model invocation, when available
    pub usage: Option<InvocationUsage>,
    /// Host tool execution requests emitted by the model callback
    pub tool_requests: Option<Vec<HostToolRequest>>,
    /// Additional deterministic state to merge into the iteration checkpoint
    pub checkpoint_state: serde_json::Value,
}

impl TurnModelResponse {
    /// Create a new TurnModelResponse with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load TurnModelResponse from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnModelResponse from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnModelResponse from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            output: value.get("output").cloned(),
            usage: value.get("usage").filter(|v| v.is_object() || v.is_array() || v.is_string()).map(|v| InvocationUsage::load_from_value(v, ctx)),
            tool_requests: value.get("toolRequests").map(|v| Self::load_tool_requests(v, ctx)),
            checkpoint_state: value.get("checkpointState").cloned().unwrap_or(serde_json::Value::Null),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        let child_path = if path.is_empty() { "usage".to_string() } else { format!("{}.usage", path) };
        if let Some(child) = value.get("usage") {
            InvocationUsage::validate_input_at(child, &child_path)?;
        }
        if let Some(entries) = value.get("toolRequests").and_then(|candidate| candidate.as_array()) {
            let collection_path = if path.is_empty() { "toolRequests".to_string() } else { format!("{}.toolRequests", path) };
            for (index, entry) in entries.iter().enumerate() {
                let entry_path = format!("{}[{}]", collection_path, index);
                HostToolRequest::validate_input_at(entry, &entry_path)?;
            }
        }
        Ok(())
    }

    /// Serialize TurnModelResponse to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(val) = self.output.as_ref() {
            result.insert("output".to_string(), val.clone());
        }
        if let Some(val) = self.usage.as_ref() {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("usage".to_string(), nested);
            }
        }
        if let Some(items) = self.tool_requests.as_ref() {
            result.insert("toolRequests".to_string(), Self::save_tool_requests(items, ctx));
        }
        if !self.checkpoint_state.is_null() {
            result.insert("checkpointState".to_string(), self.checkpoint_state.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize TurnModelResponse to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize TurnModelResponse to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_checkpoint_state_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.checkpoint_state.as_object()
    }


    /// Load a collection of HostToolRequest from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_tool_requests(data: &serde_json::Value, ctx: &LoadContext) -> Vec<HostToolRequest> {
        match data {
            serde_json::Value::Array(arr) => {
                arr.iter().map(|v| HostToolRequest::load_from_value(v, ctx)).collect()
            }

            _ => Vec::new(),

        }
    }

    /// Save a collection of HostToolRequest to a JSON value.
    fn save_tool_requests(items: &[HostToolRequest], ctx: &SaveContext) -> serde_json::Value {

        serde_json::Value::Array(items.iter().map(|item| item.to_value(ctx)).collect::<Vec<_>>())

    }
}

// Serde for `TurnModelResponse` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for TurnModelResponse {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for TurnModelResponse {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
