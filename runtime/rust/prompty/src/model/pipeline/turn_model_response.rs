// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::super::events::host_tool_request::HostToolRequest;

/// Response returned by the injected model callback to the reference turn runner.
#[derive(Debug, Clone, Default)]
pub struct TurnModelResponse {
    /// Provider-neutral final model output for the turn when no more tools are requested
    pub output: Option<serde_json::Value>,
    /// Host tool execution requests emitted by the model callback
    pub tool_requests: Vec<HostToolRequest>,
    /// Additional deterministic state to merge into the iteration checkpoint
    pub checkpoint_state: serde_json::Value,
}

impl TurnModelResponse {
    /// Create a new TurnModelResponse with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load TurnModelResponse from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnModelResponse from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnModelResponse from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            output: value.get("output").cloned(),
            tool_requests: value
                .get("toolRequests")
                .map(|v| Self::load_tool_requests(v, ctx))
                .unwrap_or_default(),
            checkpoint_state: value
                .get("checkpointState")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize TurnModelResponse to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if let Some(ref val) = self.output {
            result.insert("output".to_string(), val.clone());
        }
        if !self.tool_requests.is_empty() {
            result.insert(
                "toolRequests".to_string(),
                Self::save_tool_requests(&self.tool_requests, ctx),
            );
        }
        if !self.checkpoint_state.is_null() {
            result.insert("checkpointState".to_string(), self.checkpoint_state.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize TurnModelResponse to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize TurnModelResponse to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_checkpoint_state_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.checkpoint_state.as_object()
    }

    /// Load a collection of HostToolRequest from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_tool_requests(data: &serde_json::Value, ctx: &LoadContext) -> Vec<HostToolRequest> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| HostToolRequest::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of HostToolRequest to a JSON value.
    fn save_tool_requests(items: &[HostToolRequest], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}
