// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::invocation_context_decision::InvocationContextDecision;

use super::invocation_context_state::InvocationContextState;

use super::super::conversation::message::Message;

/// Immutable model-visible context for a single provider invocation. Retries of the same invocation MUST reuse the same snapshot.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ModelInvocationContextSnapshot {
    /// Stable identifier for this context snapshot
    pub id: String,
    /// Stable session identifier
    pub session_id: String,
    /// Stable turn identifier
    pub turn_id: String,
    /// Stable model invocation identifier
    pub invocation_id: String,
    /// Zero-based model loop iteration
    pub iteration: i32,
    /// Canonical messages visible to the model
    pub messages: Vec<Message>,
    /// Context-planning decisions used to assemble the snapshot
    pub decisions: Vec<InvocationContextDecision>,
    /// Number of leading messages eligible for provider prefix-cache reuse
    pub stable_prefix_messages: i32,
    /// Provider-context state carried into this invocation
    pub context_state: InvocationContextState,
    /// Opaque host-specific snapshot metadata
    pub metadata: serde_json::Value,
}

impl ModelInvocationContextSnapshot {
    /// Create a new ModelInvocationContextSnapshot with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ModelInvocationContextSnapshot from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelInvocationContextSnapshot from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ModelInvocationContextSnapshot from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            id: value
                .get("id")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            session_id: value
                .get("sessionId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            turn_id: value
                .get("turnId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            invocation_id: value
                .get("invocationId")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            iteration: value.get("iteration").and_then(|v| v.as_i64()).unwrap_or(0) as i32,
            messages: value
                .get("messages")
                .map(|v| Self::load_messages(v, ctx))
                .unwrap_or_default(),
            decisions: value
                .get("decisions")
                .map(|v| Self::load_decisions(v, ctx))
                .unwrap_or_default(),
            stable_prefix_messages: value
                .get("stablePrefixMessages")
                .and_then(|v| v.as_i64())
                .unwrap_or(0) as i32,
            context_state: value
                .get("contextState")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| InvocationContextState::load_from_value(v, ctx))
                .unwrap_or_default(),
            metadata: value
                .get("metadata")
                .cloned()
                .unwrap_or(serde_json::Value::Null),
        }
    }

    /// Serialize ModelInvocationContextSnapshot to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if !self.id.is_empty() {
            result.insert("id".to_string(), serde_json::Value::String(self.id.clone()));
        }
        if !self.session_id.is_empty() {
            result.insert(
                "sessionId".to_string(),
                serde_json::Value::String(self.session_id.clone()),
            );
        }
        if !self.turn_id.is_empty() {
            result.insert(
                "turnId".to_string(),
                serde_json::Value::String(self.turn_id.clone()),
            );
        }
        if !self.invocation_id.is_empty() {
            result.insert(
                "invocationId".to_string(),
                serde_json::Value::String(self.invocation_id.clone()),
            );
        }
        if self.iteration != 0 {
            result.insert(
                "iteration".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.iteration)),
            );
        }
        if !self.messages.is_empty() {
            result.insert(
                "messages".to_string(),
                Self::save_messages(&self.messages, ctx),
            );
        }
        if !self.decisions.is_empty() {
            result.insert(
                "decisions".to_string(),
                Self::save_decisions(&self.decisions, ctx),
            );
        }
        if self.stable_prefix_messages != 0 {
            result.insert(
                "stablePrefixMessages".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.stable_prefix_messages)),
            );
        }
        {
            let nested = self.context_state.to_value(ctx);
            if !nested.is_null() {
                result.insert("contextState".to_string(), nested);
            }
        }
        if !self.metadata.is_null() {
            result.insert("metadata".to_string(), self.metadata.clone());
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ModelInvocationContextSnapshot to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ModelInvocationContextSnapshot to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
    /// Returns typed reference to the map if the field is an object.
    /// Returns `None` if the field is null or not an object.
    pub fn as_metadata_dict(&self) -> Option<&serde_json::Map<String, serde_json::Value>> {
        self.metadata.as_object()
    }

    /// Load a collection of Message from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_messages(data: &serde_json::Value, ctx: &LoadContext) -> Vec<Message> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| Message::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of Message to a JSON value.
    fn save_messages(items: &[Message], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }

    /// Load a collection of InvocationContextDecision from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_decisions(
        data: &serde_json::Value,
        ctx: &LoadContext,
    ) -> Vec<InvocationContextDecision> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| InvocationContextDecision::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of InvocationContextDecision to a JSON value.
    fn save_decisions(items: &[InvocationContextDecision], ctx: &SaveContext) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }
}

// Serde for `ModelInvocationContextSnapshot` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
impl serde::Serialize for ModelInvocationContextSnapshot {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

impl<'de> serde::Deserialize<'de> for ModelInvocationContextSnapshot {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
