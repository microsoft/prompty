// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::model_invocation_context_snapshot::ModelInvocationContextSnapshot;

use super::model_tool_result::ModelToolResult;

use super::turn_commit::TurnCommit;

/// The result returned by the turn engine.
#[derive(Debug, Clone, Default)]
pub struct TurnEngineResult {
    /// The committed turn outcome
    pub commit: TurnCommit,
    /// Immutable context snapshots produced during the turn
    pub snapshots: Vec<ModelInvocationContextSnapshot>,
    /// Normalized tool results produced during the turn
    pub tool_results: Vec<ModelToolResult>,
    /// A non-fatal post-commit failure; the turn itself remains committed
    pub post_commit_error: Option<String>,
}

impl TurnEngineResult {
    /// Create a new TurnEngineResult with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load TurnEngineResult from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnEngineResult from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load TurnEngineResult from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            commit: value
                .get("commit")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| TurnCommit::load_from_value(v, ctx))
                .unwrap_or_default(),
            snapshots: value
                .get("snapshots")
                .map(|v| Self::load_snapshots(v, ctx))
                .unwrap_or_default(),
            tool_results: value
                .get("toolResults")
                .map(|v| Self::load_tool_results(v, ctx))
                .unwrap_or_default(),
            post_commit_error: value
                .get("postCommitError")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
        }
    }

    /// Serialize TurnEngineResult to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        {
            let nested = self.commit.to_value(ctx);
            if !nested.is_null() {
                result.insert("commit".to_string(), nested);
            }
        }
        if !self.snapshots.is_empty() {
            result.insert(
                "snapshots".to_string(),
                Self::save_snapshots(&self.snapshots, ctx),
            );
        }
        if !self.tool_results.is_empty() {
            result.insert(
                "toolResults".to_string(),
                Self::save_tool_results(&self.tool_results, ctx),
            );
        }
        if let Some(ref val) = self.post_commit_error {
            result.insert(
                "postCommitError".to_string(),
                serde_json::Value::String(val.clone()),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize TurnEngineResult to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize TurnEngineResult to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Load a collection of ModelInvocationContextSnapshot from a JSON value.
    /// Handles both array format `[{...}]`.
    fn load_snapshots(
        data: &serde_json::Value,
        ctx: &LoadContext,
    ) -> Vec<ModelInvocationContextSnapshot> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| ModelInvocationContextSnapshot::load_from_value(v, ctx))
                .collect(),

            _ => Vec::new(),
        }
    }

    /// Save a collection of ModelInvocationContextSnapshot to a JSON value.
    fn save_snapshots(
        items: &[ModelInvocationContextSnapshot],
        ctx: &SaveContext,
    ) -> serde_json::Value {
        serde_json::Value::Array(
            items
                .iter()
                .map(|item| item.to_value(ctx))
                .collect::<Vec<_>>(),
        )
    }

    /// Load a collection of ModelToolResult from a JSON value.
    /// Handles both array format `[{...}]` and dict format `{"name": {...}}`.
    fn load_tool_results(data: &serde_json::Value, ctx: &LoadContext) -> Vec<ModelToolResult> {
        match data {
            serde_json::Value::Array(arr) => arr
                .iter()
                .map(|v| ModelToolResult::load_from_value(v, ctx))
                .collect(),

            serde_json::Value::Object(obj) => obj
                .iter()
                .filter_map(|(name, value)| {
                    if value.is_array() {
                        return None;
                    }
                    let mut v = if value.is_object() {
                        value.clone()
                    } else {
                        serde_json::json!({ "requestId": value })
                    };
                    if let serde_json::Value::Object(ref mut m) = v {
                        m.entry("name".to_string())
                            .or_insert_with(|| serde_json::Value::String(name.clone()));
                    }
                    Some(ModelToolResult::load_from_value(&v, ctx))
                })
                .collect(),
            _ => Vec::new(),
        }
    }

    /// Save a collection of ModelToolResult to a JSON value.
    fn save_tool_results(items: &[ModelToolResult], ctx: &SaveContext) -> serde_json::Value {
        if ctx.collection_format == "array" {
            return serde_json::Value::Array(
                items
                    .iter()
                    .map(|item| item.to_value(ctx))
                    .collect::<Vec<_>>(),
            );
        }
        // Object format: use name as key
        let mut result = serde_json::Map::new();
        for item in items {
            let mut item_data = match item.to_value(ctx) {
                serde_json::Value::Object(m) => m,
                other => {
                    let mut m = serde_json::Map::new();
                    m.insert("value".to_string(), other);
                    m
                }
            };
            if let Some(serde_json::Value::String(name)) = item_data.remove("name") {
                result.insert(name, serde_json::Value::Object(item_data));
            }
        }
        serde_json::Value::Object(result)
    }
}
