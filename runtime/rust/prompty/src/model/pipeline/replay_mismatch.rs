// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use super::super::context::{LoadContext, SaveContext};

use super::replay_journal_record::ReplayJournalRecord;

/// A single mismatch produced by replay verification.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct ReplayMismatch {
    /// Zero-based record index where the mismatch was found
    pub index: i32,
    /// Expected record at this index, when present
    pub expected: Option<ReplayJournalRecord>,
    /// Actual record at this index, when present
    pub actual: Option<ReplayJournalRecord>,
    /// Human-readable mismatch explanation
    pub message: String,
}

impl ReplayMismatch {
    /// Create a new ReplayMismatch with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load ReplayMismatch from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ReplayMismatch from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load ReplayMismatch from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        Self {
            index: value.get("index").and_then(|v| v.as_i64()).unwrap_or(0) as i32,
            expected: value
                .get("expected")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| ReplayJournalRecord::load_from_value(v, ctx)),
            actual: value
                .get("actual")
                .filter(|v| v.is_object() || v.is_array() || v.is_string())
                .map(|v| ReplayJournalRecord::load_from_value(v, ctx)),
            message: value
                .get("message")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
        }
    }

    /// Serialize ReplayMismatch to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        if self.index != 0 {
            result.insert(
                "index".to_string(),
                serde_json::Value::Number(serde_json::Number::from(self.index)),
            );
        }
        if let Some(ref val) = self.expected {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("expected".to_string(), nested);
            }
        }
        if let Some(ref val) = self.actual {
            let nested = val.to_value(ctx);
            if !nested.is_null() {
                result.insert("actual".to_string(), nested);
            }
        }
        if !self.message.is_empty() {
            result.insert(
                "message".to_string(),
                serde_json::Value::String(self.message.clone()),
            );
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize ReplayMismatch to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize ReplayMismatch to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}

// Serde for `ReplayMismatch` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
impl serde::Serialize for ReplayMismatch {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

impl<'de> serde::Deserialize<'de> for ReplayMismatch {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
