// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, unexpected_cfgs, clippy::all)]

use super::super::context::{LoadContext, SaveContext};

/// Provider-neutral device authorization challenge shown to a user.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct DeviceAuthorization {
    /// The device code used when polling the token endpoint
    pub device_code: String,
    /// The short code the user enters at the verification URI
    pub user_code: String,
    /// The URI where the user authorizes the device
    pub verification_uri: String,
    /// Lifetime of the device authorization in seconds
    pub expires_in: i64,
    /// Minimum polling interval in seconds
    pub interval: i64,
    /// Human-readable authorization instructions
    pub message: String,
}

impl DeviceAuthorization {
    /// Create a new DeviceAuthorization with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load DeviceAuthorization from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load DeviceAuthorization from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load DeviceAuthorization from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            device_code: value.get("deviceCode").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            user_code: value.get("userCode").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            verification_uri: value.get("verificationUri").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
            expires_in: value.get("expiresIn").and_then(|v| v.as_i64()).unwrap_or(0),
            interval: value.get("interval").and_then(|v| v.as_i64()).unwrap_or(0),
            message: value.get("message").and_then(|v| v.as_str()).unwrap_or_default().to_string(),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        Ok(())
    }

    /// Serialize DeviceAuthorization to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert("deviceCode".to_string(), serde_json::Value::String(self.device_code.clone()));
        result.insert("userCode".to_string(), serde_json::Value::String(self.user_code.clone()));
        result.insert("verificationUri".to_string(), serde_json::Value::String(self.verification_uri.clone()));
        result.insert("expiresIn".to_string(), serde_json::Value::Number(serde_json::Number::from(self.expires_in)));
        result.insert("interval".to_string(), serde_json::Value::Number(serde_json::Number::from(self.interval)));
        result.insert("message".to_string(), serde_json::Value::String(self.message.clone()));
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize DeviceAuthorization to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize DeviceAuthorization to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }

    /// Convert to provider-specific wire format.
    pub fn to_wire(&self, provider: &str) -> serde_json::Value {
        let data = self.to_value(&SaveContext::default());
        let mut result = serde_json::Map::new();
        let wire_map: std::collections::HashMap<&str, std::collections::HashMap<&str, &str>> = std::collections::HashMap::from([
            ("deviceCode", std::collections::HashMap::from([("foundry", "device_code")])),
            ("userCode", std::collections::HashMap::from([("foundry", "user_code")])),
            ("verificationUri", std::collections::HashMap::from([("foundry", "verification_uri")])),
            ("expiresIn", std::collections::HashMap::from([("foundry", "expires_in")])),
            ("interval", std::collections::HashMap::from([("foundry", "interval")])),
            ("message", std::collections::HashMap::from([("foundry", "message")])),
        ]);
        if let serde_json::Value::Object(map) = data {
            for (key, value) in map {
                if let Some(mapping) = wire_map.get(key.as_str()) {
                    if let Some(wire_name) = mapping.get(provider) {
                        result.insert(wire_name.to_string(), value);
                    }
                }
            }
        }
        serde_json::Value::Object(result)
    }
}

// Serde for `DeviceAuthorization` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for DeviceAuthorization {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for DeviceAuthorization {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
