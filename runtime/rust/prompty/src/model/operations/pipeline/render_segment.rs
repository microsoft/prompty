// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use super::super::super::context::{LoadContext, SaveContext};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum RenderSegmentKind {
    Literal,
    Interp,
}

impl Default for RenderSegmentKind {
    fn default() -> Self {
        Self::Literal
    }
}

impl std::fmt::Display for RenderSegmentKind {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Literal => write!(f, "literal"),
            Self::Interp => write!(f, "interp"),
        }
    }
}

impl RenderSegmentKind {
    pub fn from_str_opt(s: &str) -> Option<Self> {
        match s {
            "literal" => Some(Self::Literal),
            "interp" => Some(Self::Interp),
            _ => None,
        }
    }

    pub fn from_str_ignore_case_opt(s: &str) -> Option<Self> {
        if s.eq_ignore_ascii_case("literal") {
            return Some(Self::Literal);
        }
        if s.eq_ignore_ascii_case("interp") {
            return Some(Self::Interp);
        }
        None
    }

    pub fn as_str(&self) -> &str {
        match self {
            Self::Literal => "literal",
            Self::Interp => "interp",
        }
    }
}

#[cfg(feature = "serde")]
impl serde::Serialize for RenderSegmentKind {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.as_str())
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for RenderSegmentKind {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let s = <String as serde::Deserialize>::deserialize(deserializer)?;
        Self::from_str_opt(&s).ok_or_else(|| {
            serde::de::Error::custom(format!("invalid RenderSegmentKind value: {}", s))
        })
    }
}

/// One provenance-tagged span of rendered output (Prompty Jinja Subset §7, spec/jinja-grammar.md). Concatenating every segment's `text` in source order yields the flat string returned by `render`, so `renderSegments` is the provenance-carrying superset of `render`. `literal` spans originate in the template source; `interp` spans originate in an interpolated input value and carry the originating root property `source`. That provenance lets a downstream structural parser distinguish an author-authored role boundary from one forged inside untrusted input — the injection-deterrence contract that replaces in-band sentinel scanning. A span whose `source` property is declared strict is flagged `strict`; a strict span that forges a role boundary is rejected loudly rather than emitted.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct RenderSegment {
    /// Whether this span came from the template (literal) or an input value (interp)
    pub kind: RenderSegmentKind,
    /// The rendered text of this span
    pub text: String,
    /// For interp spans, the root input property the value came from
    pub source: Option<String>,
    /// Whether the originating input property is declared strict
    pub strict: Option<bool>,
}

impl RenderSegment {
    /// Create a new RenderSegment with default values.
    pub fn new() -> Self {
        Self::default()
    }

    /// Load RenderSegment from a JSON string.
    pub fn from_json(json: &str, ctx: &LoadContext) -> Result<Self, serde_json::Error> {
        let value: serde_json::Value = serde_json::from_str(json)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load RenderSegment from a YAML string.
    pub fn from_yaml(yaml: &str, ctx: &LoadContext) -> Result<Self, serde_yaml::Error> {
        let value: serde_json::Value = serde_yaml::from_str(yaml)?;
        Self::validate_input_at(&value, "")
            .map_err(|message| <serde_yaml::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(&value, ctx))
    }

    /// Load RenderSegment from an already-parsed JSON value, returning an error
    /// instead of panicking on invalid input. Fallible companion to
    /// `load_from_value` with the same validation policy as `from_json`.
    pub fn try_load_from_value(
        value: &serde_json::Value,
        ctx: &LoadContext,
    ) -> Result<Self, serde_json::Error> {
        Self::validate_input_at(value, "")
            .map_err(|message| <serde_json::Error as serde::de::Error>::custom(message))?;
        Ok(Self::load_from_value(value, ctx))
    }

    /// Load RenderSegment from a `serde_json::Value`.
    ///
    /// Calls `ctx.process_input` before field extraction.
    pub fn load_from_value(value: &serde_json::Value, ctx: &LoadContext) -> Self {
        let value = ctx.process_input(value.clone());
        if let Err(message) = Self::validate_input_at(&value, "") {
            panic!("{}", message);
        }
        Self {
            kind: value
                .get("kind")
                .and_then(|v| v.as_str())
                .and_then(|s| RenderSegmentKind::from_str_opt(s))
                .unwrap_or(RenderSegmentKind::Literal),
            text: value
                .get("text")
                .and_then(|v| v.as_str())
                .unwrap_or_default()
                .to_string(),
            source: value
                .get("source")
                .and_then(|v| v.as_str())
                .map(|s| s.to_string()),
            strict: value.get("strict").and_then(|v| v.as_bool()),
        }
    }

    pub(crate) fn validate_input_at(value: &serde_json::Value, path: &str) -> Result<(), String> {
        Ok(())
    }

    /// Serialize RenderSegment to a `serde_json::Value`.
    ///
    /// Calls `ctx.process_dict` after serialization.
    pub fn to_value(&self, ctx: &SaveContext) -> serde_json::Value {
        let mut result = serde_json::Map::new();
        // Write base fields
        result.insert(
            "kind".to_string(),
            serde_json::Value::String(self.kind.to_string()),
        );
        result.insert(
            "text".to_string(),
            serde_json::Value::String(self.text.clone()),
        );
        if let Some(val) = self.source.as_ref() {
            result.insert("source".to_string(), serde_json::Value::String(val.clone()));
        }
        if let Some(val) = self.strict.as_ref() {
            result.insert("strict".to_string(), serde_json::Value::Bool(*val));
        }
        ctx.process_dict(serde_json::Value::Object(result))
    }

    /// Serialize RenderSegment to a JSON string.
    pub fn to_json(&self, ctx: &SaveContext) -> Result<String, serde_json::Error> {
        serde_json::to_string_pretty(&self.to_value(ctx))
    }

    /// Serialize RenderSegment to a YAML string.
    pub fn to_yaml(&self, ctx: &SaveContext) -> Result<String, serde_yaml::Error> {
        serde_yaml::to_string(&self.to_value(ctx))
    }
}

// Serde for `RenderSegment` delegates to the canonical to_value/load_from_value
// logic so its serde wire form always equals the canonical to_value/load_from_value form. Uses a default (no-op) context — no ${env:}/${file:}
// resolution here — leaving the context-aware LoadContext/SaveContext API intact.
#[cfg(feature = "serde")]
impl serde::Serialize for RenderSegment {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serde::Serialize::serialize(&self.to_value(&SaveContext::default()), serializer)
    }
}

#[cfg(feature = "serde")]
impl<'de> serde::Deserialize<'de> for RenderSegment {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let value = <serde_json::Value as serde::Deserialize>::deserialize(deserializer)?;
        Self::validate_input_at(&value, "").map_err(serde::de::Error::custom)?;
        Ok(Self::load_from_value(&value, &LoadContext::default()))
    }
}
