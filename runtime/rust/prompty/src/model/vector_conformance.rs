// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Typed @vector conformance entrypoints (issue #511 Cat 1). Each generic fn
// takes a consumer's REAL typed seam impl and runs the seam's baked-in vectors
// by calling trait methods directly — the idiomatic replacement for the
// stringly HashMap<&str, Adapter> runner + vector_adapters.rs registry. The
// `S: <Seam>` bound makes the compiler prove every op is implemented (E0046),
// so completeness is checked at COMPILE time, not by a runtime map lookup.
// References only the model crate + serde_json + std (no vector_adapters, so
// no cycle); emitted additively beside the stringly runner.
// See docs: reference/vector-conformance.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use serde_json::Value;

/// Typed @vector conformance for DiscoveryConformance. Pass your real `impl DiscoveryConformance`; the
/// `S: DiscoveryConformance` bound makes the compiler prove every op is implemented. Call
/// from a test, e.g. `run_discovery_conformance_conformance(&DiscoveryConformanceImpl).await;` (or without `.await` when sync).
pub async fn run_discovery_conformance_conformance<
    S: crate::model::DiscoveryConformance + ?Sized,
>(
    seam: &S,
) {
    let ctx = crate::model::context::LoadContext::default();
    // vector: anthropic_enrich_no_dataset_entry_is_noop
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("anthropic_enrich_no_dataset_entry_is_noop: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "claude-sonnet-4-20250514",
  "contextWindow": 200000,
  "inputModalities": [
    "text",
    "image"
  ],
  "outputModalities": [
    "text"
  ]
}
"####,
            &ctx,
        )
        .expect("anthropic_enrich_no_dataset_entry_is_noop: expected parses");
        assert_eq!(
            actual, expected,
            "anthropic_enrich_no_dataset_entry_is_noop misrouted"
        );
    }
    // vector: anthropic_model_full
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("anthropic_model_full: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "claude-sonnet-4-20250514",
  "displayName": "Claude Sonnet 4",
  "ownedBy": "anthropic",
  "contextWindow": 200000,
  "inputModalities": [
    "text",
    "image"
  ],
  "outputModalities": [
    "text"
  ],
  "additionalProperties": {
    "id": "claude-sonnet-4-20250514",
    "display_name": "Claude Sonnet 4",
    "context_length": 200000,
    "input_modalities": [
      "text",
      "image"
    ],
    "output_modalities": [
      "text"
    ],
    "type": "model"
  }
}
"####,
            &ctx,
        )
        .expect("anthropic_model_full: expected parses");
        assert_eq!(actual, expected, "anthropic_model_full misrouted");
    }
    // vector: anthropic_model_minimal
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("anthropic_model_minimal: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "claude-3-haiku-20240307",
  "ownedBy": "anthropic",
  "additionalProperties": {
    "id": "claude-3-haiku-20240307",
    "type": "model"
  }
}
"####,
            &ctx,
        )
        .expect("anthropic_model_minimal: expected parses");
        assert_eq!(actual, expected, "anthropic_model_minimal misrouted");
    }
    // vector: foundry_catalog_model
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("foundry_catalog_model: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-4",
  "ownedBy": "openai",
  "contextWindow": 8192,
  "additionalProperties": {
    "id": "gpt-4",
    "owned_by": "openai",
    "maxContextLength": 8192,
    "status": "succeeded"
  }
}
"####,
            &ctx,
        )
        .expect("foundry_catalog_model: expected parses");
        assert_eq!(actual, expected, "foundry_catalog_model misrouted");
    }
    // vector: foundry_deployment_flat_v1
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("foundry_deployment_flat_v1: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-4o",
  "displayName": "gpt-4o",
  "ownedBy": "OpenAI",
  "additionalProperties": {
    "name": "gpt-4o",
    "type": "ModelDeployment",
    "modelName": "gpt-4o",
    "modelVersion": "2024-08-06",
    "modelPublisher": "OpenAI",
    "capabilities": {
      "chatCompletion": "true"
    },
    "sku": {
      "name": "GlobalStandard",
      "capacity": 50
    }
  }
}
"####,
            &ctx,
        )
        .expect("foundry_deployment_flat_v1: expected parses");
        assert_eq!(actual, expected, "foundry_deployment_flat_v1 misrouted");
    }
    // vector: foundry_deployment_nested_arm
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("foundry_deployment_nested_arm: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "my-gpt4",
  "displayName": "gpt-4",
  "ownedBy": "OpenAI",
  "contextWindow": 8192,
  "inputModalities": [
    "text"
  ],
  "outputModalities": [
    "text"
  ],
  "additionalProperties": {
    "name": "my-gpt4",
    "properties": {
      "model": {
        "name": "gpt-4",
        "publisher": "OpenAI",
        "maxContextLength": 8192
      },
      "capabilities": {
        "supportedInputModalities": [
          "text"
        ],
        "supportedOutputModalities": [
          "text"
        ]
      }
    }
  }
}
"####,
            &ctx,
        )
        .expect("foundry_deployment_nested_arm: expected parses");
        assert_eq!(actual, expected, "foundry_deployment_nested_arm misrouted");
    }
    // vector: openai_enrich_dalle3_no_context_window
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_dalle3_no_context_window: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "dall-e-3",
  "inputModalities": [
    "text"
  ],
  "outputModalities": [
    "image"
  ]
}
"####,
            &ctx,
        )
        .expect("openai_enrich_dalle3_no_context_window: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_dalle3_no_context_window misrouted"
        );
    }
    // vector: openai_enrich_embedding_empty_output_modalities
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_embedding_empty_output_modalities: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "text-embedding-3-small",
  "contextWindow": 8191,
  "inputModalities": [
    "text"
  ],
  "outputModalities": []
}
"####,
            &ctx,
        )
        .expect("openai_enrich_embedding_empty_output_modalities: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_embedding_empty_output_modalities misrouted"
        );
    }
    // vector: openai_enrich_gpt4o
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_gpt4o: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-4o",
  "contextWindow": 128000,
  "inputModalities": [
    "text",
    "image"
  ],
  "outputModalities": [
    "text"
  ]
}
"####,
            &ctx,
        )
        .expect("openai_enrich_gpt4o: expected parses");
        assert_eq!(actual, expected, "openai_enrich_gpt4o misrouted");
    }
    // vector: openai_enrich_gpt4o_mini_longest_prefix
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_gpt4o_mini_longest_prefix: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-4o-mini-2024-07-18",
  "contextWindow": 128000,
  "inputModalities": [
    "text",
    "image"
  ],
  "outputModalities": [
    "text"
  ]
}
"####,
            &ctx,
        )
        .expect("openai_enrich_gpt4o_mini_longest_prefix: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_gpt4o_mini_longest_prefix misrouted"
        );
    }
    // vector: openai_enrich_prefix_requires_token_boundary
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_prefix_requires_token_boundary: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-45-future"
}
"####,
            &ctx,
        )
        .expect("openai_enrich_prefix_requires_token_boundary: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_prefix_requires_token_boundary misrouted"
        );
    }
    // vector: openai_enrich_provider_context_window_wins
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_provider_context_window_wins: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-4o",
  "contextWindow": 999,
  "inputModalities": [
    "text",
    "image"
  ],
  "outputModalities": [
    "text"
  ]
}
"####,
            &ctx,
        )
        .expect("openai_enrich_provider_context_window_wins: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_provider_context_window_wins misrouted"
        );
    }
    // vector: openai_enrich_provider_empty_modalities_win
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_provider_empty_modalities_win: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "gpt-4o",
  "contextWindow": 128000,
  "inputModalities": [],
  "outputModalities": [
    "text"
  ]
}
"####,
            &ctx,
        )
        .expect("openai_enrich_provider_empty_modalities_win: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_provider_empty_modalities_win misrouted"
        );
    }
    // vector: openai_enrich_unknown_id_is_noop
    {
        let base = crate::model::ModelInfo::from_json(
            r####"
{}
"####,
            &ctx,
        )
        .expect("base parses");
        let actual = seam
            .enrich(&base)
            .await
            .expect("openai_enrich_unknown_id_is_noop: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "ft:custom-model:acme::xyz"
}
"####,
            &ctx,
        )
        .expect("openai_enrich_unknown_id_is_noop: expected parses");
        assert_eq!(
            actual, expected,
            "openai_enrich_unknown_id_is_noop misrouted"
        );
    }
    // vector: openai_model_basic
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("openai_model_basic: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "o3-mini",
  "ownedBy": "system",
  "additionalProperties": {
    "id": "o3-mini",
    "object": "model",
    "created": 1715367049,
    "owned_by": "system"
  }
}
"####,
            &ctx,
        )
        .expect("openai_model_basic: expected parses");
        assert_eq!(actual, expected, "openai_model_basic misrouted");
    }
    // vector: openai_model_finetune_no_owner
    {
        let raw: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("raw parses");
        let actual = seam
            .map_model(&raw)
            .await
            .expect("openai_model_finetune_no_owner: seam ok");
        let expected = crate::model::ModelInfo::from_json(
            r####"
{
  "id": "ft:custom-model:acme::xyz",
  "additionalProperties": {
    "id": "ft:custom-model:acme::xyz"
  }
}
"####,
            &ctx,
        )
        .expect("openai_model_finetune_no_owner: expected parses");
        assert_eq!(actual, expected, "openai_model_finetune_no_owner misrouted");
    }
}

/// Typed @vector conformance for LoadConformance. Pass your real `impl LoadConformance`; the
/// `S: LoadConformance` bound makes the compiler prove every op is implemented. Call
/// from a test, e.g. `run_load_conformance_conformance(&LoadConformanceImpl).await;` (or without `.await` when sync).
pub async fn run_load_conformance_conformance<S: crate::model::LoadConformance + ?Sized>(seam: &S) {
    let ctx = crate::model::context::LoadContext::default();
    // vector: basic_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("basic_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "basic-prompt",
  "description": "A basic prompt for testing",
  "metadata": {
    "authors": [
      "testauthor"
    ]
  },
  "model": {
    "id": "gpt-4",
    "provider": "openai",
    "apiType": "chat",
    "connection": {
      "kind": "key",
      "endpoint": "https://test.openai.com"
    },
    "options": {
      "temperature": 0.7,
      "maxOutputTokens": 1000
    }
  },
  "inputs": [
    {
      "name": "firstName",
      "kind": "string",
      "default": "Jane"
    },
    {
      "name": "lastName",
      "kind": "string",
      "default": "Doe"
    },
    {
      "name": "question",
      "kind": "string",
      "default": "What is the meaning of life?"
    }
  ],
  "template": {
    "format": {
      "kind": "jinja2"
    },
    "parser": {
      "kind": "prompty"
    }
  },
  "instructions": "system:\nYou are an AI assistant who helps people find information.\n\n# Customer\nYou are helping {{firstName}} {{lastName}} to find answers to their questions.\n\nuser:\n{{question}}"
}
"####,
            &ctx,
        )
        .expect("basic_load: expected parses");
        assert_eq!(actual, expected, "basic_load misrouted");
    }
    // vector: connection_types_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("connection_types_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "model": {
    "id": "gpt-4",
    "connection": {
      "kind": "anonymous",
      "endpoint": "https://localhost:8080"
    }
  }
}
"####,
            &ctx,
        )
        .expect("connection_types_load: expected parses");
        assert_eq!(actual, expected, "connection_types_load misrouted");
    }
    // vector: embedding_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("embedding_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "embedding",
  "model": {
    "id": "text-embedding-3-small",
    "provider": "openai",
    "apiType": "embedding",
    "connection": {
      "kind": "key",
      "endpoint": "https://test.openai.com"
    }
  },
  "inputs": [
    {
      "name": "text",
      "kind": "string",
      "default": "Hello world"
    }
  ],
  "instructions": "{{text}}"
}
"####,
            &ctx,
        )
        .expect("embedding_load: expected parses");
        assert_eq!(actual, expected, "embedding_load misrouted");
    }
    // vector: empty_frontmatter_body_only
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("empty_frontmatter_body_only: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "empty-fm",
  "model": null,
  "inputs": null,
  "tools": null
}
"####,
            &ctx,
        )
        .expect("empty_frontmatter_body_only: expected parses");
        assert_eq!(actual, expected, "empty_frontmatter_body_only misrouted");
    }
    // vector: env_default
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("env_default: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "model": {
    "connection": {
      "kind": "key",
      "endpoint": "fallback_value"
    }
  }
}
"####,
            &ctx,
        )
        .expect("env_default: expected parses");
        assert_eq!(actual, expected, "env_default misrouted");
    }
    // vector: env_missing_error
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let result = seam.load(&source).await;
        assert!(result.is_err(), "env_missing_error: expected an error");
    }
    // vector: env_resolution
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("env_resolution: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "model": {
    "connection": {
      "kind": "key",
      "endpoint": "hello"
    }
  }
}
"####,
            &ctx,
        )
        .expect("env_resolution: expected parses");
        assert_eq!(actual, expected, "env_resolution misrouted");
    }
    // vector: file_reference_in_tree_allowed
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("file_reference_in_tree_allowed: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "model": {
    "connection": {
      "kind": "key",
      "endpoint": "https://in-tree.example.com"
    }
  }
}
"####,
            &ctx,
        )
        .expect("file_reference_in_tree_allowed: expected parses");
        assert_eq!(actual, expected, "file_reference_in_tree_allowed misrouted");
    }
    // vector: file_reference_traversal_rejected
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let result = seam.load(&source).await;
        assert!(
            result.is_err(),
            "file_reference_traversal_rejected: expected an error"
        );
    }
    // vector: file_resolution
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("file_resolution: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "model": {
    "connection": {
      "kind": "key",
      "endpoint": "https://shared.example.com"
    }
  }
}
"####,
            &ctx,
        )
        .expect("file_resolution: expected parses");
        assert_eq!(actual, expected, "file_resolution misrouted");
    }
    // vector: image_apitype_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("image_apitype_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "image-gen",
  "model": {
    "id": "dall-e-3",
    "provider": "openai",
    "apiType": "image",
    "options": {
      "additionalProperties": {
        "size": "1024x1024",
        "quality": "standard"
      }
    }
  }
}
"####,
            &ctx,
        )
        .expect("image_apitype_load: expected parses");
        assert_eq!(actual, expected, "image_apitype_load misrouted");
    }
    // vector: input_scalar_shorthand
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("input_scalar_shorthand: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "inputs": [
    {
      "name": "topic",
      "kind": "string",
      "default": "science"
    },
    {
      "name": "count",
      "kind": "integer",
      "default": 5
    }
  ]
}
"####,
            &ctx,
        )
        .expect("input_scalar_shorthand: expected parses");
        assert_eq!(actual, expected, "input_scalar_shorthand misrouted");
    }
    // vector: input_validation_default_fill
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("input_validation_default_fill: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "validated_inputs": {
    "name": "world"
  }
}
"####,
            &ctx,
        )
        .expect("input_validation_default_fill: expected parses");
        assert_eq!(actual, expected, "input_validation_default_fill misrouted");
    }
    // vector: input_validation_example_not_used
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("input_validation_example_not_used: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "validated_inputs": {}
}
"####,
            &ctx,
        )
        .expect("input_validation_example_not_used: expected parses");
        assert_eq!(
            actual, expected,
            "input_validation_example_not_used misrouted"
        );
    }
    // vector: input_validation_optional_omit
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("input_validation_optional_omit: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "validated_inputs": {}
}
"####,
            &ctx,
        )
        .expect("input_validation_optional_omit: expected parses");
        assert_eq!(actual, expected, "input_validation_optional_omit misrouted");
    }
    // vector: input_validation_required
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let result = seam.load(&source).await;
        assert!(
            result.is_err(),
            "input_validation_required: expected an error"
        );
    }
    // vector: instructions_from_body
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("instructions_from_body: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "instructions": "system:\nYou are an AI assistant who helps people find information.\n\n# Customer\nYou are helping {{firstName}} {{lastName}} to find answers to their questions.\n\nuser:\n{{question}}"
}
"####,
            &ctx,
        )
        .expect("instructions_from_body: expected parses");
        assert_eq!(actual, expected, "instructions_from_body misrouted");
    }
    // vector: invalid_frontmatter_error
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let result = seam.load(&source).await;
        assert!(
            result.is_err(),
            "invalid_frontmatter_error: expected an error"
        );
    }
    // vector: kind_always_prompt
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("kind_always_prompt: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt"
}
"####,
            &ctx,
        )
        .expect("kind_always_prompt: expected parses");
        assert_eq!(actual, expected, "kind_always_prompt misrouted");
    }
    // vector: minimal_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("minimal_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "minimal",
  "model": {
    "id": "gpt-4"
  },
  "instructions": "system:\nHello world.",
  "inputs": null,
  "outputs": null,
  "tools": null
}
"####,
            &ctx,
        )
        .expect("minimal_load: expected parses");
        assert_eq!(actual, expected, "minimal_load misrouted");
    }
    // vector: missing_file_error
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let result = seam.load(&source).await;
        assert!(result.is_err(), "missing_file_error: expected an error");
    }
    // vector: model_shorthand
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("model_shorthand: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "test",
  "model": {
    "id": "gpt-4o"
  }
}
"####,
            &ctx,
        )
        .expect("model_shorthand: expected parses");
        assert_eq!(actual, expected, "model_shorthand misrouted");
    }
    // vector: structured_outputs_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("structured_outputs_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "structured-output",
  "outputs": [
    {
      "name": "temperature",
      "kind": "integer",
      "description": "Temperature in Fahrenheit",
      "required": true
    },
    {
      "name": "condition",
      "kind": "string",
      "description": "Weather condition",
      "required": true
    },
    {
      "name": "city",
      "kind": "string",
      "description": "City name",
      "required": true
    }
  ]
}
"####,
            &ctx,
        )
        .expect("structured_outputs_load: expected parses");
        assert_eq!(actual, expected, "structured_outputs_load misrouted");
    }
    // vector: template_string_invalid
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let result = seam.load(&source).await;
        assert!(
            result.is_err(),
            "template_string_invalid: expected an error"
        );
    }
    // vector: tools_custom_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("tools_custom_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "tools": [
    {
      "name": "my_tool",
      "kind": "my_provider"
    }
  ]
}
"####,
            &ctx,
        )
        .expect("tools_custom_load: expected parses");
        assert_eq!(actual, expected, "tools_custom_load misrouted");
    }
    // vector: tools_function_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("tools_function_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "kind": "prompt",
  "name": "function-tools",
  "model": {
    "id": "gpt-4",
    "apiType": "chat"
  },
  "tools": [
    {
      "name": "get_weather",
      "kind": "function",
      "description": "Get the current weather in a given location",
      "strict": true,
      "parameters": [
        {
          "name": "location",
          "kind": "string",
          "description": "The city and state, e.g. San Francisco, CA"
        },
        {
          "name": "unit",
          "kind": "string",
          "description": "Temperature unit",
          "enumValues": [
            "celsius",
            "fahrenheit"
          ]
        }
      ],
      "bindings": {
        "unit": {
          "input": "preferred_unit"
        }
      }
    }
  ],
  "inputs": [
    {
      "name": "question",
      "kind": "string",
      "default": "What's the weather in Paris?"
    },
    {
      "name": "preferred_unit",
      "kind": "string",
      "default": "celsius"
    }
  ],
  "instructions": "system:\nYou are a helpful assistant with access to tools.\n\nuser:\n{{question}}"
}
"####,
            &ctx,
        )
        .expect("tools_function_load: expected parses");
        assert_eq!(actual, expected, "tools_function_load misrouted");
    }
    // vector: tools_mcp_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam.load(&source).await.expect("tools_mcp_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "tools": [
    {
      "name": "filesystem",
      "kind": "mcp",
      "serverName": "fs-server"
    }
  ]
}
"####,
            &ctx,
        )
        .expect("tools_mcp_load: expected parses");
        assert_eq!(actual, expected, "tools_mcp_load misrouted");
    }
    // vector: tools_openapi_load
    {
        let source: serde_json::Value = serde_json::from_str(
            r####"
{}
"####,
        )
        .expect("source parses");
        let actual = seam
            .load(&source)
            .await
            .expect("tools_openapi_load: seam ok");
        let expected = crate::model::Agent::from_json(
            r####"
{
  "tools": [
    {
      "name": "weather_api",
      "kind": "openapi",
      "specification": "./weather.json"
    }
  ]
}
"####,
            &ctx,
        )
        .expect("tools_openapi_load: expected parses");
        assert_eq!(actual, expected, "tools_openapi_load misrouted");
    }
}
