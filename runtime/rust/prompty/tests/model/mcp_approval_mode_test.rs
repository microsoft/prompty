
// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::McpApprovalMode;


#[test]
fn test_mcp_approval_mode_load_json() {
    let json = r####"
{
  "kind": "never",
  "alwaysRequireApprovalTools": [
    "operation1"
  ],
  "neverRequireApprovalTools": [
    "operation2"
  ]
}
"####;
    let result = McpApprovalMode::from_json(json);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "never");
}

#[test]
fn test_mcp_approval_mode_load_yaml() {
    let yaml = r####"
kind: never
alwaysRequireApprovalTools:
  - operation1
neverRequireApprovalTools:
  - operation2

"####;
    let result = McpApprovalMode::from_yaml(yaml);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "never");
}

#[test]
fn test_mcp_approval_mode_roundtrip() {
    let json = r####"
{
  "kind": "never",
  "alwaysRequireApprovalTools": [
    "operation1"
  ],
  "neverRequireApprovalTools": [
    "operation2"
  ]
}
"####;
    let result = McpApprovalMode::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}



#[test]
fn test_mcp_approval_mode_from_kind() {
    let value = serde_json::json!("never");
    let instance = McpApprovalMode::load_from_value(&value);
    assert_eq!(instance.kind, "never");
}


