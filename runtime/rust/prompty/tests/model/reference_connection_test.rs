
// Code generated by Prompty emitter; DO NOT EDIT.

use agentschema::ReferenceConnection;


#[test]
fn test_reference_connection_load_json() {
    let json = r####"
{
  "kind": "reference",
  "name": "my-reference-connection",
  "target": "my-target-resource"
}
"####;
    let result = ReferenceConnection::from_json(json);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "reference");
    assert_eq!(instance.name, "my-reference-connection");
    assert!(instance.target.is_some(), "Expected target to be Some");
    assert_eq!(instance.target.as_ref().unwrap(), &"my-target-resource");
}

#[test]
fn test_reference_connection_load_yaml() {
    let yaml = r####"
kind: reference
name: my-reference-connection
target: my-target-resource

"####;
    let result = ReferenceConnection::from_yaml(yaml);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "reference");
    assert_eq!(instance.name, "my-reference-connection");
    assert!(instance.target.is_some(), "Expected target to be Some");
}

#[test]
fn test_reference_connection_roundtrip() {
    let json = r####"
{
  "kind": "reference",
  "name": "my-reference-connection",
  "target": "my-target-resource"
}
"####;
    let result = ReferenceConnection::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}



