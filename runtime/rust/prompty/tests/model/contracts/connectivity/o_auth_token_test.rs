// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use prompty::model::OAuthToken;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_o_auth_token_load_json() {
    let json = r####"
{
  "refreshToken": "0.AXoAoffline-refresh-token-value",
  "scope": "https://cognitiveservices.azure.com/.default offline_access"
}
"####;
    let ctx = LoadContext::default();
    let result = OAuthToken::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert!(
        instance.refresh_token.is_some(),
        "Expected refresh_token to be Some"
    );
    assert_eq!(
        instance.refresh_token.as_ref().unwrap(),
        &"0.AXoAoffline-refresh-token-value"
    );
    assert!(instance.scope.is_some(), "Expected scope to be Some");
    assert_eq!(
        instance.scope.as_ref().unwrap(),
        &"https://cognitiveservices.azure.com/.default offline_access"
    );
}

#[test]
fn test_o_auth_token_load_yaml() {
    let yaml = r####"
refreshToken: 0.AXoAoffline-refresh-token-value
scope: "https://cognitiveservices.azure.com/.default offline_access"

"####;
    let ctx = LoadContext::default();
    let result = OAuthToken::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert!(
        instance.refresh_token.is_some(),
        "Expected refresh_token to be Some"
    );
    assert!(instance.scope.is_some(), "Expected scope to be Some");
}

#[test]
fn test_o_auth_token_roundtrip() {
    let json = r####"
{
  "refreshToken": "0.AXoAoffline-refresh-token-value",
  "scope": "https://cognitiveservices.azure.com/.default offline_access"
}
"####;
    let load_ctx = LoadContext::default();
    let result = OAuthToken::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}

#[cfg(feature = "serde")]
#[test]
fn test_o_auth_token_serde_roundtrip() {
    let json = r####"
{
  "refreshToken": "0.AXoAoffline-refresh-token-value",
  "scope": "https://cognitiveservices.azure.com/.default offline_access"
}
"####;
    let instance: OAuthToken =
        serde_json::from_str(json).expect("serde should deserialize canonical JSON");
    let value = serde_json::to_value(&instance).expect("serde should serialize");
    let canonical: serde_json::Value = serde_json::from_str(json).expect("canonical json parses");
    assert_eq!(
        value,
        instance.to_value(&SaveContext::default()),
        "serde serialize must equal canonical to_value"
    );
    assert_eq!(
        instance,
        OAuthToken::load_from_value(&canonical, &LoadContext::default()),
        "serde deserialize must equal canonical load_from_value"
    );
    let reparsed: OAuthToken = serde_json::from_value(value).expect("serde should re-deserialize");
    assert_eq!(instance, reparsed, "serde round-trip must be stable");
}

#[test]
fn test_o_auth_token_wire_conversion() {
    let json = r####"
{
  "refreshToken": "0.AXoAoffline-refresh-token-value",
  "scope": "https://cognitiveservices.azure.com/.default offline_access"
}
"####;
    let ctx = LoadContext::default();
    let instance = OAuthToken::from_json(json, &ctx).expect("load should succeed");
    let foundry_wire = instance.to_wire("foundry");
    let foundry_obj = foundry_wire.as_object().expect("to_wire returns an object");
    assert!(
        foundry_obj.contains_key("refresh_token"),
        "Expected foundry wire output to include refresh_token"
    );
    assert!(
        !foundry_obj.contains_key("refreshToken"),
        "Expected foundry wire output to omit refreshToken"
    );
    assert!(
        foundry_obj.contains_key("scope"),
        "Expected foundry wire output to include scope"
    );
    let foundry_restored = OAuthToken::from_wire("foundry", &foundry_wire, &ctx);
    let foundry_round = foundry_restored.to_wire("foundry");
    let mut foundry_expected: Vec<&String> = foundry_obj.keys().collect();
    let mut foundry_actual: Vec<&String> = foundry_round
        .as_object()
        .expect("to_wire returns an object")
        .keys()
        .collect();
    foundry_expected.sort();
    foundry_actual.sort();
    assert_eq!(
        foundry_expected, foundry_actual,
        "Expected foundry round-trip to preserve wire keys"
    );
}

#[test]
fn test_o_auth_token_from_json_invalid() {
    let ctx = LoadContext::default();
    assert!(
        OAuthToken::from_json("{", &ctx).is_err(),
        "malformed JSON must be rejected instead of silently defaulting"
    );
}
