// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

use prompty::model::DeviceAuthorization;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_device_authorization_load_json() {
    let json = r####"
{
  "deviceCode": "sample",
  "userCode": "sample",
  "verificationUri": "sample",
  "expiresIn": 1,
  "interval": 1
}
"####;
    let ctx = LoadContext::default();
    let result = DeviceAuthorization::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.device_code, "sample");
    assert_eq!(instance.user_code, "sample");
    assert_eq!(instance.verification_uri, "sample");
    assert_eq!(instance.expires_in, 1);
    assert_eq!(instance.interval, 1);
}

#[test]
fn test_device_authorization_load_yaml() {
    let yaml = r####"
deviceCode: sample
userCode: sample
verificationUri: sample
expiresIn: 1
interval: 1

"####;
    let ctx = LoadContext::default();
    let result = DeviceAuthorization::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.device_code, "sample");
    assert_eq!(instance.user_code, "sample");
    assert_eq!(instance.verification_uri, "sample");
    assert_eq!(instance.expires_in, 1);
    assert_eq!(instance.interval, 1);
}

#[test]
fn test_device_authorization_roundtrip() {
    let json = r####"
{
  "deviceCode": "sample",
  "userCode": "sample",
  "verificationUri": "sample",
  "expiresIn": 1,
  "interval": 1
}
"####;
    let load_ctx = LoadContext::default();
    let result = DeviceAuthorization::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}

#[cfg(feature = "serde")]
#[test]
fn test_device_authorization_serde_roundtrip() {
    let json = r####"
{
  "deviceCode": "sample",
  "userCode": "sample",
  "verificationUri": "sample",
  "expiresIn": 1,
  "interval": 1
}
"####;
    let instance: DeviceAuthorization =
        serde_json::from_str(json).expect("serde should deserialize canonical JSON");
    let value = serde_json::to_value(&instance).expect("serde should serialize");
    let canonical: serde_json::Value = serde_json::from_str(json).expect("canonical json parses");
    assert_eq!(
        value,
        instance.to_value(&SaveContext::default()),
        "serde serialize must equal canonical to_value"
    );
    assert_eq!(
        instance,
        DeviceAuthorization::load_from_value(&canonical, &LoadContext::default()),
        "serde deserialize must equal canonical load_from_value"
    );
    let reparsed: DeviceAuthorization =
        serde_json::from_value(value).expect("serde should re-deserialize");
    assert_eq!(instance, reparsed, "serde round-trip must be stable");
}

#[test]
fn test_device_authorization_wire_conversion() {
    let json = r####"
{
  "deviceCode": "sample",
  "userCode": "sample",
  "verificationUri": "sample",
  "expiresIn": 1,
  "interval": 1
}
"####;
    let ctx = LoadContext::default();
    let instance = DeviceAuthorization::from_json(json, &ctx).expect("load should succeed");
    let foundry_wire = instance.to_wire("foundry");
    let foundry_obj = foundry_wire.as_object().expect("to_wire returns an object");
    assert!(
        foundry_obj.contains_key("device_code"),
        "Expected foundry wire output to include device_code"
    );
    assert!(
        !foundry_obj.contains_key("deviceCode"),
        "Expected foundry wire output to omit deviceCode"
    );
    assert!(
        foundry_obj.contains_key("user_code"),
        "Expected foundry wire output to include user_code"
    );
    assert!(
        !foundry_obj.contains_key("userCode"),
        "Expected foundry wire output to omit userCode"
    );
    assert!(
        foundry_obj.contains_key("verification_uri"),
        "Expected foundry wire output to include verification_uri"
    );
    assert!(
        !foundry_obj.contains_key("verificationUri"),
        "Expected foundry wire output to omit verificationUri"
    );
    assert!(
        foundry_obj.contains_key("expires_in"),
        "Expected foundry wire output to include expires_in"
    );
    assert!(
        !foundry_obj.contains_key("expiresIn"),
        "Expected foundry wire output to omit expiresIn"
    );
    assert!(
        foundry_obj.contains_key("interval"),
        "Expected foundry wire output to include interval"
    );
    let foundry_restored = DeviceAuthorization::from_wire("foundry", &foundry_wire, &ctx);
    let foundry_round = foundry_restored.to_wire("foundry");
    let mut foundry_expected: Vec<&String> = foundry_obj.keys().collect();
    let mut foundry_actual: Vec<&String> = foundry_round
        .as_object()
        .expect("to_wire returns an object")
        .keys()
        .collect();
    foundry_expected.sort();
    foundry_actual.sort();
    assert_eq!(
        foundry_expected, foundry_actual,
        "Expected foundry round-trip to preserve wire keys"
    );
}

#[test]
fn test_device_authorization_from_json_invalid() {
    let ctx = LoadContext::default();
    assert!(
        DeviceAuthorization::from_json("{", &ctx).is_err(),
        "malformed JSON must be rejected instead of silently defaulting"
    );
}
