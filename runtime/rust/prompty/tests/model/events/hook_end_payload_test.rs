// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::HookEndPayload;
use prompty::model::HookEndScope;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_hook_end_payload_load_json() {
    let json = r####"
{
  "hookInvocationId": "hook_abc123",
  "hookType": "preToolUse",
  "success": true,
  "durationMs": 12,
  "error": "hook failed"
}
"####;
    let ctx = LoadContext::default();
    let result = HookEndPayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.hook_invocation_id, "hook_abc123");
    assert_eq!(instance.hook_type, "preToolUse");
    assert_eq!(instance.success, true);
    assert!(
        instance.duration_ms.is_some(),
        "Expected duration_ms to be Some"
    );
    assert_eq!(instance.duration_ms.as_ref().unwrap(), &12.0);
    assert!(instance.error.is_some(), "Expected error to be Some");
    assert_eq!(instance.error.as_ref().unwrap(), &"hook failed");
}

#[test]
fn test_hook_end_payload_load_yaml() {
    let yaml = r####"
hookInvocationId: hook_abc123
hookType: preToolUse
success: true
durationMs: 12
error: hook failed

"####;
    let ctx = LoadContext::default();
    let result = HookEndPayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.hook_invocation_id, "hook_abc123");
    assert_eq!(instance.hook_type, "preToolUse");
    assert_eq!(instance.success, true);
    assert!(
        instance.duration_ms.is_some(),
        "Expected duration_ms to be Some"
    );
    assert!(instance.error.is_some(), "Expected error to be Some");
}

#[test]
fn test_hook_end_payload_roundtrip() {
    let json = r####"
{
  "hookInvocationId": "hook_abc123",
  "hookType": "preToolUse",
  "success": true,
  "durationMs": 12,
  "error": "hook failed"
}
"####;
    let load_ctx = LoadContext::default();
    let result = HookEndPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}

#[test]
fn test_hook_end_payload_serde_roundtrip() {
    let json = r####"
{
  "hookInvocationId": "hook_abc123",
  "hookType": "preToolUse",
  "success": true,
  "durationMs": 12,
  "error": "hook failed"
}
"####;
    let instance: HookEndPayload =
        serde_json::from_str(json).expect("serde should deserialize canonical JSON");
    let value = serde_json::to_value(&instance).expect("serde should serialize");
    let canonical: serde_json::Value = serde_json::from_str(json).expect("canonical json parses");
    assert_eq!(
        value,
        instance.to_value(&SaveContext::default()),
        "serde serialize must equal canonical to_value"
    );
    assert_eq!(
        instance,
        HookEndPayload::load_from_value(&canonical, &LoadContext::default()),
        "serde deserialize must equal canonical load_from_value"
    );
    let reparsed: HookEndPayload =
        serde_json::from_value(value).expect("serde should re-deserialize");
    assert_eq!(instance, reparsed, "serde round-trip must be stable");
}
