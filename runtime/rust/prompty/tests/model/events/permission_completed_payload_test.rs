// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::PermissionCompletedPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_permission_completed_payload_load_json() {
    let json = r####"
{
  "requestId": "perm_abc123",
  "toolCallId": "call_abc123",
  "permission": "tool.execute",
  "approved": true,
  "reason": "user_approved"
}
"####;
    let ctx = LoadContext::default();
    let result = PermissionCompletedPayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert!(
        instance.request_id.is_some(),
        "Expected request_id to be Some"
    );
    assert_eq!(instance.request_id.as_ref().unwrap(), &"perm_abc123");
    assert!(
        instance.tool_call_id.is_some(),
        "Expected tool_call_id to be Some"
    );
    assert_eq!(instance.tool_call_id.as_ref().unwrap(), &"call_abc123");
    assert_eq!(instance.permission, "tool.execute");
    assert_eq!(instance.approved, true);
    assert!(instance.reason.is_some(), "Expected reason to be Some");
    assert_eq!(instance.reason.as_ref().unwrap(), &"user_approved");
}

#[test]
fn test_permission_completed_payload_load_yaml() {
    let yaml = r####"
requestId: perm_abc123
toolCallId: call_abc123
permission: tool.execute
approved: true
reason: user_approved

"####;
    let ctx = LoadContext::default();
    let result = PermissionCompletedPayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert!(
        instance.request_id.is_some(),
        "Expected request_id to be Some"
    );
    assert!(
        instance.tool_call_id.is_some(),
        "Expected tool_call_id to be Some"
    );
    assert_eq!(instance.permission, "tool.execute");
    assert_eq!(instance.approved, true);
    assert!(instance.reason.is_some(), "Expected reason to be Some");
}

#[test]
fn test_permission_completed_payload_roundtrip() {
    let json = r####"
{
  "requestId": "perm_abc123",
  "toolCallId": "call_abc123",
  "permission": "tool.execute",
  "approved": true,
  "reason": "user_approved"
}
"####;
    let load_ctx = LoadContext::default();
    let result = PermissionCompletedPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
