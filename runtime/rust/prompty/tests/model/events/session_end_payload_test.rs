// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::SessionEndPayload;
use prompty::model::SessionEndStatus;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_session_end_payload_load_json() {
    let json = r####"
{
  "sessionId": "sess_abc123",
  "status": "success",
  "reason": "complete",
  "durationMs": 12500
}
"####;
    let ctx = LoadContext::default();
    let result = SessionEndPayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert!(
        instance.session_id.is_some(),
        "Expected session_id to be Some"
    );
    assert_eq!(instance.session_id.as_ref().unwrap(), &"sess_abc123");
    assert!(instance.status.is_some(), "Expected status to be Some");
    assert_eq!(
        instance.status.as_ref().unwrap(),
        &SessionEndStatus::Success
    );
    assert!(instance.reason.is_some(), "Expected reason to be Some");
    assert_eq!(instance.reason.as_ref().unwrap(), &"complete");
    assert!(
        instance.duration_ms.is_some(),
        "Expected duration_ms to be Some"
    );
    assert_eq!(instance.duration_ms.as_ref().unwrap(), &12500.0);
}

#[test]
fn test_session_end_payload_load_yaml() {
    let yaml = r####"
sessionId: sess_abc123
status: success
reason: complete
durationMs: 12500

"####;
    let ctx = LoadContext::default();
    let result = SessionEndPayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert!(
        instance.session_id.is_some(),
        "Expected session_id to be Some"
    );
    assert!(instance.status.is_some(), "Expected status to be Some");
    assert!(instance.reason.is_some(), "Expected reason to be Some");
    assert!(
        instance.duration_ms.is_some(),
        "Expected duration_ms to be Some"
    );
}

#[test]
fn test_session_end_payload_roundtrip() {
    let json = r####"
{
  "sessionId": "sess_abc123",
  "status": "success",
  "reason": "complete",
  "durationMs": 12500
}
"####;
    let load_ctx = LoadContext::default();
    let result = SessionEndPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
