// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::ThinkingEventPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_thinking_event_payload_load_json() {
    let json = r####"
{
  "token": "Let me consider..."
}
"####;
    let ctx = LoadContext::default();
    let result = ThinkingEventPayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.token, "Let me consider...");
}

#[test]
fn test_thinking_event_payload_load_yaml() {
    let yaml = r####"
token: Let me consider...

"####;
    let ctx = LoadContext::default();
    let result = ThinkingEventPayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.token, "Let me consider...");
}

#[test]
fn test_thinking_event_payload_roundtrip() {
    let json = r####"
{
  "token": "Let me consider..."
}
"####;
    let load_ctx = LoadContext::default();
    let result = ThinkingEventPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
