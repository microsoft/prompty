// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use prompty::model::TurnStartPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_turn_start_payload_load_json() {
    let json = r####"
{
  "agent": "weather-agent",
  "maxIterations": 10
}
"####;
    let ctx = LoadContext::default();
    let result = TurnStartPayload::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.agent.is_some(), "Expected agent to be Some");
    assert_eq!(instance.agent.as_ref().unwrap(), &"weather-agent");
    assert!(instance.max_iterations.is_some(), "Expected max_iterations to be Some");
    assert_eq!(instance.max_iterations.as_ref().unwrap(), &10);
}

#[test]
fn test_turn_start_payload_load_yaml() {
    let yaml = r####"
agent: weather-agent
maxIterations: 10

"####;
    let ctx = LoadContext::default();
    let result = TurnStartPayload::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.agent.is_some(), "Expected agent to be Some");
    assert!(instance.max_iterations.is_some(), "Expected max_iterations to be Some");
}

#[test]
fn test_turn_start_payload_roundtrip() {
    let json = r####"
{
  "agent": "weather-agent",
  "maxIterations": 10
}
"####;
    let load_ctx = LoadContext::default();
    let result = TurnStartPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}
