// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::CompactionCompletePayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_compaction_complete_payload_load_json() {
    let json = r####"
{
  "removed": 5,
  "remaining": 3,
  "summaryLength": 1200
}
"####;
    let ctx = LoadContext::default();
    let result = CompactionCompletePayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.removed, 5);
    assert_eq!(instance.remaining, 3);
    assert!(
        instance.summary_length.is_some(),
        "Expected summary_length to be Some"
    );
    assert_eq!(instance.summary_length.as_ref().unwrap(), &1200);
}

#[test]
fn test_compaction_complete_payload_load_yaml() {
    let yaml = r####"
removed: 5
remaining: 3
summaryLength: 1200

"####;
    let ctx = LoadContext::default();
    let result = CompactionCompletePayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.removed, 5);
    assert_eq!(instance.remaining, 3);
    assert!(
        instance.summary_length.is_some(),
        "Expected summary_length to be Some"
    );
}

#[test]
fn test_compaction_complete_payload_roundtrip() {
    let json = r####"
{
  "removed": 5,
  "remaining": 3,
  "summaryLength": 1200
}
"####;
    let load_ctx = LoadContext::default();
    let result = CompactionCompletePayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
