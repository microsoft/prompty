// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::TurnEvent;
use prompty::model::TurnEventType;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_turn_event_load_json() {
    let json = r####"
{
  "id": "evt_abc123",
  "timestamp": "2026-06-09T20:00:00Z",
  "turnId": "turn_001",
  "iteration": 0,
  "parentId": "evt_parent",
  "spanId": "span_tool_001"
}
"####;
    let ctx = LoadContext::default();
    let result = TurnEvent::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.id, "evt_abc123");
    assert_eq!(instance.timestamp, "2026-06-09T20:00:00Z");
    assert!(instance.turn_id.is_some(), "Expected turn_id to be Some");
    assert_eq!(instance.turn_id.as_ref().unwrap(), &"turn_001");
    assert!(
        instance.iteration.is_some(),
        "Expected iteration to be Some"
    );
    assert_eq!(instance.iteration.as_ref().unwrap(), &0);
    assert!(
        instance.parent_id.is_some(),
        "Expected parent_id to be Some"
    );
    assert_eq!(instance.parent_id.as_ref().unwrap(), &"evt_parent");
    assert!(instance.span_id.is_some(), "Expected span_id to be Some");
    assert_eq!(instance.span_id.as_ref().unwrap(), &"span_tool_001");
}

#[test]
fn test_turn_event_load_yaml() {
    let yaml = r####"
id: evt_abc123
timestamp: "2026-06-09T20:00:00Z"
turnId: turn_001
iteration: 0
parentId: evt_parent
spanId: span_tool_001

"####;
    let ctx = LoadContext::default();
    let result = TurnEvent::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.id, "evt_abc123");
    assert_eq!(instance.timestamp, "2026-06-09T20:00:00Z");
    assert!(instance.turn_id.is_some(), "Expected turn_id to be Some");
    assert!(
        instance.iteration.is_some(),
        "Expected iteration to be Some"
    );
    assert!(
        instance.parent_id.is_some(),
        "Expected parent_id to be Some"
    );
    assert!(instance.span_id.is_some(), "Expected span_id to be Some");
}

#[test]
fn test_turn_event_roundtrip() {
    let json = r####"
{
  "id": "evt_abc123",
  "timestamp": "2026-06-09T20:00:00Z",
  "turnId": "turn_001",
  "iteration": 0,
  "parentId": "evt_parent",
  "spanId": "span_tool_001"
}
"####;
    let load_ctx = LoadContext::default();
    let result = TurnEvent::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}

#[test]
fn test_turn_event_serde_roundtrip() {
    let json = r####"
{
  "id": "evt_abc123",
  "timestamp": "2026-06-09T20:00:00Z",
  "turnId": "turn_001",
  "iteration": 0,
  "parentId": "evt_parent",
  "spanId": "span_tool_001"
}
"####;
    let instance: TurnEvent =
        serde_json::from_str(json).expect("serde should deserialize canonical JSON");
    let value = serde_json::to_value(&instance).expect("serde should serialize");
    let canonical: serde_json::Value = serde_json::from_str(json).expect("canonical json parses");
    assert_eq!(
        value,
        instance.to_value(&SaveContext::default()),
        "serde serialize must equal canonical to_value"
    );
    assert_eq!(
        instance,
        TurnEvent::load_from_value(&canonical, &LoadContext::default()),
        "serde deserialize must equal canonical load_from_value"
    );
    let reparsed: TurnEvent = serde_json::from_value(value).expect("serde should re-deserialize");
    assert_eq!(instance, reparsed, "serde round-trip must be stable");
}
