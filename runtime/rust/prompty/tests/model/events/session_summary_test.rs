// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use prompty::model::SessionSummary;
use prompty::model::SessionSummaryStatus;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_session_summary_load_json() {
    let json = r####"
{
  "sessionId": "sess_abc123",
  "status": "success",
  "turns": 5,
  "checkpoints": 2,
  "durationMs": 12500
}
"####;
    let ctx = LoadContext::default();
    let result = SessionSummary::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.session_id, "sess_abc123");
    assert!(instance.status.is_some(), "Expected status to be Some");
    assert_eq!(instance.status.as_ref().unwrap(), &SessionSummaryStatus::Success);
    assert!(instance.turns.is_some(), "Expected turns to be Some");
    assert_eq!(instance.turns.as_ref().unwrap(), &5);
    assert!(instance.checkpoints.is_some(), "Expected checkpoints to be Some");
    assert_eq!(instance.checkpoints.as_ref().unwrap(), &2);
    assert!(instance.duration_ms.is_some(), "Expected duration_ms to be Some");
    assert_eq!(instance.duration_ms.as_ref().unwrap(), &12500.0);
}

#[test]
fn test_session_summary_load_yaml() {
    let yaml = r####"
sessionId: sess_abc123
status: success
turns: 5
checkpoints: 2
durationMs: 12500

"####;
    let ctx = LoadContext::default();
    let result = SessionSummary::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.session_id, "sess_abc123");
    assert!(instance.status.is_some(), "Expected status to be Some");
    assert!(instance.turns.is_some(), "Expected turns to be Some");
    assert!(instance.checkpoints.is_some(), "Expected checkpoints to be Some");
    assert!(instance.duration_ms.is_some(), "Expected duration_ms to be Some");
}

#[test]
fn test_session_summary_roundtrip() {
    let json = r####"
{
  "sessionId": "sess_abc123",
  "status": "success",
  "turns": 5,
  "checkpoints": 2,
  "durationMs": 12500
}
"####;
    let load_ctx = LoadContext::default();
    let result = SessionSummary::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}
