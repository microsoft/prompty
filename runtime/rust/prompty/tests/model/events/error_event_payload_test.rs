// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::ErrorEventPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_error_event_payload_load_json() {
    let json = r####"
{
  "message": "Rate limit exceeded",
  "errorKind": "rate_limit",
  "phase": "llm"
}
"####;
    let ctx = LoadContext::default();
    let result = ErrorEventPayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.message, "Rate limit exceeded");
    assert!(
        instance.error_kind.is_some(),
        "Expected error_kind to be Some"
    );
    assert_eq!(instance.error_kind.as_ref().unwrap(), &"rate_limit");
    assert!(instance.phase.is_some(), "Expected phase to be Some");
    assert_eq!(instance.phase.as_ref().unwrap(), &"llm");
}

#[test]
fn test_error_event_payload_load_yaml() {
    let yaml = r####"
message: Rate limit exceeded
errorKind: rate_limit
phase: llm

"####;
    let ctx = LoadContext::default();
    let result = ErrorEventPayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.message, "Rate limit exceeded");
    assert!(
        instance.error_kind.is_some(),
        "Expected error_kind to be Some"
    );
    assert!(instance.phase.is_some(), "Expected phase to be Some");
}

#[test]
fn test_error_event_payload_roundtrip() {
    let json = r####"
{
  "message": "Rate limit exceeded",
  "errorKind": "rate_limit",
  "phase": "llm"
}
"####;
    let load_ctx = LoadContext::default();
    let result = ErrorEventPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
