// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use prompty::model::MessagesUpdatedPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_messages_updated_payload_load_json() {
    let json = r####"
{
  "reason": "tool_results",
  "removed": 2
}
"####;
    let ctx = LoadContext::default();
    let result = MessagesUpdatedPayload::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.reason.is_some(), "Expected reason to be Some");
    assert_eq!(instance.reason.as_ref().unwrap(), &"tool_results");
    assert!(instance.removed.is_some(), "Expected removed to be Some");
    assert_eq!(instance.removed.as_ref().unwrap(), &2);
}

#[test]
fn test_messages_updated_payload_load_yaml() {
    let yaml = r####"
reason: tool_results
removed: 2

"####;
    let ctx = LoadContext::default();
    let result = MessagesUpdatedPayload::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.reason.is_some(), "Expected reason to be Some");
    assert!(instance.removed.is_some(), "Expected removed to be Some");
}

#[test]
fn test_messages_updated_payload_roundtrip() {
    let json = r####"
{
  "reason": "tool_results",
  "removed": 2
}
"####;
    let load_ctx = LoadContext::default();
    let result = MessagesUpdatedPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}
