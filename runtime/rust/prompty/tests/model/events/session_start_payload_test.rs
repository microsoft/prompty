// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::SessionStartPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_session_start_payload_load_json() {
    let json = r####"
{
  "sessionId": "sess_abc123",
  "schemaVersion": "1",
  "producer": "prompty-agent",
  "runtime": "typescript",
  "promptyVersion": "2.0.0",
  "startTime": "2026-06-09T20:00:00Z",
  "selectedModel": "gpt-4o-mini",
  "reasoningEffort": "medium"
}
"####;
    let ctx = LoadContext::default();
    let result = SessionStartPayload::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.session_id, "sess_abc123");
    assert!(
        instance.schema_version.is_some(),
        "Expected schema_version to be Some"
    );
    assert_eq!(instance.schema_version.as_ref().unwrap(), &"1");
    assert!(instance.producer.is_some(), "Expected producer to be Some");
    assert_eq!(instance.producer.as_ref().unwrap(), &"prompty-agent");
    assert!(instance.runtime.is_some(), "Expected runtime to be Some");
    assert_eq!(instance.runtime.as_ref().unwrap(), &"typescript");
    assert!(
        instance.prompty_version.is_some(),
        "Expected prompty_version to be Some"
    );
    assert_eq!(instance.prompty_version.as_ref().unwrap(), &"2.0.0");
    assert!(
        instance.start_time.is_some(),
        "Expected start_time to be Some"
    );
    assert_eq!(
        instance.start_time.as_ref().unwrap(),
        &"2026-06-09T20:00:00Z"
    );
    assert!(
        instance.selected_model.is_some(),
        "Expected selected_model to be Some"
    );
    assert_eq!(instance.selected_model.as_ref().unwrap(), &"gpt-4o-mini");
    assert!(
        instance.reasoning_effort.is_some(),
        "Expected reasoning_effort to be Some"
    );
    assert_eq!(instance.reasoning_effort.as_ref().unwrap(), &"medium");
}

#[test]
fn test_session_start_payload_load_yaml() {
    let yaml = r####"
sessionId: sess_abc123
schemaVersion: "1"
producer: prompty-agent
runtime: typescript
promptyVersion: 2.0.0
startTime: "2026-06-09T20:00:00Z"
selectedModel: gpt-4o-mini
reasoningEffort: medium

"####;
    let ctx = LoadContext::default();
    let result = SessionStartPayload::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.session_id, "sess_abc123");
    assert!(
        instance.schema_version.is_some(),
        "Expected schema_version to be Some"
    );
    assert!(instance.producer.is_some(), "Expected producer to be Some");
    assert!(instance.runtime.is_some(), "Expected runtime to be Some");
    assert!(
        instance.prompty_version.is_some(),
        "Expected prompty_version to be Some"
    );
    assert!(
        instance.start_time.is_some(),
        "Expected start_time to be Some"
    );
    assert!(
        instance.selected_model.is_some(),
        "Expected selected_model to be Some"
    );
    assert!(
        instance.reasoning_effort.is_some(),
        "Expected reasoning_effort to be Some"
    );
}

#[test]
fn test_session_start_payload_roundtrip() {
    let json = r####"
{
  "sessionId": "sess_abc123",
  "schemaVersion": "1",
  "producer": "prompty-agent",
  "runtime": "typescript",
  "promptyVersion": "2.0.0",
  "startTime": "2026-06-09T20:00:00Z",
  "selectedModel": "gpt-4o-mini",
  "reasoningEffort": "medium"
}
"####;
    let load_ctx = LoadContext::default();
    let result = SessionStartPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
