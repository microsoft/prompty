// Code generated by Prompty emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use prompty::model::Checkpoint;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_checkpoint_load_json() {
    let json = r####"
{
  "id": "chk_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_001",
  "checkpointNumber": 3,
  "title": "Added harness contracts",
  "createdAt": "2026-06-09T20:00:00Z"
}
"####;
    let ctx = LoadContext::default();
    let result = Checkpoint::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.id.is_some(), "Expected id to be Some");
    assert_eq!(instance.id.as_ref().unwrap(), &"chk_abc123");
    assert!(instance.session_id.is_some(), "Expected session_id to be Some");
    assert_eq!(instance.session_id.as_ref().unwrap(), &"sess_abc123");
    assert!(instance.turn_id.is_some(), "Expected turn_id to be Some");
    assert_eq!(instance.turn_id.as_ref().unwrap(), &"turn_001");
    assert!(instance.checkpoint_number.is_some(), "Expected checkpoint_number to be Some");
    assert_eq!(instance.checkpoint_number.as_ref().unwrap(), &3);
    assert_eq!(instance.title, "Added harness contracts");
    assert!(instance.created_at.is_some(), "Expected created_at to be Some");
    assert_eq!(instance.created_at.as_ref().unwrap(), &"2026-06-09T20:00:00Z");
}

#[test]
fn test_checkpoint_load_yaml() {
    let yaml = r####"
id: chk_abc123
sessionId: sess_abc123
turnId: turn_001
checkpointNumber: 3
title: Added harness contracts
createdAt: "2026-06-09T20:00:00Z"

"####;
    let ctx = LoadContext::default();
    let result = Checkpoint::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.id.is_some(), "Expected id to be Some");
    assert!(instance.session_id.is_some(), "Expected session_id to be Some");
    assert!(instance.turn_id.is_some(), "Expected turn_id to be Some");
    assert!(instance.checkpoint_number.is_some(), "Expected checkpoint_number to be Some");
    assert_eq!(instance.title, "Added harness contracts");
    assert!(instance.created_at.is_some(), "Expected created_at to be Some");
}

#[test]
fn test_checkpoint_roundtrip() {
    let json = r####"
{
  "id": "chk_abc123",
  "sessionId": "sess_abc123",
  "turnId": "turn_001",
  "checkpointNumber": 3,
  "title": "Added harness contracts",
  "createdAt": "2026-06-09T20:00:00Z"
}
"####;
    let load_ctx = LoadContext::default();
    let result = Checkpoint::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}
