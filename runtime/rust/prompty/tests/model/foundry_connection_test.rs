
// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::FoundryConnection;


#[test]
fn test_foundry_connection_load_json() {
    let json = r####"
{
  "kind": "foundry",
  "endpoint": "https://myresource.services.ai.azure.com/api/projects/myproject",
  "name": "my-openai-connection",
  "connectionType": "model"
}
"####;
    let result = FoundryConnection::from_json(json);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "foundry");
    assert_eq!(instance.endpoint, "https://myresource.services.ai.azure.com/api/projects/myproject");
    assert!(instance.name.is_some(), "Expected name to be Some");
    assert_eq!(instance.name.as_ref().unwrap(), &"my-openai-connection");
    assert!(instance.connection_type.is_some(), "Expected connection_type to be Some");
    assert_eq!(instance.connection_type.as_ref().unwrap(), &"model");
}

#[test]
fn test_foundry_connection_load_yaml() {
    let yaml = r####"
kind: foundry
endpoint: "https://myresource.services.ai.azure.com/api/projects/myproject"
name: my-openai-connection
connectionType: model

"####;
    let result = FoundryConnection::from_yaml(yaml);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "foundry");
    assert_eq!(instance.endpoint, "https://myresource.services.ai.azure.com/api/projects/myproject");
    assert!(instance.name.is_some(), "Expected name to be Some");
    assert!(instance.connection_type.is_some(), "Expected connection_type to be Some");
}

#[test]
fn test_foundry_connection_roundtrip() {
    let json = r####"
{
  "kind": "foundry",
  "endpoint": "https://myresource.services.ai.azure.com/api/projects/myproject",
  "name": "my-openai-connection",
  "connectionType": "model"
}
"####;
    let result = FoundryConnection::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}



