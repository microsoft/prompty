
// Code generated by AgentSchema emitter; DO NOT EDIT.

use prompty::model::McpTool;
use prompty::model::context::{LoadContext, SaveContext};


#[test]
fn test_mcp_tool_load_json() {
    let json = r####"
{
  "kind": "mcp",
  "connection": {
    "kind": "reference"
  },
  "serverName": "My MCP Server",
  "serverDescription": "This tool allows access to MCP services.",
  "approvalMode": {
    "kind": "always"
  },
  "allowedTools": [
    "operation1",
    "operation2"
  ]
}
"####;
    let result = McpTool::from_json(json, &LoadContext::default());
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "mcp");
    assert_eq!(instance.server_name, "My MCP Server");
    assert!(instance.server_description.is_some(), "Expected server_description to be Some");
    assert_eq!(instance.server_description.as_ref().unwrap(), &"This tool allows access to MCP services.");
}

#[test]
fn test_mcp_tool_load_yaml() {
    let yaml = r####"
kind: mcp
connection:
  kind: reference
serverName: My MCP Server
serverDescription: This tool allows access to MCP services.
approvalMode:
  kind: always
allowedTools:
  - operation1
  - operation2

"####;
    let result = McpTool::from_yaml(yaml, &LoadContext::default());
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.kind, "mcp");
    assert_eq!(instance.server_name, "My MCP Server");
    assert!(instance.server_description.is_some(), "Expected server_description to be Some");
}

#[test]
fn test_mcp_tool_roundtrip() {
    let json = r####"
{
  "kind": "mcp",
  "connection": {
    "kind": "reference"
  },
  "serverName": "My MCP Server",
  "serverDescription": "This tool allows access to MCP services.",
  "approvalMode": {
    "kind": "always"
  },
  "allowedTools": [
    "operation1",
    "operation2"
  ]
}
"####;
    let result = McpTool::from_json(json, &LoadContext::default());
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json(&SaveContext::default());
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}



