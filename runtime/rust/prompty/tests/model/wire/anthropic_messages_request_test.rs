// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::AnthropicMessagesRequest;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_anthropic_messages_request_load_json() {
    let json = r####"
{
  "model": "claude-sonnet-4-20250514",
  "max_tokens": 4096,
  "system": "You are a helpful assistant.",
  "temperature": 0.7,
  "top_p": 0.9,
  "top_k": 40,
  "stop_sequences": [
    "\n\nHuman:"
  ]
}
"####;
    let ctx = LoadContext::default();
    let result = AnthropicMessagesRequest::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.model, "claude-sonnet-4-20250514");
    assert_eq!(instance.max_tokens, 4096);
    assert!(instance.system.is_some(), "Expected system to be Some");
    assert_eq!(
        instance.system.as_ref().unwrap(),
        &"You are a helpful assistant."
    );
    assert!(
        instance.temperature.is_some(),
        "Expected temperature to be Some"
    );
    assert_eq!(instance.temperature.as_ref().unwrap(), &0.7);
    assert!(instance.top_p.is_some(), "Expected top_p to be Some");
    assert_eq!(instance.top_p.as_ref().unwrap(), &0.9);
    assert!(instance.top_k.is_some(), "Expected top_k to be Some");
    assert_eq!(instance.top_k.as_ref().unwrap(), &40);
}

#[test]
fn test_anthropic_messages_request_load_yaml() {
    let yaml = r####"
model: claude-sonnet-4-20250514
max_tokens: 4096
system: You are a helpful assistant.
temperature: 0.7
top_p: 0.9
top_k: 40
stop_sequences:
  - "\n\nHuman:"

"####;
    let ctx = LoadContext::default();
    let result = AnthropicMessagesRequest::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.model, "claude-sonnet-4-20250514");
    assert_eq!(instance.max_tokens, 4096);
    assert!(instance.system.is_some(), "Expected system to be Some");
    assert!(
        instance.temperature.is_some(),
        "Expected temperature to be Some"
    );
    assert!(instance.top_p.is_some(), "Expected top_p to be Some");
    assert!(instance.top_k.is_some(), "Expected top_k to be Some");
}

#[test]
fn test_anthropic_messages_request_roundtrip() {
    let json = r####"
{
  "model": "claude-sonnet-4-20250514",
  "max_tokens": 4096,
  "system": "You are a helpful assistant.",
  "temperature": 0.7,
  "top_p": 0.9,
  "top_k": 40,
  "stop_sequences": [
    "\n\nHuman:"
  ]
}
"####;
    let load_ctx = LoadContext::default();
    let result = AnthropicMessagesRequest::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}

#[test]
fn test_anthropic_messages_request_serde_roundtrip() {
    let json = r####"
{
  "model": "claude-sonnet-4-20250514",
  "max_tokens": 4096,
  "system": "You are a helpful assistant.",
  "temperature": 0.7,
  "top_p": 0.9,
  "top_k": 40,
  "stop_sequences": [
    "\n\nHuman:"
  ]
}
"####;
    let instance: AnthropicMessagesRequest =
        serde_json::from_str(json).expect("serde should deserialize canonical JSON");
    let value = serde_json::to_value(&instance).expect("serde should serialize");
    let canonical: serde_json::Value = serde_json::from_str(json).expect("canonical json parses");
    assert_eq!(
        value,
        instance.to_value(&SaveContext::default()),
        "serde serialize must equal canonical to_value"
    );
    assert_eq!(
        instance,
        AnthropicMessagesRequest::load_from_value(&canonical, &LoadContext::default()),
        "serde deserialize must equal canonical load_from_value"
    );
    let reparsed: AnthropicMessagesRequest =
        serde_json::from_value(value).expect("serde should re-deserialize");
    assert_eq!(instance, reparsed, "serde round-trip must be stable");
}
