// Code generated by AgentSchema emitter; DO NOT EDIT.

use prompty::model::CompactionConfig;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_compaction_config_load_json() {
    let json = r####"
{
  "strategy": "summarize",
  "budget": 50000,
  "options": {
    "preserveSystemMessages": true
  }
}
"####;
    let ctx = LoadContext::default();
    let result = CompactionConfig::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.strategy.is_some(), "Expected strategy to be Some");
    assert_eq!(instance.strategy.as_ref().unwrap(), &"summarize");
    assert!(instance.budget.is_some(), "Expected budget to be Some");
    assert_eq!(instance.budget.as_ref().unwrap(), &50000);
}

#[test]
fn test_compaction_config_load_yaml() {
    let yaml = r####"
strategy: summarize
budget: 50000
options:
  preserveSystemMessages: true

"####;
    let ctx = LoadContext::default();
    let result = CompactionConfig::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.strategy.is_some(), "Expected strategy to be Some");
    assert!(instance.budget.is_some(), "Expected budget to be Some");
}

#[test]
fn test_compaction_config_roundtrip() {
    let json = r####"
{
  "strategy": "summarize",
  "budget": 50000,
  "options": {
    "preserveSystemMessages": true
  }
}
"####;
    let load_ctx = LoadContext::default();
    let result = CompactionConfig::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}

