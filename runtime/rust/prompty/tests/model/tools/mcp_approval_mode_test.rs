// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    clippy::all
)]

use prompty::model::McpApprovalMode;
use prompty::model::context::{LoadContext, SaveContext};
use prompty::model::mcpApprovalModeKind;

#[test]
fn test_mcp_approval_mode_load_json() {
    let json = r####"
{
  "kind": "never",
  "alwaysRequireApprovalTools": [
    "operation1"
  ],
  "neverRequireApprovalTools": [
    "operation2"
  ]
}
"####;
    let ctx = LoadContext::default();
    let result = McpApprovalMode::from_json(json, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.kind, mcpApprovalModeKind::Never);
}

#[test]
fn test_mcp_approval_mode_load_yaml() {
    let yaml = r####"
kind: never
alwaysRequireApprovalTools:
  - operation1
neverRequireApprovalTools:
  - operation2

"####;
    let ctx = LoadContext::default();
    let result = McpApprovalMode::from_yaml(yaml, &ctx);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.kind, mcpApprovalModeKind::Never);
}

#[test]
fn test_mcp_approval_mode_roundtrip() {
    let json = r####"
{
  "kind": "never",
  "alwaysRequireApprovalTools": [
    "operation1"
  ],
  "neverRequireApprovalTools": [
    "operation2"
  ]
}
"####;
    let load_ctx = LoadContext::default();
    let result = McpApprovalMode::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}

#[test]
fn test_mcp_approval_mode_serde_roundtrip() {
    let json = r####"
{
  "kind": "never",
  "alwaysRequireApprovalTools": [
    "operation1"
  ],
  "neverRequireApprovalTools": [
    "operation2"
  ]
}
"####;
    let instance: McpApprovalMode =
        serde_json::from_str(json).expect("serde should deserialize canonical JSON");
    let value = serde_json::to_value(&instance).expect("serde should serialize");
    let canonical: serde_json::Value = serde_json::from_str(json).expect("canonical json parses");
    assert_eq!(
        value,
        instance.to_value(&SaveContext::default()),
        "serde serialize must equal canonical to_value"
    );
    assert_eq!(
        instance,
        McpApprovalMode::load_from_value(&canonical, &LoadContext::default()),
        "serde deserialize must equal canonical load_from_value"
    );
    let reparsed: McpApprovalMode =
        serde_json::from_value(value).expect("serde should re-deserialize");
    assert_eq!(instance, reparsed, "serde round-trip must be stable");
}

#[test]
fn test_mcp_approval_mode_from_kind() {
    let value = serde_json::json!("never");
    let ctx = LoadContext::default();
    let instance = McpApprovalMode::load_from_value(&value, &ctx);
    assert_eq!(instance.kind, mcpApprovalModeKind::Never);
}
