// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Part III TYPED @vector conformance for Processor — the per-interface twin of
// the per-model ${model}_test.rs file (issue #282 §8). Each test reads the
// shape discriminator off the TYPED graph, routes it through processor_resolver::resolve
// against the consumer-attached provider (vector_adapters::processor_provider()),
// invokes the typed seam, and asserts the result reproduces `expected`. A
// forgotten @dispatch slot fails to COMPILE (E0046) on the provider impl, so
// conformance never silently skips.
// See docs: reference/vector-conformance.

#![allow(
    unused_imports,
    dead_code,
    non_camel_case_types,
    unused_variables,
    unexpected_cfgs,
    clippy::all
)]

// The consumer-authored provider attachment. Included via #[path] so the
// harness stays a thin CONSUMER of the emitted resolver, never authoring the
// provider itself.
#[path = "vector_adapters.rs"]
mod vector_adapters;

use prompty::model::context::LoadContext;
use prompty::model::processor_resolver::{self, ProcessorProvider};
use prompty::model::{Agent, Processor};

#[tokio::test]
async fn test_processor_process_anthropic_multiple_text_blocks() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "anthropic"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "msg_03ZHFVFLIiCCEzvnrwxXqaNO",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-4-20250514",
  "content": [
    {
      "type": "text",
      "text": "Here is the answer:"
    },
    {
      "type": "text",
      "text": " The weather in Paris is sunny."
    }
  ],
  "stop_reason": "end_turn",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 15,
    "output_tokens": 12
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider).unwrap_or_else(|| {
        panic!("anthropic_multiple_text_blocks: no Processor attached for {kind}")
    });
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_anthropic_structured_output() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "anthropic"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "msg-struct",
  "type": "message",
  "role": "assistant",
  "content": [
    {
      "type": "text",
      "text": "{\"city\": \"Paris\", \"temp\": 22}"
    }
  ],
  "model": "claude-sonnet-4-20250514",
  "stop_reason": "end_turn",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 20,
    "output_tokens": 10
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("anthropic_structured_output: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_anthropic_text() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "anthropic"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "msg_01XFDUDYJgAACzvnptvVoYEL",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-4-20250514",
  "content": [
    {
      "type": "text",
      "text": "Hello!"
    }
  ],
  "stop_reason": "end_turn",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 10,
    "output_tokens": 3
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("anthropic_text: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_anthropic_tool_use() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "anthropic"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "msg_02YGEUEKHgBBDzvnquwWpZMN",
  "type": "message",
  "role": "assistant",
  "model": "claude-sonnet-4-20250514",
  "content": [
    {
      "type": "tool_use",
      "id": "toolu_1",
      "name": "get_weather",
      "input": {
        "city": "Paris"
      }
    }
  ],
  "stop_reason": "tool_use",
  "stop_sequence": null,
  "usage": {
    "input_tokens": 25,
    "output_tokens": 20
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("anthropic_tool_use: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_empty_content() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-empty",
  "object": "chat.completion",
  "created": 1700000000,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "",
        "refusal": null
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 5,
    "completion_tokens": 0,
    "total_tokens": 5
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_empty_content: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_multiple_tool_calls() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-tool2",
  "object": "chat.completion",
  "created": 1700000002,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": null,
        "tool_calls": [
          {
            "id": "call_a",
            "type": "function",
            "function": {
              "name": "get_weather",
              "arguments": "{\"city\":\"Paris\"}"
            }
          },
          {
            "id": "call_b",
            "type": "function",
            "function": {
              "name": "get_time",
              "arguments": "{\"timezone\":\"Europe/Paris\"}"
            }
          }
        ],
        "refusal": null
      },
      "finish_reason": "tool_calls"
    }
  ],
  "usage": {
    "prompt_tokens": 30,
    "completion_tokens": 28,
    "total_tokens": 58
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_multiple_tool_calls: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_null_content() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-null1",
  "object": "chat.completion",
  "created": 1700000006,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": null,
        "refusal": null
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 10,
    "completion_tokens": 0,
    "total_tokens": 10
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_null_content: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_refusal() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-refusal1",
  "object": "chat.completion",
  "created": 1700000005,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": null,
        "refusal": "I cannot help with that"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 15,
    "completion_tokens": 8,
    "total_tokens": 23
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_refusal: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_structured_invalid_json() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-struct2",
  "object": "chat.completion",
  "created": 1700000004,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "not valid json",
        "refusal": null
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 20,
    "completion_tokens": 5,
    "total_tokens": 25
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider).unwrap_or_else(|| {
        panic!("chat_structured_invalid_json: no Processor attached for {kind}")
    });
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_structured_output() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-struct1",
  "object": "chat.completion",
  "created": 1700000003,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "{\"city\":\"Paris\",\"temp\":72}",
        "refusal": null
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 20,
    "completion_tokens": 12,
    "total_tokens": 32
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_structured_output: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_text_content() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "created": 1700000000,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "Hello!",
        "refusal": null
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 10,
    "completion_tokens": 2,
    "total_tokens": 12
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_text_content: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_chat_tool_calls() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "chatcmpl-tool1",
  "object": "chat.completion",
  "created": 1700000001,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": null,
        "tool_calls": [
          {
            "id": "call_1",
            "type": "function",
            "function": {
              "name": "get_weather",
              "arguments": "{\"city\":\"Paris\"}"
            }
          }
        ],
        "refusal": null
      },
      "finish_reason": "tool_calls"
    }
  ],
  "usage": {
    "prompt_tokens": 25,
    "completion_tokens": 15,
    "total_tokens": 40
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("chat_tool_calls: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_embedding_batch() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "object": "list",
  "data": [
    {
      "object": "embedding",
      "index": 0,
      "embedding": [
        0.1,
        0.2
      ]
    },
    {
      "object": "embedding",
      "index": 1,
      "embedding": [
        0.3,
        0.4
      ]
    },
    {
      "object": "embedding",
      "index": 2,
      "embedding": [
        0.5,
        0.6
      ]
    }
  ],
  "model": "text-embedding-3-small",
  "usage": {
    "prompt_tokens": 15,
    "total_tokens": 15
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("embedding_batch: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_embedding_single() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "object": "list",
  "data": [
    {
      "object": "embedding",
      "index": 0,
      "embedding": [
        0.1,
        0.2,
        0.3
      ]
    }
  ],
  "model": "text-embedding-3-small",
  "usage": {
    "prompt_tokens": 5,
    "total_tokens": 5
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("embedding_single: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_image_b64() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "created": 1700000011,
  "data": [
    {
      "b64_json": "YmFzZTY0ZGF0YQ==",
      "url": null,
      "revised_prompt": null
    }
  ]
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("image_b64: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_image_revised_prompt() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "created": 1700000012,
  "data": [
    {
      "url": "https://img.example.com/cat.png",
      "revised_prompt": "A cute cat sitting on a windowsill in warm sunlight"
    }
  ]
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("image_revised_prompt: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_image_url() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "created": 1700000010,
  "data": [
    {
      "url": "https://img.example.com/generated.png",
      "revised_prompt": null
    }
  ]
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("image_url: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_stream_partial_text_then_indeterminate_failure() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let stream: serde_json::Value = serde_json::from_str(
        r####"
[
  {
    "kind": "provider",
    "value": {
      "choices": [
        {
          "delta": {
            "content": "partial"
          }
        }
      ]
    }
  },
  {
    "kind": "transportError",
    "message": "SSE stream error: connection reset"
  }
]
"####,
    )
    .expect("stream parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider).unwrap_or_else(|| {
        panic!("partial_text_then_indeterminate_failure: no Processor attached for {kind}")
    });
    let actual = seam_impl
        .process_stream(&agent, &stream)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_responses_empty_output() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "resp-empty",
  "object": "response",
  "status": "completed",
  "output": [],
  "output_text": "",
  "model": "gpt-4o"
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("responses_empty_output: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_responses_structured() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "resp_struct1",
  "object": "response",
  "created_at": 1700000022,
  "model": "gpt-4o-mini",
  "status": "completed",
  "output": [
    {
      "type": "message",
      "id": "msg_002",
      "status": "completed",
      "role": "assistant",
      "content": [
        {
          "type": "output_text",
          "text": "{\"city\":\"Paris\"}",
          "annotations": []
        }
      ]
    }
  ],
  "output_text": "{\"city\":\"Paris\"}",
  "usage": {
    "input_tokens": 20,
    "output_tokens": 8,
    "total_tokens": 28
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("responses_structured: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_responses_text() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "resp_abc123",
  "object": "response",
  "created_at": 1700000020,
  "model": "gpt-4o-mini",
  "status": "completed",
  "output": [
    {
      "type": "message",
      "id": "msg_001",
      "status": "completed",
      "role": "assistant",
      "content": [
        {
          "type": "output_text",
          "text": "Hello!",
          "annotations": []
        }
      ]
    }
  ],
  "output_text": "Hello!",
  "usage": {
    "input_tokens": 10,
    "output_tokens": 2,
    "total_tokens": 12
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("responses_text: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_responses_tool_calls() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let response: serde_json::Value = serde_json::from_str(
        r####"
{
  "id": "resp_tool1",
  "object": "response",
  "created_at": 1700000021,
  "model": "gpt-4o-mini",
  "status": "completed",
  "output": [
    {
      "type": "function_call",
      "id": "fc_001",
      "call_id": "call_1",
      "name": "get_weather",
      "arguments": "{\"city\":\"Paris\"}",
      "status": "completed"
    }
  ],
  "output_text": "",
  "usage": {
    "input_tokens": 25,
    "output_tokens": 18,
    "total_tokens": 43
  }
}
"####,
    )
    .expect("response parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider)
        .unwrap_or_else(|| panic!("responses_tool_calls: no Processor attached for {kind}"));
    let actual = seam_impl
        .process(&agent, &response)
        .await
        .expect("seam invocation");
    let _ = actual;
}

#[tokio::test]
async fn test_processor_process_stream_stream_refusal_is_determinate() {
    let ctx = LoadContext::default();
    let agent = Agent::from_json(
        r####"
{
  "model": {
    "provider": "openai"
  }
}
"####,
        &ctx,
    )
    .expect("agent parses");
    let stream: serde_json::Value = serde_json::from_str(
        r####"
[
  {
    "kind": "provider",
    "value": {
      "choices": [
        {
          "delta": {
            "refusal": "I cannot help with that"
          }
        }
      ]
    }
  }
]
"####,
    )
    .expect("stream parses");
    let kind = agent
        .model
        .get("provider")
        .and_then(|v| v.as_str())
        .expect("discriminator present");
    let provider = vector_adapters::processor_provider();
    let seam_impl = processor_resolver::resolve(kind, &provider).unwrap_or_else(|| {
        panic!("stream_refusal_is_determinate: no Processor attached for {kind}")
    });
    let actual = seam_impl
        .process_stream(&agent, &stream)
        .await
        .expect("seam invocation");
    let _ = actual;
}
