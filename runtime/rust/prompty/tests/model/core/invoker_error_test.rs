// Code generated by AgentSchema emitter; DO NOT EDIT.

#![allow(unused_imports, dead_code, non_camel_case_types, unused_variables, clippy::all)]

use prompty::model::InvokerError;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_invoker_error_load_json() {
    let json = r####"
{
  "message": "No renderer registered for key: jinja2",
  "component": "renderer",
  "key": "jinja2"
}
"####;
    let ctx = LoadContext::default();
    let result = InvokerError::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.message, "No renderer registered for key: jinja2");
    assert_eq!(instance.component, "renderer");
    assert_eq!(instance.key, "jinja2");
}

#[test]
fn test_invoker_error_load_yaml() {
    let yaml = r####"
message: "No renderer registered for key: jinja2"
component: renderer
key: jinja2

"####;
    let ctx = LoadContext::default();
    let result = InvokerError::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.message, "No renderer registered for key: jinja2");
    assert_eq!(instance.component, "renderer");
    assert_eq!(instance.key, "jinja2");
}

#[test]
fn test_invoker_error_roundtrip() {
    let json = r####"
{
  "message": "No renderer registered for key: jinja2",
  "component": "renderer",
  "key": "jinja2"
}
"####;
    let load_ctx = LoadContext::default();
    let result = InvokerError::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}

