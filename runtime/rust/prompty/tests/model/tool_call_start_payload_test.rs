// Code generated by AgentSchema emitter; DO NOT EDIT.

use prompty::model::ToolCallStartPayload;
use prompty::model::context::{LoadContext, SaveContext};

#[test]
fn test_tool_call_start_payload_load_json() {
    let json = r####"
{
  "name": "get_weather",
  "arguments": "{\"city\": \"Paris\"}"
}
"####;
    let ctx = LoadContext::default();
    let result = ToolCallStartPayload::from_json(json, &ctx);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.name, "get_weather");
    assert_eq!(instance.arguments, "{\"city\": \"Paris\"}");
}

#[test]
fn test_tool_call_start_payload_load_yaml() {
    let yaml = r####"
name: get_weather
arguments: "{\"city\": \"Paris\"}"

"####;
    let ctx = LoadContext::default();
    let result = ToolCallStartPayload::from_yaml(yaml, &ctx);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert_eq!(instance.name, "get_weather");
    assert_eq!(instance.arguments, "{\"city\": \"Paris\"}");
}

#[test]
fn test_tool_call_start_payload_roundtrip() {
    let json = r####"
{
  "name": "get_weather",
  "arguments": "{\"city\": \"Paris\"}"
}
"####;
    let load_ctx = LoadContext::default();
    let result = ToolCallStartPayload::from_json(json, &load_ctx);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let save_ctx = SaveContext::default();
    let json_output = instance.to_json(&save_ctx);
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}

