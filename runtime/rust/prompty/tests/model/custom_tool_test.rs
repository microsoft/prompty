
// Code generated by AgentSchema emitter; DO NOT EDIT.

use prompty::model::CustomTool;
use prompty::model::context::{LoadContext, SaveContext};


#[test]
fn test_custom_tool_load_json() {
    let json = r####"
{
  "connection": {
    "kind": "reference"
  },
  "options": {
    "timeout": 30,
    "retries": 3
  }
}
"####;
    let result = CustomTool::from_json(json, &LoadContext::default());
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    let _ = instance; // load succeeded, no scalar properties to validate
}

#[test]
fn test_custom_tool_load_yaml() {
    let yaml = r####"
connection:
  kind: reference
options:
  timeout: 30
  retries: 3

"####;
    let result = CustomTool::from_yaml(yaml, &LoadContext::default());
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    let _ = instance; // load succeeded, no scalar properties to validate
}

#[test]
fn test_custom_tool_roundtrip() {
    let json = r####"
{
  "connection": {
    "kind": "reference"
  },
  "options": {
    "timeout": 30,
    "retries": 3
  }
}
"####;
    let result = CustomTool::from_json(json, &LoadContext::default());
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json(&SaveContext::default());
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}



