
// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::PropertySchema;


#[test]
fn test_property_schema_load_json() {
    let json = r####"
{
  "examples": [
    {
      "key": "value"
    }
  ],
  "strict": true,
  "properties": {
    "firstName": {
      "kind": "string",
      "sample": "Jane"
    },
    "lastName": {
      "kind": "string",
      "sample": "Doe"
    },
    "question": {
      "kind": "string",
      "sample": "What is the meaning of life?"
    }
  }
}
"####;
    let result = PropertySchema::from_json(json);
    assert!(result.is_ok(), "Failed to load from JSON: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.strict.is_some(), "Expected strict to be Some");
    assert_eq!(instance.strict.as_ref().unwrap(), &true);
}

#[test]
fn test_property_schema_load_yaml() {
    let yaml = r####"
examples:
  - key: value
strict: true
properties:
  firstName:
    kind: string
    sample: Jane
  lastName:
    kind: string
    sample: Doe
  question:
    kind: string
    sample: What is the meaning of life?

"####;
    let result = PropertySchema::from_yaml(yaml);
    assert!(result.is_ok(), "Failed to load from YAML: {:?}", result.err());
    let instance = result.unwrap();
    assert!(instance.strict.is_some(), "Expected strict to be Some");
}

#[test]
fn test_property_schema_roundtrip() {
    let json = r####"
{
  "examples": [
    {
      "key": "value"
    }
  ],
  "strict": true,
  "properties": {
    "firstName": {
      "kind": "string",
      "sample": "Jane"
    },
    "lastName": {
      "kind": "string",
      "sample": "Doe"
    },
    "question": {
      "kind": "string",
      "sample": "What is the meaning of life?"
    }
  }
}
"####;
    let result = PropertySchema::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(json_output.is_ok(), "Failed to serialize to JSON: {:?}", json_output.err());
}



