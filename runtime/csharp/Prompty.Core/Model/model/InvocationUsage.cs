// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Complete cumulative token usage for one completed model invocation.
    ///
    /// Providers emit this value at most once per invocation. `totalTokens` uses
    ///
    /// the provider total when available; otherwise the provider adapter computes
    ///
    /// it as `inputTokens + outputTokens`.
    /// </summary>
public partial class InvocationUsage
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="InvocationUsage"/>.
    /// </summary>
#pragma warning disable CS8618
    public InvocationUsage()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Number of input tokens consumed by the completed invocation
    /// </summary>
    public long InputTokens { get; set; }

    /// <summary>
    /// Number of output tokens generated by the completed invocation
    /// </summary>
    public long OutputTokens { get; set; }

    /// <summary>
    /// Total tokens consumed by the completed invocation
    /// </summary>
    public long TotalTokens { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a InvocationUsage instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded InvocationUsage instance.</returns>
    public static InvocationUsage Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new InvocationUsage();


        if (data.TryGetValue("inputTokens", out var inputTokensValue) && inputTokensValue is not null)
        {
            instance.InputTokens = Convert.ToInt64(inputTokensValue);
        }

        if (data.TryGetValue("outputTokens", out var outputTokensValue) && outputTokensValue is not null)
        {
            instance.OutputTokens = Convert.ToInt64(outputTokensValue);
        }

        if (data.TryGetValue("totalTokens", out var totalTokensValue) && totalTokensValue is not null)
        {
            instance.TotalTokens = Convert.ToInt64(totalTokensValue);
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the InvocationUsage instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["inputTokens"] = obj.InputTokens;


        result["outputTokens"] = obj.OutputTokens;


        result["totalTokens"] = obj.TotalTokens;


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }

    /// <summary>
    /// Convert this instance to a provider-specific wire-format dictionary.
    /// </summary>
    /// <param name="provider">The provider name (e.g., "openai", "anthropic").</param>
    /// <returns>A dictionary with provider-specific field names.</returns>
    public Dictionary<string, object?> ToWire(string provider)
    {
        var data = Save();
        var result = new Dictionary<string, object?>();
        var wireMap = new Dictionary<string, Dictionary<string, string>>
        {
            ["inputTokens"] = new Dictionary<string, string> { ["openai"] = "prompt_tokens", ["anthropic"] = "input_tokens" },
            ["outputTokens"] = new Dictionary<string, string> { ["openai"] = "completion_tokens", ["anthropic"] = "output_tokens" },
            ["totalTokens"] = new Dictionary<string, string> { ["openai"] = "total_tokens" },
        };
        foreach (var (key, value) in data)
        {
            if (wireMap.TryGetValue(key, out var mapping) && mapping.TryGetValue(provider, out var wireName))
                result[wireName] = value;
        }
        return result;
    }


    /// <summary>
    /// Convert the InvocationUsage instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the InvocationUsage instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a InvocationUsage instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded InvocationUsage instance.</returns>
    public static InvocationUsage FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a InvocationUsage instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded InvocationUsage instance.</returns>
    public static InvocationUsage FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
