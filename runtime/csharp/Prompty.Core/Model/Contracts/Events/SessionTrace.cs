// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
#nullable enable

using System.Linq;
using System.Text.Json;
using System.Threading;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Portable replay container for an outer harness session.
    /// </summary>
public partial class SessionTrace
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="SessionTrace"/>.
    /// </summary>
#pragma warning disable CS8618
    public SessionTrace()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Trace schema version
    /// </summary>
    public string Version { get; set; } = "1";

    /// <summary>
    /// Runtime name that produced the trace
    /// </summary>
    public string? Runtime { get; set; }

    /// <summary>
    /// Prompty library version that produced the trace
    /// </summary>
    public string? PromptyVersion { get; set; }

    /// <summary>
    /// Stable session identifier
    /// </summary>
    public string? SessionId { get; set; }

    /// <summary>
    /// Recorded session events in emission order
    /// </summary>
    public IList<SessionEvent> Events { get; set; } = [];

    /// <summary>
    /// Recorded turn traces associated with the session
    /// </summary>
    public IList<TurnTrace>? Turns { get; set; }

    /// <summary>
    /// Checkpoints created during the session
    /// </summary>
    public IList<Checkpoint>? Checkpoints { get; set; }

    /// <summary>
    /// Compact trajectory records associated with the session
    /// </summary>
    public IList<TrajectoryEvent>? Trajectory { get; set; }

    /// <summary>
    /// Files observed or touched during the session
    /// </summary>
    public IList<SessionFileRef>? Files { get; set; }

    /// <summary>
    /// Non-file references observed during the session
    /// </summary>
    public IList<SessionRef>? Refs { get; set; }

    /// <summary>
    /// Optional summary computed from the event stream
    /// </summary>
    public SessionSummary? Summary { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a SessionTrace instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded SessionTrace instance.</returns>
    public static SessionTrace Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        context ??= new LoadContext();
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new SessionTrace();


        if (data.TryGetValue("version", out var versionValue) && versionValue is not null)
        {
            instance.Version = versionValue.ToString()!;
        }

        if (data.TryGetValue("runtime", out var runtimeValue) && runtimeValue is not null)
        {
            instance.Runtime = runtimeValue.ToString()!;
        }

        if (data.TryGetValue("promptyVersion", out var promptyVersionValue) && promptyVersionValue is not null)
        {
            instance.PromptyVersion = promptyVersionValue.ToString()!;
        }

        if (data.TryGetValue("sessionId", out var sessionIdValue) && sessionIdValue is not null)
        {
            instance.SessionId = sessionIdValue.ToString()!;
        }

        if (data.TryGetValue("events", out var eventsValue) && eventsValue is not null)
        {
            instance.Events = LoadEvents(eventsValue, context!.At("events"));
        }

        if (data.TryGetValue("turns", out var turnsValue) && turnsValue is not null)
        {
            instance.Turns = LoadTurns(turnsValue, context!.At("turns"));
        }

        if (data.TryGetValue("checkpoints", out var checkpointsValue) && checkpointsValue is not null)
        {
            instance.Checkpoints = LoadCheckpoints(checkpointsValue, context!.At("checkpoints"));
        }

        if (data.TryGetValue("trajectory", out var trajectoryValue) && trajectoryValue is not null)
        {
            instance.Trajectory = LoadTrajectory(trajectoryValue, context!.At("trajectory"));
        }

        if (data.TryGetValue("files", out var filesValue) && filesValue is not null)
        {
            instance.Files = LoadFiles(filesValue, context!.At("files"));
        }

        if (data.TryGetValue("refs", out var refsValue) && refsValue is not null)
        {
            instance.Refs = LoadRefs(refsValue, context!.At("refs"));
        }

        if (data.TryGetValue("summary", out var summaryValue) && summaryValue is not null)
        {
            instance.Summary = SessionSummary.Load(summaryValue.GetDictionary(SessionSummary.ShorthandProperty), context!.At("summary"));
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of SessionEvent from a dictionary or list.
    /// </summary>
    public static IList<SessionEvent> LoadEvents(object data, LoadContext? context)
    {
        var result = new List<SessionEvent>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "events" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'events' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(SessionEvent.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(SessionEvent.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(SessionEvent.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(SessionEvent.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of TurnTrace from a dictionary or list.
    /// </summary>
    public static IList<TurnTrace> LoadTurns(object data, LoadContext? context)
    {
        var result = new List<TurnTrace>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "turns" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'turns' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(TurnTrace.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["version"] = kvp.Value
                    };
                    result.Add(TurnTrace.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(TurnTrace.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(TurnTrace.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of Checkpoint from a dictionary or list.
    /// </summary>
    public static IList<Checkpoint> LoadCheckpoints(object data, LoadContext? context)
    {
        var result = new List<Checkpoint>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "checkpoints" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'checkpoints' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Checkpoint.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(Checkpoint.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Checkpoint.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Checkpoint.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of TrajectoryEvent from a dictionary or list.
    /// </summary>
    public static IList<TrajectoryEvent> LoadTrajectory(object data, LoadContext? context)
    {
        var result = new List<TrajectoryEvent>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "trajectory" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'trajectory' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(TrajectoryEvent.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(TrajectoryEvent.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(TrajectoryEvent.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(TrajectoryEvent.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of SessionFileRef from a dictionary or list.
    /// </summary>
    public static IList<SessionFileRef> LoadFiles(object data, LoadContext? context)
    {
        var result = new List<SessionFileRef>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "files" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'files' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(SessionFileRef.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["sessionId"] = kvp.Value
                    };
                    result.Add(SessionFileRef.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(SessionFileRef.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(SessionFileRef.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of SessionRef from a dictionary or list.
    /// </summary>
    public static IList<SessionRef> LoadRefs(object data, LoadContext? context)
    {
        var result = new List<SessionRef>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "refs" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'refs' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(SessionRef.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["sessionId"] = kvp.Value
                    };
                    result.Add(SessionRef.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(SessionRef.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(SessionRef.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the SessionTrace instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["version"] = obj.Version;


        if (obj.Runtime is not null)
        {
            result["runtime"] = obj.Runtime;
        }


        if (obj.PromptyVersion is not null)
        {
            result["promptyVersion"] = obj.PromptyVersion;
        }


        if (obj.SessionId is not null)
        {
            result["sessionId"] = obj.SessionId;
        }


        result["events"] = SaveEvents(obj.Events, context);


        if (obj.Turns is not null)
        {
            result["turns"] = SaveTurns(obj.Turns, context);
        }


        if (obj.Checkpoints is not null)
        {
            result["checkpoints"] = SaveCheckpoints(obj.Checkpoints, context);
        }


        if (obj.Trajectory is not null)
        {
            result["trajectory"] = SaveTrajectory(obj.Trajectory, context);
        }


        if (obj.Files is not null)
        {
            result["files"] = SaveFiles(obj.Files, context);
        }


        if (obj.Refs is not null)
        {
            result["refs"] = SaveRefs(obj.Refs, context);
        }


        if (obj.Summary is not null)
        {
            result["summary"] = obj.Summary?.Save(context);
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of SessionEvent to object or array format.
    /// </summary>
    public static object SaveEvents(IList<SessionEvent> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of TurnTrace to object or array format.
    /// </summary>
    public static object SaveTurns(IList<TurnTrace> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of Checkpoint to object or array format.
    /// </summary>
    public static object SaveCheckpoints(IList<Checkpoint> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of TrajectoryEvent to object or array format.
    /// </summary>
    public static object SaveTrajectory(IList<TrajectoryEvent> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of SessionFileRef to object or array format.
    /// </summary>
    public static object SaveFiles(IList<SessionFileRef> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of SessionRef to object or array format.
    /// </summary>
    public static object SaveRefs(IList<SessionRef> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the SessionTrace instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the SessionTrace instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a SessionTrace instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded SessionTrace instance.</returns>
    public static SessionTrace FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a SessionTrace instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded SessionTrace instance.</returns>
    public static SessionTrace FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
