// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
#nullable enable

using System.Linq;
using System.Text.Json;
using System.Threading;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// An Agent is the root model produced from a .prompty markdown file for LLM
    ///
    /// prompts. The frontmatter defines structured metadata including model
    ///
    /// configuration, input/output schemas, tools, and template settings. The
    ///
    /// markdown body becomes the instructions.
    ///
    /// This is the single root type for the Prompty schema — there is no abstract base
    ///
    /// class or kind discriminator. A .prompty file always produces an Agent instance.
    ///
    /// Runtime loaders may resolve frontmatter references such as `${env:VAR}` and
    ///
    /// `${file:relative/path}`. File references must be treated as a host-controlled
    ///
    /// capability: by default they are scoped to the containing .prompty file's
    ///
    /// directory tree after canonicalization, and any additional allowed roots must
    ///
    /// be supplied by the host application's load options rather than frontmatter.
    /// </summary>
public partial class Agent
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="Agent"/>.
    /// </summary>
#pragma warning disable CS8618
    public Agent()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Human-readable name of the prompt
    /// </summary>
    public string Name { get; set; } = string.Empty;

    /// <summary>
    /// Display name for UI purposes
    /// </summary>
    public string? DisplayName { get; set; }

    /// <summary>
    /// Description of the prompt's purpose
    /// </summary>
    public string? Description { get; set; }

    /// <summary>
    /// Additional metadata including authors, tags, and other arbitrary properties. Values may be explicit null.
    /// </summary>
    public IDictionary<string, object?>? Metadata { get; set; }

    /// <summary>
    /// Input parameters that participate in template rendering
    /// </summary>
    public IList<Property>? Inputs { get; set; }

    /// <summary>
    /// Expected output format and structure
    /// </summary>
    public IList<Property>? Outputs { get; set; }

    /// <summary>
    /// AI model configuration
    /// </summary>
    public Model? Model { get; set; }

    /// <summary>
    /// Tools available for extended functionality
    /// </summary>
    public IList<Tool>? Tools { get; set; }

    /// <summary>
    /// Template configuration for prompt rendering
    /// </summary>
    public Template? Template { get; set; }

    /// <summary>
    /// Clear directions on what the prompt should do. In .prompty files, this comes from the markdown body.
    /// </summary>
    public string? Instructions { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a Agent instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded Agent instance.</returns>
    public static Agent Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        context ??= new LoadContext();
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new Agent();


        if (data.TryGetValue("name", out var nameValue) && nameValue is not null)
        {
            instance.Name = nameValue.ToString()!;
        }

        if (data.TryGetValue("displayName", out var displayNameValue) && displayNameValue is not null)
        {
            instance.DisplayName = displayNameValue.ToString()!;
        }

        if (data.TryGetValue("description", out var descriptionValue) && descriptionValue is not null)
        {
            instance.Description = descriptionValue.ToString()!;
        }

        if (data.TryGetValue("metadata", out var metadataValue) && metadataValue is not null)
        {
            instance.Metadata = metadataValue.GetDictionary()!;
        }

        if (data.TryGetValue("inputs", out var inputsValue) && inputsValue is not null)
        {
            instance.Inputs = LoadInputs(inputsValue, context!.At("inputs"));
        }

        if (data.TryGetValue("outputs", out var outputsValue) && outputsValue is not null)
        {
            instance.Outputs = LoadOutputs(outputsValue, context!.At("outputs"));
        }

        if (data.TryGetValue("model", out var modelValue) && modelValue is not null)
        {
            instance.Model = Model.Load(modelValue.GetDictionary(Model.ShorthandProperty), context!.At("model"));
        }

        if (data.TryGetValue("tools", out var toolsValue) && toolsValue is not null)
        {
            instance.Tools = LoadTools(toolsValue, context!.At("tools"));
        }

        if (data.TryGetValue("template", out var templateValue) && templateValue is not null)
        {
            instance.Template = Template.Load(templateValue.GetDictionary(Template.ShorthandProperty), context!.At("template"));
        }

        if (data.TryGetValue("instructions", out var instructionsValue) && instructionsValue is not null)
        {
            instance.Instructions = instructionsValue.ToString()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of Property from a dictionary or list.
    /// </summary>
    public static IList<Property> LoadInputs(object data, LoadContext? context)
    {
        var result = new List<Property>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "inputs" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'inputs' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Property.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["example"] = kvp.Value
                    };
                    result.Add(Property.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Property.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Property.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of Property from a dictionary or list.
    /// </summary>
    public static IList<Property> LoadOutputs(object data, LoadContext? context)
    {
        var result = new List<Property>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "outputs" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'outputs' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Property.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["example"] = kvp.Value
                    };
                    result.Add(Property.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Property.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Property.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of Tool from a dictionary or list.
    /// </summary>
    public static IList<Tool> LoadTools(object data, LoadContext? context)
    {
        var result = new List<Tool>();

        if (data is System.Collections.IDictionary)
        {
            var dict = data.GetDictionary();
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"{(string.IsNullOrEmpty(context?.Path) ? "tools" : context.Path)}.{kvp.Key}: invalid named collection entry category array. " +
                        $"'tools' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = new Dictionary<string, object?>(kvp.Value.GetDictionary());
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Tool.Load(itemDict, context?.At(kvp.Key)));
                }
                else
                {
                    // Value is a scalar, infer the entry shape from its runtime type
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["kind"] = kvp.Value
                    };
                    result.Add(Tool.Load(newDict, context?.At(kvp.Key)));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            var itemIndex = 0;
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Tool.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Tool.Load(itemDict, context?.AtIndex(itemIndex)));
                }
                itemIndex++;
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the Agent instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["name"] = obj.Name;


        if (obj.DisplayName is not null)
        {
            result["displayName"] = obj.DisplayName;
        }


        if (obj.Description is not null)
        {
            result["description"] = obj.Description;
        }


        if (obj.Metadata is not null)
        {
            result["metadata"] = obj.Metadata;
        }


        if (obj.Inputs is not null)
        {
            result["inputs"] = SaveInputs(obj.Inputs, context);
        }


        if (obj.Outputs is not null)
        {
            result["outputs"] = SaveOutputs(obj.Outputs, context);
        }


        if (obj.Model is not null)
        {
            result["model"] = obj.Model?.Save(context);
        }


        if (obj.Tools is not null)
        {
            result["tools"] = SaveTools(obj.Tools, context);
        }


        if (obj.Template is not null)
        {
            result["template"] = obj.Template?.Save(context);
        }


        if (obj.Instructions is not null)
        {
            result["instructions"] = obj.Instructions;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of Property to object or array format.
    /// </summary>
    public static object SaveInputs(IList<Property> items, SaveContext? context)
    {
        context ??= new SaveContext();


        var serialized = items.Select(item => new Dictionary<string, object?>(item.Save(context))).ToList();
        foreach (var itemData in serialized)
        {
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string { Length: 0 }) itemData.Remove("name");
        }

        if (context.CollectionFormat == "array")
        {
            return serialized;
        }

        var names = new HashSet<string>(StringComparer.Ordinal);
        foreach (var itemData in serialized)
        {
            if (!itemData.TryGetValue("name", out var nameValue) || nameValue is not string { Length: > 0 } name || !names.Add(name)) return serialized;
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        for (var index = 0; index < items.Count; index++)
        {
            var item = items[index];
            var itemData = serialized[index];
            var name = (string)itemData["name"]!;
            itemData.Remove("name");

            // Check if we can use shorthand
            if (context.UseShorthand && Property.ShorthandProperty is string shorthandProp)
            {
                if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                {
                    result[name] = itemData[shorthandProp];
                    continue;
                }
            }
            result[name] = itemData;
        }
        return result;

    }


    /// <summary>
    /// Save a list of Property to object or array format.
    /// </summary>
    public static object SaveOutputs(IList<Property> items, SaveContext? context)
    {
        context ??= new SaveContext();


        var serialized = items.Select(item => new Dictionary<string, object?>(item.Save(context))).ToList();
        foreach (var itemData in serialized)
        {
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string { Length: 0 }) itemData.Remove("name");
        }

        if (context.CollectionFormat == "array")
        {
            return serialized;
        }

        var names = new HashSet<string>(StringComparer.Ordinal);
        foreach (var itemData in serialized)
        {
            if (!itemData.TryGetValue("name", out var nameValue) || nameValue is not string { Length: > 0 } name || !names.Add(name)) return serialized;
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        for (var index = 0; index < items.Count; index++)
        {
            var item = items[index];
            var itemData = serialized[index];
            var name = (string)itemData["name"]!;
            itemData.Remove("name");

            // Check if we can use shorthand
            if (context.UseShorthand && Property.ShorthandProperty is string shorthandProp)
            {
                if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                {
                    result[name] = itemData[shorthandProp];
                    continue;
                }
            }
            result[name] = itemData;
        }
        return result;

    }


    /// <summary>
    /// Save a list of Tool to object or array format.
    /// </summary>
    public static object SaveTools(IList<Tool> items, SaveContext? context)
    {
        context ??= new SaveContext();


        var serialized = items.Select(item => new Dictionary<string, object?>(item.Save(context))).ToList();
        foreach (var itemData in serialized)
        {
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string { Length: 0 }) itemData.Remove("name");
        }

        if (context.CollectionFormat == "array")
        {
            return serialized;
        }

        var names = new HashSet<string>(StringComparer.Ordinal);
        foreach (var itemData in serialized)
        {
            if (!itemData.TryGetValue("name", out var nameValue) || nameValue is not string { Length: > 0 } name || !names.Add(name)) return serialized;
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        for (var index = 0; index < items.Count; index++)
        {
            var item = items[index];
            var itemData = serialized[index];
            var name = (string)itemData["name"]!;
            itemData.Remove("name");

            // Check if we can use shorthand
            if (context.UseShorthand && Tool.ShorthandProperty is string shorthandProp)
            {
                if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                {
                    result[name] = itemData[shorthandProp];
                    continue;
                }
            }
            result[name] = itemData;
        }
        return result;

    }


    /// <summary>
    /// Convert the Agent instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the Agent instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a Agent instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded Agent instance.</returns>
    public static Agent FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a Agent instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded Agent instance.</returns>
    public static Agent FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
