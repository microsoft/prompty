// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// The response body from Anthropic's model listing API.
    /// </summary>
public partial class AnthropicModelsResponse
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="AnthropicModelsResponse"/>.
    /// </summary>
#pragma warning disable CS8618
    public AnthropicModelsResponse()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Models available to the authenticated caller
    /// </summary>
    public IList<AnthropicModelInfo> Data { get; set; } = [];

    /// <summary>
    /// The first model identifier in this page
    /// </summary>
    public string? FirstId { get; set; }

    /// <summary>
    /// Whether another page is available
    /// </summary>
    public bool? HasMore { get; set; }

    /// <summary>
    /// The last model identifier in this page
    /// </summary>
    public string? LastId { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a AnthropicModelsResponse instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded AnthropicModelsResponse instance.</returns>
    public static AnthropicModelsResponse Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new AnthropicModelsResponse();


        if (data.TryGetValue("data", out var dataValue) && dataValue is not null)
        {
            instance.Data = LoadData(dataValue, context);
        }

        if (data.TryGetValue("first_id", out var first_idValue) && first_idValue is not null)
        {
            instance.FirstId = first_idValue?.ToString()!;
        }

        if (data.TryGetValue("has_more", out var has_moreValue) && has_moreValue is not null)
        {
            instance.HasMore = Convert.ToBoolean(has_moreValue);
        }

        if (data.TryGetValue("last_id", out var last_idValue) && last_idValue is not null)
        {
            instance.LastId = last_idValue?.ToString()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of AnthropicModelInfo from a dictionary or list.
    /// </summary>
    public static IList<AnthropicModelInfo> LoadData(object data, LoadContext? context)
    {
        var result = new List<AnthropicModelInfo>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'data' format: key '{kvp.Key}' has an array value. " +
                        $"'data' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(AnthropicModelInfo.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(AnthropicModelInfo.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(AnthropicModelInfo.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(AnthropicModelInfo.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the AnthropicModelsResponse instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["data"] = SaveData(obj.Data, context);


        if (obj.FirstId is not null)
        {
            result["first_id"] = obj.FirstId;
        }


        if (obj.HasMore is not null)
        {
            result["has_more"] = obj.HasMore;
        }


        if (obj.LastId is not null)
        {
            result["last_id"] = obj.LastId;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of AnthropicModelInfo to object or array format.
    /// </summary>
    public static object SaveData(IList<AnthropicModelInfo> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the AnthropicModelsResponse instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the AnthropicModelsResponse instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a AnthropicModelsResponse instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded AnthropicModelsResponse instance.</returns>
    public static AnthropicModelsResponse FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a AnthropicModelsResponse instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded AnthropicModelsResponse instance.</returns>
    public static AnthropicModelsResponse FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
