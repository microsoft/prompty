// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
#nullable enable

using System.Linq;
using System.Text.Json;
using System.Threading;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// One provenance-tagged span of rendered output (Prompty Jinja Subset §7,
    ///
    /// spec/jinja-grammar.md). Concatenating every segment's `text` in source order
    ///
    /// yields the flat string returned by `render`, so `renderSegments` is the
    ///
    /// provenance-carrying superset of `render`.
    ///
    /// `literal` spans originate in the template source; `interp` spans originate in
    ///
    /// an interpolated input value and carry the originating root property `source`.
    ///
    /// That provenance lets a downstream structural parser distinguish an
    ///
    /// author-authored role boundary from one forged inside untrusted input — the
    ///
    /// injection-deterrence contract that replaces in-band sentinel scanning. A span
    ///
    /// whose `source` property is declared strict is flagged `strict`; a strict span
    ///
    /// that forges a role boundary is rejected loudly rather than emitted.
    /// </summary>
public partial class RenderSegment
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="RenderSegment"/>.
    /// </summary>
#pragma warning disable CS8618
    public RenderSegment()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Whether this span came from the template (literal) or an input value (interp)
    /// </summary>
    public RenderSegmentKind Kind { get; set; } = RenderSegmentKind.Literal;

    /// <summary>
    /// The rendered text of this span
    /// </summary>
    public string Text { get; set; } = string.Empty;

    /// <summary>
    /// For interp spans, the root input property the value came from
    /// </summary>
    public string? Source { get; set; }

    /// <summary>
    /// Whether the originating input property is declared strict
    /// </summary>
    public bool? Strict { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a RenderSegment instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded RenderSegment instance.</returns>
    public static RenderSegment Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        context ??= new LoadContext();
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new RenderSegment();


        if (data.TryGetValue("kind", out var kindValue) && kindValue is not null)
        {
            instance.Kind = RenderSegmentKindParser.Parse(kindValue.ToString()!);
        }

        if (data.TryGetValue("text", out var textValue) && textValue is not null)
        {
            instance.Text = textValue.ToString()!;
        }

        if (data.TryGetValue("source", out var sourceValue) && sourceValue is not null)
        {
            instance.Source = sourceValue.ToString()!;
        }

        if (data.TryGetValue("strict", out var strictValue) && strictValue is not null)
        {
            instance.Strict = Convert.ToBoolean(strictValue);
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the RenderSegment instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["kind"] = RenderSegmentKindParser.ToValue(obj.Kind);


        result["text"] = obj.Text;


        if (obj.Source is not null)
        {
            result["source"] = obj.Source;
        }


        if (obj.Strict is not null)
        {
            result["strict"] = obj.Strict;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Convert the RenderSegment instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the RenderSegment instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a RenderSegment instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded RenderSegment instance.</returns>
    public static RenderSegment FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a RenderSegment instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded RenderSegment instance.</returns>
    public static RenderSegment FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
