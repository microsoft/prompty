// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
#nullable enable

using System.Text.Json;
using System.Threading;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Provider-neutral initialization result for an authorization-code flow.
    /// </summary>
public partial class AuthorizationCodeFlow
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="AuthorizationCodeFlow"/>.
    /// </summary>
#pragma warning disable CS8618
    public AuthorizationCodeFlow()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// The authorization URL the host opens for the user
    /// </summary>
    public string AuthUrl { get; set; } = string.Empty;

    /// <summary>
    /// The PKCE verifier retained by the host for token exchange
    /// </summary>
    public string CodeVerifier { get; set; } = string.Empty;



    #region Load Methods

    /// <summary>
    /// Load a AuthorizationCodeFlow instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded AuthorizationCodeFlow instance.</returns>
    public static AuthorizationCodeFlow Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        context ??= new LoadContext();
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new AuthorizationCodeFlow();


        if (data.TryGetValue("authUrl", out var authUrlValue) && authUrlValue is not null)
        {
            instance.AuthUrl = authUrlValue.ToString()!;
        }

        if (data.TryGetValue("codeVerifier", out var codeVerifierValue) && codeVerifierValue is not null)
        {
            instance.CodeVerifier = codeVerifierValue.ToString()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the AuthorizationCodeFlow instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["authUrl"] = obj.AuthUrl;


        result["codeVerifier"] = obj.CodeVerifier;


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }

    /// <summary>
    /// Convert this instance to a provider-specific wire-format dictionary.
    /// </summary>
    /// <param name="provider">The provider name (e.g., "openai", "anthropic").</param>
    /// <returns>A dictionary with provider-specific field names.</returns>
    public Dictionary<string, object?> ToWire(string provider)
    {
        var data = Save();
        var result = new Dictionary<string, object?>();
        var wireMap = new Dictionary<string, Dictionary<string, string>>
        {
            ["authUrl"] = new Dictionary<string, string> { ["foundry"] = "auth_url" },
            ["codeVerifier"] = new Dictionary<string, string> { ["foundry"] = "code_verifier" },
        };
        foreach (var (key, value) in data)
        {
            if (wireMap.TryGetValue(key, out var mapping) && mapping.TryGetValue(provider, out var wireName))
                result[wireName] = value;
        }
        return result;
    }


    /// <summary>
    /// Convert the AuthorizationCodeFlow instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the AuthorizationCodeFlow instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a AuthorizationCodeFlow instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded AuthorizationCodeFlow instance.</returns>
    public static AuthorizationCodeFlow FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a AuthorizationCodeFlow instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded AuthorizationCodeFlow instance.</returns>
    public static AuthorizationCodeFlow FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
