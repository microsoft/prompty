// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// The result returned by the turn engine.
    /// </summary>
public partial class TurnEngineResult
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="TurnEngineResult"/>.
    /// </summary>
#pragma warning disable CS8618
    public TurnEngineResult()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// The committed turn outcome
    /// </summary>
    public TurnCommit Commit { get; set; }

    /// <summary>
    /// Immutable context snapshots produced during the turn
    /// </summary>
    public IList<ModelInvocationContextSnapshot>? Snapshots { get; set; }

    /// <summary>
    /// Normalized tool results produced during the turn
    /// </summary>
    public IList<ModelToolResult>? ToolResults { get; set; }

    /// <summary>
    /// A non-fatal post-commit failure; the turn itself remains committed
    /// </summary>
    public string? PostCommitError { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a TurnEngineResult instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded TurnEngineResult instance.</returns>
    public static TurnEngineResult Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new TurnEngineResult();


        if (data.TryGetValue("commit", out var commitValue) && commitValue is not null)
        {
            instance.Commit = TurnCommit.Load(commitValue.GetDictionary(TurnCommit.ShorthandProperty), context);
        }

        if (data.TryGetValue("snapshots", out var snapshotsValue) && snapshotsValue is not null)
        {
            instance.Snapshots = LoadSnapshots(snapshotsValue, context);
        }

        if (data.TryGetValue("toolResults", out var toolResultsValue) && toolResultsValue is not null)
        {
            instance.ToolResults = LoadToolResults(toolResultsValue, context);
        }

        if (data.TryGetValue("postCommitError", out var postCommitErrorValue) && postCommitErrorValue is not null)
        {
            instance.PostCommitError = postCommitErrorValue?.ToString()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of ModelInvocationContextSnapshot from a dictionary or list.
    /// </summary>
    public static IList<ModelInvocationContextSnapshot> LoadSnapshots(object data, LoadContext? context)
    {
        var result = new List<ModelInvocationContextSnapshot>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'snapshots' format: key '{kvp.Key}' has an array value. " +
                        $"'snapshots' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ModelInvocationContextSnapshot.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(ModelInvocationContextSnapshot.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ModelInvocationContextSnapshot.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ModelInvocationContextSnapshot.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of ModelToolResult from a dictionary or list.
    /// </summary>
    public static IList<ModelToolResult> LoadToolResults(object data, LoadContext? context)
    {
        var result = new List<ModelToolResult>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'toolResults' format: key '{kvp.Key}' has an array value. " +
                        $"'toolResults' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ModelToolResult.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["requestId"] = kvp.Value
                    };
                    result.Add(ModelToolResult.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ModelToolResult.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ModelToolResult.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the TurnEngineResult instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["commit"] = obj.Commit?.Save(context);


        if (obj.Snapshots is not null)
        {
            result["snapshots"] = SaveSnapshots(obj.Snapshots, context);
        }


        if (obj.ToolResults is not null)
        {
            result["toolResults"] = SaveToolResults(obj.ToolResults, context);
        }


        if (obj.PostCommitError is not null)
        {
            result["postCommitError"] = obj.PostCommitError;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of ModelInvocationContextSnapshot to object or array format.
    /// </summary>
    public static object SaveSnapshots(IList<ModelInvocationContextSnapshot> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of ModelToolResult to object or array format.
    /// </summary>
    public static object SaveToolResults(IList<ModelToolResult> items, SaveContext? context)
    {
        context ??= new SaveContext();


        if (context.CollectionFormat == "array")
        {
            return items.Select(item => item.Save(context)).ToList();
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        foreach (var item in items)
        {
            var itemData = item.Save(context);
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string name)
            {
                itemData.Remove("name");

                // Check if we can use shorthand
                if (context.UseShorthand && ModelToolResult.ShorthandProperty is string shorthandProp)
                {
                    if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                    {
                        result[name] = itemData[shorthandProp];
                        continue;
                    }
                }
                result[name] = itemData;
            }
            else
            {
                // No name, can't use object format for this item
                throw new InvalidOperationException("Cannot save item in object format: missing 'name' property");
            }
        }
        return result;

    }


    /// <summary>
    /// Convert the TurnEngineResult instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the TurnEngineResult instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a TurnEngineResult instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded TurnEngineResult instance.</returns>
    public static TurnEngineResult FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a TurnEngineResult instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded TurnEngineResult instance.</returns>
    public static TurnEngineResult FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
