// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Provider-context state carried into or out of an invocation.
    /// </summary>
public partial class InvocationContextState
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="InvocationContextState"/>.
    /// </summary>
#pragma warning disable CS8618
    public InvocationContextState()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Whether the model-visible context is portable across providers
    /// </summary>
    public InvocationContextPortability Portability { get; set; } = InvocationContextPortability.Portable;

    /// <summary>
    /// Explicit references to provider-held context state
    /// </summary>
    public IList<DelegatedStateReference>? DelegatedState { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a InvocationContextState instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded InvocationContextState instance.</returns>
    public static InvocationContextState Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new InvocationContextState();


        if (data.TryGetValue("portability", out var portabilityValue) && portabilityValue is not null)
        {
            instance.Portability = InvocationContextPortabilityParser.Parse(portabilityValue?.ToString()!);
        }

        if (data.TryGetValue("delegatedState", out var delegatedStateValue) && delegatedStateValue is not null)
        {
            instance.DelegatedState = LoadDelegatedState(delegatedStateValue, context);
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of DelegatedStateReference from a dictionary or list.
    /// </summary>
    public static IList<DelegatedStateReference> LoadDelegatedState(object data, LoadContext? context)
    {
        var result = new List<DelegatedStateReference>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'delegatedState' format: key '{kvp.Key}' has an array value. " +
                        $"'delegatedState' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(DelegatedStateReference.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["provider"] = kvp.Value
                    };
                    result.Add(DelegatedStateReference.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(DelegatedStateReference.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(DelegatedStateReference.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the InvocationContextState instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["portability"] = InvocationContextPortabilityParser.ToValue(obj.Portability);


        if (obj.DelegatedState is not null)
        {
            result["delegatedState"] = SaveDelegatedState(obj.DelegatedState, context);
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of DelegatedStateReference to object or array format.
    /// </summary>
    public static object SaveDelegatedState(IList<DelegatedStateReference> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the InvocationContextState instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the InvocationContextState instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a InvocationContextState instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded InvocationContextState instance.</returns>
    public static InvocationContextState FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a InvocationContextState instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded InvocationContextState instance.</returns>
    public static InvocationContextState FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
