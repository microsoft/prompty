// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Request accepted by a replay verifier implementation.
    /// </summary>
public partial class ReplayVerificationRequest
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="ReplayVerificationRequest"/>.
    /// </summary>
#pragma warning disable CS8618
    public ReplayVerificationRequest()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Expected normalized replay records
    /// </summary>
    public IList<ReplayJournalRecord> Expected { get; set; } = [];

    /// <summary>
    /// Actual normalized replay records
    /// </summary>
    public IList<ReplayJournalRecord> Actual { get; set; } = [];



    #region Load Methods

    /// <summary>
    /// Load a ReplayVerificationRequest instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded ReplayVerificationRequest instance.</returns>
    public static ReplayVerificationRequest Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new ReplayVerificationRequest();


        if (data.TryGetValue("expected", out var expectedValue) && expectedValue is not null)
        {
            instance.Expected = LoadExpected(expectedValue, context);
        }

        if (data.TryGetValue("actual", out var actualValue) && actualValue is not null)
        {
            instance.Actual = LoadActual(actualValue, context);
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of ReplayJournalRecord from a dictionary or list.
    /// </summary>
    public static IList<ReplayJournalRecord> LoadExpected(object data, LoadContext? context)
    {
        var result = new List<ReplayJournalRecord>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'expected' format: key '{kvp.Key}' has an array value. " +
                        $"'expected' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ReplayJournalRecord.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["kind"] = kvp.Value
                    };
                    result.Add(ReplayJournalRecord.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ReplayJournalRecord.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ReplayJournalRecord.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of ReplayJournalRecord from a dictionary or list.
    /// </summary>
    public static IList<ReplayJournalRecord> LoadActual(object data, LoadContext? context)
    {
        var result = new List<ReplayJournalRecord>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'actual' format: key '{kvp.Key}' has an array value. " +
                        $"'actual' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ReplayJournalRecord.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["kind"] = kvp.Value
                    };
                    result.Add(ReplayJournalRecord.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ReplayJournalRecord.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ReplayJournalRecord.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the ReplayVerificationRequest instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["expected"] = SaveExpected(obj.Expected, context);


        result["actual"] = SaveActual(obj.Actual, context);


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of ReplayJournalRecord to object or array format.
    /// </summary>
    public static object SaveExpected(IList<ReplayJournalRecord> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of ReplayJournalRecord to object or array format.
    /// </summary>
    public static object SaveActual(IList<ReplayJournalRecord> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the ReplayVerificationRequest instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the ReplayVerificationRequest instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a ReplayVerificationRequest instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded ReplayVerificationRequest instance.</returns>
    public static ReplayVerificationRequest FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a ReplayVerificationRequest instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded ReplayVerificationRequest instance.</returns>
    public static ReplayVerificationRequest FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
