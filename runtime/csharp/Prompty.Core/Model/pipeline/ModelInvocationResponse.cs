// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Provider-neutral result of a single model invocation.
    ///
    /// This is the live provider boundary. It is intentionally distinct from
    ///
    /// TurnModelResponse, which is the deterministic reference-runner callback.
    /// </summary>
public partial class ModelInvocationResponse
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="ModelInvocationResponse"/>.
    /// </summary>
#pragma warning disable CS8618
    public ModelInvocationResponse()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Provider-neutral final output when no additional tool calls are requested
    /// </summary>
    public object? Output { get; set; }

    /// <summary>
    /// Complete cumulative token usage for this completed invocation, when available
    /// </summary>
    public InvocationUsage? Usage { get; set; }

    /// <summary>
    /// Assistant messages to commit before tool results are made visible
    /// </summary>
    public IList<Message>? AssistantMessages { get; set; }

    /// <summary>
    /// Tool requests returned by the provider
    /// </summary>
    public IList<ModelToolRequest>? ToolRequests { get; set; }

    /// <summary>
    /// Provider-context state to carry into the next invocation
    /// </summary>
    public InvocationContextState? NextContextState { get; set; }

    /// <summary>
    /// Opaque provider-specific response metadata
    /// </summary>
    public IDictionary<string, object>? Metadata { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a ModelInvocationResponse instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded ModelInvocationResponse instance.</returns>
    public static ModelInvocationResponse Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new ModelInvocationResponse();


        if (data.TryGetValue("output", out var outputValue) && outputValue is not null)
        {
            instance.Output = outputValue;
        }

        if (data.TryGetValue("usage", out var usageValue) && usageValue is not null)
        {
            instance.Usage = InvocationUsage.Load(usageValue.GetDictionary(InvocationUsage.ShorthandProperty), context);
        }

        if (data.TryGetValue("assistantMessages", out var assistantMessagesValue) && assistantMessagesValue is not null)
        {
            instance.AssistantMessages = LoadAssistantMessages(assistantMessagesValue, context);
        }

        if (data.TryGetValue("toolRequests", out var toolRequestsValue) && toolRequestsValue is not null)
        {
            instance.ToolRequests = LoadToolRequests(toolRequestsValue, context);
        }

        if (data.TryGetValue("nextContextState", out var nextContextStateValue) && nextContextStateValue is not null)
        {
            instance.NextContextState = InvocationContextState.Load(nextContextStateValue.GetDictionary(InvocationContextState.ShorthandProperty), context);
        }

        if (data.TryGetValue("metadata", out var metadataValue) && metadataValue is not null)
        {
            instance.Metadata = metadataValue.GetDictionary()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of Message from a dictionary or list.
    /// </summary>
    public static IList<Message> LoadAssistantMessages(object data, LoadContext? context)
    {
        var result = new List<Message>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'assistantMessages' format: key '{kvp.Key}' has an array value. " +
                        $"'assistantMessages' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Message.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["role"] = kvp.Value
                    };
                    result.Add(Message.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Message.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Message.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of ModelToolRequest from a dictionary or list.
    /// </summary>
    public static IList<ModelToolRequest> LoadToolRequests(object data, LoadContext? context)
    {
        var result = new List<ModelToolRequest>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'toolRequests' format: key '{kvp.Key}' has an array value. " +
                        $"'toolRequests' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ModelToolRequest.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(ModelToolRequest.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ModelToolRequest.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ModelToolRequest.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the ModelInvocationResponse instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        if (obj.Output is not null)
        {
            result["output"] = obj.Output;
        }


        if (obj.Usage is not null)
        {
            result["usage"] = obj.Usage?.Save(context);
        }


        if (obj.AssistantMessages is not null)
        {
            result["assistantMessages"] = SaveAssistantMessages(obj.AssistantMessages, context);
        }


        if (obj.ToolRequests is not null)
        {
            result["toolRequests"] = SaveToolRequests(obj.ToolRequests, context);
        }


        if (obj.NextContextState is not null)
        {
            result["nextContextState"] = obj.NextContextState?.Save(context);
        }


        if (obj.Metadata is not null)
        {
            result["metadata"] = obj.Metadata;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of Message to object or array format.
    /// </summary>
    public static object SaveAssistantMessages(IList<Message> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of ModelToolRequest to object or array format.
    /// </summary>
    public static object SaveToolRequests(IList<ModelToolRequest> items, SaveContext? context)
    {
        context ??= new SaveContext();


        if (context.CollectionFormat == "array")
        {
            return items.Select(item => item.Save(context)).ToList();
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        foreach (var item in items)
        {
            var itemData = item.Save(context);
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string name)
            {
                itemData.Remove("name");

                // Check if we can use shorthand
                if (context.UseShorthand && ModelToolRequest.ShorthandProperty is string shorthandProp)
                {
                    if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                    {
                        result[name] = itemData[shorthandProp];
                        continue;
                    }
                }
                result[name] = itemData;
            }
            else
            {
                // No name, can't use object format for this item
                throw new InvalidOperationException("Cannot save item in object format: missing 'name' property");
            }
        }
        return result;

    }


    /// <summary>
    /// Convert the ModelInvocationResponse instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the ModelInvocationResponse instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a ModelInvocationResponse instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded ModelInvocationResponse instance.</returns>
    public static ModelInvocationResponse FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a ModelInvocationResponse instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded ModelInvocationResponse instance.</returns>
    public static ModelInvocationResponse FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
