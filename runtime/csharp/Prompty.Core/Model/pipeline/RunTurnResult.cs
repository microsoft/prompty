// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Result returned by a reference turn runner implementation.
    /// </summary>
public partial class RunTurnResult
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="RunTurnResult"/>.
    /// </summary>
#pragma warning disable CS8618
    public RunTurnResult()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Stable harness session identifier
    /// </summary>
    public string SessionId { get; set; } = string.Empty;

    /// <summary>
    /// Stable turn identifier within the session
    /// </summary>
    public string TurnId { get; set; } = string.Empty;

    /// <summary>
    /// Final semantic status for the deterministic turn
    /// </summary>
    public RunTurnStatus Status { get; set; } = RunTurnStatus.Success;

    /// <summary>
    /// Provider-neutral final output returned by the injected model callback
    /// </summary>
    public object? Output { get; set; }

    /// <summary>
    /// Number of model loop iterations executed
    /// </summary>
    public int Iterations { get; set; }

    /// <summary>
    /// Host tool results produced during the turn
    /// </summary>
    public IList<HostToolResult>? ToolResults { get; set; }

    /// <summary>
    /// Checkpoints created during the turn
    /// </summary>
    public IList<Checkpoint>? Checkpoints { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a RunTurnResult instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded RunTurnResult instance.</returns>
    public static RunTurnResult Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new RunTurnResult();


        if (data.TryGetValue("sessionId", out var sessionIdValue) && sessionIdValue is not null)
        {
            instance.SessionId = sessionIdValue?.ToString()!;
        }

        if (data.TryGetValue("turnId", out var turnIdValue) && turnIdValue is not null)
        {
            instance.TurnId = turnIdValue?.ToString()!;
        }

        if (data.TryGetValue("status", out var statusValue) && statusValue is not null)
        {
            instance.Status = Enum.Parse<RunTurnStatus>(statusValue?.ToString()!, true);
        }

        if (data.TryGetValue("output", out var outputValue) && outputValue is not null)
        {
            instance.Output = outputValue;
        }

        if (data.TryGetValue("iterations", out var iterationsValue) && iterationsValue is not null)
        {
            instance.Iterations = Convert.ToInt32(iterationsValue);
        }

        if (data.TryGetValue("toolResults", out var toolResultsValue) && toolResultsValue is not null)
        {
            instance.ToolResults = LoadToolResults(toolResultsValue, context);
        }

        if (data.TryGetValue("checkpoints", out var checkpointsValue) && checkpointsValue is not null)
        {
            instance.Checkpoints = LoadCheckpoints(checkpointsValue, context);
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of HostToolResult from a dictionary or list.
    /// </summary>
    public static IList<HostToolResult> LoadToolResults(object data, LoadContext? context)
    {
        var result = new List<HostToolResult>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'toolResults' format: key '{kvp.Key}' has an array value. " +
                        $"'toolResults' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(HostToolResult.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["requestId"] = kvp.Value
                    };
                    result.Add(HostToolResult.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(HostToolResult.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(HostToolResult.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of Checkpoint from a dictionary or list.
    /// </summary>
    public static IList<Checkpoint> LoadCheckpoints(object data, LoadContext? context)
    {
        var result = new List<Checkpoint>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'checkpoints' format: key '{kvp.Key}' has an array value. " +
                        $"'checkpoints' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Checkpoint.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(Checkpoint.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Checkpoint.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Checkpoint.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the RunTurnResult instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["sessionId"] = obj.SessionId;


        result["turnId"] = obj.TurnId;


        result["status"] = obj.Status.ToString().ToLowerInvariant();


        if (obj.Output is not null)
        {
            result["output"] = obj.Output;
        }


        result["iterations"] = obj.Iterations;


        if (obj.ToolResults is not null)
        {
            result["toolResults"] = SaveToolResults(obj.ToolResults, context);
        }


        if (obj.Checkpoints is not null)
        {
            result["checkpoints"] = SaveCheckpoints(obj.Checkpoints, context);
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of HostToolResult to object or array format.
    /// </summary>
    public static object SaveToolResults(IList<HostToolResult> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of Checkpoint to object or array format.
    /// </summary>
    public static object SaveCheckpoints(IList<Checkpoint> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the RunTurnResult instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the RunTurnResult instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a RunTurnResult instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded RunTurnResult instance.</returns>
    public static RunTurnResult FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a RunTurnResult instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded RunTurnResult instance.</returns>
    public static RunTurnResult FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
