// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Portable durable checkpoint emitted after a committed model/tool round.
    ///
    /// A resumed run rebuilt from a checkpoint MUST NOT duplicate a model or tool
    ///
    /// effect that the checkpoint already records as committed. Run identity is
    ///
    /// carried so delegated runs checkpoint independently under their own run.
    /// </summary>
public partial class EngineCheckpoint
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="EngineCheckpoint"/>.
    /// </summary>
#pragma warning disable CS8618
    public EngineCheckpoint()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Stable unique identifier for this checkpoint
    /// </summary>
    public string Id { get; set; } = string.Empty;

    /// <summary>
    /// Stable session identifier
    /// </summary>
    public string SessionId { get; set; } = string.Empty;

    /// <summary>
    /// Stable turn identifier
    /// </summary>
    public string TurnId { get; set; } = string.Empty;

    /// <summary>
    /// Stable identifier of this engine run
    /// </summary>
    public string RunId { get; set; } = string.Empty;

    /// <summary>
    /// Run identifier of the parent run when this run was delegated
    /// </summary>
    public string? ParentRunId { get; set; }

    /// <summary>
    /// Zero-based delegation nesting depth; 0 for a top-level run
    /// </summary>
    public int DelegationDepth { get; set; } = 0;

    /// <summary>
    /// Zero-based model loop iteration captured by this checkpoint
    /// </summary>
    public int Iteration { get; set; }

    /// <summary>
    /// Last committed event sequence included in this checkpoint
    /// </summary>
    public long LastSequence { get; set; }

    /// <summary>
    /// Canonical conversation messages at checkpoint time
    /// </summary>
    public IList<Message> Messages { get; set; } = [];

    /// <summary>
    /// Number of leading messages eligible for provider prefix-cache reuse
    /// </summary>
    public int StablePrefixMessages { get; set; } = 0;

    /// <summary>
    /// Turn inputs captured for deterministic resume
    /// </summary>
    public object? Inputs { get; set; }

    /// <summary>
    /// Active model invocation identifier, when one is in flight
    /// </summary>
    public string? ActiveInvocationId { get; set; }

    /// <summary>
    /// Tool requests awaiting execution or commitment
    /// </summary>
    public IList<ModelToolRequest>? PendingToolRequests { get; set; }

    /// <summary>
    /// Tool results already executed in this round
    /// </summary>
    public IList<ModelToolResult>? CompletedToolResults { get; set; }

    /// <summary>
    /// Number of fully completed model iterations
    /// </summary>
    public int CompletedModelIterations { get; set; } = 0;

    /// <summary>
    /// Whether the turn is blocked pending model outcome reconciliation
    /// </summary>
    public bool ReconciliationRequired { get; set; } = false;

    /// <summary>
    /// Typed provider state when model outcome reconciliation is required
    /// </summary>
    public ModelReconciliationState? ModelReconciliation { get; set; }

    /// <summary>
    /// Final output pending commit, when computed
    /// </summary>
    public object? PendingOutput { get; set; }

    /// <summary>
    /// Whether the final output is ready to commit
    /// </summary>
    public bool FinalOutputReady { get; set; } = false;

    /// <summary>
    /// Model response retained until all tool results form one conversation batch
    /// </summary>
    public ModelInvocationResponse? PendingModelResponse { get; set; }

    /// <summary>
    /// Resume this exact iteration because the checkpoint precedes an external model effect
    /// </summary>
    public bool ResumeSameIteration { get; set; } = false;

    /// <summary>
    /// Whether the host policy rewrite is already durable for this iteration
    /// </summary>
    public bool PolicyAppliedForIteration { get; set; } = false;

    /// <summary>
    /// Provider-context state carried by this checkpoint
    /// </summary>
    public InvocationContextState ContextState { get; set; }

    /// <summary>
    /// Opaque host-specific checkpoint metadata
    /// </summary>
    public IDictionary<string, object>? Metadata { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a EngineCheckpoint instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded EngineCheckpoint instance.</returns>
    public static EngineCheckpoint Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new EngineCheckpoint();


        if (data.TryGetValue("id", out var idValue) && idValue is not null)
        {
            instance.Id = idValue?.ToString()!;
        }

        if (data.TryGetValue("sessionId", out var sessionIdValue) && sessionIdValue is not null)
        {
            instance.SessionId = sessionIdValue?.ToString()!;
        }

        if (data.TryGetValue("turnId", out var turnIdValue) && turnIdValue is not null)
        {
            instance.TurnId = turnIdValue?.ToString()!;
        }

        if (data.TryGetValue("runId", out var runIdValue) && runIdValue is not null)
        {
            instance.RunId = runIdValue?.ToString()!;
        }

        if (data.TryGetValue("parentRunId", out var parentRunIdValue) && parentRunIdValue is not null)
        {
            instance.ParentRunId = parentRunIdValue?.ToString()!;
        }

        if (data.TryGetValue("delegationDepth", out var delegationDepthValue) && delegationDepthValue is not null)
        {
            instance.DelegationDepth = Convert.ToInt32(delegationDepthValue);
        }

        if (data.TryGetValue("iteration", out var iterationValue) && iterationValue is not null)
        {
            instance.Iteration = Convert.ToInt32(iterationValue);
        }

        if (data.TryGetValue("lastSequence", out var lastSequenceValue) && lastSequenceValue is not null)
        {
            instance.LastSequence = Convert.ToInt64(lastSequenceValue);
        }

        if (data.TryGetValue("messages", out var messagesValue) && messagesValue is not null)
        {
            instance.Messages = LoadMessages(messagesValue, context);
        }

        if (data.TryGetValue("stablePrefixMessages", out var stablePrefixMessagesValue) && stablePrefixMessagesValue is not null)
        {
            instance.StablePrefixMessages = Convert.ToInt32(stablePrefixMessagesValue);
        }

        if (data.TryGetValue("inputs", out var inputsValue) && inputsValue is not null)
        {
            instance.Inputs = inputsValue;
        }

        if (data.TryGetValue("activeInvocationId", out var activeInvocationIdValue) && activeInvocationIdValue is not null)
        {
            instance.ActiveInvocationId = activeInvocationIdValue?.ToString()!;
        }

        if (data.TryGetValue("pendingToolRequests", out var pendingToolRequestsValue) && pendingToolRequestsValue is not null)
        {
            instance.PendingToolRequests = LoadPendingToolRequests(pendingToolRequestsValue, context);
        }

        if (data.TryGetValue("completedToolResults", out var completedToolResultsValue) && completedToolResultsValue is not null)
        {
            instance.CompletedToolResults = LoadCompletedToolResults(completedToolResultsValue, context);
        }

        if (data.TryGetValue("completedModelIterations", out var completedModelIterationsValue) && completedModelIterationsValue is not null)
        {
            instance.CompletedModelIterations = Convert.ToInt32(completedModelIterationsValue);
        }

        if (data.TryGetValue("reconciliationRequired", out var reconciliationRequiredValue) && reconciliationRequiredValue is not null)
        {
            instance.ReconciliationRequired = Convert.ToBoolean(reconciliationRequiredValue);
        }

        if (data.TryGetValue("modelReconciliation", out var modelReconciliationValue) && modelReconciliationValue is not null)
        {
            instance.ModelReconciliation = ModelReconciliationState.Load(modelReconciliationValue.GetDictionary(ModelReconciliationState.ShorthandProperty), context);
        }

        if (data.TryGetValue("pendingOutput", out var pendingOutputValue) && pendingOutputValue is not null)
        {
            instance.PendingOutput = pendingOutputValue;
        }

        if (data.TryGetValue("finalOutputReady", out var finalOutputReadyValue) && finalOutputReadyValue is not null)
        {
            instance.FinalOutputReady = Convert.ToBoolean(finalOutputReadyValue);
        }

        if (data.TryGetValue("pendingModelResponse", out var pendingModelResponseValue) && pendingModelResponseValue is not null)
        {
            instance.PendingModelResponse = ModelInvocationResponse.Load(pendingModelResponseValue.GetDictionary(ModelInvocationResponse.ShorthandProperty), context);
        }

        if (data.TryGetValue("resumeSameIteration", out var resumeSameIterationValue) && resumeSameIterationValue is not null)
        {
            instance.ResumeSameIteration = Convert.ToBoolean(resumeSameIterationValue);
        }

        if (data.TryGetValue("policyAppliedForIteration", out var policyAppliedForIterationValue) && policyAppliedForIterationValue is not null)
        {
            instance.PolicyAppliedForIteration = Convert.ToBoolean(policyAppliedForIterationValue);
        }

        if (data.TryGetValue("contextState", out var contextStateValue) && contextStateValue is not null)
        {
            instance.ContextState = InvocationContextState.Load(contextStateValue.GetDictionary(InvocationContextState.ShorthandProperty), context);
        }

        if (data.TryGetValue("metadata", out var metadataValue) && metadataValue is not null)
        {
            instance.Metadata = metadataValue.GetDictionary()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of Message from a dictionary or list.
    /// </summary>
    public static IList<Message> LoadMessages(object data, LoadContext? context)
    {
        var result = new List<Message>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'messages' format: key '{kvp.Key}' has an array value. " +
                        $"'messages' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Message.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["role"] = kvp.Value
                    };
                    result.Add(Message.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Message.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Message.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of ModelToolRequest from a dictionary or list.
    /// </summary>
    public static IList<ModelToolRequest> LoadPendingToolRequests(object data, LoadContext? context)
    {
        var result = new List<ModelToolRequest>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'pendingToolRequests' format: key '{kvp.Key}' has an array value. " +
                        $"'pendingToolRequests' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ModelToolRequest.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["id"] = kvp.Value
                    };
                    result.Add(ModelToolRequest.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ModelToolRequest.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ModelToolRequest.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of ModelToolResult from a dictionary or list.
    /// </summary>
    public static IList<ModelToolResult> LoadCompletedToolResults(object data, LoadContext? context)
    {
        var result = new List<ModelToolResult>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'completedToolResults' format: key '{kvp.Key}' has an array value. " +
                        $"'completedToolResults' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(ModelToolResult.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["requestId"] = kvp.Value
                    };
                    result.Add(ModelToolResult.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(ModelToolResult.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(ModelToolResult.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the EngineCheckpoint instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        result["id"] = obj.Id;


        result["sessionId"] = obj.SessionId;


        result["turnId"] = obj.TurnId;


        result["runId"] = obj.RunId;


        if (obj.ParentRunId is not null)
        {
            result["parentRunId"] = obj.ParentRunId;
        }


        result["delegationDepth"] = obj.DelegationDepth;


        result["iteration"] = obj.Iteration;


        result["lastSequence"] = obj.LastSequence;


        result["messages"] = SaveMessages(obj.Messages, context);


        result["stablePrefixMessages"] = obj.StablePrefixMessages;


        if (obj.Inputs is not null)
        {
            result["inputs"] = obj.Inputs;
        }


        if (obj.ActiveInvocationId is not null)
        {
            result["activeInvocationId"] = obj.ActiveInvocationId;
        }


        if (obj.PendingToolRequests is not null)
        {
            result["pendingToolRequests"] = SavePendingToolRequests(obj.PendingToolRequests, context);
        }


        if (obj.CompletedToolResults is not null)
        {
            result["completedToolResults"] = SaveCompletedToolResults(obj.CompletedToolResults, context);
        }


        result["completedModelIterations"] = obj.CompletedModelIterations;


        result["reconciliationRequired"] = obj.ReconciliationRequired;


        if (obj.ModelReconciliation is not null)
        {
            result["modelReconciliation"] = obj.ModelReconciliation?.Save(context);
        }


        if (obj.PendingOutput is not null)
        {
            result["pendingOutput"] = obj.PendingOutput;
        }


        result["finalOutputReady"] = obj.FinalOutputReady;


        if (obj.PendingModelResponse is not null)
        {
            result["pendingModelResponse"] = obj.PendingModelResponse?.Save(context);
        }


        result["resumeSameIteration"] = obj.ResumeSameIteration;


        result["policyAppliedForIteration"] = obj.PolicyAppliedForIteration;


        result["contextState"] = obj.ContextState?.Save(context);


        if (obj.Metadata is not null)
        {
            result["metadata"] = obj.Metadata;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of Message to object or array format.
    /// </summary>
    public static object SaveMessages(IList<Message> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of ModelToolRequest to object or array format.
    /// </summary>
    public static object SavePendingToolRequests(IList<ModelToolRequest> items, SaveContext? context)
    {
        context ??= new SaveContext();


        if (context.CollectionFormat == "array")
        {
            return items.Select(item => item.Save(context)).ToList();
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        foreach (var item in items)
        {
            var itemData = item.Save(context);
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string name)
            {
                itemData.Remove("name");

                // Check if we can use shorthand
                if (context.UseShorthand && ModelToolRequest.ShorthandProperty is string shorthandProp)
                {
                    if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                    {
                        result[name] = itemData[shorthandProp];
                        continue;
                    }
                }
                result[name] = itemData;
            }
            else
            {
                // No name, can't use object format for this item
                throw new InvalidOperationException("Cannot save item in object format: missing 'name' property");
            }
        }
        return result;

    }


    /// <summary>
    /// Save a list of ModelToolResult to object or array format.
    /// </summary>
    public static object SaveCompletedToolResults(IList<ModelToolResult> items, SaveContext? context)
    {
        context ??= new SaveContext();


        if (context.CollectionFormat == "array")
        {
            return items.Select(item => item.Save(context)).ToList();
        }

        // Object format: use name as key
        var result = new Dictionary<string, object?>();
        foreach (var item in items)
        {
            var itemData = item.Save(context);
            if (itemData.TryGetValue("name", out var nameValue) && nameValue is string name)
            {
                itemData.Remove("name");

                // Check if we can use shorthand
                if (context.UseShorthand && ModelToolResult.ShorthandProperty is string shorthandProp)
                {
                    if (itemData.Count == 1 && itemData.ContainsKey(shorthandProp))
                    {
                        result[name] = itemData[shorthandProp];
                        continue;
                    }
                }
                result[name] = itemData;
            }
            else
            {
                // No name, can't use object format for this item
                throw new InvalidOperationException("Cannot save item in object format: missing 'name' property");
            }
        }
        return result;

    }


    /// <summary>
    /// Convert the EngineCheckpoint instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the EngineCheckpoint instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a EngineCheckpoint instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded EngineCheckpoint instance.</returns>
    public static EngineCheckpoint FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a EngineCheckpoint instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded EngineCheckpoint instance.</returns>
    public static EngineCheckpoint FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
