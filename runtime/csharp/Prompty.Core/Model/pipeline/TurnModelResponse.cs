// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Response returned by the injected model callback to the reference turn runner.
    /// </summary>
public partial class TurnModelResponse
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="TurnModelResponse"/>.
    /// </summary>
#pragma warning disable CS8618
    public TurnModelResponse()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Provider-neutral final model output for the turn when no more tools are requested
    /// </summary>
    public object? Output { get; set; }

    /// <summary>
    /// Complete cumulative token usage reported for this model invocation, when available
    /// </summary>
    public InvocationUsage? Usage { get; set; }

    /// <summary>
    /// Host tool execution requests emitted by the model callback
    /// </summary>
    public IList<HostToolRequest>? ToolRequests { get; set; }

    /// <summary>
    /// Additional deterministic state to merge into the iteration checkpoint
    /// </summary>
    public IDictionary<string, object>? CheckpointState { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a TurnModelResponse instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded TurnModelResponse instance.</returns>
    public static TurnModelResponse Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new TurnModelResponse();


        if (data.TryGetValue("output", out var outputValue) && outputValue is not null)
        {
            instance.Output = outputValue;
        }

        if (data.TryGetValue("usage", out var usageValue) && usageValue is not null)
        {
            instance.Usage = InvocationUsage.Load(usageValue.GetDictionary(InvocationUsage.ShorthandProperty), context);
        }

        if (data.TryGetValue("toolRequests", out var toolRequestsValue) && toolRequestsValue is not null)
        {
            instance.ToolRequests = LoadToolRequests(toolRequestsValue, context);
        }

        if (data.TryGetValue("checkpointState", out var checkpointStateValue) && checkpointStateValue is not null)
        {
            instance.CheckpointState = checkpointStateValue.GetDictionary()!;
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of HostToolRequest from a dictionary or list.
    /// </summary>
    public static IList<HostToolRequest> LoadToolRequests(object data, LoadContext? context)
    {
        var result = new List<HostToolRequest>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'toolRequests' format: key '{kvp.Key}' has an array value. " +
                        $"'toolRequests' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(HostToolRequest.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["requestId"] = kvp.Value
                    };
                    result.Add(HostToolRequest.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(HostToolRequest.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(HostToolRequest.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the TurnModelResponse instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        var result = new Dictionary<string, object?>();


        if (obj.Output is not null)
        {
            result["output"] = obj.Output;
        }


        if (obj.Usage is not null)
        {
            result["usage"] = obj.Usage?.Save(context);
        }


        if (obj.ToolRequests is not null)
        {
            result["toolRequests"] = SaveToolRequests(obj.ToolRequests, context);
        }


        if (obj.CheckpointState is not null)
        {
            result["checkpointState"] = obj.CheckpointState;
        }


        if (context is not null)
        {
            result = context.ProcessDict(result);
        }

        return result;
    }


    /// <summary>
    /// Save a list of HostToolRequest to object or array format.
    /// </summary>
    public static object SaveToolRequests(IList<HostToolRequest> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the TurnModelResponse instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the TurnModelResponse instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a TurnModelResponse instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded TurnModelResponse instance.</returns>
    public static TurnModelResponse FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a TurnModelResponse instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded TurnModelResponse instance.</returns>
    public static TurnModelResponse FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
