// <auto-generated by typra-emitter>
// Copyright (c) Microsoft. All rights reserved.
using System.Text.Json;
using YamlDotNet.Serialization;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130

    /// <summary>
    /// Represents a JSON Schema union property.
    ///
    /// Use `oneOf` when exactly one branch must match, or `anyOf` when one or more
    ///
    /// branches may match. The alternatives are full Prompty properties so unions
    ///
    /// remain portable across generated runtimes.
    /// </summary>
public partial class UnionProperty : Property
{
    /// <summary>
    /// The shorthand property name for this type, if any.
    /// </summary>
    public new static string? ShorthandProperty => null;

    /// <summary>
    /// Initializes a new instance of <see cref="UnionProperty"/>.
    /// </summary>
#pragma warning disable CS8618
    public UnionProperty()
    {
    }
#pragma warning restore CS8618

    /// <summary>
    /// Kind
    /// </summary>
    public override string Kind { get; set; } = "union";

    /// <summary>
    /// Alternative property schemas where exactly one branch must match
    /// </summary>
    public IList<Property>? OneOf { get; set; }

    /// <summary>
    /// Alternative property schemas where one or more branches may match
    /// </summary>
    public IList<Property>? AnyOf { get; set; }



    #region Load Methods

    /// <summary>
    /// Load a UnionProperty instance from a dictionary.
    /// </summary>
    /// <param name="data">The dictionary containing the data.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded UnionProperty instance.</returns>
    public new static UnionProperty Load(Dictionary<string, object?> data, LoadContext? context = null)
    {
        if (context is not null)
        {
            data = context.ProcessInput(data);
        }


        // Create new instance
        var instance = new UnionProperty();


        if (data.TryGetValue("kind", out var kindValue) && kindValue is not null)
        {
            instance.Kind = kindValue?.ToString()!;
        }

        if (data.TryGetValue("oneOf", out var oneOfValue) && oneOfValue is not null)
        {
            instance.OneOf = LoadOneOf(oneOfValue, context);
        }

        if (data.TryGetValue("anyOf", out var anyOfValue) && anyOfValue is not null)
        {
            instance.AnyOf = LoadAnyOf(anyOfValue, context);
        }
        if ((instance.OneOf?.Count > 0) == (instance.AnyOf?.Count > 0))
        {
            throw new ArgumentException("UnionProperty requires exactly one non-empty composition: oneOf XOR anyOf");
        }

        if (context is not null)
        {
            instance = context.ProcessOutput(instance);
        }
        return instance;
    }


    /// <summary>
    /// Load a list of Property from a dictionary or list.
    /// </summary>
    public static IList<Property> LoadOneOf(object data, LoadContext? context)
    {
        var result = new List<Property>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'oneOf' format: key '{kvp.Key}' has an array value. " +
                        $"'oneOf' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Property.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["example"] = kvp.Value
                    };
                    result.Add(Property.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Property.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Property.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    /// <summary>
    /// Load a list of Property from a dictionary or list.
    /// </summary>
    public static IList<Property> LoadAnyOf(object data, LoadContext? context)
    {
        var result = new List<Property>();

        if (data is Dictionary<string, object?> dict)
        {
            // Convert named dictionary to list
            foreach (var kvp in dict)
            {
                if (kvp.Value is IEnumerable<object>)
                {
                    throw new ArgumentException(
                        $"Invalid 'anyOf' format: key '{kvp.Key}' has an array value. " +
                        $"'anyOf' must be a flat list of objects or a name-keyed dict — " +
                        "not a nested {" + kvp.Key + ": [...]} structure.");
                }
                var itemDict = kvp.Value.GetDictionary();
                if (itemDict.Count > 0)
                {
                    // Value is an object, add name to it
                    itemDict["name"] = kvp.Key;
                    result.Add(Property.Load(itemDict, context));
                }
                else
                {
                    // Value is a scalar, use it as the primary property
                    var newDict = new Dictionary<string, object?>
                    {
                        ["name"] = kvp.Key,
                        ["example"] = kvp.Value
                    };
                    result.Add(Property.Load(newDict, context));
                }
            }
        }
        else if (data is IEnumerable<object> list)
        {
            foreach (var item in list)
            {
                var itemDict = item.GetDictionary(Property.ShorthandProperty);
                if (itemDict.Count > 0)
                {
                    result.Add(Property.Load(itemDict, context));
                }
            }
        }

        return result;
    }


    #endregion

    #region Save Methods

    /// <summary>
    /// Save the UnionProperty instance to a dictionary.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The dictionary representation of this instance.</returns>
    public override Dictionary<string, object?> Save(SaveContext? context = null)
    {
        var obj = this;
        if (context is not null)
        {
            obj = context.ProcessObject(obj);
        }


        // Start with parent class properties
        var result = base.Save(context);


        result["kind"] = obj.Kind;


        if (obj.OneOf is not null)
        {
            result["oneOf"] = SaveOneOf(obj.OneOf, context);
        }


        if (obj.AnyOf is not null)
        {
            result["anyOf"] = SaveAnyOf(obj.AnyOf, context);
        }


        return result;
    }


    /// <summary>
    /// Save a list of Property to object or array format.
    /// </summary>
    public static object SaveOneOf(IList<Property> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Save a list of Property to object or array format.
    /// </summary>
    public static object SaveAnyOf(IList<Property> items, SaveContext? context)
    {
        context ??= new SaveContext();

        // This collection type does not have a 'name' property, only array format is supported
        return items.Select(item => item.Save(context)).ToList();

    }


    /// <summary>
    /// Convert the UnionProperty instance to a YAML string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The YAML string representation of this instance.</returns>
    public new string ToYaml(SaveContext? context = null)
    {
        context ??= new SaveContext();
        return context.ToYaml(Save(context));
    }

    /// <summary>
    /// Convert the UnionProperty instance to a JSON string.
    /// </summary>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <param name="indent">Whether to indent the output. Defaults to true.</param>
    /// <returns>The JSON string representation of this instance.</returns>
    public new string ToJson(SaveContext? context = null, bool indent = true)
    {
        context ??= new SaveContext();
        return context.ToJson(Save(context), indent);
    }

    /// <summary>
    /// Load a UnionProperty instance from a JSON string.
    /// </summary>
    /// <param name="json">The JSON string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded UnionProperty instance.</returns>
    public new static UnionProperty FromJson(string json, LoadContext? context = null)
    {
        using var doc = JsonDocument.Parse(json);
        Dictionary<string, object?> dict;
        dict = JsonSerializer.Deserialize<Dictionary<string, object?>>(json, JsonUtils.Options)
            ?? throw new ArgumentException("Failed to parse JSON as dictionary");

        return Load(dict, context);
    }

    /// <summary>
    /// Load a UnionProperty instance from a YAML string.
    /// </summary>
    /// <param name="yaml">The YAML string to parse.</param>
    /// <param name="context">Optional context with pre/post processing callbacks.</param>
    /// <returns>The loaded UnionProperty instance.</returns>
    public new static UnionProperty FromYaml(string yaml, LoadContext? context = null)
    {
        var dict = YamlUtils.Deserializer.Deserialize<Dictionary<string, object?>>(yaml)
            ?? throw new ArgumentException("Failed to parse YAML as dictionary");

        return Load(dict, context);
    }

    #endregion
}
