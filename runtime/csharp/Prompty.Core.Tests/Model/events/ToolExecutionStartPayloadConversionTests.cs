// <auto-generated by typra-emitter>
using Xunit;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130


public class ToolExecutionStartPayloadConversionTests
{
    [Fact]
    public void LoadYamlInput()
    {
        string yamlData = """
requestId: exec_abc123
toolCallId: call_abc123
toolName: powershell
workingDirectory: /workspace/project

""";

        var instance = ToolExecutionStartPayload.FromYaml(yamlData);

        Assert.NotNull(instance);
        Assert.Equal("exec_abc123", instance.RequestId);
        Assert.Equal("call_abc123", instance.ToolCallId);
        Assert.Equal("powershell", instance.ToolName);
        Assert.Equal("/workspace/project", instance.WorkingDirectory);
    }

    [Fact]
    public void LoadJsonInput()
    {
        string jsonData = """
{
  "requestId": "exec_abc123",
  "toolCallId": "call_abc123",
  "toolName": "powershell",
  "workingDirectory": "/workspace/project"
}
""";

        var instance = ToolExecutionStartPayload.FromJson(jsonData);
        Assert.NotNull(instance);
        Assert.Equal("exec_abc123", instance.RequestId);
        Assert.Equal("call_abc123", instance.ToolCallId);
        Assert.Equal("powershell", instance.ToolName);
        Assert.Equal("/workspace/project", instance.WorkingDirectory);
    }

    [Fact]
    public void RoundtripJson()
    {
        // Test that FromJson -> ToJson -> FromJson produces equivalent data
        string jsonData = """
{
  "requestId": "exec_abc123",
  "toolCallId": "call_abc123",
  "toolName": "powershell",
  "workingDirectory": "/workspace/project"
}
""";

        var original = ToolExecutionStartPayload.FromJson(jsonData);
        Assert.NotNull(original);

        var json = original.ToJson();
        Assert.False(string.IsNullOrEmpty(json));

        var reloaded = ToolExecutionStartPayload.FromJson(json);
        Assert.NotNull(reloaded);
        Assert.Equal("exec_abc123", reloaded.RequestId);
        Assert.Equal("call_abc123", reloaded.ToolCallId);
        Assert.Equal("powershell", reloaded.ToolName);
        Assert.Equal("/workspace/project", reloaded.WorkingDirectory);
    }

    [Fact]
    public void RoundtripYaml()
    {
        // Test that FromYaml -> ToYaml -> FromYaml produces equivalent data
        string yamlData = """
requestId: exec_abc123
toolCallId: call_abc123
toolName: powershell
workingDirectory: /workspace/project

""";

        var original = ToolExecutionStartPayload.FromYaml(yamlData);
        Assert.NotNull(original);

        var yaml = original.ToYaml();
        Assert.False(string.IsNullOrEmpty(yaml));

        var reloaded = ToolExecutionStartPayload.FromYaml(yaml);
        Assert.NotNull(reloaded);
        Assert.Equal("exec_abc123", reloaded.RequestId);
        Assert.Equal("call_abc123", reloaded.ToolCallId);
        Assert.Equal("powershell", reloaded.ToolName);
        Assert.Equal("/workspace/project", reloaded.WorkingDirectory);
    }

    [Fact]
    public void ToJsonProducesValidJson()
    {
        string jsonData = """
{
  "requestId": "exec_abc123",
  "toolCallId": "call_abc123",
  "toolName": "powershell",
  "workingDirectory": "/workspace/project"
}
""";

        var instance = ToolExecutionStartPayload.FromJson(jsonData);
        var json = instance.ToJson();

        // Verify it's valid JSON by parsing it
        var parsed = System.Text.Json.JsonDocument.Parse(json);
        Assert.NotNull(parsed);
    }

    [Fact]
    public void ToYamlProducesValidYaml()
    {
        string yamlData = """
requestId: exec_abc123
toolCallId: call_abc123
toolName: powershell
workingDirectory: /workspace/project

""";

        var instance = ToolExecutionStartPayload.FromYaml(yamlData);
        var yaml = instance.ToYaml();

        // Verify it's valid YAML by parsing it
        var deserializer = new YamlDotNet.Serialization.DeserializerBuilder().Build();
        var parsed = deserializer.Deserialize<object>(yaml);
        Assert.NotNull(parsed);
    }
}
