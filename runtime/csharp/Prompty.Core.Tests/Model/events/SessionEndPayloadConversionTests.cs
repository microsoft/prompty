// <auto-generated by typra-emitter>
using Xunit;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130


public class SessionEndPayloadConversionTests
{
    [Fact]
    public void LoadYamlInput()
    {
        string yamlData = """
sessionId: sess_abc123
status: success
reason: complete
durationMs: 12500

""";

        var instance = SessionEndPayload.FromYaml(yamlData);

        Assert.NotNull(instance);
        Assert.Equal("sess_abc123", instance.SessionId);
        Assert.Equal(SessionEndStatus.Success, instance.Status);
        Assert.Equal("complete", instance.Reason);
        Assert.Equal(12500, instance.DurationMs);
    }

    [Fact]
    public void LoadJsonInput()
    {
        string jsonData = """
{
  "sessionId": "sess_abc123",
  "status": "success",
  "reason": "complete",
  "durationMs": 12500
}
""";

        var instance = SessionEndPayload.FromJson(jsonData);
        Assert.NotNull(instance);
        Assert.Equal("sess_abc123", instance.SessionId);
        Assert.Equal(SessionEndStatus.Success, instance.Status);
        Assert.Equal("complete", instance.Reason);
        Assert.Equal(12500, instance.DurationMs);
    }

    [Fact]
    public void RoundtripJson()
    {
        // Test that FromJson -> ToJson -> FromJson produces equivalent data
        string jsonData = """
{
  "sessionId": "sess_abc123",
  "status": "success",
  "reason": "complete",
  "durationMs": 12500
}
""";

        var original = SessionEndPayload.FromJson(jsonData);
        Assert.NotNull(original);

        var json = original.ToJson();
        Assert.False(string.IsNullOrEmpty(json));

        var reloaded = SessionEndPayload.FromJson(json);
        Assert.NotNull(reloaded);
        Assert.Equal("sess_abc123", reloaded.SessionId);
        Assert.Equal(SessionEndStatus.Success, reloaded.Status);
        Assert.Equal("complete", reloaded.Reason);
        Assert.Equal(12500, reloaded.DurationMs);
    }

    [Fact]
    public void RoundtripYaml()
    {
        // Test that FromYaml -> ToYaml -> FromYaml produces equivalent data
        string yamlData = """
sessionId: sess_abc123
status: success
reason: complete
durationMs: 12500

""";

        var original = SessionEndPayload.FromYaml(yamlData);
        Assert.NotNull(original);

        var yaml = original.ToYaml();
        Assert.False(string.IsNullOrEmpty(yaml));

        var reloaded = SessionEndPayload.FromYaml(yaml);
        Assert.NotNull(reloaded);
        Assert.Equal("sess_abc123", reloaded.SessionId);
        Assert.Equal(SessionEndStatus.Success, reloaded.Status);
        Assert.Equal("complete", reloaded.Reason);
        Assert.Equal(12500, reloaded.DurationMs);
    }

    [Fact]
    public void ToJsonProducesValidJson()
    {
        string jsonData = """
{
  "sessionId": "sess_abc123",
  "status": "success",
  "reason": "complete",
  "durationMs": 12500
}
""";

        var instance = SessionEndPayload.FromJson(jsonData);
        var json = instance.ToJson();

        // Verify it's valid JSON by parsing it
        var parsed = System.Text.Json.JsonDocument.Parse(json);
        Assert.NotNull(parsed);
    }

    [Fact]
    public void ToYamlProducesValidYaml()
    {
        string yamlData = """
sessionId: sess_abc123
status: success
reason: complete
durationMs: 12500

""";

        var instance = SessionEndPayload.FromYaml(yamlData);
        var yaml = instance.ToYaml();

        // Verify it's valid YAML by parsing it
        var deserializer = new YamlDotNet.Serialization.DeserializerBuilder().Build();
        var parsed = deserializer.Deserialize<object>(yaml);
        Assert.NotNull(parsed);
    }
}
