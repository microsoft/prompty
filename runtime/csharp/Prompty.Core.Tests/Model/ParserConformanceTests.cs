// <auto-generated by typra-emitter>
// Code generated by Typra emitter; DO NOT EDIT.
//
// Part III TYPED @vector conformance for Parser — the per-interface
// twin of the per-model ConversionTests file (issue #282 §8). Each [Fact] builds
// the operation inputs from the vector JSON, reads the shape discriminator, routes
// it through the emitted ParserResolver against the consumer-attached provider, invokes
// the typed seam, and asserts the result reproduces `expected`. The provider VALUE
// is authored by the consumer as VectorProviders.Parser() in the
// namespace named by 'vector-adapter-path' — a dropped @dispatch slot fails to
// compile, so conformance never silently skips.
// See docs: reference/vector-conformance.
#nullable enable
using System.Text.Json;
using System.Threading.Tasks;
using Xunit;
using Prompty.Core.Conformance;

namespace Prompty.Core;

public class ParserConformanceTests
{
    // The consumer-attached provider of typed seam impls, one slot per @dispatch
    // variant. Authored outside this tree; a forgotten slot cannot compile.
    private static IParserProvider Provider() => VectorProviders.Parser();

    [Fact]
    public async Task AssistantWithCodeBlock()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "assistant:\nHere is code:\n```python\nprint('hello')\n```\n\nuser:\nThanks"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task ConsecutiveNewlinesBetweenRoles()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nHelper\n\n\n\nuser:\nHi"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task ContentTrimmed()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\n  \n  You are helpful.  \n  \n\nuser:\n  Hello  "
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task DeveloperRole()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "developer:\nInternal instructions."
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task EmptyContent()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\n\nuser:\nHello"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task InlineMarkdownImagePreservedAsText()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "user:\nLook at this ![photo](https://example.com/img.png)"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task MarkdownInContent()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\n# Title\n\nSome **bold** text\n\n- item 1\n- item 2\n\nuser:\nHello"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task MultilineContent()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nLine one.\nLine two.\nLine three.\n\nuser:\nQuestion"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task MultipleAssistantTurns()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful.\n\nuser:\nHi\n\nassistant:\nHello!\n\nuser:\nThanks\n\nassistant:\nYou're welcome!"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task MultipleUserTurns()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nHelper\n\nuser:\nFirst question\n\nassistant:\nFirst answer\n\nuser:\nSecond question"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task NoRoleMarkerDefaultsToSystem()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "Just a plain message with no role marker"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task RoleMarkerAtLineStartOnly()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nThe user: said hello\n\nuser:\nHi"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task RoleMarkerColonInContent()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nHere is a tip: always be kind.\n\nuser:\nTime is 3:30pm"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task RoleWithMultipleUnquotedAttributes()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "user[name=Bob, id=7]:\nHi"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task RoleWithQuotedAttribute()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "user[name=\"Alice\"]:\nHello!"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task SingleSystem()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful."
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task SystemUser()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful.\n\nuser:\nHello"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task SystemUserAssistant()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nBe concise.\n\nuser:\nHi\n\nassistant:\nHello! How can I help?"
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

    [Fact]
    public async Task ThreadNonceExpansion()
    {
        string inputJson = """
{
  "agent": {
    "template": {
      "format": {
        "kind": "jinja2"
      },
      "parser": {
        "kind": "prompty"
      }
    }
  },
  "rendered": "system:\nYou are helpful.\n\n__PROMPTY_THREAD_abcd1234_conversation__\n\nuser:\nHi",
  "thread_inputs": {
    "conversation": [
      {
        "role": "user",
        "content": [
          {
            "kind": "text",
            "value": "prev question"
          }
        ]
      },
      {
        "role": "assistant",
        "content": [
          {
            "kind": "text",
            "value": "prev answer"
          }
        ]
      }
    ]
  }
}
""";
        using var document = JsonDocument.Parse(inputJson);
        var root = document.RootElement;
        var agent = Agent.FromJson(root.GetProperty("agent").GetRawText());
        var rendered = JsonSerializer.Deserialize<string>(root.GetProperty("rendered").GetRawText());
        var context = root.TryGetProperty("context", out var contextEl)
            ? JsonSerializer.Deserialize<Dictionary<string, object?>?>(contextEl.GetRawText())
            : default;
        var kind = agent.Template.Parser.Kind;
        var impl = ParserResolver.Resolve(kind, Provider());
        Assert.NotNull(impl);
        var actual = await impl!.ParseAsync(agent, rendered, context);
        Assert.NotNull(actual);
    }

}
