// <auto-generated by typra-emitter>
#nullable enable

using Xunit;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130


public class RetryPolicyRequestConversionTests
{
    [Fact]
    public void LoadYamlInput()
    {
        string yamlData = """
failedAttempts: 1
nextAttempt: 1
maxAttempts: 1
reason: sample

""";

        var instance = RetryPolicyRequest.FromYaml(yamlData);

        Assert.NotNull(instance);
        Assert.Equal(1, instance.FailedAttempts);
        Assert.Equal(1, instance.NextAttempt);
        Assert.Equal(1, instance.MaxAttempts);
        Assert.Equal("sample", instance.Reason);
    }

    [Fact]
    public void LoadJsonInput()
    {
        string jsonData = """
{
  "failedAttempts": 1,
  "nextAttempt": 1,
  "maxAttempts": 1,
  "reason": "sample"
}
""";

        var instance = RetryPolicyRequest.FromJson(jsonData);
        Assert.NotNull(instance);
        Assert.Equal(1, instance.FailedAttempts);
        Assert.Equal(1, instance.NextAttempt);
        Assert.Equal(1, instance.MaxAttempts);
        Assert.Equal("sample", instance.Reason);
    }

    [Fact]
    public void RoundtripJson()
    {
        // Test that FromJson -> ToJson -> FromJson produces equivalent data
        string jsonData = """
{
  "failedAttempts": 1,
  "nextAttempt": 1,
  "maxAttempts": 1,
  "reason": "sample"
}
""";

        var original = RetryPolicyRequest.FromJson(jsonData);
        Assert.NotNull(original);

        var json = original.ToJson();
        Assert.False(string.IsNullOrEmpty(json));

        var reloaded = RetryPolicyRequest.FromJson(json);
        Assert.NotNull(reloaded);
        Assert.Equal(1, reloaded.FailedAttempts);
        Assert.Equal(1, reloaded.NextAttempt);
        Assert.Equal(1, reloaded.MaxAttempts);
        Assert.Equal("sample", reloaded.Reason);
    }

    [Fact]
    public void RoundtripYaml()
    {
        // Test that FromYaml -> ToYaml -> FromYaml produces equivalent data
        string yamlData = """
failedAttempts: 1
nextAttempt: 1
maxAttempts: 1
reason: sample

""";

        var original = RetryPolicyRequest.FromYaml(yamlData);
        Assert.NotNull(original);

        var yaml = original.ToYaml();
        Assert.False(string.IsNullOrEmpty(yaml));

        var reloaded = RetryPolicyRequest.FromYaml(yaml);
        Assert.NotNull(reloaded);
        Assert.Equal(1, reloaded.FailedAttempts);
        Assert.Equal(1, reloaded.NextAttempt);
        Assert.Equal(1, reloaded.MaxAttempts);
        Assert.Equal("sample", reloaded.Reason);
    }

    [Fact]
    public void ToJsonProducesValidJson()
    {
        string jsonData = """
{
  "failedAttempts": 1,
  "nextAttempt": 1,
  "maxAttempts": 1,
  "reason": "sample"
}
""";

        var instance = RetryPolicyRequest.FromJson(jsonData);
        var json = instance.ToJson();

        // Verify it's valid JSON by parsing it
        var parsed = System.Text.Json.JsonDocument.Parse(json);
        Assert.NotNull(parsed);
    }

    [Fact]
    public void ToYamlProducesValidYaml()
    {
        string yamlData = """
failedAttempts: 1
nextAttempt: 1
maxAttempts: 1
reason: sample

""";

        var instance = RetryPolicyRequest.FromYaml(yamlData);
        var yaml = instance.ToYaml();

        // Verify it's valid YAML by parsing it
        var deserializer = new YamlDotNet.Serialization.DeserializerBuilder().Build();
        var parsed = deserializer.Deserialize<object>(yaml);
        Assert.NotNull(parsed);
    }
}
