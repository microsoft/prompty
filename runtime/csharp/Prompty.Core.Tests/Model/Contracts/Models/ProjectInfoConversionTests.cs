// <auto-generated by typra-emitter>
#nullable enable

using Xunit;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130


public class ProjectInfoConversionTests
{
    [Fact]
    public void LoadYamlInput()
    {
        string yamlData = """
name: sample
displayName: sample
endpoint: sample

""";

        var instance = ProjectInfo.FromYaml(yamlData);

        Assert.NotNull(instance);
        Assert.Equal("sample", instance.Name);
        Assert.Equal("sample", instance.DisplayName);
        Assert.Equal("sample", instance.Endpoint);
    }

    [Fact]
    public void LoadJsonInput()
    {
        string jsonData = """
{
  "name": "sample",
  "displayName": "sample",
  "endpoint": "sample"
}
""";

        var instance = ProjectInfo.FromJson(jsonData);
        Assert.NotNull(instance);
        Assert.Equal("sample", instance.Name);
        Assert.Equal("sample", instance.DisplayName);
        Assert.Equal("sample", instance.Endpoint);
    }

    [Fact]
    public void RoundtripJson()
    {
        // Test that FromJson -> ToJson -> FromJson produces equivalent data
        string jsonData = """
{
  "name": "sample",
  "displayName": "sample",
  "endpoint": "sample"
}
""";

        var original = ProjectInfo.FromJson(jsonData);
        Assert.NotNull(original);

        var json = original.ToJson();
        Assert.False(string.IsNullOrEmpty(json));

        var reloaded = ProjectInfo.FromJson(json);
        Assert.NotNull(reloaded);
        Assert.Equal("sample", reloaded.Name);
        Assert.Equal("sample", reloaded.DisplayName);
        Assert.Equal("sample", reloaded.Endpoint);
    }

    [Fact]
    public void RoundtripYaml()
    {
        // Test that FromYaml -> ToYaml -> FromYaml produces equivalent data
        string yamlData = """
name: sample
displayName: sample
endpoint: sample

""";

        var original = ProjectInfo.FromYaml(yamlData);
        Assert.NotNull(original);

        var yaml = original.ToYaml();
        Assert.False(string.IsNullOrEmpty(yaml));

        var reloaded = ProjectInfo.FromYaml(yaml);
        Assert.NotNull(reloaded);
        Assert.Equal("sample", reloaded.Name);
        Assert.Equal("sample", reloaded.DisplayName);
        Assert.Equal("sample", reloaded.Endpoint);
    }

    [Fact]
    public void ToJsonProducesValidJson()
    {
        string jsonData = """
{
  "name": "sample",
  "displayName": "sample",
  "endpoint": "sample"
}
""";

        var instance = ProjectInfo.FromJson(jsonData);
        var json = instance.ToJson();

        // Verify it's valid JSON by parsing it
        var parsed = System.Text.Json.JsonDocument.Parse(json);
        Assert.NotNull(parsed);
    }

    [Fact]
    public void ToYamlProducesValidYaml()
    {
        string yamlData = """
name: sample
displayName: sample
endpoint: sample

""";

        var instance = ProjectInfo.FromYaml(yamlData);
        var yaml = instance.ToYaml();

        // Verify it's valid YAML by parsing it
        var deserializer = new YamlDotNet.Serialization.DeserializerBuilder().Build();
        var parsed = deserializer.Deserialize<object>(yaml);
        Assert.NotNull(parsed);
    }
}
