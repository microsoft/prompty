// <auto-generated by typra-emitter>
using Xunit;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130


public class DelegatedStateReferenceConversionTests
{
    [Fact]
    public void LoadYamlInput()
    {
        string yamlData = """
provider: openai
kind: response
id: resp_abc123

""";

        var instance = DelegatedStateReference.FromYaml(yamlData);

        Assert.NotNull(instance);
        Assert.Equal("openai", instance.Provider);
        Assert.Equal("response", instance.Kind);
        Assert.Equal("resp_abc123", instance.Id);
    }

    [Fact]
    public void LoadJsonInput()
    {
        string jsonData = """
{
  "provider": "openai",
  "kind": "response",
  "id": "resp_abc123"
}
""";

        var instance = DelegatedStateReference.FromJson(jsonData);
        Assert.NotNull(instance);
        Assert.Equal("openai", instance.Provider);
        Assert.Equal("response", instance.Kind);
        Assert.Equal("resp_abc123", instance.Id);
    }

    [Fact]
    public void RoundtripJson()
    {
        // Test that FromJson -> ToJson -> FromJson produces equivalent data
        string jsonData = """
{
  "provider": "openai",
  "kind": "response",
  "id": "resp_abc123"
}
""";

        var original = DelegatedStateReference.FromJson(jsonData);
        Assert.NotNull(original);

        var json = original.ToJson();
        Assert.False(string.IsNullOrEmpty(json));

        var reloaded = DelegatedStateReference.FromJson(json);
        Assert.NotNull(reloaded);
        Assert.Equal("openai", reloaded.Provider);
        Assert.Equal("response", reloaded.Kind);
        Assert.Equal("resp_abc123", reloaded.Id);
    }

    [Fact]
    public void RoundtripYaml()
    {
        // Test that FromYaml -> ToYaml -> FromYaml produces equivalent data
        string yamlData = """
provider: openai
kind: response
id: resp_abc123

""";

        var original = DelegatedStateReference.FromYaml(yamlData);
        Assert.NotNull(original);

        var yaml = original.ToYaml();
        Assert.False(string.IsNullOrEmpty(yaml));

        var reloaded = DelegatedStateReference.FromYaml(yaml);
        Assert.NotNull(reloaded);
        Assert.Equal("openai", reloaded.Provider);
        Assert.Equal("response", reloaded.Kind);
        Assert.Equal("resp_abc123", reloaded.Id);
    }

    [Fact]
    public void ToJsonProducesValidJson()
    {
        string jsonData = """
{
  "provider": "openai",
  "kind": "response",
  "id": "resp_abc123"
}
""";

        var instance = DelegatedStateReference.FromJson(jsonData);
        var json = instance.ToJson();

        // Verify it's valid JSON by parsing it
        var parsed = System.Text.Json.JsonDocument.Parse(json);
        Assert.NotNull(parsed);
    }

    [Fact]
    public void ToYamlProducesValidYaml()
    {
        string yamlData = """
provider: openai
kind: response
id: resp_abc123

""";

        var instance = DelegatedStateReference.FromYaml(yamlData);
        var yaml = instance.ToYaml();

        // Verify it's valid YAML by parsing it
        var deserializer = new YamlDotNet.Serialization.DeserializerBuilder().Build();
        var parsed = deserializer.Deserialize<object>(yaml);
        Assert.NotNull(parsed);
    }
}
