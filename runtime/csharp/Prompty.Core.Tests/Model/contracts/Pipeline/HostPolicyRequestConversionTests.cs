// <auto-generated by typra-emitter>
#nullable enable

using Xunit;

#pragma warning disable IDE0130
namespace Prompty.Core;
#pragma warning restore IDE0130


public class HostPolicyRequestConversionTests
{
    [Fact]
    public void LoadYamlInput()
    {
        string yamlData = """
sessionId: sample
turnId: sample
iteration: 1
messages: []

""";

        var instance = HostPolicyRequest.FromYaml(yamlData);

        Assert.NotNull(instance);
        Assert.Equal("sample", instance.SessionId);
        Assert.Equal("sample", instance.TurnId);
        Assert.Equal(1, instance.Iteration);
    }

    [Fact]
    public void LoadJsonInput()
    {
        string jsonData = """
{
  "sessionId": "sample",
  "turnId": "sample",
  "iteration": 1,
  "messages": []
}
""";

        var instance = HostPolicyRequest.FromJson(jsonData);
        Assert.NotNull(instance);
        Assert.Equal("sample", instance.SessionId);
        Assert.Equal("sample", instance.TurnId);
        Assert.Equal(1, instance.Iteration);
    }

    [Fact]
    public void RoundtripJson()
    {
        // Test that FromJson -> ToJson -> FromJson produces equivalent data
        string jsonData = """
{
  "sessionId": "sample",
  "turnId": "sample",
  "iteration": 1,
  "messages": []
}
""";

        var original = HostPolicyRequest.FromJson(jsonData);
        Assert.NotNull(original);

        var json = original.ToJson();
        Assert.False(string.IsNullOrEmpty(json));

        var reloaded = HostPolicyRequest.FromJson(json);
        Assert.NotNull(reloaded);
        Assert.Equal("sample", reloaded.SessionId);
        Assert.Equal("sample", reloaded.TurnId);
        Assert.Equal(1, reloaded.Iteration);
    }

    [Fact]
    public void RoundtripYaml()
    {
        // Test that FromYaml -> ToYaml -> FromYaml produces equivalent data
        string yamlData = """
sessionId: sample
turnId: sample
iteration: 1
messages: []

""";

        var original = HostPolicyRequest.FromYaml(yamlData);
        Assert.NotNull(original);

        var yaml = original.ToYaml();
        Assert.False(string.IsNullOrEmpty(yaml));

        var reloaded = HostPolicyRequest.FromYaml(yaml);
        Assert.NotNull(reloaded);
        Assert.Equal("sample", reloaded.SessionId);
        Assert.Equal("sample", reloaded.TurnId);
        Assert.Equal(1, reloaded.Iteration);
    }

    [Fact]
    public void ToJsonProducesValidJson()
    {
        string jsonData = """
{
  "sessionId": "sample",
  "turnId": "sample",
  "iteration": 1,
  "messages": []
}
""";

        var instance = HostPolicyRequest.FromJson(jsonData);
        var json = instance.ToJson();

        // Verify it's valid JSON by parsing it
        var parsed = System.Text.Json.JsonDocument.Parse(json);
        Assert.NotNull(parsed);
    }

    [Fact]
    public void ToYamlProducesValidYaml()
    {
        string yamlData = """
sessionId: sample
turnId: sample
iteration: 1
messages: []

""";

        var instance = HostPolicyRequest.FromYaml(yamlData);
        var yaml = instance.ToYaml();

        // Verify it's valid YAML by parsing it
        var deserializer = new YamlDotNet.Serialization.DeserializerBuilder().Build();
        var parsed = deserializer.Deserialize<object>(yaml);
        Assert.NotNull(parsed);
    }
}
