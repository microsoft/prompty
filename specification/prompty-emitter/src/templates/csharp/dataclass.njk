using static {{namespace}}.Utils;

namespace {{ namespace }} {
  public partial class {{ node.typeName }}{% if node.baseType.length > 0 -%} : {{ node.baseType }}{% endif %}
  {
    public {{ node.typeName }}() 
    {
    }

    internal {{ node.typeName }}(Dictionary<string, object> init) : this()
    {
      {% for prop in node.properties -%}
      {{ renderPropertyName(prop) }} = init.GetValueOrDefault<{{ renderType(prop) }}>("{{prop.name}}");
      {% endfor %}
    }

    {% for prop in node.properties -%}
    public {{ renderType(prop) }} {{ renderPropertyName(prop) }} { get; set; }{{ renderDefault(prop) | safe }}
    {% endfor %}
  }
}