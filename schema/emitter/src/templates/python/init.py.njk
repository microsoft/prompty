{#
  Python __init__.py Template
  ===========================
  Renders the __init__.py file that exports all types.
  
  Expected context (PythonInitContext):
    - baseTypes: TypeNode[] - Types without a parent for imports
    - types: TypeNode[] - All types for __all__ export
#}
##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################
from ._context import LoadContext, SaveContext
{%- for type in baseTypes %}
{% if type.childTypes.length > 0 %}
from ._{{ type.typeName.name }} import (
  {{ type.typeName.name }}
  {%- for child in type.childTypes %},
  {{ child.typeName.name }}
  {%- endfor %}
)
{% else %}
from ._{{ type.typeName.name }} import {{ type.typeName.name }}
{% endif %}
{%- endfor %}

__all__ = [
    "LoadContext",
    "SaveContext",
    {%- for type in types %}
    "{{ type.typeName.name }}",
    {%- endfor %}
]
