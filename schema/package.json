{
  "private": true,
  "name": "prompty-schema",
  "description": "TypeSpec models and code generation for Prompty",
  "scripts": {
    "format:tsp": "npx tsp format \"model/**/*.tsp\"",
    "format:tsp:check": "npx tsp format \"model/**/*.tsp\" --check",
    "format:rust": "cargo fmt --all --manifest-path ../runtime/rust/prompty/Cargo.toml",
    "generate": "npx tsp compile model/main.tsp --config tspconfig.yaml",
    "build": "npm run format:tsp && npm run generate && npm run format:rust"
  },
  "dependencies": {
    "@typra/emitter": "0.2.4",
    "@typespec/compiler": "latest",
    "@typespec/json-schema": "latest"
  }
}
