// Named sample constants for model/discovery.tsp.
// Auto-extracted from inline @sample decorators.

namespace Prompty;

const availableIdSample = #{ id: "gpt-4o" };

const availableDisplayNameSample = #{ displayName: "GPT-4o" };

const availableOwnedBySample = #{ ownedBy: "openai" };

const availableContextWindowSample = #{ contextWindow: 128000 };

const availableInputModalitiesSample = #{ inputModalities: #["text", "image"] };

const availableOutputModalitiesSample = #{ outputModalities: #["text"] };

const availableAdditionalPropertiesSample = #{
  additionalProperties: #{ supportsStreaming: true },
};
