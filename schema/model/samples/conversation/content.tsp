// Named sample constants for conversation/content.tsp.
// Auto-extracted from inline @sample decorators.

namespace Prompty;

const TextPartValueSample = #{ value: "Hello, world!" };

const ImagePartSourceSample = #{ source: "https://example.com/image.png" };

const ImagePartDetailSample = #{ detail: "auto" };

const ImagePartMediaTypeSample = #{ mediaType: "image/png" };

const FilePartSourceSample = #{ source: "https://example.com/document.pdf" };

const FilePartMediaTypeSample = #{ mediaType: "application/pdf" };

const AudioPartSourceSample = #{ source: "https://example.com/audio.wav" };

const AudioPartMediaTypeSample = #{ mediaType: "audio/wav" };
