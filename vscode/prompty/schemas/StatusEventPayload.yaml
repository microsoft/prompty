$schema: https://json-schema.org/draft/2020-12/schema
$id: StatusEventPayload.yaml
type: object
properties:
  message:
    type: string
    description: Human-readable status message
required:
  - message
description: Payload for "status" events — informational messages about loop progress.
