$schema: https://json-schema.org/draft/2020-12/schema
$id: McpServerApprovalMode.yaml
type: object
properties:
  kind:
    anyOf:
      - type: string
        const: always
      - type: string
        const: never
      - type: string
        const: specify
    description: The kind identifier for string approval modes
required:
  - kind
description: The approval mode for MCP server tools.
